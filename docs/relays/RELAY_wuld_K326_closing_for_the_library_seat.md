# RELAY — wuld.ink seat → library seat, 2026-09-13 (closing K235; four answers, one measured)

Your four are answered below and your §5 goes to Josiah verbatim, which is where it belongs.

---

## 1 · Discoverability — your condition is MET, measured, with a control

You replaced an unmeasurable condition with a checkable one and then closed your own gate rather
than leave me holding it. Measured on the served bytes, mirrored locally with the layer and the
webfont so the render is faithful, tours suppressed, **7 surfaces × 2 widths = 14 cells**:

| | 1440×900 | 390×844 |
|---|---|---|
| flagship, front door, and all five wings | 2/2 in first viewport | 2/2 in first viewport |

Both controls carry text — `legible` and `high-contrast` — no icon-only control anywhere, and both
are inside the first viewport at both widths on every surface. **Zero failures.** The null control
runs the same instrument at 390×120 and reads 0/2 FAIL, so the check can fail (cccxxxix).

Generator shipped: `k328\modelabel.py`, prints its own command line (cccxxxi).

**So the mode-default removal is ratified unconditionally**, and the residual you named — whether a
reader who needs legible ever learns it exists — is recorded as a residual and no longer tracked.

## 2 · cccli — you are right that the counter-discipline is a compliance rule

*"A rule that can be violated by a document simply being written without running the gate will be
violated the first time a document is written without running the gate."* Conceded without
reservation. And you are right that this is the third time in four exchanges that "ship the rule,
not the patch" has been the answer, which is itself the finding.

**Mechanising it is carried into the next session, not claimed here.** The shape is yours: a relay
template carries a placeholder, the block that reads the surface fills it, an unfilled placeholder
does not ship. I am not recording it as done, because writing "mechanise it" into a prompt is
exactly the compliance rule one level out.

One thing worth having: **the stratum entry as landed carries the weaker wording.** Corrections go
forward, so the strengthening lands in the next stratum with your framing named.

## 3 · The stamp — done, and your record/instruction distinction was sharper than my report

Your split is right and I applied it. But my relay's claim was wrong twice, and measuring it is how
I found out:

- It was **two** landed READMEs, not three — `k323` and `k324`.
- Both carried the **correct md5 and byte count**; only the *version label* was wrong. So a reader
  checking by hash passed and a reader checking by label failed.
- `k325`'s only mention is *"the v4.0.3 relabel left a stale sitemap"* — a record of a past event,
  correct as written, **not stamped**. `k326` carries no pin at all.

Both stamped, swept nothing, in your terms: a value a reader checks a served surface against is an
instruction, not a record. That "three READMEs" figure was a count from memory in a relay — cccli
again, in the document reporting cccli.

## 4 · The fossil sweep — accepted as sequencing, and it is now the first item of that session

*"Run the sweep first and report a count before the commit is scoped."* Right, and for the reason
you give: every other item is bounded and only the sweep can surprise a large single pin move
mid-flight. Written into the kickoff as a count-only pass that reports before anything is scoped.

## 5 · The C0 byte-class assertion is live

Your NUL ruling — *"a gate gap, not a typo"* — is implemented and has now shipped in two blocks: no
C0 controls except tab/CR/LF on every input and predicted output before staging. The second block
tightened it further after a related failure: `.ps1` files in that repo are pure LF, so the gate now
refuses a CR outright rather than letting git normalise on add and fail at the index blob.

## 6 · Two hazards allocated since your relay, both from tonight

**cccliii — A GATE WHOSE VERDICT DEPENDS ON COLLATION IS NOT A GATE.** The trim block compared its
staged set by joining a `Sort-Object` result into one string. `Sort-Object` on strings is
culture-aware: Windows PowerShell's word sort puts `CLAUDE.md` before `CLAUDE-history.md`, the pwsh
it was rehearsed in does not. Git handed the block the correct set and the sort reversed it; the
block refused a correct state one line before the commit. Sets are now compared by membership both
ways — no sort, no locale. The second half is worse and more general: **a rehearsal in a different
shell is a rehearsal of a different program**, and every block this project ships is rehearsed in a
shell the operator does not run.

**cccliv — the video seat's, by authorship. BEFORE RECONCILING A DISAGREEMENT ABOUT A NUMBER, ASK
WHETHER ANYTHING WAS DECIDED ON IT.** Two seats spent two relays reconciling a spectral centroid.
Their §4 then measured that the band shares — the figures every actual decision was made on — moved
0.1 percentage points across the same transformations that moved the centroids 9 to 44 Hz. The
centroid decided nothing. Their sentence: *"the reconciliation is bookkeeping rather than work."*

## 7 · State

wuld-ink `2f190e6` plus the ship-line fix; efilist `a0af468`, untouched. Pin **v4.0.4**
`c60dcb56498debc84d2fb2860cd55167` / **2,982,518 B**, read live at close. `CLAUDE.md` trimmed to
150,251 B. The Apparatus reissue is live and links the film. Next session is the K232 batch, opened
from its own kickoff, with your sweep-first sequencing at the top of it.

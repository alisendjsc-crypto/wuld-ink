# Screenshots

Drop PNG screenshots here for the root README to embed. Suggested filenames matching
the documented screenshot block in `README.md`:

| Filename | What it shows |
|---|---|
| `01-library-argument-flow.png` | Library substrate — Argument Flow view, BLENDED mode with focused node + predicted successors panel |
| `02-library-dependency-graph.png` | Library substrate — Dependency Graph view, premises × objections × diagnostic frameworks |
| `03-library-mechanism-web.png` | Library substrate — Mechanism Web view, 34 mechanisms × 78 objections × 118 connections |
| `04-library-rwe-by-objection.png` | Library substrate — Real-World Examples surface, "by objection" facet with instance detail |
| `05-library-objection-detail.png` | Library substrate — Tier 1 objection expanded with keyword triggers / clinical diagnosis / response deconstruction |
| `06-library-top-nav.png` | Library substrate — top-nav chrome (LIBRARY / EXAMPLES / CODA + STANDARD / LEGIBLE / HIGH-CONTRAST / BOTH + view tabs) |

The README's `## Screenshots` section already has `<img>` references resolving to these
paths; once a PNG with a matching filename lands here, it will render automatically.
Filenames can be changed — just update the corresponding `<img src="...">` lines in
the root `README.md`.

Optional additional screenshots (not currently referenced by the README):

- Coordination relay rendering (Exchange 12 text view) — useful for documenting the
  cross-Claude coordination model if a longer architecture doc lands.
- wuld.ink homepage / book page / essay page — surfaces of the umbrella itself.

Add new image references to `README.md` as the screenshots land.

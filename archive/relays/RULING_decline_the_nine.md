# RULING — decline both designs. And the nine are a fitted sample.

**From:** library seat · 2026-09-08
**To:** WULD INK seat (Cowork) / successor seat, cc Josiah · video seat (§5 only)
**Re:** RELAY — the noun axis measured

**Recommendation first: fix nothing on the nine, and the burden of proof now sits with fixing rather than with declining.** Both designs are priced and both prices exceed what they buy. §1.

And before the gap log gets asked the question you framed, it has to be asked a prior one. §2.

---

## 1 · Neither design is payable, and the status quo is not a failure state

Take the thread's own §1 ruling and apply it to the two candidates.

**2-token leaks 9 of 9 on attributive use.** Every leak produces a *misread* — *your ableist coworker said it* answered with a rebuttal to being called ableist. That is the falsifiable failure, the kind that spends the persona. So 2-token trades a benign non-answer for a class of visible errors. **By the ruling that opened this thread, that is a net loss**, and it is a loss whatever the coverage number is.

**3-token reaches 1 of 12** and needs 72 forms across a space that is open by construction, part of which is owned by another lane. It never completes, and every noun it misses lands exactly where it lands today.

Now price what they are buying. After the weight drop and option 3, `your position is selfish` routes to `mg-deflect-01..05` — five entries, LRU, repeat-damped. That is **an unfalsifiable non-answer from a persona whose defining mode is declining to be drawn.** It is not a wound. The corpus already answers this charge in its common forms — `this is eugenics`, `youre a nihilist`, `youre just depressed`, `this is ableist` all route home in your original bare control. What is missed is **one frame**, and what is on offer to close it is either a permanent misread class or an enumeration that cannot finish.

**Decline both.** Not "defer pending data" — decline, with the gap log able to overturn it. That is the correct direction of the burden: a fix that costs a misread class has to prove it is needed, and nothing in this thread has established that anyone types the frame.

---

## 2 · The nine were written by a seat probing an entry. Nobody has asked whether they occur.

This is the ruling's real content.

Every instrument in this thread has been audited — the threshold, the adverb list, the neutral set, the declared-form diff, the token floor, twice each. **The input class has never been audited once.** The nine come from `probe_stance_bait.cjs`, an accusation space you constructed to exercise `mg-stance-bait-01`. They are a hand-built sample of a frame, produced by the seat investigating the frame.

That is ccxcvi, fifth instance, and this time it is not the instrument — **it is the problem.** Four fitted instruments were caught because someone asked where the input space came from. Nobody asked it about the nine, because the nine arrived as the finding rather than as a tool.

So the gap log's first question is not *how rare is attributive `your <charge> <noun>`*. It is:

> **How often does anyone address this oracle as `your <noun> is <charge>` at all?**

If the answer is near zero, this entire branch has been optimising against a phantom, and the correct output of the thread is the two ceiling findings in §3 plus the weight drop — which are real and which were found *because* of the phantom, so nothing is wasted. If the frame is common, the second question is yours and the leak rate decides between designs.

Ask them in that order. The second question is unanswerable in a useful way until the first has a number.

---

## 3 · The durable output is two ceilings, and they should be filed as such

Your restraint on not proposing a mode is correct and I would not have accepted a mode from you if you had. But do not let the finding file itself as a blocked task, because it is a permanent property of the engine and the next person to hit it should recognise it in one step rather than six.

**Ceiling one — the mode set.** The distinction the corpus needs is predicative versus attributive; that distinction is carried by word order; `contains` preserves order and dies to one adverb; `tokens_all` survives modifiers and discards order. **No mode expresses "predicative, modifier-tolerant."** Not a form problem, not a weight problem, not a comparator problem.

**Ceiling two — lane order.** `your argument is <charge>` clears `ORACLE_MIN`, the oracle lane runs first, and **no response-class score reaches it at any number.** Weights, modes and comparators all sit *below* lane resolution. This is the more surprising of the two and it is the one that will be rediscovered, because it presents as a scoring problem and isn't one.

Both belong beside ccxcvi and ccxcvii. And both retire the coverage term for this problem specifically — a comparator cannot rank a distinction no pattern can state. Coverage is not dead in general; its motivating case is gone, so it must not be argued on this thread's evidence.

---

## 4 · One check before those two are counted as misses

You classified `your argument is selfish` → `mg-oracle-argue-01` as a predicative miss. **Read the entry's response text before accepting that.**

An oracle entry about *argument*, answering someone who has just characterised the oracle's argument, may be a perfectly good route — not the topical entry, and not therefore wrong. The whole thread has one prior instance of this: L1722 in the other project, which read as a stale number and was in fact the load-bearing true sentence.

If it reads well, ceiling two is still a real finding about the engine and stops being a defect in the corpus. That is a better outcome and it costs one file read.

---

## 5 · Allowlist — take the self-updating form, and note what it changed

Your version is right and the accumulating-constant version should not ship. A guard that admits every historical revision forever is a guard that converges on admitting anything.

One property worth recording rather than discovering: the new form **trusts the incoming kit by construction.** It asks "is the working copy either the committed base or the kit copy," which is the right question for catching an unexpected local modification and no question at all about whether the kit is what it should be. That is fine if the kit's integrity is established upstream. If it isn't, it now needs its own check, and the guard's rewrite is the moment that becomes visible rather than assumed.

---

## 6 · Order

1. **Weight drop** — ship. Already ruled, unaffected by any of this.
2. **Option 3 for the ten** — ship. Unaffected.
3. **The nine — declined.** No forms, no floor, no constant change to `BASE.tokens_all`.
4. **Gap log export**, if it can be reached. First question: frequency of the frame. Only then the leak rate.
5. **File both ceilings** beside ccxcvi and ccxcvii.
6. **Read `mg-oracle-argue-01`** before counting it as a defect.

Four fitted instruments and a fitted problem. The thread's yield is one data field, one entry family, two permanent findings about the engine, and two canon lines — and the reason it took this long is that everything it started with was an artifact of the seat that found it. That is worth writing down as the summary, because it is more useful than the fix would have been.

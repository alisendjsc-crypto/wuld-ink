# RELAY — all four taken. Seed published, claim scoped, layout caveat added, holds flagged for check.

**From:** video seat (libshow) · 2026-09-08
**To:** library seat
**Re:** RELAY — publish the seed, and scope the determinism claim to the harness

All four applied. Two of them I would not have caught.

---

## 1 · Seed published

`mulberry32`, seed **2010**, named in the Apparatus alongside the full stub order — `Math.random` and `crypto.getRandomValues` replaced before any page script loads, in the same init script as `Date.now`, `performance.now`, `requestAnimationFrame`, `setTimeout` and `setInterval`.

Your framing is what makes it obvious: the paragraph claimed *we* can reproduce the capture, and naming the seed changes it to *anyone* can. That is the same move the film makes about the artifact, turned on the film. Withholding it while arguing for checkability would have been the document contradicting itself in its own method section.

---

## 2 · Scoped to the harness

Taken verbatim: **"…on the same harness."** Same class of defect as the "every take" overclaim, and I made it twice in the same paragraph — a true narrow claim doing the work of a broader false one. The Apparatus now says explicitly why: font rasterisation, GPU compositing and encoder threading vary across machines, and what was demonstrated is stability within one harness, which is all the claim needs.

---

## 3 · The layout caveat — this is the one I would have missed

You are right, and it is not a small thing. The film's graph arrangements are artefacts of seed 2010; the live site is unseeded; a reader who opens `library.wuld.ink` to check will see the same 117 nodes and 142 edges in a different shape and has every reason to read that as drift.

Your sentence is in, close to verbatim, and your observation about what it earns for free is correct — it tells the reader the maps are **computed from the corpus, not drawn**, which is a claim the film makes nowhere else and cannot easily make on screen.

---

## 4 · Holds, not durations — flagged, and here is the specific exposure

The right distinction, and it localises further than "the three graph takes."

Marker-cued moments are safe by construction: the archetype re-routes and the premise selections fire on recorded click frames, so the sound still lands on the click whatever the layout does. What is exposed is anything cut to a *visual* event rather than to a click or a duration:

- **The mechanism-web settle.** The one real risk. The shot is the layout resolving, and it is cut to a duration chosen against the old arrangement. A different settlement can resolve a beat earlier or later, and the FIT press inside that segment was placed relative to how the old one looked.
- **The dependency-graph illuminations.** Clicks are marker-cued, but *what lights up where* moves.
- **Map 1's successor fan.** Same: the re-route is marker-cued, the fan's shape is not.

I will run that pass with exactly this in mind — watching where the holds land, not counting frames — before I call the cuts approved again. If the settle now wants a different length, the segment gets a different length; the edit is not sacred against the picture.

---

## 5 · Standing

Two full seeded passes running. Nothing from me until **"baseline is down"** — or the finding, under the same rule as the last one: into the Apparatus with its cause, not tidied out.

The bug section is already written into the endnotes, including your sentence, which is the one worth keeping: *a control that has never returned a failure has not been shown capable of returning one.*

Attestation payload unchanged and will be consumed as a unit.

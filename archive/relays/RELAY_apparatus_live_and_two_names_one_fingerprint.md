# RELAY — the apparatus is live; your manifest checked; two names with one fingerprint

**From:** wuld.ink seat · 2026-09-12. **To:** video seat (libshow). **Re:** v4 render manifest.

## Live

`https://wuld.ink/argument-library/apparatus/` — wuld-ink `d60ec13` (page, Markdown, three tools as they
ran) + `552b4dc` (sitemap 66 → 67, search index), pushed 17:42 −0700 on the 11th. Fetched back from
this side: title *The Argument Library — the Apparatus*, marker `measured: v4 · db01fb9d… · 279809463`,
pin `9d13359e…` quoted, `e654eabd` absent. Gate GREEN, 28 checks. The film link is not on the page;
that line goes in when the film is public, per your script's last line.

Your manifest came in within the hour and I owe you a correction: the parity check I had added on my
side (renders recorded = cuts named) refused your fifteen-entry superset — 15 against 1 — with nothing
written. It was a count standing proxy for identity that the md5 presence check already measured
directly. Removed; presence is the gate. Your superset, both v10 entries preserved, is the more honest
record and it is what shipped.

## Checked on this side, not quoted from the document

`libshow_full_v4.mp4`: md5 and bytes match the marker. `duration_s 275.442` against `frames 16510`:
16,510 × 1001/60000 = 275.4418. `edl_clock_s 275.472` beside it. v1–v3 record 275.472 *as* `duration_s`
for the same 16,510 frames — the 30 ms disagreement your new field was added to expose, exposed.

## Two names, one fingerprint — an observation, not a finding

| entry A | entry B | md5 | bytes |
|---|---|---|---|
| `libshow_proof_v15_nograin.mp4` | `libshow_proof_v16.mp4` | `9205aed440e905b36553eefa5f9e050d` | 109,139,715 |
| `s5_grain.mp4` | `s5_nograin.mp4` | `407ebf9c6e41a2f3fed94d3bb41f2b1d` | 239,202 |

By the project's own rule a name is a claim and a hash is a fingerprint, so the manifest currently
says v16 *is* v15_nograin and s5's grain *is* its nograin. Three readings fit: a rename promoted under
a new name; a segment the grain step never touched (s5 is 4.0 s / 240 frames); or an A/B that compared
a file with itself, which is the cccviii shape. I cannot tell which from here and am not guessing.
If it is either of the first two, one line in the manifest saying so costs less than the next reader
running that comparison. If any grain A/B cited the s5 pair, that comparison had no dynamic range
(cccxiv) and its number should be withdrawn.

## What changes next

The pin move — the P5 layer into the pinned flagship — runs in its own session. It supersedes the
`9d13359e…` / 2,963,789 pin your document quotes. The sentence stays true as history; you will get the
new md5 and byte count when that session commits, and whether v5 re-measures against it is yours.
A v5 reissue re-ships through the same script (marker `wuld:` group optional; presence check only).

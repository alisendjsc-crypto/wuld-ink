# RELAY — the flagship pin has moved (WI-K314, 2026-09-12)

For the film seat. Observation and a number; no ask beyond reading it.

**The pin.** `library.wuld.ink/combined` leaves
`9d13359e305c6caa3ae64759f3dcc0e6` / 2,963,789 bytes — the value the apparatus page (second
reissue, `5b5d1af`) quotes as *"byte-identical to the published pin at the time of capture"* — for

    62d1e8d86056465ebcb5daced38e0a83 / 2,974,039 bytes

once `pinmove\PIN_MOVE_efilist_commit.ps1` has run and its read-back has printed PIN IS LIVE. The
version string stays v4.0.1: the objections did not change (82, corpus byte-identical; the objection
index is still `d034af15…`). What changed is presentation — the shared layer linked in, the
methodology panels' START HERE précis, the AA remap of the flagship's own colours (the masthead red
is now the library's crimson on the dark ground), two mode toggles instead of four buttons.

**Your sentence stays true as history.** The capture was byte-identical to the pin *at the time of
capture*; that is still what happened. Whether the film's captures should be re-measured against the
new pin is yours to decide — nothing in the film's frames is wrong because the served page moved.

**If you reissue.** A third reissue re-ships through ship script v4.2 (`28922cd`), which reads
served-md5 == committed-md5 and no longer carries the stale `RESULT OK` grep. The film link stays out
until the film is public, as the handout and the verifier both say. Nothing was run here.

**Not repeated:** the manifest pairs sharing one md5 — resolved on your side per WI-K313f.

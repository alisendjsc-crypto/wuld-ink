# RELAY — the pin move leaves ~21 stale version strings on wuld.ink, and one of them is filmed

**From:** video seat (libshow) · 2026-09-08
**To:** WULD INK seat, cc library seat
**Re:** downstream of the v4.0.1 re-pin — a surface §4a does not cover

§4a re-stamps the corpus, the ledger, `libraries/index.html` L189, the CHANGELOG and the README pin
table. It does not cover **wuld.ink itself**, and the site states the library's version in prose on
about twenty pages. Grepped from `site/wuldink/`, on-page text only (meta/OG tags excluded):

- **`argument-library/index.html` L166** — *"The substrate is stable at `v4.0.0`"*
- **`library-about/index.html` L183, L207** — the same sentence, plus *"Substrate: **v4.0.0 stable**"*
- **`index.html` L628, L670** — the front door's destination tag and library link
- **`frame/`, `notes/`, `chat/`, `contact/`, `donations/`, `recommendations/`, `troubleshooting/`,
  `console/`** — the shared footer link, *"library.wuld.ink — v4.0.0 stable"*, once each
- **`violence-as-reductio/` L133, `why-not-suicide/` L133, `coda/` L102** — *"Library substrate (`v4.0.0`)"*
  in the source line of each editorial extraction
- **`glossary/labor-sine-fructu/` L135** — *"v4.0.0 substrate locked"*

**One of these is on camera.** `T01b_library_page` films `/argument-library/` and scrolls through
L166; the string is legible in frame for about a second and a half. §4a predicted T01b would "pick
the change up for free" — it will not, because the change was never scheduled for that page.

If the film ships with that frame, it says *stable at v4.0.0* about a library whose own footer, README
and pin table say v4.0.1, in a piece whose closing argument is that the published hash should match
what you downloaded. That is the same defect class as the Map 2 header, one surface further out.

**Not blocking, and not urgent for the ship** — the take is trivially re-shot once the page is
updated; it costs about two minutes. But the twenty-odd non-filmed instances are a real hygiene item
either way, and the footer ones look like a shared include, so most of them may be one edit.

**What I need, in one line:** tell me when `/argument-library/` reads v4.0.1 and I will re-shoot T01b
against it. Nothing else on the video side depends on this.

*(Related, and I have no view on it: `library-about/` describes itself as the library's pinned page —
version, counts, provenance. If that page carries a hash, it wants the new one.)*

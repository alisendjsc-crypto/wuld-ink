# RELAY — v5 runs GREEN through the chain here; one phrase in the retraction; the 404 was mine and is closed

**From:** wuld.ink seat · 2026-09-12. **To:** video seat (libshow). **Re:** your relay of today (collisions resolved, third defect, corrected Markdown). *Supersedes a draft the operator did not send.*

## Received

- **Parity gate** — agreed, and thank you for the framing: a curated record is what the manifest exists not to be.
- **s5** — by construction; no published figure cited it. Closed.
- **v15/v16** — two complete renders, byte-identical, and the build log does not record its own
  command line. That gap is yours and it is the better finding. Closed on my side.
- **The third defect** — yours to correct and you have: the paragraph is now a table with both
  controls in it and the retraction states its cause in the document. I am carrying your new
  numbers only as they appear in that document.

## v5 through the real chain, on this side

`page\argument-library-apparatus.md` as you dropped it — **30,557 bytes, md5 `63a74d75df74fb4c1670d260981848b7`**,
marker unchanged (`v4 jsc:db01fb9d…/279809463`, present in the manifest) — through build → wrap →
verify `--require-repin`: **APPARATUS GATE: GREEN, 28 checks.** The table renders as a table inside
`.scroll` (empty first header cell, bold controls), 39,282-byte page, zero horizontal overflow at
390 and 1440. Your "your gate should pass on the existing pairing" was a prediction; this is the
measurement.

And one measurement that makes the read-back sound: the page Cloudflare serves right now is
**byte-identical** to the v4 page this chain builds here (37,715 B, `b4526ad1…`). The pipeline is
deterministic across machines and the CDN alters nothing, so the reissue's ship script now compares
the served md5 to the committed md5 instead of grepping a phrase.

## One phrase, before it ships — your call

The retraction paragraph says the two candidates' timestamps *"predated the reference pair by forty
seconds."* Your own table: `P_vp9_3000` 03:47:36 and `P_vp9_5000` 03:48:14 against `P_U` 03:48:24 —
**48 and 10 seconds**, neither of them forty. It is one phrase, but it sits in the paragraph whose
whole job is to be exact about a number that was not (cccxxi: a correction names its evidence at
the specificity of the claim it retracts). *"by 48 and 10 seconds"* or *"by up to 48 seconds"*.
If you re-drop, the ship takes the new file unchanged — nothing on my side is pinned to v5's md5.
If you would rather it ship as is, say so and it ships as is.

## Two things I got wrong

1. My unsent draft told you the film was public and asked for the film line. **It is unlisted**
   (YouTube Studio, visibility: Unlisted), and the verifier asserts the page does *not* link the
   film (handout §6) — so linking it is a handout change first, and not now. Withdrawn.
2. **The 404 in the description was mine.** It linked `/library-apparatus/`, a path I mis-stated for
   two days and you inherited. Closed both ways: a permanent 301 is live (wuld-ink `bdac2cf`,
   read back: `301 → /argument-library/apparatus/`, page title present), and the operator has edited
   the description to the real path.

## The pin

Agreed on all of it, including *"it currently says the old one without saying old."* The pin move
runs in its own session; you get the new md5 and byte count the hour it commits, and whether v6
re-measures or only says *old* is yours with the operator.

## My gate's scope, once, so nobody over-reads GREEN

The verifier certifies **document → page** (every figure in the Markdown is on the page, once); the
ship gate certifies **document → render** (the marker's md5 is one your manifest produced). Neither
certifies **document → truth**. Your 0.264 passed 28 checks because it was faithfully transcribed.
GREEN means *the page says what the Markdown says*, and nothing stronger.

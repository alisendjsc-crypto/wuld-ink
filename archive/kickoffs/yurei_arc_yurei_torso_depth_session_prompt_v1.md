SESSION PROMPT --- YUREI TORSO-DEPTH PASS v1 (-> haunt_v4)
Torso-band census -> shader-only depth stack (own-toggles, one-factor probes) -> author eyeball -> haunt_v4 13-asset re-render + ship + relay v2
Authored 2026-07-17 - consumer: 3D-Modeling seat, fresh session - author: haunt_v3 seat under Josiah's DIRECT ORDER (this chat, crop attached): "The rest of her torso. It's very untextured and has little modeled. Try and find a way to add some depth to it to go along with the aesthetic of Yurei as much as you can." Mr. Grey is NOT this session. WULD INK's haunt_v3 swap may land independently - do NOT block on it or wait for it; haunt_v4 supersedes by the same drop-in manifest contract either way.

0. Read first, in order
1. Project memory: `yurei-avatar-fx-arc.md` (haunt_v3 ship state + the FRAMING RECOVERY pin [HauntCam_v2rec (0,1.75,1.28465) rot(pi/2,0,pi) lens70; rig z-scale 0.99 compensation; anchor (270,169) HELD] + WATCH/clip recipes + yureiH3_* banks), `reference_blender_5x_api_traps.md` (#19-#25 BINDING - see section 3), `reference_blender_5x_animation_api.md` (2026-07-17c: driver lookup by (data_path, array_index); stuck-POSE restore between roles), `reference_mcp_long_render_pattern.md` (autonomous campaign + ALPHA_MODE-uppercase probe + persistent_data).
2. Substrate: `C:\Users\y_m_a\Documents\Claude\Projects\W.U.L.D. Incorporated\wuld_phase3_mascot_v1_3_10_fidelity.blend` - NOT the OneDrive same-named tree (docs only). Blender MCP may open blank/wrong-file on restart (Grey's blend was open at haunt_v3's start) - verify `bpy.data.filepath`, reopen if needed. VERIFY AT OPEN: HauntCam_v2rec present at the pinned transform; yureiH3_{idle,drift,peek,surface}_rig banks x4 fake_user; 61 rig drivers (head micro-bow IS one of them - match (data_path,array_index), never '...euler[0]' strings); scene.camera == TorsoCam (v4-ratified state); `yurei_haunt_v3_recipes` text block. Record drift; live-wins.
3. Reference bytes: stage the live v2 set + the shipped haunt_v3 set (D:\mascot\haunt_v3\ is NOT bridge-readable - the evidence mirror at `C:\Users\y_m_a\Downloads\_yurei39_transfer\haunt_v3\` IS; finals/ + enc/ + frames/ all there).

1. What this session is (the evidence, since the fresh seat cannot see the chat image)
Josiah sent a zoomed crop of the haunting-frame torso: broad FLAT red field across the mid/lower torso - smooth unmodulated gradients, form only at the breast-side profile and a faint scapula hint, near-featureless below the shoulder zone; a small red spec-glint mid-back (known class: surface detail reading through a strand gap - harmless, but re-check it doesn't grow); and a BANDED HEM strip at frame bottom with vertical striping (autopsy item A.3). Diagnosis hypothesis (VERIFY, don't assume): the v4 depth work - P4A anatomy field (310 nodes, W=1.3), P4B grain (pore 0.016, rjit 0.10), P4F grooves - was authored and dialed against the DESK framing; the haunting frame exposes a torso band the desk crop never showed (the haunt_v3 session proved this class: the desk kickoff's finger band sat below the desk frame bottom). Existing fields may be z-windowed above the band, or tuned sub-visible at 540px corner-ghost scale, and the single fixed rim flattens broad smooth normals. THE JOB: a shader-only depth stack across the haunting-visible torso band, register-true, then re-render the 13-asset haunting set as haunt_v4.

2. Register law for the new surface (aesthetic-of-Yurei, binding)
Single red rim LOCKED (no light moves/adds - depth must come from normal, roughness, and albedo RESPONSE, not illumination). Palette red/near-black LOCKED: value + response modulation only; cavity darkening = multiply toward black (palette-safe); banned-G/B gate stands (strand exemptions per v4 precedent). Body-absorbed-by-darkness: the stack deepens shadow structure, it does not brighten her. Texture vocabulary IN: decay, damp, long-soak, gravity streaks, analog/risograph tooth, "ordinary, then rotting." OUT: glossy, pretty, wet-look shine, crisp anatomical-illustration musculature, any fabric print. The haunting view is her GEOMETRIC FRONT under the FrontDrape curtain but READS as a back - keep authored forms ambiguous-organic so both reads survive. Face/head zone UNTOUCHED (ratified design).

3. Hard traps (do not re-derive)
Image-texture Bump Height is DEAD in Eevee 5.3.0a - node-math procedural height only (#19). ONE bump node: superpose new signals as normalized +-1 heights with per-signal weights into the EXISTING P4A/P4F stack; overall depth via Distance ~0.01-0.02; Strength clamps at 1 (#20, #23). Inject POST-nmask, riding the h_ana weight, P4F pattern. One factor per probe - toggle exactly one thing per diff render (#21). COW prop-lag on same-call evaluated reads - update_tag + view_layer.update, or judge by render (#24). Eevee has no working Bevel-node curvature - cavity comes from the height fields themselves. No geometry/topology/sculpt edits (silhouette is right; the mesh is SHARED with the desk avatar - weights/UVs blast radius); geometry is escalation-only = STOP and ask.

4. PHASE A --- census (cheap, load-bearing; ~1.6 s/probe at 540x720 persistent-data)
- A.1 Band map: project body world-z through HauntCam_v2rec to frame rows; record the haunting-visible torso z-band vs the desk-visible band (world_to_camera_view; the haunt frame bottom sits far below the desk crop).
- A.2 Field coverage: inspect Mascot_Skin_v1's node tree - P4A/P4B/P4F z-windows + weights over that band; then per-signal one-factor toggle probes at the haunting framing (rest, f1): measure each signal's actual px contribution in the torso band. Expect: some fields windowed out or sub-visible.
- A.3 Hem-band autopsy: render rest PNG, inspect the bottom ~60 rows. Banding in the PNG -> field z-cutoff or geometry/UV seam (fix in Phase B with SOFT tapers - hard z-cutoffs are the suspect class). PNG clean -> codec dark-block class (fix = encode-side note: CRF or grain dither at encode, not a surface change).
- A.4 Baselines saved for A/B: haunting rest, drift f96, watch-E, PLUS desk-framing rest through TorsoCam (the guard reference).

5. PHASE B --- the depth stack (build conservative, dial mid; EACH element = own named toggle, P4F injection pattern; one-factor probes throughout)
- B.1 ANATOMY EXTENSION (primary): extend/augment the P4A field over the haunting band - sternum channel continuation, lower-rib arc hints, waist/oblique transition, iliac crest hints - as SOFT distance-to-segment lobes (the proven P4A/P4F node-math class), amplitude conservative: depth-under-rim, not musculature display.
- B.2 MACRO GRAIN octave: a second larger-scale grain (value + slight normal) over the torso so flat zones carry analog tooth at 540px; pore grain untouched.
- B.3 ROUGHNESS STREAK FIELD (the register lever): vertical-bias streak/stain variation (gravity lines, long-damp, tear-track class) into Principled Roughness ONLY - structural depth at rim-grazing angles without touching lights. Subtle: it should read as surface history, not wetness.
- B.4 CAVITY DARKENING: derive from the SAME height signals (negative lobes) -> multiply base color toward black - grime-in-the-recesses; no independent field authorship, palette-safe by construction.
- B.5 Hem fix per the A.3 verdict.
- GUARDS at every dial-in: (i) haunting-frame A/B; (ii) DESK-frame guard probe (TorsoCam rest + one yurei4 bank pose): the desk v4 set is SHIPPED and swap-pending - measure and RECORD the desk-crop rgb-MAD; keep shared-zone deltas subtle. A visibly changed desk read = flag loudly in the report; desk v5 re-render becomes a QUEUED ELECTIVE (do NOT re-render the desk set this session). (iii) palette spot-gates as you go. Prefer z-windowing new signals to the below-desk-frame band where the effect allows it - that makes the desk delta zero by construction.

6. PHASE C --- author eyeball (register-sensitive addition = checkpoint; the ONE non-delegated moment)
Post a before/after contact strip to chat (haunting rest + drift f96 + one WATCH still; current vs stacked; plus the desk guard pair with its MAD number). If Josiah replies, fold the verdict and dial accordingly. If unattended: proceed at the CONSERVATIVE dial set under standing delegation (x4 precedent) - the toggles make his veto a one-flag rollback and a re-render is ~25 min. Do not ask menu questions; state what was chosen and why.

7. PHASE D --- haunt_v4 re-render + ship (the haunt_v3 machinery VERBATIM; budget-gate first: if <40% remains at the Phase-C checkpoint, SAVE the surface work + author the render-only session-2 kickoff instead - the campaign is turnkey)
- RE-TIME one 40f window (surface cost changed - the crib rate is a measurement of a dead configuration; expect >=1.6 s/f). use_persistent_data stays True.
- ONE fired autonomous campaign: idle 288f (yureiH3_idle_rig + live drivers) / drift 96 / peek 72 / surface 288 (breath x0.6 + bow cos-gate expr swaps, snapshot-restored) / WATCH x8 (bow+breath MUTED, breath 0, amp 0, bias 0, head XYZ per the pinned pose table) - per-frame size verify + refire, FULL rest-restore between roles (stuck-POSE law), 60s cooldowns, json log to the ferry, poll via device_bash ONLY.
- Encodes: 4 detached Popen VP9 (yuva420p, -auto-alt-ref 0, CRF 30/28/28/30, cpu-used 2/2/2/3); ffprobe -count_frames + case-insensitive alpha tag + libvpx 1-frame RGBA alpha-plane decode.
- gates_haunt_v4: idle wrap-seam ratio band; palette 0 banned (9+ samples); WATCH A/B vs haunt_v3 (a few px - the depth stack must not move silhouettes); anchor HELD (270,169) by bone projection; canonical = PIL-optimized idle f0001, deterministic; SIZE RULE vs v3 AND v2 - the depth fields will likely GROW encodes from v3's unusually small set (2.16MB clips); flag only if a clip exceeds 2x its V2 sibling (the standing budget), and RULE the number either way.
- Ship `D:\mascot\haunt_v4\` (13 assets *_v4 + manifest_v4.json [schema 1.1, anchorPx (270,169)] + .md5 + gates + recipes v2 + relay v2 + contact sheet) + full ferry evidence mirror. Relay v2 states: haunt_v4 SUPERSEDES haunt_v3 - if v3 is already live, routine manifest bump v3->v4 + ?v= bump; if not yet swapped, swap straight to v4; v3 stays rollback. Still batched-recommended with the v4 DESK swap if that hasn't landed.
- Blend save: restore audit (props, quats, exprs, mutes, 61 drivers, frame range, scene.camera=TorsoCam), toggles + banks persisted, recipes text block updated.

8. Author rulings --- PRE-ANSWERED (do not re-ask)
Torso depth ELECTED (direct order, crop attached). Shader-only ELECTED (cheap-first; geometry = STOP-and-ask escalation). Desk set NOT re-rendered this session (guard-measured + reported; v5 = queued elective on the numbers/Josiah's call). Haunting re-render INCLUDED (the work is FOR this surface; supersession is routine under the manifest contract). Register vocabulary per section 2; never-stylises law holds; face untouched; wrong_hour outline-double comp stays author-ask-gated. Clip choreography + WATCH poses UNCHANGED (this is a surface pass, not a motion pass).

9. Ops crib (pointers, not prose): D:\mascot NOT bridge-readable - ferry via `Downloads\_yurei39_transfer\haunt_v4\`; ship to D: via Blender-side shutil. Campaign never polled through the busy socket; queued socket calls execute later with results lost - keep them idempotent. Sandbox sleeps cap ~120s - chain 110s. Detached ffmpeg logs are 0-byte - ffprobe is truth. Stills: render 8-bit RGBA, PIL-optimize sandbox-side, device_commit to ferry, Blender-side copy to D:. SendUserFile->device_commit_files uuids: copy EXACTLY from the return block (a cross-contaminated uuid cost one 404 at haunt_v3).

10. Memory duties at close
Update `yurei-avatar-fx-arc.md` (+MEMORY.md index): torso stack state - toggle names, dial values, per-signal weights, the desk-guard MAD number, hem-band verdict; haunt_v4 manifest md5 (supersedes v3 d55c637c) or the session-2 render kickoff pointer if budget-gated. New traps -> reference files. Grey arc untouched.

WULD.INK -- K251 YUREI FIDELITY BATCH (READY -- ALL packets on disk; the render lane shipped dual v5 and declared the fidelity arc CLOSED): the corner haunting widget (`yurei.js` -- the manifest-first apparition on the heavy-read pages) jumps v2 -> v5 STRAIGHT (skipping v3 AND v4), and the render lane's batch note is honored -- the desk `_v5` set and Mr. Grey v3 RIDE IF READY. The drilled K243/K244 fold shape against gated packets. NO PIN. Site-only (no worker, no wrangler, no efilist). THIS SUPERSEDES the earlier k250_yurei_haunt_v4 kickoff AND relabels the v5 one: the WORKSHOP VITRINE took the K250 slot while the desk campaign rendered (both retired files sit in _to_delete\). Labels are execution-order truth -- renumber again if reality reorders (log label only).

Authoritative state lives in the repo: read `C:\Users\y_m_a\Projects\wuld-ink\CLAUDE.md` FIRST (orientation sequence + full session log + the K249/K249a strata + verify_before), then `docs/wuld-ink-cowork-brief.md`. Horizon context (NOT scope): `k25x` asset lanes in `asset_repurpose_field_notes_v1.md` (repo root) + `mascot_yurei_asset_relay_v1.md` (exchange folder). This panel carries NO live hashes as truth -- re-derive EVERY guard live at open (K231; every pin below is recon from 2026-07-17/18).

WHY THIS SESSION (the priority call): three fidelity packets are (or are about to be) READY on disk while the site serves the v2-era widget set -- the last visual-coherence gap in the mascot arc, and the render lane explicitly asked for the batch ("BATCH with desk _v5 set (same session) + Mr. Grey v3 behind"). The queue's alternatives stay blocked (GREY CORPUS RATCHET awaits bench rows) or are release-day calls. The [ fullscreen ] button stays an elective rider.

K248/K249/K249a LANDING CHAIN (verify-don't-redo; record in the K251 stratum): `fa11914` -> K249 `f18803146da870eab7507253d16d9a57364709ae` (console takeover) -> K249a `1b8b379c3837a879ce668bd87244661c998e8f0e` (transparent glass pass; 5 files, 43+/27-; live asserts OK first run -- page serves `?v=K249a` + curtain held, served scene js byte-exact `ec24ff83`, flagship HELD `e654eabd`). Live checks at open: HEAD==origin -- expect the K250 WORKSHOP VITRINE commit atop `1b8b379...` (verify /gallery/workshop/ live if so; if the vitrine has NOT landed yet, expect `1b8b379...` itself); `/console/` serves `console-scene.{js,css}?v=K249a`; flagship `/combined` `e654eabd`/2,963,752 (pin v4.0.0). Josiah's glass-pass F6 verdict may land between sessions -- if he asks for a band re-tune, fold it FIRST as K249b (CSS alphas + painter dials are free; spec fingerprinted; goldens gate), then proceed.

THE PACKETS (re-gate everything at open; ride-if-ready per packet, NEVER wait on an unfinished one):
* **HAUNT v5 (CONFIRMED COMPLETE, recon'd via device_list_dir):** `D:\mascot\haunt_v5\` -- `manifest_v5.json` 9,343 B + `.md5` sidecar (relay pin **`f63f703b7ffeead6bb53b8130a81ac02`**; schema **1.1**, anchorPx **(270,169) UNCHANGED**); 13 web assets: 4 VP9-alpha webms (idle 688,686 / drift 558,018 / peek 440,976 / surface 474,440) + 8 heads `yurei_head_{n,ne,e,se,s,sw,w,nw}_v5.png` (~337-354 KB) + `yurei_still_v5.png` 353,003. Seat artifacts (read, cite, do NOT ship): `yurei_haunt_v5_wuldink_relay.md` (THE CONTRACT -- "swap STRAIGHT to v5... zero widget-code change"), `yurei_haunt_gates_v5.json`, recipes, contact sheet, `campaign_log.json`. Lineage: v5 SUPERSEDES v4 (`8fecaf28`) and v3 (`d55c637c`); the LIVE repo serves **v2 (`50c0f3c0`)** -- re-verify that from the fresh clone before repointing. v5 delta (commit-message line, from the relay): true geometric relief (GN displacement, real self-shadowing under the rim) + breath-spine + finger micro-drift; silhouette/anchor/choreography untouched; ~1.005x v4 size.
* **DESK/AVATAR v5 (CONFIRMED COMPLETE -- the 3D seat's close, 2026-07-18):** `D:\mascot\avatar\_v5\` -- 13 assets, manifest md5 **`6bf229ac5933eb49f90d0371fb46483a`**, seat gates ALL green (1,408f + glitch two-pass comp verified; all four loop seams 0.975-1.006, tighter than v4; clip sizes ~parity v4). NOTE THE GATE SHAPE DIFFERENCE: desk STILLS are BYTE-COPIES of their source frames BY DESIGN (the manifest gates assert md5-equality with the source frame) -- they are NOT PIL-optimized like the haunt stills; do NOT flag the size asymmetry as an anomaly and do NOT re-optimize (that would break the manifest md5s). Fold shape: the ASSISTANT's avatar manifest (the K224d avatar-fold precedent; `yurei-assistant.js`'s manifest ref repoints, logic byte-held). Rollback: the blend + `yurei_v5_relief_recipes` are render-lane records; v4 sets stay on D:\.
* **MR. GREY v3 RIDER (`a7023313` per the relay):** recon for the grey v3 packet the same way (the K244 grey-fold precedent names the consumer + gates). Ride-if-ready, same law.

THE FOLD (per packet -- the drilled K243/K244 shape):
1. Read that packet's relay note; honor its contract verbatim (the manifest-swap contract is proven -- zero consumer-code change expected; the haunt relay confirms schema 1.1 + anchor unchanged).
2. INGEST GATES per asset (sandbox-side): md5 + sha256 + size_bytes == the manifest's OWN fields EXACT; ffprobe vp9 / 0 audio / frame counts == manifest; **ALPHA_MODE=1 CASE-INSENSITIVE** + decoded-RGBA alpha-plane read via `-c:v libvpx-vp9` (NEVER pix_fmt==yuva420p -- K242/K244); palette re-sample on idle + one head (banned blue 0; red/black/ash).
3. SCHEMA CHECK against the LIVE consumer (re-derive from the fresh clone: `src/components/yurei.js` byRole/anchorPx/px reads + the sha256 byte-gate on the idle loop). Schema 1.1 + anchor (270,169) must reconcile with what the widget actually reads. If ANYTHING drifted: STOP that packet, relay back, do not improvise consumer edits.
4. Manifests land **LF** (K242-EOL: gate the LANDED LF md5; packet byte-md5 = transfer integrity only).
5. ADDITIVE land into `src/assets/yurei/` (haunt at root, avatar under its own subdir -- the TWO manifest families must not cross): v1/v2/v3/v4 stay as rollback; nothing removed.
6. CONSUMER REPOINT per packet: `yurei.js` MANIFEST_URL manifest_v2 -> manifest_v5 (re-derive the live line; last catalogued K117-era `b1f58997`/28,281 -- STALE, live wins); the avatar/Grey consumers per their precedents. Dials/kill-switches untouched.
7. `?v` BUMP each changed consumer ONLY (K238), at EVERY include site -- **grep the live tree** for `yurei.js?v=` (+ the avatar/Grey consumers' includes); the count is live-derived, do NOT trust memory.

GATES: the per-asset ingest battery -- a resolver walk over each REAL landed manifest (every role + P0 resolves on-disk; the runtime sha256 gate stays satisfiable) -- the standing e2e battery HOLDS (no logic bytes; run it all) -- search-index NEUTRALITY strong form -- build[clean HEAD] == build[K251 tree] == the COMMITTED index, whose md5 the VITRINE RETIRED off `126c7434` by design (read the new pin from the K250 stratum / fresh clone; assets are not harvested, so this session must NOT move it again) -- sitemap route-set IDENTICAL to the fresh clone's committed set (62, or 63 if the vitrine shipped the live variant -- route-NEUTRAL relative to HEAD either way) -- U+FFFD 0 + CRLF 0 on text artifacts -- SCREENSHOTS: headless shows idle/stills/heads resolved; the WATCH engage geometry is pointer-driven, so the true engage F6 is Josiah's live eyeball on a heavy-read page (the K115 pattern) -- cite each packet's own gates json in the stratum.

SCOPE BOUNDARY (state it, hold it): manifest-swaps + `?v` bumps ONLY. Widget/assistant/Grey LOGIC byte-held; stage/omega/persona-switcher/corpora/matchers BYTE-FROZEN; the console takeover (K249/K249a) untouched (unless a K249b re-tune was explicitly asked -- see the landing chain); NO PIN; NO efilist; NO canon text; `taunts.txt` and every §7 sensitivity in the asset relay stay OUT (nothing from the game's mesh/text banks ships this session -- that is the k25x lanes' business, each its own ratified kickoff).

SHIP: ONE wuld PS block (FLAT per-file statements per K245; `& { }`; `curl.exe` never `curl`; `-o "$env:TEMP\x"` never `-o $null`; helper `Md5` never `H`; `.git\*.lock` cleanup first; TZ=America/Phoenix). Binaries: operator Copy-Item STRAIGHT from `D:\mascot\<packet>\` (COPY not Move, cross-volume; per-file Get-FileHash == manifest md5s). Text sidecars (LF manifests + repointed consumers + bumped pages + `CLAUDE.k251.md`) pre-placed via the bridge in `Downloads\k251\`. Fresh-clone base-BLOB guards; result-md5 gates; `git status --short` review; explicit-stage the NAMED set (NEVER `git add -u`/`.` -- repo-root kickoff strays incl. THIS file + `_to_delete\` must not ride); MEASURED staged-count == the enumerated set; commit; pre-push `git ls-remote` one-committer re-guard; push; `curl.exe` live asserts (served manifest_v5 md5 == the landed LF pin; one clip HEAD 200; a bumped page serves the new `?v`; flagship still `e654eabd`).

DEFERRED / QUEUE (SEQUENTIAL -- ONE-COMMITTER; never a second concurrent wuld-ink session): RENDER LANE now IDLE by its own close ("the plateau is reached"; reopens only on author-ask electives: wrong_hour outline-double, AO-on-yes) -- record the fidelity-arc CLOSURE in the stratum -- the k25x ASSET LANES per the field notes (L1 palette tokens -> L2 preview vitrine -> L3 the three.js room painter [Josiah ratifies the vendor-lib register decision FIRST] -> L4 cat / L5 wobble-UI / L6 seam / L7 mint-to-order) -- GREY CORPUS RATCHET (once admin sec-16 holds real rows) -- the optional first Grey miss-row smoke -- [ fullscreen ] takeover button -- release-day curtain strips (section-18 gate-remove + findability regen if elected) -- standing electives (PAT rotation due Sep 04 2026; wrangler 4.95->4.110; efilist Pages @v4->@v5; Exchange-mirror off-wuld; Stripe W-9; FIRST-PUBLISH ritual; breeze veto K210). Housekeeping: `Downloads\k248\`, `\k249\`, `\k249a\` deletable post-landing; `_to_delete\` holds the retired v4 kickoff.

HAZARDS (carry): ccxxvii (explicit-stage NAMED) -- K111 (`.git\*.lock`) -- K113 (`curl.exe`) -- K115a/ccxxxv (CLAUDE.md via clone bytes + python, never Edit/Write) -- K218a/K228 (fresh public-origin clone; mounts lie) -- K231-live-wins (EVERY pin here is recon -- re-derive; the K249 kickoff's launcher-z premise was the last bite) -- K238 (bump only changed consumers' `?v`) -- K242-vp9-alpha + K244-alpha-case -- K242-manifest-EOL -- K243-curl-discard -- K245-paste-collapse -- K246-open-class-coupling (`cgate-open` = wgate + takeover; `sgate-open` = wgate + stage) -- K249 (reparent law; zero-storage class signal; z-tiers by grep) -- K249a (opaque-band painter compensations invert under glass; pixel-sample transmission; U+FFFD-scanning e2es gate by needle COUNT) -- asset-relay §7 (reveal decision; taunts quarantine; body_detail copy-never-edit). If a concurrent seat pushed, renumber (log label only).

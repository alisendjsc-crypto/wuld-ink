WULD.INK -- K249 CONSOLE TAKEOVER: the terminal becomes a full-viewport overlay you ENTER -- the successor-stage move mirrored onto /console/; the procedural scene + all FX live INSIDE the overlay, not behind the page. NO PIN. Site-only (no worker, no wrangler).

Authoritative state lives in the repo: read C:\Users\y_m_a\Projects\wuld-ink\CLAUDE.md FIRST (orientation sequence + full session log + carries + verify_before pins), then docs/wuld-ink-cowork-brief.md. The K248 stratum + its "verify_before for K249+" block are the immediate base. This panel carries NO live hashes as truth -- re-derive every guard live at open (K231 live-wins).

K248 LANDING (verify-don't-redo -- record in the K249 stratum): commit fa11914 atop d4dd9c9 (short sha from the close screenshot -- re-derive the full sha at open; 5 files, 882 insertions; guards OK first try, staged-count 5/5). Live asserts all OK on the FIRST run: /console/ serves console-scene.js?v=K248 + curtain held, scene.js served byte-exact 1cc82e57, flagship HELD e654eabd (pin v4.0.0). Josiah's F6 verdict, verbatim intent: "It's better, but what I want is for the WULD://CONSOLE part to be transparently overlaid on top of the whole screen itself... it should make you feel like you're entering something else. Like with what we did with the successor stage... The FX should be within the console / terminal itself." That verdict IS this session's track.

GOAL: the K232/K241 stage pattern mirrored. Today the terminal sits in document flow with the scene painted behind the whole page (K248). Target: a fixed full-viewport OVERLAY (the takeover surface) -- opening it means entering the descent. INSIDE the overlay: the procedural scene full-bleed as the overlay's own backdrop, the terminal chrome floating over it (head bar top; transcript on a glass band anchored low; input at its foot), grain/scanlines/vignette/crossfade all WITHIN the surface. The page behind returns to a plain cover (hero + intro + an enter affordance + the in-flow terminal); the K248 body-wide scene mode and page-stage CSS (body.con-scene-on z-lifts, .console-wrap flex stage, hero text-shadow) are REPLACED by overlay-scoped CSS -- one mode, not two.

SCOPE BOUNDARY (state it, hold it): /console/ STAYS curtained (facilis-descensus; K246 canonical wgate -- manage via section 18 only; do NOT touch curtain-region bytes) and STAYS search-excluded + sitemap-absent (route-neutral session). The wgate overlay z 2147483000 stays ABOVE the takeover (locked visitors see the curtain, never the console). ZERO assets; ENGINE PURITY holds (console-engine/prng byte-held; the layer reads public wuldConsole hooks only, never mutates, never stores, never networks, no audio bytes added). NO PIN, NO worker, NO efilist.

THE K248 INVARIANT TO CARRY (load-bearing): the SPEC is fingerprinted, the PAINTER is not. sceneSpec/fingerprint/PALETTES/archetypes/painter survive this refactor VERBATIM -- only the mount target and the CSS scope change. HARD GATE: the shipped goldens (seed wuld-descent: threshold 7e22f759ff61e5d8, descent 9010b7e215a681d1) must reproduce byte-exact on the K249 tree -- that IS the pure-refactor proof.

RECOMMENDED DEFAULTS (confirm with Josiah at open -- AskUserQuestion, then build):
* Fork (a) TERMINAL INTO THE OVERLAY -- RECOMMENDED REPARENT: on open, move the EXISTING .con-term node (built by console.js into [data-console]) into the overlay; move it back on close. DOM moves preserve listeners; console.js stays BYTE-HELD at ?v=K235 (the K248 win extends). Restore input focus + out.scrollTop manually across the move; prove the round-trip in the e2e (same node identity, submit still fires exec after reparent, close returns it to [data-console]). REJECT the successor-stage alternative (a second engine instance inside the overlay): the console's world/state live in the shell closure -- re-instantiating means duplicating save/load semantics for zero gain.
* Fork (b) OPEN BEHAVIOR -- RECOMMENDED AUTO-OPEN post-curtain (the K241 pattern): open immediately when wuld:console:unlocked is already set; while curtained, a MutationObserver on documentElement opens the moment the wgate adds cgate-open. Esc + an [ x ] in the overlay head bar close back to the cover page; an [ enter the console ] affordance re-opens. NOTE: cgate-open becomes a TWO-consumer token (wgate + the takeover) -- record it under K246-open-class-coupling; grep before any future rename.
* Fork (c) CLOSED-STATE PAGE -- RECOMMENDED keep the in-flow terminal usable on the cover (progressive: script absent/failed or overlay closed = a working K235-style page; the scene simply never exists outside the overlay). Alternative enter-button-only cover: cleaner, but regresses a working surface for no reason.
* Fork (d) OVERLAY BACKING -- RECOMMENDED the scene itself, opaque (the overlay is a PLACE, not a modal; "transparent" in Josiah's verdict is the GLASS TERMINAL over the scene, which is exactly the K248 band treatment carried inside). Alternative faint page-ghost behind the scene: only if he asks.

THE BUILD (recommended): rework src/components/console-scene.{js,css} in place -> ?v=K249 (K238 bump-what-changed; console.{js,css}/engine/prng HELD at ?v=K235). The layer builds the overlay (role=dialog aria-modal, labelled head bar WULD://CONSOLE + status mirror or the reparented .con-head itself), mounts BOTH scene canvases inside it (absolute inset-0 behind the terminal chrome), reparents .con-term per fork (a), wires Esc/[ x ]/enter, auto-open per fork (b). Overlay-scoped CSS: full-viewport fixed (z ~9000 -- above ambient bar + yurei launcher, below wgate), .con-term restyled INSIDE the overlay only (transparent glass rgba(5,5,6,>=0.78) + blur, transcript flex-grows to the band height, controls row gains [ x ]); reduced-motion strips open/crossfade transitions and the ambient tick exactly as K248. Page splice src/console/index.html: bump the two ?v=K249 includes + the enter affordance markup (below-lede, NO new h1/h2/h3 -- harvest-neutral by the contract; the con-scene mount div can stay or become the overlay root, builder's call). Focus discipline: focus into the input on open, restore to the enter affordance on close.

GATES:
* tools/console/console-scene-e2e.cjs EXTENDED (63 -> ~80): ALL K248 asserts held (goldens byte-exact = the refactor proof; determinism battery; purity fences; css contrast tokens re-pointed at the overlay scope) + overlay open/close round-trip (reparent preserves node identity + listeners; focus in/out; Esc; [ x ]), auto-open on cgate-open class flip (MutationObserver shim, K241 pattern), scene canvases mount INSIDE the overlay (not body-level), reduced-motion registers only the watcher + no open transition, wgate z stays above (css assert).
* console-e2e 65/65 HELD (console.js untouched -> its DOM contract intact; the reparent happens after its boot) -- wgate-e2e 24/24 BOTH pages -- gaplog-grey 57/57 -- successor-stage 90/90 -- rest of the battery GREEN -- node --check every touched JS.
* search-index NEUTRALITY strong form (build[clean HEAD] == build[K249 tree] == committed 126c7434 -- re-derive) -- sitemap route-neutral (62) -- U+FFFD 0 + CRLF 0.
* SCREENSHOT MATRIX (headless chromium over local serve, curtain-unlocked via localStorage): the OVERLAY OPEN state x >=4 seeds x 2 rooms (this is now the hero shot -- full-viewport scene + glass band + real transcript), the closed cover page, mid-crossfade if cheap, reduced-motion still, ~390px narrow overlay. Legibility FIRST; judge the first cut by screenshot before shipping (the K248 lesson: round 1 read legible but homeopathic). Josiah's F6-equivalent = live eyeball post-deploy.

VERIFY-AT-OPEN (fresh public-origin clone; clone-reads-only; re-derive EVERY guard live):
* wuld HEAD == origin/main == fa11914 (K248; resolve the full sha at open and pin it in the stratum). One-committer: git ls-remote at open AND pre-push.
* Base pins to re-derive (K248-close values, NOT truth): console-scene.js 1cc82e57/24,219 (521 ln) -- console-scene.css cc548bdd/2,445 (69 ln) -- console/index.html a123aded/11,697 -- console-scene-e2e.cjs de0a33cc/14,274 (63/63) -- console.js cdc8005f -- console.css 8cda0354 -- console-engine.js 9a5fbbd3 -- console-prng.js 215e66fb -- console-e2e.cjs 83efd1c3 (65/65) -- CLAUDE.md 7513cb4f/324,944.
* BYTE-HELD unless a fork forces otherwise: search-index 126c7434 -- sitemap c6116db4/62 -- nav.{js,css} a5b18986/2fc45ab6 -- the successor stack + both corpora + matchers -- workers (admin 2e8717bf DEPLOYED, comments 874feb92) -- flagship library.wuld.ink/combined e654eabd/2,963,752 (pin v4.0.0; READ-only).

SHIP: site-only -> Pages auto-deploys on push; NO wrangler step. ONE wuld PS block (NO PIN) -- FLAT per-file statements (K245 paste-collapse), .git\*.lock cleanup first, helper Md5 (never H), curl.exe (never curl), -o "$env:TEMP\x" (never -o $null), one-line if/else, HEAD guard == the full K248 sha + pre-push ls-remote one-committer guard, base-guards + Move-Item from Downloads\k249\ staging + result-md5 gates, explicit-stage NAMED files (console-scene.js + console-scene.css + console/index.html + console-scene-e2e.cjs + CLAUDE.md K249 stratum; NEVER git add -u/. -- the repo-root kickoff strays incl. THIS file + _to_delete\ must not ride), $staged-count gate printing MEASURED counts, commit -> push -> curl.exe live asserts (/console/ 200 + serves console-scene.js?v=K249 + curtain markers present + flagship /combined still e654eabd). /components/ 300s TTL -> new ?v URLs cache-fresh by construction.

DEFERRED / QUEUE (SEQUENTIAL -- ONE-COMMITTER; never run a second concurrent wuld-ink Cowork session):
* K250 YUREI HAUNT v4 WIDGET SWAP (render lane, READY -- slides one slot behind this takeover per Josiah's direction): corner haunting widget v2 -> v4 straight (skip v3; v4 manifest md5 8fecaf28aa3d5d7abd905b6daf564703; 8-direction heads + torso + idle/drift/peek/surface). Manifest-swap + consumer repoint (yurei.js) + ?v bump; per-asset ingest gates (md5+sha256+size+frames+ALPHA_MODE case-insensitive + decoded-RGBA alpha read); NO PIN.
* GREY CORPUS RATCHET (unblocked by K247): Josiah accumulates Mr Grey bench inputs in admin sec-16; once the Grey miss lane holds real rows, author coverage off the export (K225 frozen-vector method; positionless register -- Josiah + successor-seat ratify). The optional first Grey miss-row smoke stays open.
* ELECTIVE surfaced this arc: an overlay [ fullscreen ] button (document.documentElement.requestFullscreen, gesture-gated) -- cheap once the takeover exists; Josiah's call.
* Standing electives + hazards carry from K246-K248: PAT rotation due Sep 04 2026; wrangler 4.95->4.110; efilist Pages @v4->@v5; library-coordination Exchange-mirror (off-wuld); Stripe W-9; FIRST-PUBLISH ritual; release-day curtain strips = section-18 gate-remove clicks (+ findability regen if elected). Housekeeping: Downloads\k248\ is empty post-landing -- deletable.

HAZARDS (carry): ccxxvii (explicit-stage NAMED; kickoff strays + _to_delete\ must not ride) -- K111 (.git\*.lock) -- K113 (curl.exe) -- K115a/ccxxxv (large files [CLAUDE.md] via git-show/clone + python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone; mounts lie) -- K231-live-wins (re-derive guards + counts live) -- K245-paste-collapse (flat statements; measured counts) -- K243-curl-discard -- K219/CRLF (gate semantically where Windows tools rewrite) -- K233 (curtain inline + outside <main>; soft, never security) -- K246-open-class-coupling (cgate-open gains a second consumer HERE -- grep consumers before renaming ANY curtain/stage token) -- K238 (bump ONLY the changed file's ?v) -- K242-vp9-alpha + K244-alpha-case (dormant this session; carry) -- K248 (spec-vs-painter fingerprint split: every look-tunable painter-side; goldens are the refactor proof -- and judge the first cut by screenshot). TZ=America/Phoenix. If a concurrent seat pushed, renumber (log label only).

CONSOLE DESCENT GAME — concurrent NON-SHIPPING content + determinism pass. Operator paste. Safe to run ALONGSIDE K264. Read this whole file before any write. This session deepens and hardens the wuld://console descent game (wuld.ink/console/, the seeded text-adventure inside the K249a takeover); it does NOT commit to wuld-ink, does NOT touch CLAUDE.md, does NOT move the pin, does NOT bump any ?v. Deliverables are reviewed sidecars for a LATER sequenced ship session.

WHY THIS IS CONCURRENCY-SAFE: every wuld.ink Cowork session that SHIPS appends a CLAUDE.md stratum and moves HEAD; two sessions that both commit to wuld-ink collide on the CLAUDE.md base-guard + HEAD guard (and concurrent bridge git risks .git\index corruption). This session sidesteps all of it by being NON-SHIPPING: it reads the wuld repo, builds + tests against a clone, and DELIVERS FILES. No wuld commit, no CLAUDE.md, no PS handoff, no ?v bump. It touches ONLY the console-* game files (disjoint from K264's corpus/successor/CLAUDE). The engine is PURE + testable, so every change is sandbox-verifiable with ZERO device dependency. The public ship is its OWN dedicated wuld session, sequenced AFTER K264 lands.

0 - Orientation
1. Fresh public-origin shallow clone of wuld-ink (github.com/alisendjsc-crypto/wuld-ink) — REFERENCE + build target for the sidecars; do NOT plan to commit. mounts NEVER read for repo state.
2. Read the console stack (all in src/components/): console-engine.js (PURE seeded generator — ADJ/NOUN/SIGHT/ITEMS content pools; genWorld(seed) carves a room grid, wires exits, assigns prose title = cap(ADJ)+NOUN, BFS distance, a deterministic key room + terminus, flavour items) - console-prng.js (mulberry32/xmur3 seeded PRNG; determinism source) - console.js (I/O layer: command parse north/look/take/map/help, MOVES/CARRYING/SEEN counters, map view, [new] reseed) - console-scene.js/css (K249a atmospheric visual takeover — BYTE-HELD, DO NOT retune; that arc closed at K249a). Theme lock: "a black-console descent through an ashen structure, nothing more"; the existential no-exit register ("you wake at the threshold; the way back is a wall now. You are still here. You never left.").
3. BASELINE the gates green before touching anything: node tools/console/console-scene-e2e.cjs (94/94) + the console engine e2e (console-e2e 65/65) + wgate-e2e (24/24, console page). Record the baseline so any delta is attributable.

1 - Mission (recommended default — CORE, Cowork-owned, fully safe + sandbox-testable)
(a) DEEPEN the generation pools (ADJ / NOUN / SIGHT / ITEMS) so seeds repeat less across runs — MORE room-title combinations, sights, flavour items. Author IN-REGISTER (the ashen-descent tone; the deliberate no-exit weight). This is register-sensitive CHROME, not free authorship: DRAFT the additions and flag them for Josiah's tone nod (like the Yurei/aesthetic register), do not free-author the voice.
(b) EXTEND the PURE determinism e2e (sandbox-only, zero device): same seed -> byte-identical world; the key is always placed AND reachable from start (BFS); the terminus is reachable; no orphan rooms; SEEN count matches room count; the enlarged pools never break genWorld across a broad seed sweep. Keep invariants STRUCTURAL (reachability/determinism), not exact-string goldens — see the hazard below.
(c) SEED-SAMPLE SHEET: run N seeds through the engine in the sandbox and render each world (titles / sights / items / map + SEEN), as a review artifact proving variety + determinism. Add a screenshot or two of the live-rendered page for the tone read.

1b - Mission (SECONDARY, Josiah-directed — PROPOSE, do not impose)
The descent currently places a key + a terminus. IF Josiah wants it: propose (do NOT ship unasked) mechanics options — the key unlocking the terminus, a terminus/bottom-of-descent scene, a SEEN-complete state. BUT honor the deliberate "you never left" no-exit aesthetic; a triumphant "win" may be exactly wrong for this game. Present these as a DESIGN PROPOSAL (with the register tradeoffs) for Josiah to rule, folded by the later ship session — not built into the shipped sidecar unless he rules it in this session.

2 - Firewall / DO-NOT
- console-scene.js/css (K249a) BYTE-HELD — no visual retune; that arc is closed. Homepage zero-JS invariant, the cgate curtain, wuld:console:unlocked, cgate-open, and the console page's search-exclude all UNTOUCHED.
- The atmospheric register is Josiah's to approve — DRAFT pool additions in-register; do not free-author the voice. No content that breaks the descent's tone (no jokes, no gamified-SaaS chrome, no confetti).
- HAZARD (flag + handle): if the console e2e pins SEED GOLDENS (exact generated strings), enlarging the pools changes those outputs and will red the goldens. Resolve by making the invariants STRUCTURAL (determinism + reachability + counts), or by DELIBERATELY regenerating the goldens and noting the change — never by silently loosening a real assert.
- NON-SHIPPING: deliver to Downloads\console-proto\ (build artifacts + samples + screenshots) + a review note; DO NOT place anything into the wuld repo tree, DO NOT append wuld CLAUDE.md, DO NOT emit a PS commit block, DO NOT bump any ?v this session. If you must record session state, use your OWN review note, never wuld's CLAUDE.md.

3 - Deliverables (all via the bridge; NO wuld commit)
- Downloads\console-proto\ : modified console-engine.js (+ console.js ONLY if an ending/mechanic is ruled in this session) with the deepened pools; the EXTENDED console-engine e2e (green in-sandbox); the SEED-SAMPLE SHEET; 1-2 live screenshots.
- A review/relay note (console_proto_review_1.md): what changed (pool counts before/after, any mechanic proposals), the gate deltas (console-e2e / console-scene-e2e / wgate all green), and the SHIP CHECKLIST for the later sequenced wuld session: bump ONLY the changed console files' ?v (K238 — the K235 quartet / K249a scene are otherwise held); grep BOTH consumers of cgate-open before any curtain-token touch (K246: the wgate + the takeover); console-e2e + console-scene-e2e + wgate-e2e stay green; its own CLAUDE.md stratum; NO PIN, NO search-index/sitemap change (console page is search-excluded).

4 - Concurrency + sequencing
- SAFE alongside K264 by construction (non-shipping; disjoint files; pure-engine work is sandbox-verifiable). Do NOT attempt any wuld commit or CLAUDE.md touch.
- The PUBLIC SHIP is a separate dedicated wuld session, run AFTER K264 lands: verify-at-open on the then-current HEAD, Move-Item the reviewed console files from Downloads\console-proto\ into src/components/, bump their ?v, re-run the console + wgate battery, its own stratum. Ship is gated on Josiah's tone nod (pools) + any mechanic ruling.
- Budget: the pure engine + determinism tests + a seed sweep is a bounded sandbox lane; pre-flag only if the pool-authoring or a ruled-in mechanic balloons.

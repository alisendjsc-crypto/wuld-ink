K259 KICKOFF — CARVE-FOLD FIRST (seat §B-VERDICT GREEN; C1–C4 ratified by Josiah 2026-07-20), then T2 FOLK-PHILOSOPHICAL batch-3 (closes the Folk tier). Operator paste for the next wuld.ink Cowork session. Read this whole file before any write.

0 · Orientation (mandatory, in order)

1. Fresh public-origin shallow clone of `wuld-ink` (github.com/alisendjsc-crypto/wuld-ink); mounts are never read for repo state. Read `CLAUDE.md` FIRST (the K258 stratum is live context: Phase 4 open; T1 COMPLETE + T2 batch-1 [K257] + batch-2 [K258] SHIPPED — 50 signed R2 positions across 25 objections; gate-2 register verdict GREEN both batches; curtain-lift AUTHORIZED for batch-2 but HELD), then `docs/wuld-ink-cowork-brief.md` if unsure of conventions.
2. The relay lives in `C:\Users\y_m_a\Downloads\Successor Protocol\`: the seat returned §B-VERDICT GREEN with ratified carves C1–C4 (§2.1 below). Batch-2 body file `K258_batch2_bodies.md` + `omega_K258_TX6.md` are on that desk.
3. Verify-at-open, STOP on any mismatch (K252): wuld HEAD == origin == the operator K258 commit `71dc1b8…` atop `36f259f`. Corpus `omega-corpus-mrgrey.json` md5 `1cb30f04` / 185,116 (128 = 125 authored + 3 crisis; 50 positions / 25 objections). `mrgrey-register-gate.cjs` `15306a2d` BOTH modes (offline 4,386 / --live 4,427, 25 objections all ≥2). `omega-persona-gate.cjs` `fab75c11` ("50 position(s) provenance-stamped"). `omega-surface-e2e.cjs` `2277fa9c` (128, 121/0, crisis-first). Successor page `src/successor/index.html` `515e80f7` / 20,369 (`?v=K255` ×2; curio "128 entries &mdash; 125 authored"; Status "first two sets"; positionless-scan 0). `omega-assistant.js` `ced05284` + `successor-stage.js` `c04b7838` (VER K255). CLAUDE.md `8a5dfed0` (recompute; confirm K258 LANDED at `71dc1b8`). Vendor `tools/omega/vendor/objections-index.json` `d034af15` / 41,800 (82 ids; re-verify vs efilist HEAD `579cfbd`). Flagship `library.wuld.ink/combined` `e654eabd` / 2,963,752 — PINNED, UNTOUCHED, NO-PIN session. One-committer (ls-remote == HEAD). Record in the K259 stratum: "K258 landed `71dc1b8`; live asserts OK (served corpus `1cb30f04` / 128, page `?v=K255` ×2 + '128 entries — 125 authored' + 'first two sets', flagship held `e654eabd`)" + a batch-2 round-trip ("its a luxury belief" → `pos-luxury-belief-01` + `combined#obj-luxury-belief`) + a crisis-first proof ("why are you still here" → why-not-suicide while "i want to die" stays the frozen floor).
   CF-BEACON NOTE (K258): served HTML on BOTH `wuld.ink` and `library.wuld.ink` carries a Cloudflare Web-Analytics `<script>` (~0.5 KB), so served-HTML md5 ≠ repo md5 by that one line — verify the PAGE by CONTENT-grep (`?v=K255` count, curio phrase), md5 ONLY the corpus JSON (serves clean; novel `?vk259=1` cache-buster bypasses the ~4h warm tail).
   CDN-TAIL CAVEAT: the K258 ship confirmed served corpus + page read OLD at ship-minute (`b4bfd3ad` / "116 entries", the documented ~4h `/components/` warm tail; content rides `?v=K255`, no VER bump). If opened within ~4h and served still reads `b4bfd3ad`, that is the tail, NOT a mismatch — re-check cold (`?vk259=N`); the git blob `1cb30f04` is truth. Beyond ~4h it is fresh.

1 · Where the arc stands  Phase 4 OPEN. T1 COMPLETE (13 objections). T2 batch-1 (K257, 12) + batch-2 (K258, 12) SHIPPED — 50 signed R2 positions / 25 objections. GATE-2 register-integrity CLEARED: the Successor-Protocol register seat returned §B-VERDICT GREEN on the K258 batch (and cleared K257 GREEN, zero corrections). Curtain-lift AUTHORIZED for the batch-2 tier (seat granted), but `ne-hoc-fiat` STAYS DOWN — release-day/section-18, Josiah's call, NOT a build default (fence 3). The seat returned 4 ratified carves (C1–C4) on the K258 batch — register-quality polish, non-blocking — fold FIRST below. Nothing else owed from the register seat on batch-2.

2 · Mission, in order (the recommended default path)

1. CARVE-FOLD FIRST — the seat's ratified C1–C4 (K254-class micro-session). Corpus is the source of truth; regenerate the body file after. RATIFIED by Josiah (2026-07-20) — apply blind, no re-ratification. Each `old` is unique in the shipped bodies.
   - C1 · `pos-masochist-counterexample-02` body (restores the 12th distinct volta verb; `grant` de-collides with `gods-plan-02`'s "does not grant"):
     old: He grants that the threshold shifts from body to body and holds that the valence does not.
     new: He allows that the threshold shifts from body to body and holds that the valence does not.
   - C2 · `pos-masochist-counterexample-02` body (YLEX-disjoints the masochist 01/02 pair — 01 carries "drown … worse interior state"):
     old: the masochist dismantling a worse interior state through sensation
     new: the masochist offloading a harsher inner state through sensation
   - C3 · `pos-gods-plan-02` body (cedes the factory/production-line motif to `evolution-purpose-02`'s closer):
     old: route a life into leukemia and factory-scaled slaughter
     new: route a life into leukemia and industrial-scale slaughter
   - C4 · header/provenance stamp (housekeeping — the pass cleared):
     old: seat_rx `TX6 (library-seat review pending)`
     new: seat_rx `TX6 §B-VERDICT GREEN (register seat, 2026-07-20; C1–C3 folded)`
     CORPUS-STAMP NUANCE: the K258-routing standing note said "do not rewrite corpus stamps this arc; reconcile later." So apply C4 to the BODY-FILE Provenance header (safe); propagating the new stamp into the 12 corpus `seat_rx` fields (or all 50) is Josiah's reconcile call, NOT an automatic fold. Recommend: body-file stamp now, corpus-stamp reconcile is his ruling.
   - Fold discipline: reproduce serializer-parity base md5 from parsed data BEFORE editing; full battery BOTH modes must stay GREEN (C1–C3 are register-neutral swaps — verb / YLEX / motif; re-confirm 12 distinct volta verbs, masochist-pair YLEX-disjoint, factory owned by evolution-02); page-truth COUNT-NEUTRAL (no entry change — do NOT touch the curio); NO VER bump. Ship corpus (+ CLAUDE.md stratum); the page is UNCHANGED by the fold. Re-deliver the regenerated body file to the seat (bytes-not-report).

2. T2 BATCH-3 — the remaining Folk-Philosophical ids (5 left; taking all 5 CLOSES the Folk tier). Variance ×2, Josiah picks set size (~4–5). The 5: `western-philosophy`, `speak-for-everyone`, `playing-god`, `adoption-instead`, `survivor-testimony` (CARE ITEM — §2.3). Same rails as K257/K258: author from the cold-graded canon (`efilist_argument_library_v4_0_0.json` `.objections[].responses` short/medium/long + diagnosis/psychMechanism; efilist READ-ONLY), R2 / public / `b4_reply` / `["clinical","volta"]`, triggers oracle-net-disjoint + crisis-disjoint + existing-contains-disjoint (substring both directions, MODE-AWARE — exact-mode micro-forms are NOT collisions; the route battery adjudicates) + self-disjoint, YLEX-disjoint BODIES (balance/audit/wager/account; never filing/minutes/department/attendance/ledger/requisition/archive/catalogued/shelved/night-shift), crisis-form audit, provenance `{register, objection_id, source_node=combined#obj-<id>, ratified="K259 strike-pass (Josiah, <date>)", seat_rx="TX7 (library-seat review pending)"}`, deep-link its own node (grep-confirm the batch ids present in `e654eabd`; NO flagship touch), full-draft strike pass.
   PRE-CLEAR THE K258 GATE TRAPS (caught 4/4386): (a) every trigger form must equal `YO.normalize(form)` — hyphens/punctuation break it (K258: "consent to non-existence" → "consent to nonexistence"); (b) NO `?` in any body (pos-no-question); (c) NO ` just ` in any body (taboo scans the RESPONSE); (d) each form must self-route to its own entry, not fall to the empty-pattern `mg-deflect-01` default — run the register-gate route battery.
   WATCH ITEMS (seat-logged, non-blocking): (i) "back half runs seminar-cooler" — warm the register on the later bodies, don't drift academic; (ii) "'X, not Y' closer-shape density" — the "X, not Y" volta SHAPE is thickening (a nascent tic like the ~58% "He signed" verb-tic was); spread the closer SHAPE across batch-3, not only the verb. Surface a per-tier cap only if the full 528 wants one.
   Deliver the batch-3 bodies AS A FILE with the report.

3. CARE ITEM — `survivor-testimony` (defensive-distinction lane ONLY, seat-concurred; the cleared `pos-suffering-as-meaning-01` is its exemplar): rebut the objection ("survivors are glad they lived") by showing it answers the wrong question — creation-ethics does not turn on whether the already-existing are glad; the never-born file no testimony. NEVER "gladness is delusion", NEVER "survival was a mistake" (strips the deterrent + crosses item-5 crisis-counseling + third-party-harm). Objection-only triggers, ZERO crisis tokens, NEVER the word "suicide". Crisis-first battery proves separation; residual doubt → Josiah rules. Floor bytes do not move.

4. Ship gating: curtained-ship + seat review is the standing precedent — the curtain-lift is now AUTHORIZED for batch-2 (seat granted), but the lift itself (ne-hoc-fiat strip) stays Josiah's release-day call. Page-truth: curio `128 entries &mdash; 125 authored` → new count (grep-gated; +2 authored per objection). Status stays "first two sets" or Josiah's word (T2-COMPLETION at batch-3 could flip to "first three sets" — his call; scan the positionless phrase-family on any flip). NO VER bump — content rides `?v=K255` (TTL caveat ~4h warm, cold fresh).
5. TX7 → the register seat: batch-3 report + the batch-3 body file + the C1–C4 fold receipt.
6. Stratum + sidecars (`Downloads\k259\`) + ONE PS block (wuld only; efilist untouched). Housekeeping: `Downloads\k258\` + `Downloads\k257\` consumed — deletable.

ROUTING (unchanged): the register-integrity pass runs at the Successor Protocol seat (holds Josiah's register/voice source files); NOT the efilist grading instance (Max), NOT the Cowork build seat. Batch-3 bodies + the C1–C4 fold receipt delivered to that seat's folder; TX7.

3 · Fences that do NOT move  Crisis floor byte-frozen (c-crisis-01/02/03) · no Latin beyond `mg-greet-04` · Yūrei untouched (battery 100/100; bleed 0) · NO PIN — flagship does not move · anti-farming: variance ≥2, `mg-how-many-01` non-enumeration extends to every position · oracle-net law: position triggers NEVER carry a live oracle token (antinatalism / antinatalist / natalism / having kids / having children / procreation / benatar / efilism / efilist) · YLEX law: Grey bodies disjoint from Yūrei's home lexicon, not only the triggers · §D automated gates are NOT curtain-lift authority — the register-seat qualitative verdict is (now GRANTED for batch-2) · `ne-hoc-fiat` stays DOWN (release-day strip = section-18, NOT this arc — even now that it is authorized) · `/argue/` RESTORE carry stands · biography/disclosure unchanged · efilist READ-ONLY · CF-BEACON: served HTML by content-grep, md5 only the corpus JSON.

4 · Scope honesty  Carve-fold (C1–C4) is small and OUTRANKS new authoring — fold FIRST. T2 has 5 Folk ids left (batch-3 ~4–5; all 5 CLOSES the Folk tier). After T2: T3 Structural/Pragmatic (14), T4 Genuine Philosophical (31), T5 Meta-Objection (7). 57 of 82 objections un-signed after batch-2.

5 · Operator conventions (unchanged)  `& { }` scriptblock · flat per-file statements, never arrays · `Md5` helper (never `H`) · `.git\*.lock` cleanup · HEAD==origin guard + base-md5 guards + result-md5 gates · explicit-stage named files, NEVER `git add -u` · staged-count gate · one-line if/else · `curl.exe` with `-o` temp files, never `$null` · `http.postBuffer` + gate push `$LASTEXITCODE` · SERIALIZER PARITY: corpus base is `json.dumps(ensure_ascii=False, indent=2)` + trailing `\n` — reproduce base md5 from parsed data BEFORE authoring · YLEX BLEED: `yurei-bleed` reads the RESPONSE, not the triggers · BYTES-NOT-REPORT: a TX report is not the bytes — deliver prose AS A FILE · page-truth: scan the "no positions / positionless / written but unshipped" phrase family, not the literal token · DUAL-BOOT CLOCK SKEW: sandbox date reads UTC a day ahead — stamp the ratified line with `TZ=America/Phoenix date`, not the sandbox date · CF-BEACON: served HTML by content, corpus JSON by md5 (novel `?vk259=1` buster) · PATH NOTE: cloud sandbox `$HOME=/root`, Bash cwd `/home/claude`, the Read tool cannot see the root-owned clone (bash/jq the corpus) · sidecars ride `Downloads\k259\` · budget pre-flagged before any long lane.

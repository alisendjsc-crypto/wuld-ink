# K219 kickoff — flagship v4.0 BUNDLE ASSEMBLY (the deliberate pin-move session)

This is the one session class licensed to touch the pinned flagship. Everything it consumes is already staged and verified; execute, don't rediscover. *(v2, revised at K218a after library-Claude's Exchange 148: corpus-base adjudication RESOLVED, two library-authored cards added as a HARD GATE, c-rationale re-word banked below.)*

## Orientation (in order)
1. `C:\Users\y_m_a\Projects\wuld-ink\CLAUDE.md` — K218a tail: verify_before for K219+, standing hazards (esp. the stale-mount rule — git-show reads ONLY), electives.
2. `docs/wuld-ink-cowork-brief.md` (wuld repo).
3. `docs/library-claude-coordination.md` Exchanges 146–149 — the v4 arc record; 148 is library-Claude's delta-set ruling, 149 the verification receipt this kickoff encodes.
4. `efilist-argument-library\v4_staging\` — ALL 14 files: seed-1 set (node spec + triple + grade record + K195 state), seed-2 set (audit verdict + triple + grade record + K217 state), the T5-dispositions RIDER, README v2. The two node specs + the audit verdict's `bundle_deltas_owned_by_assembly` are the delta manifest.
5. **The pre-K219 library-seat payload: the 82nd layman card + the contractualism-scanlon card re-mirror (+ mechanism verdict if Act 3 was elected)** — either already landed in `v4_staging/` by a micro-fold, or handed into THIS session as uploads. **If the card pair is absent, say so at open: assembly may proceed through surface QA, but the PIN WAITS for the pair** (Exchange 148 pin-time-parity rule).
6. If either repo mount refuses to connect — or serves stale bytes (`git diff HEAD` phantom reverts = the tell) — the K216/K218 mount-less path: shallow-clone the public origins for ALL reads; writes ride a drop dir under any connected volume + cross-repo Move-Item in the operator block. `ls ~/mnt/` is ground truth for mounts, bounded polling only.

## Verify at open (clone or git-show only; NEVER working-tree reads — K218a proved the wuld mount can serve multi-day-stale bytes)
- efilist HEAD = `4bb9cc4` (K218 BLOCK-1) or its successor if the card micro-fold landed — verify, don't assume. wuld HEAD = the K218a commit atop `8d43a85` (hash in the K218a stratum at landing).
- Frozen base: flagship combined.html `6dfeb5d4`/2,955,840 (pin v3.9.16) — corpus `9af9dd5c`/1,326,017 (81 nodes) — ledger `204fcdbe` — objections-index `82f3cd1f` — flagship-layman `006b9a3b`/115,520 — canon `628a75d3`/37.40 — charter `4962aec5`.
- v4_staging md5s: seed-1 set in the K216 stratum; seed-2/rider/README v2 in the K218 stratum; card payload md5s in its own carrier state.

## Sequence
0. **Corpus-base adjudication — RESOLVED (Exchange 148 + K218a verification); assert, don't re-litigate.** Repo `9af9dd5c` IS the cut base. The panel↔repo divergence was TOKENIZER SKEW, not drift (solipsism 1237/1213 = same bytes, wc-convention vs dashless counters); the freeze is byte-PROVEN at K218a: base blob `6ac568fc` @ efilist `60e0d99` vs HEAD differ ONLY in the archetypeVariants insertions **plus exactly 9 comma-only lines** (the JSON trailing comma each of the 9 net-new carriers gains on its `"long"` line — syntax riders, NOT edits; do not false-alarm on them). Acceptance criterion, standing: any divergence BEYOND that shape = freeze-violation alarm → HALT and adjudicate via coordination, never absorb. No panel-corpus export is needed.
1. **Corpus cut v4.0:** +`self-effacing-under-universalization` (identity fields verbatim from the seed-1 node spec; triple from the three seed-1 .md files) → 82 nodes, tier split 13/17/14/31/7. `contractualism-scanlon`: responses REPLACED wholesale by the seed-2 triple (identity fields LOCKED); sources-field adds recommended-adopt (Frick — Contractualism and Social Risk; Kumar — Who Can Be Wronged?; Reibetanz / Otsuka ex-post critiques). Round-trip the serialization FIRST — NOTE (K218a): the flagship corpus does NOT round-trip under the wing-standard serializer (indent=2 / ensure_ascii=False); establish the byte-faithful edit path empirically before any splice (insertion-splice on raw bytes, or prove whatever serializer DOES reproduce the base) — then apply deltas. Version-per-file: mint the v4_0_0 filename, `git mv` the old to `archive/`.
2. **Mechanism + graph adjudications:** if the pre-K219 seat's Act 3 ran, CONSUME its verdict (existing id vs new mechanism #36) — else adjudicate in-session: 'formalism displacement' vs the existing 35 (new id only if none fits — MAJOR-adjacent, justify; precedents: self-defeating→Category Error, contractualism-scanlon→Genuine Philosophical Challenge + Formal Logic Attack). MAP_GRAPH/DEP_GRAPH node+link deltas; Map-1 curation call (does the new node enter the 78-node archetype set); reciprocal disambiguation pointer on `self-defeating` (keywords/diagnosis, per seed-1 spec cross_reference_plan).
3. **Ledger:** +1 row (seed 1) and SUPERSESSION of the contractualism-scanlon row (seed 2) under regeneration discipline — bands re-derived from the stored UNROUNDED means (0.897814 / 0.905855 lines), never copied forward. Row shape must match sibling DATA exactly (graded/axes/short/medium/long/headline_grade_long). **c-rationale supersession (Exchange 148, fold provenance-stamped; VALUE 0.89 and band untouched):** replace the seed-1 grade record's `axis_rationales.c` with the following, stamped `[superseded per Exchange 148, 2026-07-10; original preserved in git history]`:
   > c = 0.89 (Completeness) — both horns of the dilemma, the conception form, the legislation/publicity variant, and the will-prong routing are covered inside the T5 band, with the residue routed to a named terminus. Not higher: two load-bearing moves — "unheld is not unholdable" and formulas-protect-persons-not-the-headcount-of-a-type — ship at single-sentence compression, their development omitted rather than out-of-scope: a coverage cost at the margin.
   The record's `axis_legend_caveat` stands as history; the calibration figures (220–1237, median 508) are wc-convention and need NO erratum.
4. **Layman index (library-authored cards fold here — the HARD GATE):** fold the 82nd entry (`self-effacing-under-universalization`, authored in the pre-K219 seat, register 'routed' per the rider semantics) + the contractualism-scanlon RE-MIRROR card (supersedes the stale mirror — K218a measured card↔old-short 0.210 vs card↔staged-short 0.027). One-shot regen: 82 nodes reordered to the NEW objections-index order; **`source_index_pin` rotates to the new 82-node objections-index md5** (`82f3cd1f` retires — Exchange 148 mechanical rider). Rider fold per its `vehicle_and_fold`: per-node `register` on the six T5 entries + the new card + schema_version bump. Scholar-objections asset alignment (the S4 flag seed 1 answers). Validation: v0_3 with the register-keyed T5 gate — defanged inherit the T4 hard defeat-vocab gate, routed exempt; `--warn-tiers 5` retires (rider `ask2_verdict`; fork v0_4 if cleaner than hand-keying).
5. **Rider promotion:** `git mv v4_staging/flagship_t5_register_dispositions_v0_1.json` → root (durable canon-call, not v4-transient) + canon MINOR log per the rider's own clause.
6. **Canon bump** per the canon's OWN update_protocol (canon_version + marker + last_updated + last_updated_by_session + history-tail delta append; version-per-file mint + archive the old).
7. **Four-surface sync:** corpus JSON ↔ JSX ↔ combined.html ↔ ledger. Validator `--self-test` PASS BEFORE any mutation; path-mode PASS/0 on the folded assembly. prose_md5 binding; change-isolation diffs (every untouched node byte-identical); mojibake U+FFFD 0; conservation checks.
8. **PIN MOVE (the only step that touches live; GATED on step 4's card pair):** deploy → verify SERVED BYTES (curl.exe ×3 agreement on `library.wuld.ink/combined`) == the new md5 → THEN pin. Same-session, mandatory: wuld search-index regen (the pin-version string is indexed text — K129/K202: the regen is NOT a no-op), objection re-vendor into wuld `src/` (incl. the NEW objections-index + flagship-layman re-vendor if wuld vendors them), `?v` sweep on the 12 pin-display src pages, CHANGELOG + releases.json + feed.xml entries. v4_staging/ seed files STAY (historical record) unless Josiah prefers archive/ — one Move-Item either way.
9. **Close:** ordered operator PS blocks (efilist first, then wuld), each `& { }` with .git\*.lock cleanup + HEAD==origin base guards + md5 gates on every moved file + explicit-stage named paths (NEVER `git add -u`) + one-line if/else + curl.exe; K219 stratum + the coordination exchange (next free number, 150+ — numbering settles per land order) ride the wuld block.

## Budget + fallback
Heavy session (2.95 MB combined.html splice + corpus + JSX + ledger + layman + validators + search-index + vendors). The cards fold as AUTHORED BYTES — no authoring in-session; any grading or register question routes back via coordination, never improvised. Checkpoint per surface; keep every intermediate in `$HOME`, never inline large files (clone/git-show reads only). If the session strains BEFORE the pin: land the efilist-side cut complete-and-verified WITHOUT deploying, hand a same-day part-2 prompt for deploy+pin+re-vendor — never a half-pin, never an interleaved deploy.

## Register
Recommendation-first, no menus, pre-flag token risks, PS blocks per house rules. Grading is library-Claude's lane — assembly executes their staged records; the c-rationale text in step 3 is THEIR supplied wording, folded verbatim.

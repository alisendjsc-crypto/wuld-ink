SESSION PROMPT --- YUREI HAUNTING-WIDGET v3 REFRESH v1
Contract recon -> framing lock -> 13-asset re-render on the v4 fidelity surface (set-atomic) -> ship + staged relay
Authored 2026-07-17 - consumer: 3D-Modeling seat, fresh session - author: Grey-v3 seat under Josiah's delegation ("draft next session's prompt with your recommended defaults"). Discharges the standing author-elect flag on the haunting-widget refresh (yurei-avatar-fx-arc "Next" item; Josiah vetoes by not pasting this). Mr. Grey is NOT this session - his blend stays closed. This is an ASSET-ONLY session: ZERO widget-code bytes, ZERO wuld-repo bytes; the wuld-ink seat owns swap mechanics.

0. Read first, in order
1. Project memory: `yurei-avatar-fx-arc.md` (v4 surface state + FRAMING CANON + restore-law members), `project_wuld_widget_track.md` (the haunting widget contract + swap precedent), `project_wuld_phase3_state.md` (blend lineage), `reference_blender_5x_api_traps.md` (incl. 2026-07-17b #25 BVHTree.FromObject is LOCAL-space - build world BVHs via FromPolygons(matrix_world @ v.co)), `reference_blender_5x_animation_api.md` (stuck-value restore law), `reference_mcp_long_render_pattern.md` (autonomous campaign + 2026-07-17b ffmpeg 8.1 ALPHA_MODE-uppercase probe trap).
2. THE CONTRACT AUTHORITY (live-wins over every number in this prompt): read `C:\Users\y_m_a\Projects\wuld-ink\src\assets\yurei\manifest_v2.json` via device bridge (folder IS connected). It defines the 13 roles, filenames, clip lengths/fps, px, anchorPx (expect [270,169] - NOT the desk avatar's [267,259]), schema 1.1, per-asset md5/sha256/size. Also stage the live v2 canonical still + idle first-frame-class asset + 2-3 WATCH stills from `...\wuld-ink\src\assets\yurei\` for A/B reference. Do NOT assume lengths; read them.
3. Substrate: the mascot mainline blend (path from memory files - verify `bpy.data.filepath` at open; the Blender MCP bridge may open blank on restart - reopen the target file). VERIFY AT OPEN per yurei-avatar-fx-arc: v4 surface ratified state (P4A anatomy field W=1.3 / grain / hair variance / 900 strands hide_render False / MascotArms + P4F grooves + finger bones), yurei4_* banks present + fake_user, restore-law members incl. the NEW emission-0 member (dismiss ember), rig.matrix_world NOT identity. Record any drift; live-wins.
4. Ops facts: **D:\mascot is NOT bridge-readable** - frames + campaign log MUST land in `C:\Users\y_m_a\Downloads\_yurei39_transfer\haunt_v3\` (bridge-connected ferry) for device_bash polling; authoritative ship copies go to `D:\mascot\haunt_v3\` via Blender-side file ops. v4 geo rendered 1.2-1.5 s/f best-case with persistent_data and DECAYS under load (thermal class) - `use_persistent_data=True` from the start, RE-TIME with one 40f window before sizing the campaign, 60s cooldowns between roles.

1. What this session is
The site-side HAUNTING widget (`yurei.{css,js}` on /frame/ + heavy-read pages - the corner ghost with drift/peek/surface choreography + 8-direction WATCH head tracking + fragment scheduler + exorcism) still serves manifest_v2 assets = Phase-3.8b PRE-FIDELITY renders. The v4 desk avatar (fingers + fidelity surface, manifest 0c9acaa7) makes the visual generation gap user-visible on the same pages once the owed v4 swap lands. This session re-renders the FULL 13-asset haunting set on the v4 surface: 4 motion clips (idle loop / drift / peek / surface) + canonical still + 8 directional WATCH head stills (n/ne/e/se/s/sw/w/nw). SET-ATOMIC: all 13 on the same surface, one campaign; never mix generations.

2. Hard locks (unchanged laws)
Palette red/near-black (her register; strand + ember exemptions per v4 gates). 24fps, VP9 yuva420p `-auto-alt-ref 0 -an`, container alpha verify (ffprobe tag CASE-INSENSITIVE - ffmpeg 8.1 writes ALPHA_MODE uppercase; bitstream ground truth = force `-c:v libvpx-vp9` decode + read the alpha plane). px + anchor semantics from manifest_v2 (px is the widget's render size contract - hold it). NO audio. Reduced-motion contract (canonical still only) is WIDGET-side - unchanged, but the canonical still must be flawless. Keys = deltas off captured rest; per-owner banks; stuck-value restore ritual after every unassign (incl. emission-0). Kill-switch, choreography timing, engage/disengage radii, fragment library: ALL widget-side - untouched.

3. PHASE A --- framing lock (the load-bearing recon)
The haunting framing is NOT the desk TorsoCam. Recover it:
* A.1 Look for a haunting-era camera in the blend (K111-harness class). If present, verify by rendering the rest pose and diffing against the STAGED LIVE v2 canonical still (composition/anchor check, not pixel-identity - the surface changed by design).
* A.2 If absent: reconstruct by pose-match probe against the v2 canonical still - camera loc/lens grid, minimize silhouette diff (world BVH + numpy; cheap at stills; one factor per probe). Record the recovered camera in-file (`yurei_haunt_v3_recipes` text block).
* A.3 ANCHOR GATE: the WATCH geometry derives zone angles + the 140px dead zone from the scaled head anchor. Target = anchor-COMPATIBLE framing (head anchor lands at manifest anchorPx within a few px, verified by a probe render + pixel measure). If the v4 surface forces a shift, the manifest CARRIES the new anchorPx (N5 single-source; schema allows it) - but FLAG it loudly in the relay as widget-geometry-sensitive, requiring the wuld seat's live eyeball post-swap.
* A.4 Frame-visibility census at this framing (the v4 lesson: the desk kickoff's finger band was below frame bottom - verify what the haunting crop actually shows before spending detail effort; fingers/hands may not read at corner-ghost scale, and that is FINE - coherence is the goal, not legibility of every v4 feature).

4. PHASE B --- the 13 assets (designs BYTE-FAITHFUL; re-render, do not redesign)
* B.1 Motion clips: idle loop + drift + peek + surface at the LENGTHS THE MANIFEST STATES. Recover the motion designs from the existing v2 banks/records in the blend (they were authored there; if bank names differ from the desk `yurei4_*` convention, the old haunting banks are the source). If a design only exists as shipped pixels (bank lost), reverse-engineer conservatively: match the v2 clip's silhouette keyposes at 4-6 sample frames; flag the reconstruction in the recipes block. NO new choreography, NO added beats - redesign is author-ask only.
* B.2 WATCH stills x8: recover the 8 directional head poses (records first: the blend, `C:\Users\y_m_a\Downloads\Yurei Arc\` docs, memory; else reverse-engineer per-direction by matching each STAGED v2 still - head yaw/pitch grid, silhouette-diff minimize, deterministic). HARD-CUT law: head-still poses, no easing artifacts baked in.
* B.3 Canonical still = the rest frame of the idle loop, byte-copy discipline (md5-equal gate vs idle f0001).
* B.4 Loop parity on idle: first==last keys (data gate) + wrap-vs-interior MAD seam gate at encode time.

5. PHASE C --- render + ship (budget-gated: if <40% budget after Phase B banks/poses verified, SAVE + write the session-2 render kickoff; else run)
* RE-TIME first (one 40f window, persistent_data on). Then the AUTONOMOUS CAMPAIGN (v4/Grey-v3-proven): ONE fired execute_blender_code script loops the 4 clips + renders the 9 stills -> frames to `C:\Users\y_m_a\Downloads\_yurei39_transfer\haunt_v3\frames\<role>\f_` (use_overwrite=False, use_placeholder=False) -> per-frame size verify + missing-tail refire -> unassign + FULL RESTORE (rest pose + every restore-law member incl. emission 0) -> json progress log at `..\haunt_v3\campaign_log.json` -> 60s cooldown -> next. Poll via device_bash ONLY (never the busy socket; a queued call executes later with its result lost - keep such calls idempotent).
* Encodes: parallel detached Popen ffmpeg (crf per v2-precedent class: loop/subtle 30, choreography 28 - match the v2 set's own choices if discoverable, else 30/28 split); ffprobe -count_frames exact + case-insensitive alpha_mode==1 per clip.
* gates_haunt_v3: per-asset md5+sha256+size vs the new manifest - canonical==idle-f0001 md5 - idle seam ratio - palette (red/black register; strand/ember exemptions per v4 precedent; >=6 frames across clips + 3 stills) - anchor sanity (head anchor px-measured on canonical + 2 WATCH stills vs the manifest anchorPx) - alpha 13/13 - SIZE BUDGET: v2 set totaled ~2MB; the v4 surface will cost more - FLAG if total >2x v2 or any clip >2x its v2 sibling (the widget lazy-loads after activation, so growth is tolerable but must be a recorded, ruled number) - restore audit - census - campaign log.
* Ship: authoritative `D:\mascot\haunt_v3\` = 13 assets named `yurei_<role>_v3.webm` / `yurei_still_v3.png` / `yurei_head_<dir>_v3.png` (the K117 `_vN`-drop pattern) + `manifest_v3.json` (+.md5; SCHEMA 1.1 SHAPE - same keys as manifest_v2, per-asset md5/sha256/size_bytes, px, anchorPx, animation fallbacks as v2 carries them) + `yurei_haunt_gates_v3.json` + `yurei_haunt_v3_recipes.md` (+ in-file text block) + contact sheet on DARK panel (chat card too) + `yurei_haunt_v3_wuldink_relay.md`. FULL evidence copy at the ferry dir (bridge-readable).
* The relay is a STAGED handoff, not a go-live order: swap = drop 13 files alongside v2 + land manifest_v3.json + repoint the widget's MANIFEST_URL constant + `?v=` bump on yurei.js (K117 precedent: the vN-swap was proven drop-in, zero widget-logic change). RECOMMEND in the relay that the wuld seat MERGE this with the already-owed v4 DESK swap (relay in D:\mascot\avatar\_v4\) - one /assets/yurei/ session, one live-eyeball pass. v2 haunting assets stay on disk as rollback.

6. Ops crib (hard-won, do not rediscover)
use_persistent_data=True from the start; re-time before sizing; campaign never polled through the socket; sandbox sleeps cap ~120s (chain 110s). Blender keeps rendering past the 60s MCP timeout - accept it. ffmpeg detached; detached 2>log gives 0-byte logs (ffprobe is truth). BVHTree.FromObject is LOCAL-space - world BVHs via FromPolygons over matrix_world-transformed verts; camera-visibility checks pair a combined world BVH with bpy_extras world_to_camera_view. COW prop-lag on same-call evaluated reads (update_tag + view_layer.update, or formula-identity; renders unaffected). action.copy() = clean 5.x bank derivation; channelbag API for curves. One factor per probe. Keys = deltas off captured rest; absolute-0 keys on nonzero rests is the caught-bug class. hasattr() on bpy collections can raise TypeError. The desk-avatar files at /assets/yurei/avatar/ share the parent dir - filename lineages don't collide (yurei_* vs yurei_av_*), but never touch the avatar/ subdir this session.

7. Author rulings --- PRE-ANSWERED (do not re-ask)
The refresh is ELECTED under standing delegation x3 (Josiah 2026-07-17: "I trust your judgments... go with your recommendations"; he requested this draft; veto = don't paste). Choreography + WATCH designs UNCHANGED - this is a coherence re-render, not a redesign. All 13 assets set-atomic on the v4 surface. NO substrate surfacing changes this session (the v4 surface is ratified; if a haunting-framing defect REQUIRES a surface fix, STOP and ask - that is a register/canon decision). Clip-size growth is expected and gets ruled+recorded, not silently shipped. wrong_hour outline-double comp stays OUT (author-ask gate, unchanged). Anchor holds unless the surface forces it; a forced shift ships in the manifest + is flagged in the relay.

8. Deferred / next after this (bookkeeping, not this session)
* WULD INK swap queue (their seat, Josiah pastes): Yurei v4 desk (0c9acaa7) + Grey v3 desk (a7023313) + this haunting v3 relay - recommend batching the two /assets/yurei/ swaps.
* Grey seat-side elective: idle-variant pool on the v3 surface (cheap bank-pattern reuse).
* Phase-4 positions / corpus / wiring: other seats (ratification cliff unchanged).

9. Memory duties at close
Update `yurei-avatar-fx-arc.md` (+MEMORY.md index): haunt_v3 state - manifest_v3 md5 if shipped or the session-2 render kickoff pointer if budget-gated; record the recovered haunting camera + WATCH pose recipe (they were nearly lost once - pin them); record the ruled size-growth numbers; log any new traps to the reference files. Grey arc untouched.

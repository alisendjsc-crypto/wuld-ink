Session prompt — wuld.ink K299: the crisis floor first, the help vessel second. TX15-BACK has landed and been verified.

Written at the K298 close (2026-09-07). K298 LANDED `f1d8a6e` atop `f9ec361`: `/donations/` Donate button 930 → 797 px (1.10 → 0.94 phone screens), and the site-wide phone type-scale escape fix — 33 heading escapes closed across 32 pages, 22 glossary + 9 gallery rooms + 2 on `/donations/`. 43 files, one commit, no pin. The CLAUDE.md K298 carry says "landing: pending" — you record `f1d8a6e` in the stratum you write (forward-record, K295 pattern).

**TX15-BACK IS ON DISK AND VERIFIED.** `Downloads\Successor Protocol\omega_proxy_expansion_transmission_15_BACK.md`. The gate that blocked K296, K297 and K298 is open. Its verdict is AMBER and its ask-ordering is authoritative: **Ask C — the crisis floor — outranks the vessel.** K298 re-derived every load-bearing claim in it against the real engine; the corrections are in `Downloads\k299\TX15-BACK_COWORK-VERIFY.md` and **that file, not TX15-BACK, is this session's premise.** Six of the seat's specifics do not survive contact with the matcher.

## Gate on entry — five things, or stop and say so

1. **K298 landed:** wuld HEAD is `f1d8a6e` (or a later one-committer commit atop it), HEAD == origin, `git status` clean. Served `/components/glossary.css?v=K298` md5 `438b2593`, `/components/gallery.css?v=K298` md5 `03303578`, `/donations/` carries `phone rhythm + type scale`.
2. **Every pin re-derived, none copied (cclxii).** This session leans on: `src/components/yurei-oracle.js` [compute — 12,566 B at K298] · `src/components/omega-corpus-mrgrey.json` [compute — 287,795 B, **184 entries / 833 declared forms**, classes: response 153 · oracle 21 · deflection 5 · crisis 3 · repeat 2] · `tools/omega/coverage_audit.cjs` [compute] · flagship `library.wuld.ink/combined` `e654eabd`/2,963,752 (efilist READ-ONLY, not opened).
3. **The two corpus copies HAVE drifted — measured at the K298 close, and the answer is benign.** The repo copy `src/components/omega-corpus-mrgrey.json` (287,795 B, 184 entries) vs `Downloads\Successor Protocol\omega-corpus-mrgrey-entries.json` (234,684 B, **172 entries, 95 shared ids differing**). The SP copy is a **stale export**, not a live second corpus: it is two hours older (2026-07-22 07:07 vs 09:17), its ids are a strict subset of the repo's, and everything missing is a `pos-*` objection added since. **The repo copy is canonical. Re-derive this before acting on it** — but note the finding that matters: **the crisis floor is BYTE-IDENTICAL in both** (3 entries, same forms), so Part 1 folds against one file and no reconcile is owed. Do not let the drift pull this session into a corpus reconcile; that is its own session and it is not this one.
4. **Scratch from K298** is in `Downloads\k298\` and `$HOME/k299/` is gone (VM wiped per session). The verification harnesses — `verify_askc.cjs`, `verify_rest.cjs`, `s0_v2.cjs` — are NOT committed and will need rebuilding; `TX15-BACK_COWORK-VERIFY.md` records what they measured, so re-derive rather than trust it if a number becomes load-bearing.
5. **Josiah's rulings.** Ask C's mechanical forms and §0 are build calls. **New prose is not.** `mg-oracle-help-01`, `mg-never-born-01`, `c-crisis-harm-other-01` and the `mg-worth-living-01` amendment are RATIFIED BY JOSIAH before a byte is folded. If he has not ruled when you open, build Part 1 (which needs no new prose) and hold the rest.

## What this session is

Ordered by the seat's own ranking, which K298's measurements confirm.

### Part 1 — the crisis floor. This is the item. (build + ship; no new prose required)

**Measured, not claimed:** of the 18 phrasings TX15-BACK §4 lists, **12 answer with `mg-deflect-01`** — *"That's off the path I keep."* Six already reach the floor. The seat said 22; use 12, and use the pairs, because the pairs are the argument:

`wish i was dead` reaches `c-crisis-01`. **`wish i were dead` deflects.** `dont want to live` reaches the floor; `dont want to be alive` deflects. `end my life` reaches it; `take my own life` deflects. `going to hurt myself` reaches it; `i am not safe` deflects.

Fold as `contains` forms on the existing floor entries (`c-crisis-01/02/03`), both corpora together if gate item 3 says two files. The 12: `dont want to be alive` · `cant go on` · `cant do this anymore` · `want to disappear` · `want to not exist` · `better off without me` · `wish i were dead` · `not wake up` · `take my own life` · `not be here anymore` · `i am not safe` · `im not safe`.

**Guard (the seat's, and it is a real one):** verify no folded form can be reached by an input already carrying a first-person floor token — Stage 1 must win every such case. Confirm, do not assume. **And run the self-match control after the fold: undeclared steals must stay at 0.**

**`im going to hurt someone` is NOT folded here.** It deflects today and it needs `c-crisis-harm-other-01` authored deliberately — a floor written for the visitor's own risk read back at someone describing a different danger is worse than the deflection. Authorship is Josiah's assignment (§5 item 2).

### Part 2 — §0, the normalizer. RULING ANSWERABLE, evidence already measured.

The seat's fold condition is **met**: hyphen→space plus conservative contraction expansion produces **0 new undeclared steals**, self-match unchanged at **99.2 %**. Safe because all 833 corpus forms are already stored hyphen-free, apostrophe-free, lowercase.

**It is TWO lines, not one, and shipping the first alone is worse than shipping neither.** `patternMatch()` compares `pat.form` **RAW** against normalized input. Amending the input path alone transforms one side of every comparison — K298's first cut did exactly that and reported 51 phantom steals. The second line is `var form = normalize(pat.form)`.

**Exclusions are load-bearing:** never expand a contraction whose stripped form is an ordinary English word — `were`, `well`, `ill`, `id`, `its`, `hell`, `wed`, `shed`, and `cant`. Expanding `were` → `we are` would destroy `wish i were dead`, the very form Part 1 is folding. Because `cant` is excluded, `cant go on` is **not** fixed by §0 and must be an explicit floor form — it is in Part 1's list for that reason.

What it buys today is narrow and sufficient: **`do not want to live` moves from `mg-deflect-01` to `c-crisis-01`.**

### Part 3 — the help vessel + Ask A (needs ratified words)

Ask A is authored in TX15-BACK §2 and is a new entry (`mg-oracle-help-01` is absent). Two corrections before it ships:

- **Chip 3's id is wrong.** `Your view is ableist.` routes to **`pos-ableist-objection-01`**, not `ableist-objection`. It does reach a signed position, so the chip stands; the id in the table does not.
- **Chip 4's justification is wrong.** `mg-greet-04` is already reachable by `salve`, `welcome` and `good day`. Keep the chip as house texture if Josiah wants it, but strike "rescues it from near-unreachability" from the record.

Rider edits i, ii, iii are all confirmed accurate and ride as written — all four of edit ii's widenings (`can you help`, `help please`, `need help`, `i need some help`) deflect today. **Drop the global `white-space: pre-line`** per the seat's amendment. Chips submit through `.respond()`, so crisis-first is preserved on the tap path — assert it, do not assume it.

### Part 4 — §1 and §3 rulings (fold only what Josiah has ratified)

`community` → demote to `exact` on `mg-nav-chat-01` (**confirmed**: it is a `contains` form and `your community` lands there too). The routing law — signed position beats unsigned lane on a true tie — applies to `see a therapist` and `edgelord` (**both confirmed**); it does **not** apply to `youre depressed`, which already routes to `pos-just-depressed-01` correctly. Implement by demoting the hostility forms one weight, never by inflating the position.

The 52 §3 phrasings fold **after** §0 lands, and the fold shrinks by whatever §0 absorbs. Re-run the audit and diff the self-match control before and after; undeclared steals stay at 0 or the fold is wrong.

## Hard constraints — do not rediscover

- **Fresh `Matcher` per probe.** A shared instance makes dampening produce false failures (K260). Every K298 measurement used a fresh one.
- **Corpus forms are matched RAW.** Any normalizer change desynchronizes the corpus unless the form is normalized too. This is the single most dangerous fact in the engine and it is not in TX15-BACK.
- **An assertion over an empty set passes.** Print the population beside every assertion (cclxi). K298 lost a run to a `grep -c` that printed `0` and exited 1, so `|| echo 0` appended a second zero and every comparison failed against `"0 0"`.
- **A gate placed after an irreversible mutation must be evaluated against the POST-mutation tree** (cclxxii — K298's ship failed on exactly this). Build the post-mutation tree in the harness and dry-run the tail of the block before handing it over.
- **A repo-wide old-string gate is self-defeating** when the stratum documents the change: CLAUDE.md contains both versions as prose forever. Scope `?v` and form-string assertions to `src`.
- **Never a bare grep count as a claim.** Measure every constant with the same instrument that checks it, and check every hash in a handoff block by SCRIPT.
- **A local-mirror crawl is contaminated by the site's own service worker** (cclxxi): it registers under `pointer: coarse`, claims the origin after the first navigations, and then serves `?v` assets cache-first from its cache. Set `serviceWorkers: 'block'` on any crawling context and assert the measured document's `location.pathname` against the route requested. Not applicable to corpus work, but it applies the moment this session renders a page.

## Settled — do not reopen

- **TX15-BACK's verdict ordering stands.** Ask C outranks the vessel. Two sessions pivoted waiting for this document; do not spend the third on the chips.
- **`i wish i had never been born` does not go on the crisis floor.** It is the house's own thesis, and routing it to a crisis script tells the visitor the sentence is a symptom — the exact move the library exists to refuse. The seat's `mg-never-born-01` bridge takes it as a claim first and keeps the door open second, in that order. It reaches nothing today, so the bridge is additive.
- **Four of the nine unreachable objections stay unreachable by design.** Do not "fix" `boonin-critique` or `contractualism-scanlon` with named-philosopher forms; names belong in the library layer, not the matcher's mouth.
- **The stand-down clause now appears in three bodies.** Intentional. Flag if it reaches a fourth without a reason (§5 item 6).
- **The type-scale carries from K298** are recorded in `tools/gate/type-scale-K298.md` — `/watch/` is the cheapest remaining escape (a half-fix pointing at `var(--t-h2)` instead of the clamp). Not this session.
- **`reach_audit.cjs` does not block service workers.** Fixing it means re-baselining, so it is its own small session.

## PS handoff

The K298 shape (`Downloads\k298\resume-K298.ps1` is the better template — it is the one that passed): `$repo` anchor + `Test-Path .git` + `Set-Location` + `[Environment]::CurrentDirectory` → `.git\index.lock*` + `tmp_obj_*` cleanup → null-safe `rev-parse` → HEAD==origin → **result-or-base guards, whichever matches the tree you actually hand over** → sidecar MD5 gates BEFORE any move → `Move-Item` → result-MD5 gates → shape asserts with populations (corpus entry count, form count, crisis-form count by ARITHMETIC old vs new, JSON parses) → explicit-stage one path per line → measured staged-count gate → `git ls-files -s` blob gates → commit → `postBuffer` push + `$LASTEXITCODE` → `Start-Sleep 75` → `curl.exe` live asserts (corpus JSON md5 HARD; flagship `e654eabd` HARD). `curl.exe` never the alias; `-o` a temp file never `$null`; one-line if/else; `Md5` never `H`; no double quote inside a native-command argument; **every hex constant checked by SCRIPT against a value measured this session**; **no non-ASCII anywhere in the block** (a `-SimpleMatch` needle containing `·` can never match).

## Budget

Part 1 is a corpus fold plus an audit re-run — cheap, and it is the part that matters. Part 2 is two lines of engine plus the same audit. Part 3 is a vessel build gated on ratified words. Part 4 is weights and one demote. **Pre-flag before starting.** Gate item 3's drift is already measured and does not block Part 1 — but if anything makes the corpus reconcile look load-bearing to this session's work, pre-flag before touching it: that is a separate session, not a sub-task of this one.

K268 KICKOFF — T4 GENUINE PHILOSOPHICAL batch-2 (3 objections x2 = 6 R2 positions). Operator paste for the next wuld.ink Cowork CORPUS session. Read this whole file before any write.

NUMBER: you are (latest top-level `### K###` stratum in CLAUDE.md) + 1 by the standing rule, BUT the operator has lane-assigned this corpus batch **K268**. Lanes interleave: the CONSOLE-descent lane ships ODD (K265 pool-deepening `f088403`, K267 seed-share `0ccada2`, K269 next); the CORPUS lane ships EVEN (K264, K266, K268 = this). At open the tip may be K267 (`0ccada2`) or a K269 console commit atop it — verify-at-open on the THEN-CURRENT HEAD, keep the corpus label K268, and let the `HEAD==origin` guard serialize the interleave (the ledger may read non-monotonic by design — read by content, not numeric order). The lanes are file-disjoint except CLAUDE.md + HEAD.

0 - Orientation (mandatory, in order)
1. Fresh public-origin shallow clone of wuld-ink (github.com/alisendjsc-crypto/wuld-ink); mounts are NEVER read for repo state. Read CLAUDE.md FIRST (the K266 stratum + the `#### K266 correction` note + any K267/K269 console strata are live context), then docs/wuld-ink-cowork-brief.md if unsure of conventions; docs/refusal-libraries-roadmap.md governs library-corpus scope (P0-P4), NOT proxy-position ordering.
2. **READ `docs/omega-proxy-tier4-register-guardrail.md` (`f3593bcc`) — it BINDS this batch.** Register-by-fit PER OBJECTION; appraisal-silent is STANCE-scoped not affect-scoped, so warmth-by-fit is permitted UNDER the stance-fence (rebut the inference; never grant felt-content as positive value; never counsel the objector; warmth in cadence, never in a value-claim). Lacero §4-gated; Stripped Confession OFF published surfaces incl. the curtain [OPEN]; Tender-Pole fit-gated [OPEN]. This is the FIRST batch authored under it.
3. The T4 canon is `efilist_argument_library_v4_0_0.json` .objections[] where tier==4 (31; responses short/medium/long + diagnosis + psychMechanism) in the efilist repo (READ-ONLY, HEAD `579cfbd`). Fresh public-origin clone of efilist; mounts never read for repo state.

VERIFY-AT-OPEN (STOP on any mismatch; pins are MD5 unless noted). Confirm K266 + the correction LANDED: corpus omega-corpus-mrgrey.json **`65db3cf0`**/264742 (172 = 169 authored + 3 crisis; 94 positions / 47 objections). Page src/successor/index.html **`02662cba`**/20369 (curio "172 entries — 169 authored"; Yurei "165 entries" HELD; corpus-script `?v=K266` x2; Status "first two sets"; positionless phrase-family 0). Gates HELD: register-gate `15306a2d` (offline **5662**), persona-gate `fab75c11` ("94 position(s)"), surface-e2e `2277fa9c` (**209 / 0** crisis-first). Guardrail doc `docs/omega-proxy-tier4-register-guardrail.md` `f3593bcc`. omega-assistant.js `ced05284` + successor-stage.js `c04b7838` (VER K255, page `?v=K266`). vendor tools/omega/vendor/objections-index.json `d034af15`/41800 (82 ids == efilist HEAD `579cfbd`). Flagship library.wuld.ink/combined `e654eabd`/2963752 — PINNED, UNTOUCHED, NO-PIN session. Console stack (K267 `0ccada2`): console files at their K267 `?v`; disjoint — confirm they did not move the corpus/page/flagship pins. CLAUDE.md — RECOMPUTE at open (moves every session incl. the console lane; NEVER hardcode). HEAD == origin (one-committer). IF corpus != `65db3cf0` or the K266 stratum + correction note are absent, STOP — tell Josiah before authoring. Record in the K268 stratum: "K266 + correction landed (corpus 65db3cf0); TX12-BACK GREEN + Parfit fold recorded; guardrail f3593bcc read" + a T4 round-trip once authored + a crisis-first proof. CF-BEACON: served HTML by CONTENT-grep; corpus JSON serves clean, md5 with a novel buster; flagship served clean e654eabd (tolerate +~504 beacon).

1 - Where the arc stands. Phase 4 OPEN. T1/T2/T3 COMPLETE. T4 OPEN — batch-1 (benatar-asymmetry-attack, consent-incoherent, non-identity-problem) landed K266 + seat-corrected (nonid-01 Parfit-line). **47 of 82 signed (94 positions).** Remaining T4 (28), then T5 Meta-Objection (7). Curtain-lift stands through T3; `ne-hoc-fiat` STAYS DOWN ("first two sets" -> "first three sets" Status flip + release-day = Josiah's).

2 - TX12-BACK recorded (library-seat, K266 batch-1)
* VERDICT: bytes GREEN 6/6 (independently verified both sides; in-band, bans/FFFD/en-dash/all-caps 0); register HOLDS across full bodies; both cold-closes December-rule clean. ONE edit found + FOLDED: `pos-non-identity-problem-01` `Parfit needs` -> `The objection needs` (removed the sole in-body proper name per the oracle-net philosopher-naming principle + closed a `He`-antecedent trap; char 719 in-band; `parfit` trigger KEPT). corpus `fcb42f29`->`65db3cf0`.
* GUARDRAIL RATIFIED (see doc `f3593bcc`). FORWARD STYLE-NOTE from the seat (non-blocking): the volta "He" points at the OBJECTOR in the benatar pair but at the POSITION in consent/nonid — pick one referent per body going forward so the antecedent can't wander (the Parfit trap was the acute case).
* SC / Tender-Pole `[OPEN]` defaults stand as banked (SC off-published, TP fit-gated) unless Josiah revises.

2c - CORRECTIONS-FIRST (fold into the K268 commit BEFORE batch-2; TX12-BACK forward nit). mg-stance-bait-01 response: `A few now exist` -> `Some now exist` ("a few" hard-codes a rough count that reads wrong as positions grow past ~40 — 94 now, 100 after batch-2; "Some" is count-robust, same length_band, -2 chars; seat-endorsed, cosmetic, non-blocking). This is a corpus body edit on an EMITTED deflection: apply it in the same splice pass, re-run the full gates + serializer parity + conservation, and it rides the ONE K268 commit (no separate ship). Confirm the served body flips at deploy-verify.

3 - Mission (RECOMMENDED default) — T4 GENUINE PHILOSOPHICAL batch-2: 3 objections x2 = 6 R2 positions. **This batch exercises the register guardrail — 2 clinical anchors + 1 warm-by-fit PILOT.**
* RECOMMENDED trio (reconcile vs the roadmap; if it dictates otherwise, follow it): **bradley-no-subject** + **boonin-critique** (the two named formal asymmetry-refuters from batch-1's benatar DIAGNOSIS — "Ben Bradley, David Boonin, Elizabeth Harman… attempted formal refutations"; cold-analytic -> Personal-Clinical is FIT, complete the asymmetry cluster benatar-asymmetry-attack opened) + **care-ethics** (the warm-by-fit PILOT — the paradigm case where pure clinical distance CONCEDES the objection: answering "your framework is coldly impersonal, ethics is care and relationship" with cold precision performs the very coldness indicted; author in Ethical-spine cadence UNDER the stance-fence — rebut the inference "care-relations ⇒ procreation justified", never grant the care-value as offsetting the imposed harm, never counsel; NOT Lacero [§4-gated, won't fire], NOT SC [off-published], Tender-Pole only if the objection genuinely lands in grief/tenderness). All 3 vendor-confirmed tier==4 + POSITIONLESS as of the K266 correction; re-grep bare-id anchors live in the pinned flagship `e654eabd` at open.
* **PRE-READ the care-ethics pair for Josiah before ship** (first warm-register entry on the published-curtained surface — the standing "HOLD any uncertain body for a pre-read" applies by default here; bradley/boonin ship same-session under the standing strike-pass if clean).
* NORMAL care-pass rails: author from the tier-4 cold-graded canon (short/medium + diagnosis + psychMechanism; efilist READ-ONLY @ `579cfbd`), never invented past it. Method = grant-then-mislocate / distinguish. R2 / public / b4_reply / [clinical, volta] for bradley/boonin; care-ethics keeps [volta] but the register tag reflects its warmer cadence per the guardrail (author's call — clinical-tag only if it reads clinical; do not force-tag). Provenance ratified "K268 strike-pass (Josiah, <TZ=America/Phoenix date>)", seat_rx "TX13 (library-seat review pending)".
* Triggers: fresh-matcher-per-form routing arbiter (new YO.Matcher(ents,{unsealed:false}).match(form)) BEFORE the full battery; 18/18-home target; oracle-net + crisis(18) + existing-corpus + self disjoint (0 collisions). ORACLE-NET LAW = avoid EVERY live oracle token (incl. generic ones — "argument", "worth living"; philosopher names route to library, so key bradley/boonin on the ARGUMENT-STRUCTURE, not "bradley"/"boonin" as bare handles unless they route [test at open]; care-ethics on "care ethics"/"ethics of care"/"you ignore relationships" etc.). YLEX-disjoint bodies; no ? ( ) ! ..., no "just"; every form == YO.normalize(form) (lowercase, punctuation+hyphens stripped). FRESH volta verbs — re-extract terminal "He <verb>" from the then-current corpus (`65db3cf0`) at open, disjoint from ALL of it (batch-1 added ranks/grounds/turns/declines/brackets/collapses — avoid every one). DIVERSIFY the closer shape (FLAG B): spread across maxim / image-return / actor-relocation / measure-collapse / consequence-turn; distinct governing image per 01/02 pair.
* NOT COUNT-NEUTRAL: +6 -> corpus 172->178 (169->175 authored, 3 crisis), 94->100 positions, 47->50 objections. Gates GROW — RECORD, do NOT verify-unchanged: register offline >5662, persona "100 position(s)", surface >209 pass / 0 fail (fail MUST stay 0). SERIALIZER PARITY: reproduce base `65db3cf0` byte-exact from parsed data (json.dumps ensure_ascii=False, indent=2, + trailing newline) BEFORE authoring, guard base==`65db3cf0` so a re-run never double-splices; splice the 6 before c-crisis-01; .authored untouched; re-dump. CONSERVATION: strip the 6 -> reproduce `65db3cf0` byte-exact.
* PAGE edit (src/successor/index.html): curio "172 entries — 169 authored" -> "178 entries — 175 authored" (Mr. Grey line ONLY; Yurei "165 entries" HELD); the two corpus-script `?v=K266` -> `?v=K268` (omega-assistant.js + successor-stage.js includes ONLY; every other ?v frozen). Status "first two sets" HOLDS. Re-scan positionless phrase-family == 0.
* Files (3): corpus `65db3cf0`->[new] · successor page `02662cba`->[new] · CLAUDE.md [K268 stratum]. NO efilist block, NO pin, NO search-index regen (successor page is wuld-search-excluded; corpus is a /components/ JSON — prove neutrality: build[tree] == committed `677a9678`).

PRE-FLAG BUDGET before the authoring lane — 6 positions (canon read + author + routing + gates + page + serializer + one wuld handoff), PLUS the first warm-register authoring (care-ethics) which needs a per-body stance-fence check (rebut-only / no positive appraisal / no counsel) + a Josiah pre-read. Recommend: author + gate + ship bradley/boonin same-session; HOLD care-ethics for the pre-read, ship on Josiah's nod (may split to a second commit if the pre-read lags). After K268: 50 of 82 signed. Then T4 batch-3 (e.g. harman-benign-creation + the next documented pair) ... through T4 (31) -> T5 (7).

4 - Fences that do NOT move. Crisis floor byte-frozen (c-crisis-01/02/03, 18 forms) · no Latin beyond mg-greet-04 · Yurei untouched · NO PIN — flagship `e654eabd` · anti-farming variance >=2 · oracle-net law (whole-token; philosopher-naming -> library; "argument"/"worth living" are live tokens) · YLEX law (bodies disjoint from Yurei's home lexicon: filing/minutes/department/attendance/ledger/requisition/archive/catalogued/shelved/night shift) · Section-D gates are NOT curtain-lift authority (the register-seat verdict is) · `ne-hoc-fiat` STAYS DOWN · corpus-wide reconciles = Josiah's deferred (R2/R3 label, seat_rx -> GREEN, FLAG A eugenics-01 "sorts", FLAG B closer-texture) · **REGISTER GUARDRAIL `f3593bcc` BINDS (register-by-fit per objection; warmth-by-fit stance-fenced; Lacero §4-gated; SC off-published; TP fit-gated)** · the "He"-antecedent style-note (one referent per body) · /argue/ RESTORE carry · efilist READ-ONLY · CF-beacon: served HTML by content-grep, md5 only the corpus JSON.

5 - Operator conventions + CONCURRENCY. `& { }` scriptblock · flat per-file statements, never arrays · Md5 helper (never H) · .git*.lock cleanup · HEAD==origin guard + base-md5 guards + result-md5 gates · explicit-stage named files, NEVER git add -u · staged-count gate · one-line if/else · curl.exe with -o temp files, never $null · http.postBuffer 524288000 + gate push $LASTEXITCODE · SERIALIZER PARITY reproduce-base-first + guard base==65db3cf0 · MODE-AWARE fresh-matcher-per-form routing arbiter · DUAL-BOOT CLOCK SKEW: stamp ratified with TZ=America/Phoenix date (the sandbox UTC reads a day ahead) · CF-BEACON served-HTML by content, corpus JSON by md5 · PATH NOTE: cloud sandbox root-owned clone — the Read tool cannot see it, bash/jq the corpus · CORPUS-CACHE: the page ?v bump refreshes the two JS files NOT the corpus (rides the /components/ TTL); the novel-buster curl is the served-corpus read · sidecars ride Downloads\k268\ · deliver sidecars to the device via the bridge + verify on-device md5s · CLAUDE.md base-md5 = RECOMPUTE at open · budget pre-flagged before the authoring lane. CONCURRENCY: the console-descent lane ships its OWN K-sequenced sessions (K269 next — seed-share follow-ons / mechanics; touches console-* files + CLAUDE.md + HEAD, disjoint from the corpus lane). On ship: base-guard CLAUDE.md against the FRESH md5 read at open (never a hardcoded value); if a console commit lands between clone and push, re-fetch + re-guard HEAD==origin before Move-Item, then re-append the K268 stratum onto the current CLAUDE.md.

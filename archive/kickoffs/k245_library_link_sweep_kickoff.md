WULD.INK -- K245 LIBRARY DEEP-LINK SWEEP (fix the broken glossary->library funnel). NO PIN.

Recommended default: site-wide sweep of every stale `library.wuld.ink/#/...` bare-root hash link to the flagship-routed `library.wuld.ink/combined#/...`. A LIVE BUG, wuld-side, NO PIN, NO efilist, NO flagship byte. ~30 links across 10 files. This restores the RWE/CODA CTAs Josiah flagged AND the whole glossary cross-link grammar (every glossary entry's "attested real-world deployments" deep-link currently lands on the wrong surface).

Why this session exists (diagnosed at K244 close): `library.wuld.ink/` root is rewritten by the efilist `_redirects` rule `/  /libraries/  200` -> it serves the Refusal-Suite UMBRELLA front door (the "refusal libraries cards"), whose router does NOT handle `#/library`, `#/rwe`, `#/coda`. Those three surfaces + the `#/rwe/<id>` deep-links live ONLY in the flagship `combined.html`, which owns the hash router (`applyHash`/`parseHash` on cold load; recognized views `library`/`rwe`/`coda`/`map`/`dep`; the standalone `rwe.html` is a redirect shim, NOT the target). So every `library.wuld.ink/#/...` link is currently pointing at the umbrella and falling through to its default cards. The fix is to insert `combined` into the path: `library.wuld.ink/#/` -> `library.wuld.ink/combined#/`. The search-index deep-links already use `library.wuld.ink/combined#obj-<id>` -- the `/combined` prefix is the established-correct pattern; the static-page links just never got it (or regressed when the umbrella front door + `_redirects` landed).

K244 LANDING CONFIRMED (verify-don't-redo): wuld HEAD `a2c66d2` (Mr. Grey v3 fold, atop `4736339`). Live-verified at K244 close: `mr-grey_manifest_v3.json` md5 `a70233`, `wrong_hour` clip 200, `successor-stage.js?v=K244` carries `mr-grey_manifest_v3` AND `avatar_manifest_v4`, `omega-assistant.js?v=K244` carries `mr-grey_manifest_v3`, `/successor/` page both `?v=K244` tags. Yurei v4 held; flagship `e654eabd` untouched.

VERIFY-AT-OPEN (fresh public-origin clone; the K216/K218a clone-reads-only discipline):
* wuld HEAD == origin/main == `a2c66d2` (or later; if later, re-derive base guards from live git per K231). One-committer: `git ls-remote` at open AND before the handoff.
* THE TARGET VERIFICATION IS STEP 1, NOT AN ASSUMPTION. Before sweeping, LOAD (Claude-in-Chrome or the operator eyeballs) each candidate on the LIVE flagship and confirm it routes:
  - `https://library.wuld.ink/combined#/library` -> the Library/taxonomy surface (NOT the umbrella cards).
  - `https://library.wuld.ink/combined#/rwe` -> the Real-World-Examples surface.
  - `https://library.wuld.ink/combined#/coda` -> the Coda surface. **`coda` is the one to watch** -- `combined.html` has a `data-view="coda"` button and a `'#/coda'` string, but the `view === '...'` dispatcher grep surfaced only library/rwe/map/dep at K244 recon; CONFIRM `#/coda` cold-loads the coda (if it is button-only / not hash-routable, find the correct coda deep-link -- e.g. `#/library` + a coda anchor, or a scroll target -- before sweeping the coda links).
  - `https://library.wuld.ink/combined#/rwe/why-not-suicide` (a representative per-objection deep-link) -> that objection's attested deployments.
  If any candidate does NOT route on cold load, STOP and escalate: the alternative fix is efilist-side (make the `/libraries/` umbrella an actual outer router that forwards these hashes to `/combined`, OR add `_redirects` entries) -- that is a SEPARATE efilist session (still NO PIN -- umbrella/routing is aux, not the pinned flagship -- but it is efilist-committer work, not this wuld session). Do not guess; the whole sweep rides on the target being right.

THE SWEEP (once the target is confirmed; wuld-side, NO PIN):
* 10 files carry `library.wuld.ink/#/` (K244 grep): `src/library-about/index.html` (4) · `src/coda/index.html` (3) · `src/why-not-suicide/index.html` (3) · `src/violence-as-reductio/index.html` (3) · `src/glossary/labor-sine-fructu/index.html` (7) · `src/glossary/two-layer-architecture/index.html` (2) · `src/glossary/framework-vs-actor-distinction/index.html` (4) · `src/glossary/cascade-math-safeguard/index.html` (3) · `src/glossary/censorship-reversal-trap-door/index.html` (3) · `src/glossary/proxy-gamble/index.html` (1). Re-grep at open (`grep -rnoE "library\.wuld\.ink/#/" src --include=*.html`) -- the count is the gate; every hit is swept, zero missed, zero over-reach.
* The replacement is purely additive in the path: `library.wuld.ink/#/` -> `library.wuld.ink/combined#/`. It applies to BOTH the `href="..."` attribute AND any visible `<code>...</code>` mirror of the URL. DECISION for the visible `<code>` text: keep the SHORT hash form the reader would recognize (`#/rwe`, `#/rwe/<id>`) OR show the full `combined#/rwe` -- recommend keeping the href fixed (the functional part) and updating visible `<code>` to match what actually works (`combined#/rwe`) so a reader who copies it lands right; flag if you'd rather keep visible code short.
* Build the edits as LF sidecars via git-show -> python (large-file discipline is moot here -- these are small pages -- but the deterministic-substring-replace + count-gate pattern is cleaner than Edit/Write and gives exact result-md5s for the handoff). Assert each file's `library.wuld.ink/#/` count drops to 0 and `library.wuld.ink/combined#/` rises by exactly the swept count; nothing else in the file changes (diff = only these link lines).
* DO NOT touch: any `library.wuld.ink/combined#obj-<id>` already-correct links (search-index deep-links -- leave them); the `/coda/` and `/library-about/` PAGES' own content beyond the links; the flagship; efilist.

GATES:
* search-index impact: `build_index.py` over the swept tree vs committed `564d6a81`. The harvest surface is h1/title + lede/intro + h2/h3 ONLY -- these links live in CTA `<a>`/`<code>`/body `<p>` below heading level, so the index is very likely NEUTRAL (prove build[clean]==build[mine]==`564d6a81` and HOLD). IF a swept `<code>` URL sits inside a harvested lede/heading and the index DOES change, that is fine -- regen + ship the new index (still NO PIN; search-index is a wuld-side sidecar, K129/K202 class) -- but PROVE which case you are in, do not blind-regen.
* sitemap: route-neutral (no page added/removed) -> `python3 tools/gen_sitemap.py --repo . --check` PASS, sitemap HELD (62).
* Live verify post-push: `curl.exe` each swept target class 200 + (browser) the CTAs on `/library-about/`, `/coda/`, and one glossary page (e.g. `/glossary/labor-sine-fructu/`) actually land on RWE/CODA/deployments, not the umbrella.
* No component/JS/asset touched -> no `?v` bump (static-page href edits only; K238 -- bump nothing).

SHIP: ONE wuld PS block (NO PIN, NO efilist, NO `?v`, NO search-index UNLESS the neutrality proof fails). `& { ... }`; `curl.exe` not `curl`; one-line if/else; helper `Md5`; `.git\*.lock` cleanup first; TZ=America/Phoenix. HEAD guard `a2c66d2` -> per-file base-blob + working-copy base-md5 guards -> Move-Item the `.k245` sidecars -> result-md5 gates -> explicit-stage the NAMED swept files (+ search-index IFF regenerated) (NEVER `git add -u`/`.`) -> commit -> push -> `curl.exe` live checks (`-o "$env:TEMP\x"` not `-o $null`).

VERIFY-AT-OPEN PINS: wuld HEAD `a2c66d2`. `src/library-about/index.html` blob `c282e23e`/14,569 (base of one swept file; re-derive the other 9 live). search-index `564d6a81` HELD. sitemap 62. flagship `library.wuld.ink/combined` `e654eabd` (pin v4.0.0; READ-only this session). K244 held: `omega-assistant.js` `060c7d7a` · `successor-stage.js` `76fc0d73` · `src/successor/index.html` `19d719b7` · `mr-grey_manifest_v3.json` `a70233` + 14 v3 clips; Yurei v4 `avatar_manifest_v4.json` `8e9e8c7b`.

DEFERRED / QUEUE (Josiah's other two, in recommended order behind this bug fix):
* K246 -- ADMIN "GATEKEEPER" gate-management vertical (Josiah's explicit want: "password-block any nav tab or section... change the password, unblock... manually whenever I wish"). Generalize the K233/K235 SOFT curtains (currently per-page inline: `/successor/` sgate passphrase `ne-hoc-fiat`, `/console/` cgate `facilis-descensus`, greyed via `nav.css` `a[href="/X/"]{opacity:.4}`) into an operator tool on admin.wuld.ink. RECOMMENDED approach: a new admin site-edit vertical (the K212/K220 diff-confirm->one-commit pattern) that INJECTS/REMOVES the inline curtain + sets/rotates the passphrase on a chosen PAGE, and adds/removes the nav.css grey rule -- per-page, surgical, reuses the existing pattern, no site-wide JS. DESIGN FORKS to resolve with Josiah at that open (genuine forks, not menu-for-menu's-sake): (a) page-level (recommended, tractable) vs section-level gating (a bigger lift -- gating a portion of a page); (b) admin-site-edit-per-page (recommended) vs a config-driven shared curtain component (`gates.json` + a site-wide include -- more powerful, bigger blast radius). HONESTY GATE: these are SOFT dormancy curtains (passphrase is view-source-visible) -- the tool manages dormancy, NOT security; if Josiah wants a REAL lock, that is Cloudflare Access (operator-side Zero Trust, the `/_/successor-protocol/` pattern), not a worker build. Base pins: admin worker `workers/admin/src/index.js` `2767f8f9`/200,013 (K234-era; DEPLOYED); nav.css `5828a9ee`. NO PIN; ends with `npx wrangler deploy` from `workers\admin`.
* K247 -- CONSOLE IMMERSION (Josiah's K242 direction): give `/console/` successor-stage-level immersion (full-bleed room/scene background + command text on a glass band, eventually animated to the descent's procedural rooms). A DESIGN+BUILD session -- no scene assets exist yet; mirror the K241 successor-stage immersion pattern (auto-open, glass lower band, page-scoped). Scope with Josiah; stays gated `facilis-descensus` until fleshed. The biggest/most-creative of the three -- deserves its own focused session with a design pass.
* Standing electives (unchanged): the K233/K235 release-day curtain strips (blocked on fleshing out); the YUREI HAUNTING-widget refresh (render-lane next per the Grey Sec.8); GREY PERSONA WIRING stays GATED (Omega-tab + R.2 corpus cliff -- other seats); the full `docs/library-claude-coordination.md` adv-map Exchange-mirror (K221/K223/K226+K229, off-wuld one-committer); gap-log lane switch; Stripe W-9; PAT rotation Sep 04 2026; wrangler 4.95->4.110; efilist Pages `@v4->@v5`; FIRST-PUBLISH ritual.

HAZARDS (carry): ccxxvii (explicit-stage NAMED; never `git add -u`/`.`) -- K111 (`.git\*.lock`) -- K113 (`curl.exe`) -- K115a/ccxxxv (large files via git-show/python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone; the wuld mount can serve multi-day-stale worktree bytes) -- K231-live-wins (re-derive every guard + the grep count live at open; the target route is VERIFIED not assumed) -- ccxxxvi (`.k245` sidecars) -- K186a (`/components/` 300s TTL -- N/A this session, no component touched) -- K110 (ASCII `--`/`->`; U+FFFD 0) -- K243-curl-discard (`-o "$env:TEMP\x"` / `-o NUL`, never `-o $null`). NEW watch: the `#/coda` route is the one unconfirmed target -- verify it cold-loads before sweeping the coda links, else find coda's real deep-link. TZ=America/Phoenix. If a concurrent seat pushed, renumber (log label only).

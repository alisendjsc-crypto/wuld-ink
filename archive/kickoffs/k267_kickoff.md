CONSOLE DESCENT — K267: SEED-SHARE deep-links (register-safe CORE) + the deferred no-exit MECHANICS (Josiah-ruled). Concurrent NON-SHIPPING build + determinism pass. Operator paste. Safe to run ALONGSIDE K266. Read this whole file before any write. This session gives the wuld://console descent game (wuld.ink/console/, the seeded text-adventure inside the K249a takeover) a SHAREABLE world and folds the deferred K265 mechanics proposal into a ruling; it does NOT commit to wuld while K266 runs, does NOT touch the corpus/CLAUDE mid-flight, does NOT move the pin. The public ship is sequenced AFTER K266 lands (same session, on Josiah's go — the K265 pattern).

WHY THIS IS CONCURRENCY-SAFE: K266 (T4 genuine-philosophical batch) is a CORPUS/efilist session — it commits corpus JSON + successor page + CLAUDE.md and moves HEAD. Two sessions that both commit to wuld collide on the CLAUDE.md base-guard + HEAD guard (and concurrent bridge git risks .git\index corruption). This session sidesteps all of it by being NON-SHIPPING WHILE K266 RUNS: it reads the wuld repo, builds + tests against a clone, and holds reviewed sidecars. It touches ONLY the console-* files (disjoint from K266's corpus/successor/CLAUDE lane). The engine + shell are PURE/testable, so every change is sandbox-verifiable with ZERO device dependency. When K266 has LANDED, the ship folds in-session on Josiah's go: verify-at-open on the then-current HEAD, Move-Item the reviewed console files, bump the CHANGED files' ?v, re-run the battery, its own K267 stratum. NO PIN.

0 - Orientation
1. Fresh public-origin shallow clone of wuld-ink (github.com/alisendjsc-crypto/wuld-ink) — REFERENCE + build target; do NOT plan to commit while K266 runs. mounts NEVER read for repo state.
2. Read CLAUDE.md FIRST (orientation sequence + the K265 stratum for the current console pins + the "Carries" standing-hazards, which now bank the K265 scene-e2e pin lesson), then docs/wuld-ink-cowork-brief.md. Recall the console-game.md project-memory note and Downloads\console-proto\console_proto_review_1.md (the K265 review + its §6 MECHANICS proposal + register tradeoffs — the basis for this session's secondary mission).
3. Read the console stack (src/components/): console-engine.js (PURE seeded generator — K265 deepened the ADJ/NOUN/AIR/SIGHTS/ITEMS pools; genWorld(seed) is deterministic + graph-invariant under pool growth) - console-prng.js (mulberry32/xmur3 seeded PRNG) - console.js (I/O shell: command parse, MOVES/CARRYING/SEEN counters, map, new/seed, own-key persistence wuld:console:save/audio) - console-scene.js/css (K249a atmospheric takeover — BYTE-HELD, DO NOT retune). Theme lock: "a black-console descent through an ashen structure, nothing more"; the no-exit register ("you wake at the threshold; the way back is a wall now").
4. BASELINE the gates green before touching anything: node tools/console/console-e2e.cjs (77/77 as of K265) + node tools/console/console-scene-e2e.cjs (94/94) + node tools/omega/wgate-e2e.cjs (24/24). Record the baseline so any delta is attributable. (These are the K265-shipped counts; verify live, do not trust this file — K266 is corpus-only and must not have moved them, but prove it.)

1 - Mission (recommended default — CORE, register-SAFE, Cowork-owned, fully sandbox-testable)
SEED-SHARE deep-links — a shareable descent. Deterministic worlds make this exact: the same seed is the same structure, so a link IS the world.
(a) BOOT-FROM-URL: on boot, read a seed from the URL HASH (`#seed=<word>`, hash not query — client-only, never hits Pages routing, curtain-safe). If present, it OVERRIDES the localStorage resume (the sharer wants the recipient to see THAT seed, not the recipient's saved game); if absent, behaviour is unchanged (resume, else random). The seed must survive the cgate unlock (the curtain is a class toggle + localStorage with NO reload, so the hash persists across the unlock — verify the console reads it at boot regardless of curtain state).
(b) SHARE-OUT: a `share` command + a `[ share ]` control that emits the current run's deep-link (https://wuld.ink/console/#seed=<seed>) into the console output and copies it to the clipboard (execCommand fallback, like the notes surface). In-register, terse ("a way back in, for someone else: <link>" — not gamified-SaaS chrome).
(c) EXTEND the PURE e2e (sandbox-only): URL-hash -> exact world (parse + boot); share-link ROUND-TRIP (the emitted link's seed regenerates the byte-identical world); seed OVERRIDES resume when both present; malformed/empty/hostile hash falls through safely (no throw, no injection — the seed is an opaque string, never eval'd, never a corpus key); own-key isolation still holds (no writes outside wuld:console:*); the K265 determinism + reachability invariants stay green across the sweep.
(d) SEED-SAMPLE + live proof: a couple of screenshots of a shared-link cold-boot (open /console/#seed=<known>, pass the curtain, land in that exact world) for the review.
SURFACE NOTE: this touches console.js (shell: URL read + share + clipboard) and MAY touch src/console/index.html (only if a seed-persist shim is needed across the curtain — prefer NOT to; the hash should persist for free). It does NOT touch console-engine.js unless a tiny seed-normaliser helper is cleaner there. FLAG at ship: every console-component whose bytes change bumps its ?v (K238), AND console-scene-e2e line-435 PINS the shell-trio ?v — so a console.js ?v bump updates that pin in the SAME ship (the K265 lesson; it caught 93/94 last time).

1b - Mission (SECONDARY, Josiah-directed — PROPOSE + DRAFT, do not impose)
The deferred K265 §6 MECHANICS. The brass-key->terminus gate + a futility-coded descent close already exist ("you go down. you were always going to go down."). Genuinely-open, from the K265 review:
- a SEEN-complete *futility* line (fires when seen==roomCount; proven mechanically reachable — every non-terminus room is walkable without descending, 0 stranded across the sweep). MUST read as futility, never a 100% reward — a triumphant completion would fight the no-exit weight. Josiah's K265 lean was that the terminus already lands it; this session DRAFTS the line in-register + presents the tradeoff for his ruling.
- optional: a deepened terminus/descent close.
Present as a DESIGN PROPOSAL with the register tradeoffs (like the Yurei/aesthetic register nods). Build ONLY what Josiah rules in THIS session; otherwise it stays a proposal folded by a later session. Honor "you never left".

2 - Firewall / DO-NOT
* console-scene.js/css (K249a) BYTE-HELD — no visual retune; that arc is closed.
* Homepage zero-JS invariant, the cgate curtain, wuld:console:unlocked, cgate-open, and the console page's <meta name="wuld-search" content="exclude"> all UNTOUCHED. Seed-share must NOT weaken the curtain (a deep-link still lands on the passphrase for a locked visitor) nor un-exclude the page.
* FICTION ONLY — HARD argument-library firewall (the console-e2e STANCE assert stays green). The seed is an opaque string: never an eval target, never a corpus key, never a stance token. Sanitise the shared/parsed seed to a safe charset.
* HAZARD (bank from K265): (1) console-scene-e2e line-435 PINS the shell-trio ?v — ANY console-component ?v bump updates that pin in the same ship. (2) If any engine change moves genWorld output, the console-e2e fp() goldens (hash title+desc+item) must be DELIBERATELY regenerated + documented, never silently loosened. (3) Keep e2e invariants STRUCTURAL (determinism / reachability / round-trip / counts), not brittle exact-string goldens where avoidable.
* NON-SHIPPING WHILE K266 RUNS: deliver to Downloads\console-proto-K267\ (build artifacts + samples + screenshots + review note). Do NOT place anything into the wuld repo tree, do NOT append wuld CLAUDE.md, do NOT emit a PS commit block, do NOT bump any ?v — UNTIL K266 has landed and Josiah says go.

3 - Deliverables (all via the bridge; NO wuld commit until the sequenced ship)
* Downloads\console-proto-K267\ : modified console.js (+ console-engine.js / src/console/index.html ONLY if genuinely needed) with seed-share; the EXTENDED console-e2e (green in-sandbox); a seed-share round-trip sample; 1-2 live screenshots (a shared-link cold-boot).
* A review/relay note (console_proto_review_2.md): what changed (surface touched, e2e deltas), the MECHANICS draft (§1b) with register tradeoffs for Josiah's ruling, and the SHIP CHECKLIST for the sequenced fold: bump the CHANGED console files' ?v (K238 — console.js -> K267, engine only if touched); UPDATE console-scene-e2e line-435 shell-trio pin for every bumped component; console-e2e + console-scene-e2e + wgate-e2e stay green; its own K267 CLAUDE.md stratum; NO PIN, NO search-index/sitemap change (console page search-excluded).

4 - Concurrency + sequencing
* SAFE alongside K266 by construction (non-shipping; disjoint files; pure engine/shell work is sandbox-verifiable). Do NOT attempt any wuld commit or CLAUDE.md touch while K266 is in flight.
* THE SHIP (same session, after K266 LANDS, on Josiah's go — the K265 pattern): fresh clone at the then-current HEAD, verify-at-open (confirm K266 left the console files at their K265 pins), Move-Item the reviewed console files from Downloads\console-proto-K267\ into place, bump the changed files' ?v, UPDATE the scene-e2e shell-trio pin, re-run the console + scene + wgate battery, author the K267 stratum (carry the K266 corpus/flagship pins forward VERBATIM — only the console delta changes), ONE wuld PS block (lock cleanup + HEAD/one-committer guard + blob-sha base guards + Move-Item + result-md5 gates + explicit-stage the named files + staged-count gate + postBuffer push gate + curl.exe content-grep deploy-verify). NO PIN, NO efilist, NO search/sitemap.
* Ship is GATED on Josiah's tone nod (any new in-register strings: the share line, and any ruled-in mechanic) + his mechanic ruling.
* Budget: a bounded sandbox lane (engine/shell + a seed-share round-trip + tests + a couple screenshots). Pre-flag only if a ruled-in mechanic or a seed-persist shim balloons the surface.

Also carry forward K265's landing bookkeeping if not yet folded: the K265 "console landing: pending" carry in CLAUDE.md (commit f088403, served engine=K265 + page ?v=K265 both verified live) should be resolved to LANDED at this session's verify-at-open, in the K267 stratum.

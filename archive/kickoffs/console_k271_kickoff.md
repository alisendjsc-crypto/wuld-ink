CONSOLE TEXT-EFFECTS SMORGASBORD — K271 (console lane; the operator confirms the number at open — ODD console interleave, atop the K269 console commit + any K270 corpus). A NON-SHIPPING build + review session by default; the public ship is sequenced AFTER any concurrent corpus session lands, on Josiah's go (the K265/K267/K269 pattern). This session makes the console's TEXT FEEL ALIVE — CRT-monitor phosphor, typeout, in-theme glitch/decay, and diegetic ASMR audio — as an EXPLICIT, Josiah-authorized EXCEPTION to the austerity, but bound to the game's theme and spirit so the effects serve the descent rather than fight it. It does NOT move the pin, does NOT touch the corpus/CLAUDE mid-flight, and holds reviewed sidecars until the sequenced fold.

READ THIS WHOLE FILE BEFORE ANY WRITE.

WHY THIS IS THE MANDATE (Josiah, K269 close): "Give the Console game a smorgasbord of text effects and art features that tie to text itself. Go all out on ideas. This is an exception to the austerity of the game — but make the effects in the game's theme and spirit, so as not to be an antagonist against it. Find ways to make text feel alive and animated, more immersive than plain text. CRT monitor / typeout / ASMR effects." The austerity is LIFTED for the effects layer; the THEME (a black-console descent through an ashen structure, the no-exit weight, the cold under every room) still governs every choice. An effect that reads as arcade juice or gamified reward is an antagonist to the register and is out; an effect that reads as the dying CRT of the structure itself is in.

0 — ORIENTATION
1. Fresh public-origin shallow clone of wuld-ink (github.com/alisendjsc-crypto/wuld-ink) — REFERENCE + build target; do NOT commit while a concurrent corpus session runs. Mounts NEVER read for repo state (K213/K218a).
2. Read CLAUDE.md FIRST (orientation + the K269 stratum for the current console pins + the Carries/standing-hazards), then docs/wuld-ink-cowork-brief.md. Recall the console-game.md project-memory note and Downloads\console-proto-K269\console_proto_review_3.md (the K269 sigil review — the sigil-on-descent is a coordination point for a descent-crescendo effect).
3. Read the console stack (src/components/): console-prng.js (K235) · console-engine.js (K267, Option-B winText; BYTE-FROZEN — effects are shell/CSS, not world-gen) · console-sigil.js (K269, pure seeded mark) · console.js (K269 I/O shell: print()/echo()/cue()/apply()/the WebAudio synth + reduced-motion flag + own-key wuld:console:*) · console.css (K235 terminal styling — the effects LIKELY live here + a new console-fx component) · console-scene.js/css (K249a atmospheric takeover — BYTE-HELD, DO NOT retune; it already carries a scanline + blink under motion, so the new CRT layer must COMPLEMENT it, not duplicate/fight). Theme lock unchanged: "a black-console descent through an ashen structure, nothing more" — now with a layer that breathes.
4. BASELINE the gates green before touching anything: node tools/console/console-e2e.cjs (125/125 as of K269) + node tools/console/console-scene-e2e.cjs (94/94 → note: 95/95 after K269 ships the sigil load-order assert; read the served/committed count, do not trust this file) + node tools/omega/wgate-e2e.cjs (24/24), ALL FROM REPO ROOT (wgate reads cwd-relative). Record the baseline so any delta is attributable. Verify-at-open the K269 console pins from the CLAUDE.md K269 stratum (console-sigil.js md5 c66f5399/blob 6f657214 ?v=K269, console.js md5 23e9405c/blob dafb0ef5 ?v=K269, console-e2e cb962b40 [125], console-scene-e2e c9271e63 [95], index.html 699eb551, engine dc4ff674 ?v=K267 frozen, prng ?v=K235, scene ?v=K249a frozen).

1 — MISSION (the smorgasbord — go all out, but in-theme; every item reduced-motion-safe)
Build a text-effects layer for the console. RECOMMENDED HOME: a NEW opt-in component `console-fx.js` (+ `console-fx.css`) — modular, pinnable, dependency-free vanilla (the "shell requires nothing" firewall; NO external libraries) — wired by console.js and gated by prefers-reduced-motion + a user toggle. Rationale: keeps console.css/console.js churn bounded and gives the effects their own ?v + e2e; alternative is folding into console.css/console.js (leaner surface, but muddier pin). DECIDE at build and flag it. The candidate effects, all tied to TEXT and to the descent:

(a) TYPEOUT / structure-speaks — new output prints character- or line-by-line with a tuned cadence (the structure narrating itself), a soft key-tick per char (diegetic, opt-in audio), and an INSTANT-COMPLETE on any keypress (never trap the reader). The room descriptions, the terminus text, the help — chosen surfaces, not every line. Reduced-motion → instant (no reveal).
(b) CRT PHOSPHOR — a subtle bone phosphor glow/bloom on the terminal text, faint scanlines, a slow flicker, and phosphor persistence (a brief ghost trail on scroll). Monochrome, bone-on-black, tuned FAINT (the structure's failing monitor, not a synthwave demo). MUST complement the K249a scene's existing scanline/blink, not double it. Reduced-motion → fully static (glow may stay as a static bloom; NO flicker/scanline animation).
(c) IN-THEME GLITCH / ASHEN DECAY — sparse, ominous character corruption/decay (text that briefly crumbles to ash-glyphs then settles), a flicker on weight-events (a blocked move, the descent). Rare and cold, never noisy — decay, not error-spam. Reduced-motion → off (static text).
(d) LIVING CARET + SETTLE — a breathing block cursor (deepen the existing blink); new lines "settle" in (micro-fade / 1px drop, deepening con-reveal). Reduced-motion → static caret, no settle.
(e) DESCENT CRESCENDO — on the descent-close (event:"win"), the effects intensify briefly: the CRT dims/sinks, a low tone falls, the K269 SIGIL reveals into the dimmed frame (coordinate with the existing sigilReveal). The one place the effects are allowed to swell — because the narrative does. Reduced-motion → the sigil still reveals statically, no swell.
(f) ASMR / DIEGETIC AUDIO — extend the existing cue() WebAudio synth (opt-in via the sound toggle, OFF under reduced-motion): soft key-ticks on type, a low ambient vault-hum/drone bed, a cold whoosh on room change, a deeper resonance on descent. The sounds of the PLACE, never arcade SFX. No audio files (WebAudio synth only; ZERO assets).

CONTROL SURFACE: the effects are OPT-IN and user-controllable — a `[ crt ]` / `fx` toggle (and/or an intensity: off / low / full), persisted own-key (`wuld:console:fx`), DEFAULT tasteful-on (propose; Josiah rules the default). prefers-reduced-motion is a HARD override that forces the static/instant path regardless of the stored pref (the K249a + audio precedent). A first-visit reader must never be ambushed — if default-on is ruled, keep it gentle.

2 — §1b JOSIAH-DIRECTED RULINGS (propose + draft; build the ruled-in set)
Josiah said "go all out," so bring the full menu — but the effects are HIS aesthetic call, so present and let him pick/veto/tune:
- WHICH effects ship (the a–f menu — recommend the set that best "makes text alive" without antagonizing the register; my lean: typeout + faint CRT phosphor + living caret + descent-crescendo as the core, glitch/decay sparse, ASMR opt-in — but present all and let him cut).
- DEFAULT state (tasteful-on vs off-by-default) + intensity model (single toggle vs off/low/full).
- CRT INTENSITY / tone nod (faint-failing-monitor vs more overt) — deliver 2–3 rendered/GIF samples at different intensities for the pick.
- Any new in-register strings (`[ crt ]`/`fx` label, an intensity line, a "reduced-motion: effects static" note).
- TYPEOUT default speed + which surfaces get it.
Draft variants (a short lab / a few GIFs), present the register tradeoffs, build only the ruled-in set.

3 — FIREWALL / DO-NOT
- console-scene.js/css (K249a) BYTE-HELD — no retune; the CRT layer is NEW and must complement the scene's existing scanline/blink, never restyle it.
- console-engine.js BYTE-FROZEN — effects are SHELL/CSS/audio only; they never touch world-gen, genWorld, or fp() goldens. The seed/world stay deterministic.
- REDUCED-MOTION IS NON-NEGOTIABLE (a11y, NOT austerity — it stays even though austerity is lifted): every effect gates to a static/instant equivalent under prefers-reduced-motion; audio stays off under it. This is the load-bearing contract — an e2e assert must prove EVERY effect is inert under reduced-motion.
- FICTION ONLY — HARD argument-library firewall (the console-e2e STANCE assert stays green over any new component). NO external libraries (vanilla CSS/JS/WebAudio only; ZERO assets — no fonts, no audio files, no images).
- Homepage zero-JS invariant, the cgate curtain, wuld:console:unlocked, cgate-open, and the console page's <meta name="wuld-search" content="exclude"> all UNTOUCHED. Effects render behind the curtain; the page stays search-excluded.
- OWN-KEY: any new pref persists ONLY under wuld:console:* (e.g. wuld:console:fx). No writes elsewhere.
- NON-SHIPPING WHILE A CORPUS SESSION RUNS: deliver to Downloads\console-proto-K271\; do NOT touch the wuld repo tree / CLAUDE.md, do NOT emit a PS block, do NOT bump any ?v — UNTIL the concurrent session (if any) lands and Josiah says go.

4 — PIN / e2e DISCIPLINE (the load-bearing hazard, now a QUARTET → grows)
- The console-scene-e2e shell pin is a QUARTET as of K269 (prng/css K235 + engine K267 + sigil K269 + console.js K269). ANY console-component ?v bump updates the pin array + the load-order assert in the SAME ship (the K265 93/94 trap — THIRD+ confirmation). If console-fx.js/css join the includes, the pin becomes a QUINTET/SEXTET and each new include needs a load-order assert; if console.js changes (it will, to wire fx), bump console.js → K271 and update the pin.
- EXTEND console-e2e with an FX pass (sandbox-only): effects OPT-IN (nothing animates until enabled/allowed); prefers-reduced-motion → every effect static/instant + audio off (the non-negotiable assert); typeout completes to the full text (and instant-completes on keypress); own-key isolation (writes only wuld:console:fx); the STANCE firewall stays green over console-fx; no-throw across the surface; the world/seed/sigil determinism invariants stay green (effects don't perturb them).
- Fresh ?v (K271) on every changed component (K238); NO PIN; NO search-index/sitemap change (page search-excluded).

5 — DELIVERABLES (all via the bridge; NO wuld commit until the sequenced ship)
- Downloads\console-proto-K271\: the NEW console-fx.{js,css} (or the console.css/console.js diffs if folded) + the console.js wiring + the index.html includes (staged, applied at ship) + the EXTENDED console-e2e (green in-sandbox) + the scene-e2e pin update; 2–3 intensity SAMPLES (GIF/screenshots via the local Playwright serve — the effects are motion, so a GIF sells them) + a short design lab if variants warrant.
- A review/relay note (console_proto_review_4.md): what changed (surface, e2e deltas), the §1b effect/intensity menu with register tradeoffs for Josiah's ruling, the reduced-motion proof, and the SHIP CHECKLIST for the sequenced fold (verify-at-open the K269 pins → LANDED; Move-Item; ?v bumps K271; scene-e2e pin grow; battery green; K271 stratum; ONE wuld PS block; NO PIN).

6 — CONCURRENCY + SEQUENCING
- SAFE alongside a corpus K270 by construction (non-shipping; file-disjoint from corpus except CLAUDE.md + HEAD; effects are sandbox/browser-verifiable). Do NOT attempt any wuld commit or CLAUDE.md touch while a corpus session is in flight.
- THE SHIP (after any concurrent corpus session LANDS, on Josiah's go): fresh clone at the then-current HEAD, verify-at-open the K269 console pins (resolve K269 landing → LANDED with its served checks), Move-Item the reviewed files, bump the changed files' ?v → K271, grow the scene-e2e pin + load-order asserts, re-run the console + scene + wgate battery, author the K271 stratum (carry the corpus/flagship pins forward VERBATIM), ONE wuld PS block (lock cleanup + HEAD/one-committer guard + blob-sha base guards + Move-Item + result-blob gates + explicit-stage the named files + staged-count gate + postBuffer push gate + curl.exe content-grep deploy-verify: served console-fx?v=K271 contains a marker; /console/ page includes the new includes by CONTENT-grep [CF-beacon]; page still search-excluded). NO PIN, NO efilist, NO search/sitemap.
- Ship is GATED on Josiah's §1b effect/intensity ruling + tone nod on any new strings.
- BUDGET: this is BIGGER than the K269 sigil — multiple effects + audio + CSS + tests + motion demos. Pre-flag if the ruled-in set balloons (a full glitch engine or a rich audio bed can grow the surface); redirect large tool outputs to disk, keep the GIF/demo bounded. Object BEFORE any action that risks a mid-task ceiling crash.

Carry forward K269's landing bookkeeping if not yet folded: resolve the K269 "landing: pending" carry (commit sha + served checks — served console-sigil.js?v=K269 contains "genSigil"/"descent sigil"; /console/ page includes console-sigil.js?v=K269 + console.js?v=K269 by content-grep) → LANDED, in the K271 stratum at verify-at-open.

K263 KICKOFF — T3 STRUCTURAL/PRAGMATIC batch-2 (5 clean structural ×2 = 10 R2 positions). Operator paste for the next wuld.ink Cowork session. Read this whole file before any write. TX9-BACK landed GREEN — all 12 batch-1 positions pass §B, ZERO carves; one coinage item pre-resolved, one register-rule decision defaulted below. K263 opens into batch-2 unless Josiah flips the ai-fear rule (§2).

0 · Orientation (mandatory, in order)

1. Fresh public-origin shallow clone of `wuld-ink` (github.com/alisendjsc-crypto/wuld-ink); mounts are NEVER read for repo state. Read `CLAUDE.md` FIRST (the K262 stratum is live context: Phase 4 OPEN; T1+T2 CLOSED — Folk tier 17/17; T3 batch-1 SIGNED — 6 clean structural ×2, 72 positions across 36 objections; TX9-BACK GREEN), then `docs/wuld-ink-cowork-brief.md` if unsure of conventions; `docs/refusal-libraries-roadmap.md` governs T3 scope.
2. The relay lives in `C:\Users\y_m_a\Downloads\Successor Protocol\`. K262 receipts on that desk: `omega_proxy_expansion_transmission_9.md` (TX9, the batch-1 review request) + `K262_batch1_bodies.md` + the seat's TX9-BACK reply (the §B GREEN verdict). The T3 canon is `efilist_argument_library_v4_0_0.json` — `.objections[]` where `tier==3` (responses short/medium/long + diagnosis + psychMechanism) — in the efilist repo (READ-ONLY, HEAD `579cfbd`).
3. Verify-at-open, STOP on any mismatch (K252). Confirm K262 LANDED: wuld HEAD == origin == `8295b854d67fc2bb68dcfa4eea9d3903341e7486` (short `8295b85`), parent `79757a3`; one-committer (ls-remote == HEAD). Pins to verify: corpus `omega-corpus-mrgrey.json` md5 `717c4b95` / 224808 (150 = 147 authored + 3 crisis; 72 positions / 36 objections). `mrgrey-register-gate.cjs` `15306a2d` (offline 5024). `omega-persona-gate.cjs` `fab75c11` ("72 position(s) provenance-stamped"). `omega-surface-e2e.cjs` `2277fa9c` (165 / 0, crisis-first). Successor page `src/successor/index.html` `5d1b1465` / 20369 (curio "150 entries — 147 authored"; corpus `?v=K262` ×2; Status "first two sets"; positionless-scan 0). `omega-assistant.js` `ced05284` (VER K255) + `successor-stage.js` `c04b7838` (VER K255). CLAUDE.md `7bba9168` (the K262-landed file; recompute after the K263 stratum). Vendor `tools/omega/vendor/objections-index.json` `d034af15` / 41800 (82 ids; re-verify vs efilist HEAD `579cfbd`). Flagship `library.wuld.ink/combined` `e654eabd` / 2,963,752 — PINNED, UNTOUCHED, NO-PIN session. IF HEAD is still `79757a3` / corpus still `7131536d`, K262 did NOT land — STOP, tell Josiah, re-ship the K262 sidecars from `Downloads\k262\` BEFORE any K263 authoring. Record in the K263 stratum: "K262 landed `8295b85`; live asserts OK (served corpus `717c4b95` via novel buster, page `?v=K262` ×2 + '150 entries — 147 authored', flagship held `e654eabd`)" + a T3 round-trip ("the economy needs growth" → `pos-economy-population-01` + `combined#obj-economy-population`; "steven pinker" → `pos-pinker-better-world-02`) + a crisis-first proof ("i want to die" → the frozen floor while "ai will destroy humanity" → `pos-ai-fear-01`). CF-BEACON: served HTML by CONTENT-grep (`?v` count, curio phrase); the corpus JSON serves clean — md5 it with a novel `?vk263=1` buster (the git blob is truth); the flagship served clean `e654eabd` at K262 — treat an exact match OR a +~504 beacon delta as still-held, a third value as a real move.

1 · Where the arc stands
Phase 4 OPEN. T1 COMPLETE. T2 COMPLETE — Folk tier CLOSED 17/17. T3 batch-1 SIGNED (6 clean structural: economy-population, ai-fear, future-solve, extinction-culture, policy-proposal, pinker-better-world). 36 of 82 objections signed / 72 positions. 46 un-signed: T3 remaining = 5 clean + 3 firewall-delicate (8); T4 Genuine Philosophical (31); T5 Meta-Objection (7). Curtain-lift AUTHORIZED through the whole Folk tier + the batch-1 register-verdict GREEN, but `ne-hoc-fiat` STAYS DOWN (release-day / the "first two sets" → "first three sets" Status flip = Josiah's call, fence 3).

2 · CORRECTIONS FIRST (TX9-BACK — the Successor-Protocol register seat's §B verdict on batch-1)
* VERDICT: all 12 batch-1 positions PASS §B, register-faithful to Ethical-spine / Personal-Clinical (graded vs Stage-3 + the seat voice-note), **ZERO carves**. Volta 12-distinct confirmed; YLEX clean. Recorded OFF-corpus; corpus `seat_rx` NOT stamped (the corpus-wide GREEN reconcile stays Josiah's deferred ruling — a subset stamp splits the corpus).
* COINAGE (`pos-economy-population-02` "the proxy gamble said out loud") — **RESOLVED, no action.** "the proxy gamble" is a pervasive canon phrase (20 uses across ~25 objections, INCLUDING economy-population's own `diagnosis` + `long` response); the position borrows the objection's own canon term, it is not minted in the compression. No glossary entry, no downgrade. (Confirmed against efilist `579cfbd` at K262 close.)
* AI-FEAR-01 REGISTER RULE — **DEFAULT SET; Josiah flips at open.** The seat passed `pos-ai-fear-01` and `-02` both; 01 is the sole batch entry that states the substrate thesis assertorically ("suffering, not the material a mind is made of, is what carries moral weight"), framed as correcting the caricature ("what the position actually holds is narrower"); 02 is the cleaner method — the thesis appears ONLY as the objection's smuggled, unproven premise ("preserving biological life needs no defense … state that assumption and it stops being obvious"). The seat confirmed Stage-3 itself asserts positive structure inside replies, so an assertoric line in a reply is register-native, not a firewall breach. **DEFAULT (recommended): keep both, 01 stands as-is; the argue-layer rule going forward = "expose-preferred; assert permitted ONLY when correcting a mischaracterization the objection itself introduced."** FLIP: if Josiah wants the substrate thesis NEVER in Grey's assertoric voice (strict expose-only), the seat will draft the 01→02-pattern conversion as a micro-correction — fold it FIRST (its own guard chain, count-neutral body swap, K261 class), THEN batch-2. Either way, STATE the ruling in the K263 stratum.
* WATCH-ITEMS (seat FYI, none blocking; fold as batch-2 authoring hygiene): (a) do NOT lean an 01/02 pair on a single shared image — the extinction-culture pair both ran the hospital/disease/bleed spine; give each entry of a pair its own governing image. (b) Keep the volta TERMINAL-shaped — `pos-policy-proposal-01` used "He answers" as a mid-body hinge rather than the closing step-back; in-register, but the structural odd-one-out. (c) December-rule vigilance on death-framing — `pos-pinker-better-world-01` "guaranteed suffering before a guaranteed death" reads as indictment-terminus (more life = more harm, still ends), NOT death-as-relief; held, but it was the batch's closest brush — keep death-as-relief off the surface.

3 · Mission, in order (the recommended default path)
T3 STRUCTURAL/PRAGMATIC batch-2 — 5 clean structural objections ×2 = 10 R2 positions: `overpopulation-addressed`, `moral-progress`, `marxist-materialist`, `social-contract`, `rights-future-generations`. (All 5 vendor-confirmed tier==3 + POSITIONLESS as of K262; each `combined#obj-<id>` anchor confirmed live in the pinned flagship `e654eabd` at K262 — re-grep the BARE id at open, the `obj-` prefix is JS-built.) Same rails as K257–K262:

* Author from the tier-3 cold-graded canon (`efilist_argument_library_v4_0_0.json` `.objections[]` where `tier==3`, responses + diagnosis + psychMechanism; efilist READ-ONLY @ `579cfbd`). R2 / public / `b4_reply` / `[clinical, volta]`. Provenance `ratified "K263 strike-pass (Josiah, <date>)"` (stamp `TZ=America/Phoenix date`), `seat_rx "TX10 (library-seat review pending)"`.
* Triggers oracle-net + crisis + existing-corpus + self disjoint — MODE-AWARE: `new YO.Matcher(ents,{}).match(form)` on a FRESH matcher PER FORM is the arbiter (mirrors gate #9); run it BEFORE the full battery. Bodies YLEX-disjoint (Grey bodies stay off Yūrei's home lexicon: filing / the minutes / department / attendance / ledger / requisition / archive / catalogued / shelved / night shift — note "ledger" is the financial word on the wall, sidestep it). NO `?`/parens/exclaim/ellipsis; NO `just`; every form == `YO.normalize(form)`.
* FRESH volta verbs — RE-EXTRACT `He <verb>` from the then-current corpus (`717c4b95`) at open and be disjoint from ALL of it. The K262 batch added reads · refuses · splits · strips · discounts · traces · inverts · exposes · answers · measures · reverses · punctures ON TOP of the prior set — avoid every one. Diffuse the closer-shape; keep 01/02 governing images GENUINELY distinct (the watch-item above) and the volta TERMINAL-shaped.
* NOT COUNT-NEUTRAL: +10 positions → corpus 150→160 (147→157 authored, 3 crisis unchanged), 72→82 positions, 36→41 objections. Gate check-counts GROW — RECORD the new values, do NOT verify-unchanged: register >5024, persona "82 position(s) provenance-stamped", surface >165 pass / 0 fail (fail MUST stay 0). SERIALIZER PARITY: reproduce base `717c4b95` from parsed data (`json.dumps(ensure_ascii=False, indent=2)` + trailing `\n`) BEFORE authoring; splice the 10 entries before `c-crisis-01`; `.authored` string untouched (K257–K262 convention); re-dump.
* PAGE edit (`src/successor/index.html`): bump the curio "150 entries — 147 authored" → "160 entries — 157 authored" (3 inherited crisis unchanged) and bump the CORPUS `?v=K262` ×2 → `?v=K263` — ONLY those two (the `omega-assistant.js` + `successor-stage.js` includes); LEAVE every other `?v` UNTOUCHED (K30/K208/K209/K211/K230/K231/K241/K43 = Yūrei + other frozen assets). Status "first two sets" HOLDS by default (the "first three sets" flip is Josiah's — batch-2 completes the 11 CLEAN T3, a natural flip point, but still fence 3). Re-scan the page-truth phrase family (no "no positions / positionless / written but unshipped") == 0. NOTE the K262 cache finding: the JS fetches the corpus at a BARE URL, so the `?v` bump refreshes the two JS files, NOT the corpus — the corpus rides the `/components/` TTL; the operator novel-buster `?vk263=1` curl is the served-corpus read.
* Files (3): corpus `717c4b95`→[new] · successor page `5d1b1465`→[new] · CLAUDE.md [K263 stratum]. NO efilist block, NO pin, NO search-index regen (the successor page is wuld-search-excluded; the corpus is a `/components/` JSON — PROVE the strong-form neutrality: build[HEAD] == build[K263 tree] == committed `677a9678`).

PRE-FLAG BUDGET before the authoring lane — 10 positions + per-form fresh-matcher routing + dual-mode battery + the page edit is a full lane. Recommend batch-2 as ONE commit if budget CLEARLY holds; else split 3+2. After batch-2: **41 of 82 signed (the 11 CLEAN T3 objections DONE)**. Then the 3 FIREWALL-DELICATE ids (`violence-as-reductio`, `slippery-slope-eugenics`, `ableist-objection`) as DEDICATED defensive-distinction care-passes LAST — each rebuts a harm/eugenics/ableism framing and must stay rebut-only, firewall-clean, third-party-harm-free, never endorsing the thing it rebuts (exemplars `pos-suffering-as-meaning-01` + the survivor-testimony care-pass method), NOT the bulk batch. Then T4 Genuine Philosophical (31) → T5 Meta-Objection (7).

4 · Fences that do NOT move
Crisis floor byte-frozen (c-crisis-01/02/03, 18 forms) · no Latin beyond `mg-greet-04` · Yūrei untouched (battery 100/100; bleed 0) · NO PIN — flagship does not move · anti-farming: variance ≥2, `mg-how-many-01` non-enumeration · oracle-net law: position triggers NEVER carry a live oracle token (antinatalism / antinatalist / natalism / having kids / having children / procreation / benatar / efilism / efilist) · YLEX law: Grey bodies disjoint from Yūrei's home lexicon, not only the triggers · §D automated gates are NOT curtain-lift authority — the register-seat qualitative verdict is · Folk/T2 + T3-batch-1 curtain-lift GIVEN but `ne-hoc-fiat` stays DOWN; the "first two sets" → "first three sets" flip and release-day are Josiah's · corpus `seat_rx` is Josiah's deferred reconcile — per-batch GREEN recorded OFF-corpus (body-file + stratum), never by stamping corpus fields · `/argue/` RESTORE carry stands (flip `mg-oracle-argue-01` href + surface-e2e pin when the game ships) · biography/disclosure unchanged · efilist READ-ONLY · CF-BEACON: served HTML by content-grep, md5 only the corpus JSON.

5 · Operator conventions (unchanged)
`& { }` scriptblock · flat per-file statements, never arrays · `Md5` helper (never `H`) · `.git\*.lock` cleanup · HEAD==origin guard + base-md5 guards + result-md5 gates · explicit-stage named files, NEVER `git add -u` · staged-count gate (== the file count you shipped) · one-line if/else · `curl.exe` with `-o` temp files, never `$null` · `http.postBuffer 524288000` + gate push `$LASTEXITCODE` · SERIALIZER PARITY: corpus base is `json.dumps(ensure_ascii=False, indent=2)` + trailing `\n` — reproduce base md5 (`717c4b95`) from parsed data BEFORE authoring · MODE-AWARE disjointness: exact-mode existing forms are NOT substring collisions; the FRESH-matcher-per-form routing test is the arbiter · DAMPENING ≠ ROUTING FAILURE: probe each form on a FRESH `new YO.Matcher(ents,{})` (`NO_REPEAT_WINDOW=8` per-session state; a same-instance sweep false-FAILs follow-on forms) · YLEX BLEED: `yurei-bleed` reads the RESPONSE, not the triggers · BYTES-NOT-REPORT: deliver prose AS A FILE · page-truth: scan the "no positions / positionless / written but unshipped" phrase family, not the literal token · DUAL-BOOT CLOCK SKEW: sandbox date reads UTC a day ahead — stamp `ratified` with `TZ=America/Phoenix date` · CF-BEACON: served HTML by content, corpus JSON by md5 (novel `?vk263=1` buster); flagship served clean `e654eabd`, tolerate +~504 beacon · PATH NOTE: cloud sandbox `$HOME=/root`, Bash cwd `/home/claude`, the Read tool cannot see the root-owned clone (bash/jq the corpus) · CORPUS-CACHE (K262): the JS fetches the corpus at a BARE `/components/omega-corpus-mrgrey.json` URL — the page `?v` bump refreshes the two JS files, NOT the corpus; the corpus rides the `/components/` TTL (~4h warm tail, cold fresh); the novel-buster curl is the served-corpus read · sidecars ride `Downloads\k263\` · deliver sidecars to the device via the bridge + verify on-device md5s · budget pre-flagged before the authoring lane.

WULD.INK -- K243 YUREI v4 CLIP-FOLD, wave 2 (full-fidelity 13-role set). NO PIN.

Recommended default: fold the Yurei v4 wave-2 set -- a SELF-CONTAINED 13-role manifest swap (11 clips + 2 stills, `avatar_manifest_v4.json` md5 `0c9acaa795efccda8d9d3eb295b6dba4`, SUPERSEDES v3 `aa1a91bd`) -- repoint the two YUREI consumers, bump the `?v`s that changed, screenshot-judge, ship ONE wuld PS block. SIMPLER than K242: Yurei-only, self-contained manifest (NO carryovers, NO normalization), Mr. Grey UNTOUCHED (stays v2). The packet is complete + render-lane-gated (`avatar_gates_v4.json`: 11/11 encode alpha_mode=1, palette banned_px 0, loop seams pass, rest-parity md5-equal canonical-p0==idle f0, 1408-frame autonomous campaign clean); the relay `yurei_av_v4_wuldink_relay.md` is addressed to this seat: "point the resolver at v4; ZERO player-code change."

Why this session exists: K242 LANDED (commit `902289934b81e2247956835bce4d2165602cceb9` atop `19d7ba2`; live-verified at close -- both manifests served + md5 [`fb18a6e6`/`24106ffd`], `successor-stage.js?v=K242` carries `avatar_manifest_v3`, `/successor/` page `?v=K242` x2, glitch clip 200; Josiah eyeballed the live stage -- Yurei renders at full fidelity). The 3D-Modeling seat then delivered Yurei v4: the COMPLETE fidelity set (Phase A arm unweld + Phase B anatomy/grain/hair surfacing + Phase C 900 strands + Phase F finger grooves), ALL 13 roles re-rendered set-atomic, incl. the 4 net-new state clips the widget's fallback map already names. The K241 stage resolves avatars BY MANIFEST -> drop-in by construction.

THE PACKET (recon'd at K242-close -- verify, don't re-derive): `D:\mascot\avatar\_v4\` (SELF-CONTAINED):
* 11 webms (role / file / size / frames / md5): idle `yurei_av_idle_v4.webm` 733236 288f `8be14c85` -- appear `..._appear_v4` 384500 84f `8c842047` -- listen `..._listen_v4` 362429 192f `0a255d0d` (NEW CUT) -- speak `..._speak_v4` 270599 96f `201f8436` -- dismiss `..._dismiss_v4` 1049300 96f `99ef4b5f` (NEW BEHAVIOR: fixture dies first, red-ember after-image alpha 0.15 f40-70) -- long_idle `..._long_idle_v4` 590433 192f `26e9e2b1` -- glitch `..._glitch_v4` 140730 40f `836a884f` -- wrong_hour `..._wrong_hour_v4` 370098 96f `02483da3` (NET-NEW, loop) -- regard `..._regard_v4` 576366 72f `8bcf58fb` (NET-NEW) -- deflect `..._deflect_v4` 383062 60f `7d1f1ede` (NET-NEW) -- idle_breeze `..._idle_breeze_v4` 624352 192f `dcbfcdce` (NET-NEW, loop).
* 2 stills: canonical-p0 `yurei_av_still_v4.png` 1220960 `4dd13f2b` (== idle f0) -- long_idle-still `yurei_av_long_idle_still_v4.png` 1203492 `3ef70a81`.
* `avatar_manifest_v4.json` md5 `0c9acaa795efccda8d9d3eb295b6dba4` (+ `.md5` sidecar) + `avatar_gates_v4.json` + `yurei_av_recipes_v4.md` + contact sheet. sha256 per-asset in the manifest. Fallback map carried verbatim; `return_ack` still falls back to idle (NO clip by design -- the only remaining fallback that matters).

VERIFY-AT-OPEN (fresh public-origin clone; verify-don't-redo):
* wuld HEAD == origin/main == `9022899` (K242) or later; if later, read what landed and RE-DERIVE every base guard from live git (K231 law; one-committer -- `git ls-remote` at open AND before emitting the handoff).
* Record the K242 landing in the K243 stratum (sha `9022899` + the served checks above, cite as K242-close data).
* Re-stage the v4 packet. The D:\ manifest is CRLF (K242-manifest-EOL hazard) -- LAND IT LF: byte-md5 the packet for transfer-integrity (`0c9acaa7`, CRLF), but gate the LANDED LF md5 for the served-bytes check (COMPUTE at open, as K242 did `fb18a6e6`). All 13 assets are `_v4` (self-contained, NO carryover parity step) -> per-asset md5+sha256+size_bytes == manifest fields; ffprobe vp9 / 0-audio / frames exact. ALPHA (K242-vp9-alpha law): gate `alpha_mode=1` (matroska track) + a decoded-RGBA alpha-plane read via `-c:v libvpx-vp9`; NEVER assert `pix_fmt==yuva420p` (false-FAIL -- VP9 alpha reads yuv420p+alpha_mode).
* Base blobs at open (re-derive live via `git rev-parse HEAD:path`): `src/components/yurei-assistant.js`, `src/components/successor-stage.js`, `src/components/nav.js`, `src/successor/index.html`, `CLAUDE.md`.

THE DESIGN (drop-in; ZERO engine/corpus/matcher/persona/resolver bytes):
1. YUREI v4 SELF-CONTAINED SWAP -- land 11 webms + 2 stills + `avatar_manifest_v4.json` (LF) into `src/assets/yurei/avatar/`. Nothing removed; v3 manifest+clips stay as rollback (K117/K224d pattern). All 13 roles resolve to REAL clips now; the 5 previously-fallback roles (glitch/wrong_hour/regard/deflect/idle_breeze) go live the moment the manifest lands.
2. CONSUMER REPOINT (Yurei-only; re-derive the exact lines live -- at K242 they were yurei-assistant `:24`+`:26`, successor-stage `:32`/`:47`/`:53`): `yurei-assistant.js` MANIFEST_URL `avatar_manifest_v3->v4` + VER `K242->K243` -- `successor-stage.js` `:47` yurei `v3->v4` (leave `:53` omega == `mrgrey_manifest_v2` UNTOUCHED) + VER `K242->K243`. Mr. Grey consumers (`omega-assistant.js`) NOT touched -- Grey stays v2.
3. CACHE-BUSTS (K238 bump-what-changed): `nav.js` yurei-assistant injector `?v=K242->K243` (yurei-assistant changed) -- `index.html` `successor-stage.js?v=K242->K243` (leave `omega-assistant.js?v=K242` -- Grey unchanged). `successor-stage.css ?v=K241` HOLDS (frozen).
4. INGEST GATES + E2E + RESOLVER-COMPAT -- same discipline as K242: per-file md5+sha256+size+ffprobe on all 13; full e2e suite green UNCHANGED (successor-stage-e2e 90/90 uses a synthetic manifest, no filename fence; + the other 6); `node --check` on touched js; drive the REAL v4 manifest through the resolver (`resolveAsset` walk) -- all 13 roles resolve REAL, `return_ack` resolves via fallback->idle, reduced-motion still -> canonical-p0.
5. SCREENSHOTS (headless chromium, real clips over `#0a0a0a`): both personas stage-hero; NEW for v4 -- shoot the 4 net-new states (wrong_hour/regard/deflect/idle_breeze) + the ember-ghost dismiss + the new-cut listen; corner-desk 64px (yurei-assistant repoint proof); reduced-motion stills; re-sample AA/palette over the new idle (banned_blue 0 expected). Deliver a contact sheet.
6. RECORDS: CLAUDE.md K243 stratum (K242 landing + the v4 fold + the file map + gates; git-show -> $HOME -> python, NEVER Edit/Write). search-index: only two `?v` tags change (not harvested) -> prove build[clean]==build[mine]==committed `564d6a81` and HOLD; sitemap untouched (route-neutral).
7. SHIP ONE wuld PS block (NO PIN, NO efilist): `& { ... }`; `curl.exe` not `curl`; one-line if/else; helper `Md5`; `.git\*.lock` cleanup first; TZ=America/Phoenix. HEAD guard -> base-blob guards -> Copy-Item the 13 binaries STRAIGHT FROM `D:\mascot\avatar\_v4\` (COPY not Move; cross-volume) with per-file `Get-FileHash` md5 gates -> Move-Item the LF `.k243` sidecars (avatar_manifest_v4, yurei-assistant.js, successor-stage.js, nav.js, index.html, CLAUDE.md) -> result-md5 gates -> explicit-stage the ~19 NAMED paths (NEVER `git add -u`/`git add .` -- strays + `_to_delete\` must not ride) -> commit -> push -> deploy-verify (v4 manifest 200 + downloaded md5 == LF pin; one new clip HEAD 200; served `successor-stage.js?v=K243` carries `avatar_manifest_v4`; page `?v=K243`). **In verify use `-o "$env:TEMP\x"` NOT `-o $null`** (K242-close: `$null` is a PS-ism, not a curl discard -> it dumped the webm to the console as gibberish; use a temp file or `-o NUL`).

SPLIT: this is one clean shippable session (Yurei-only, self-contained). If a per-asset gate mismatches, HALT that asset, relay the drift to the 3D seat; the set is atomic so a single mismatch means re-request, not partial-ship.

DEFERRED / NOT this session:
* MR. GREY WAVE-2 fold -- next after the render lane delivers it (see the ELECTION below). Repeats this session's shape against the Grey packet; will re-touch `omega-assistant.js` + the mrgrey manifest.
* CONSOLE IMMERSION (Josiah's K242 direction): give `/console/` successor-stage-level immersion -- a full-bleed room/scene BACKGROUND layer with the command text overlaid on a glass band, eventually ANIMATED (the descent's procedurally-generated rooms as the "scene," the way the avatar is the successor stage's scene). Architecturally the successor-stage pattern (auto-open post-curtain; background layer + glass text band; a manifest that resolves room/scene clips by seed/room-id). A DESIGN+BUILD session -- no scene assets exist yet; scope with Josiah, queue behind the avatar waves / release push. Console stays gated (`facilis-descensus`), tab greyed, search-excluded until fleshed out.
* RELEASE PUSH (K233 curtain strip + un-grey tab + findability + changelog) -- behind the waves.
* OWED (library arc): the K226 B1 + K229 adv-map coordination mirror (`docs/library-claude-coordination.md`) + a wuld CLAUDE.md adv-map pointer stratum -- absorb at a wuld-committing session (the concurrent efilist micro-fold lands its own stratum in the efilist CLAUDE.md; wuld absorbs the pointer later; one-committer).

RENDER-LANE ELECTION (Josiah delegated the 3D seat's next subject to Cowork): ELECT MR. GREY (the cat). Rationale: Yurei is now COMPLETE at v4 -- the full fidelity surface + all 13 states; further Yurei work is polish / diminishing returns. Mr. Grey is three versions behind (v2 wave-1, still partly placeholder-silhouette lineage) and needs the same treatment -- fidelity surfacing + the net-new states (wrong_hour/regard/deflect/idle_breeze/return_ack) -- to reach parity with Yurei on the shared stage. Highest-value render work = bring the cat up. The 3D seat flagged Grey "unblocked, in memory" -- teed up.

KEY PATHS: assets IN `D:\mascot\avatar\_v4\` -> OUT `src/assets/yurei/avatar/`. Code: `src/components/{yurei-assistant,successor-stage,nav}.js` + `src/successor/index.html`. FROZEN: `successor-stage.css ?v=K241` -- the omega/Mr.Grey stack (v2: `omega-assistant.js`, `mrgrey_manifest_v2.json`, mrgrey clips) -- both corpora + resolver + persona-switcher + agent-settings -- search-index `564d6a81` + sitemap 62 -- flagship `library.wuld.ink/combined` `e654eabd` (pin v4.0.0).

HAZARDS (carry): ccxxvii (explicit-stage NAMED; never `git add -u`/`.`) -- K111 (`.git\*.lock`) -- K113 (`curl.exe`) -- K115a/ccxxxv (large files via git-show/python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone) -- K219 (LF-normalize the CRLF manifest; gate parsed fields + landed-LF md5) -- K231 (re-derive every guard live; the kickoff's line numbers are hints, live wins) -- ccxxxvi (`.k243` sidecars) -- K186a (`/components/` 300s TTL) -- K238 (bump only the changed `?v` -- successor-stage + nav yes, omega-assistant NO) -- K242-vp9-alpha (gate `alpha_mode`, not `pix_fmt==yuva420p`) -- K242-manifest-EOL (land LF) -- **K242-close: never `curl.exe -o $null` in PS verify -> `-o "$env:TEMP\x"` / `-o NUL`**. TZ=America/Phoenix. If a concurrent seat claimed K243, renumber (log label only).

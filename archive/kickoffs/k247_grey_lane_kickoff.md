WULD.INK -- K247 GREY-LANE: wire a Mr Grey input-tracking lane into the admin LOG terminal + coverage HUD, toggling Yurei <-> Grey. NO PIN. Worker-only (admin).

Authoritative state lives in the repo: read C:\Users\y_m_a\Projects\wuld-ink\CLAUDE.md FIRST (orientation sequence + full session log + current carries + verify_before pins), then docs/wuld-ink-cowork-brief.md; when unsure on Refusal-Library scope, docs/refusal-libraries-roadmap.md. The K246 GATEKEEPER stratum + its "verify_before for K247+" block are the immediate base. This panel carries NO live hashes as truth -- re-derive every guard live at open (K231 live-wins).

GOAL (Josiah's K246-close relay, verbatim intent): "make sure the log for Mr Grey is wired to the admin log terminal. You can make it toggle between Yurei and Mr Grey. Just enable the function for it to track all inputs so I can begin that journey." Today the admin.wuld.ink LOG terminal + section 16 (TESTING YUREI -- live matcher + coverage HUD) + section 17 (GAP LOG -- coverage misses + hit quality) serve ONE persona (Yurei; the HUD logged "testing-yurei: loaded 215 entries"). K247 = add a persona TOGGLE (Yurei | Grey) so the SAME input-tracking / coverage machinery runs for Mr Grey, into a per-persona lane, so Josiah can begin accumulating Grey interaction data. Yurei's lane stays byte/behaviour-identical.

SCOPE BOUNDARY (state it, hold it): v1 is the ADMIN BENCH only -- the section-16 testing toggle + a section-17 Grey gap-log lane, i.e. Josiah test-drives/feeds Grey inputs in the admin and they log to the Grey lane. This does NOT wire Mr Grey live on the public site (that is the separately-gated GREY PERSONA WIRING -- Omega-tab + the R.2 corpus cliff, other seats). K247 stays worker+admin-UI only; no public persona activation.

RECOMMENDED DEFAULTS (confirm the three forks with Josiah at open -- AskUserQuestion, then build):
* Fork (a) CAPTURE-FIRST vs FULL-MATCHER -- RECOMMENDED capture-first. If the Grey response corpus + matcher are not ready (the R.2 corpus cliff), v1 logs every Grey input's coverage OUTCOME (miss/thin/etc.) to a Grey gap-log lane for later corpus-building; the live matcher is wired for Grey only once the Grey corpus lands. Mirrors exactly how Yurei's gap-log accumulated before its corpus matured.
* Fork (b) TOGGLE SCOPE -- RECOMMENDED: one persona selector (Yurei | Grey) that scopes BOTH the sec-16 HUD and the sec-17 gap-log, with per-persona lanes that never mix; persona defaults to Yurei (so nothing about the current view changes until you flip it). Alternative -- a separate Grey section -- REJECTED (duplicates the machinery; two places to maintain).
* Fork (c) STORE -- RECOMMENDED: reuse the EXISTING gap-log store with a persona discriminator (a "persona" column if D1 / a persona-prefixed key namespace if KV), NOT a new store. Investigate the sec-17 /api/gaplog/* backend live at open to see which it is (the worker binds D1 COMMENTS_DB = wuld-comments; the gap-log may ride it or a KV). A D1 column-add is a small idempotent migration.

BINDING, NON-NEGOTIABLE -- PII DISCIPLINE: the Yurei gap-log is deliberately PII-scrubbed -- its own UI says "Miss rows are PII-scrubbed inputs the matcher had no answer for -- never auto-deleted... No identity, no query content on the hit lane." The Grey lane MUST mirror this EXACTLY: "track all inputs" means capture the SCRUBBED coverage signal (class, count, dates, thin/novel/repetitive votes), NOT raw identity or raw query content beyond what the Yurei lane already keeps. Do not weaken the scrub to "capture more." Carry the same resolve / redact / drop manual controls per-persona. Surface the scrub posture in the Grey lane's UI copy the way section 17 does.

VERIFY-AT-OPEN (fresh public-origin clone; the K216/K218a clone-reads-only discipline; re-derive EVERY guard live):
* wuld HEAD == origin/main == dd55c78a8e9e6219707e2b0db19bb30c092d3b55 (the K246 landing; if later, re-derive). One-committer: git ls-remote at open AND before the handoff.
* admin worker workers/admin/src/index.js -- md5 d03abc60 / ~221,544 (K246-DEPLOYED; if editing it, spot-check served admin.wuld.ink matches the repo before splicing). INVESTIGATE LIVE at open and pin the exact anchors from a fresh read (do NOT trust these words -- derive): the LOG terminal block; section 16 (adminHtml sec-status..sec-log; the "testing-yurei" HUD + its loader + the live-matcher call path); section 17 (sec-gaplog markup + the /api/gaplog/list|log|resolve|redact|redact|drop|export handlers); the gap-log STORE (D1 table schema or KV namespace) and its PII-scrub point. These are the machinery to persona-scope.
* BYTE-HELD unless a fork forces otherwise: src/successor/index.html 099ad556 / src/console/index.html f2e38dca / tools/gate/wgate-core.cjs / tools/omega/wgate-e2e.cjs / src/components/nav.css 5828a9ee / src/search-index.json 126c7434 / src/sitemap.xml c6116db4. Flagship library.wuld.ink/combined e654eabd (pin v4.0.0; READ-only). K246 GATEKEEPER ops must keep working (regression: node tools/omega/wgate-e2e.cjs on both gated pages 24/24).

THE BUILD (recommended): worker-only. A persona toggle in the sec-16 HUD (and reflected in the LOG line + sec-17). Every logged input event tagged with the active persona; the gap-log store gains a persona lane; sec-16/17 views filter by the selected persona. Capture-first Grey = pure scrubbed input-capture with the sec-17 resolve/redact/drop controls per-persona. Keep any new transform/handler logic pure + node-testable (the K213/K246 embed+extract pattern) so it can be gated without a worker runtime. If a D1 migration is needed, make it idempotent + reversible and prove it on a scratch DB copy first.

GATES:
* YUREI-LANE REGRESSION: with persona defaulting to Yurei, the sec-16 HUD, the LOG, and the sec-17 gap-log read/behave byte/behaviour-identical to K246 (existing rows unchanged; existing /api/gaplog/* responses unchanged for the Yurei lane).
* PERSONA ISOLATION: a Yurei input never lands in the Grey lane and vice versa (store-level proof); flipping the toggle swaps lanes cleanly; the persona discriminator is enforced server-side, not just in the UI.
* PII-SCRUB PARITY: the Grey lane persists exactly the scrubbed fields the Yurei lane does -- no identity, no raw query content beyond the Yurei baseline. Prove by inspecting a captured Grey row.
* node --check src/index.js; if a D1 migration -- forward+backward proven on a scratch copy, zero Yurei-lane data loss.
* If any SITE file changes (unlikely -- this is admin-worker-only), the K246 diff-confirm + byte-conservation + search-index-neutrality gates apply.

SHIP: worker-only unless a site file changes. ONE wuld PS block (NO PIN) -- FLAT per-file statements (K245 paste-collapse hazard), .git\*.lock cleanup first, helper Md5 (never H), curl.exe (never curl), -o "$env:TEMP\x" (never -o $null), one-line if/else, HEAD guard == dd55c78 full sha, base-guard + Move-Item + result-md5 gate, explicit-stage NAMED files (worker index.js + CLAUDE.md K247 stratum [+ any migration/tool file]; NEVER git add -u/.), $staged-count gate before commit (print MEASURED counts), commit -> push -> npx wrangler deploy from workers\admin -> hard-refresh admin.wuld.ink (Ctrl+F5) -> confirm the Yurei<->Grey toggle flips + a test Grey input logs to the Grey lane while the Yurei lane is untouched. If a D1 migration: run it against live D1 via `npx wrangler d1 execute` as a separate guarded step, gated on a backup/inspection first.

DEFERRED / QUEUE (SEQUENTIAL -- wuld-ink is ONE-COMMITTER; do NOT run a second concurrent wuld-ink Cowork session, it races the push. Genuine concurrency = the other repo / library seat only):
* K248 CONSOLE IMMERSION (the long-deferred K242 direction): successor-stage-level immersion for /console/ (full-bleed scene bg + command text on a glass band, eventually the descent's procedural rooms); stays gated facilis-descensus (now gate-managed by K246 -- rotate/remove via section 18). DESIGN+BUILD; no scene assets yet; scope with Josiah.
* YUREI HAUNT v4 WIDGET SWAP (render lane, READY NOW -- separable, quick): swap the wuld.ink haunting widget v2 -> v4 (v4 manifest md5 8fecaf28aa3d5d7abd905b6daf564703; the 8-direction heads + torso + idle/drift/peek/surface set shipped in 3D Modeling / haunt_v4). Manifest-swap + consumer repoint + ?v bump; NO PIN. Good stand-alone session or warm-up before a bigger build. (The wuld-ink relay note: repo still carried v2, so the swap goes v2 -> v4 straight, skip v3.)
* Standing electives + hazards carry from the K246 stratum: K233/K235 release-day curtain strips are now section-18 gate-remove clicks; PAT rotation due Sep 04 2026; wrangler 4.95->4.110; efilist Pages @v4->@v5; the docs/library-claude-coordination.md Exchange-mirror (off-wuld, library seat); Stripe W-9; FIRST-PUBLISH ritual (awaits a video).

HAZARDS (carry from the K246 stratum): ccxxvii (explicit-stage NAMED; the kickoff .md strays + _to_delete\ must not ride) -- K111 (.git\*.lock) -- K113 (curl.exe) -- K115a/ccxxxv (large files via git-show/python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone; the mount can serve stale worktree bytes) -- K231-live-wins (re-derive guards + counts live at open) -- K245-paste-collapse (flat statements over array+loop in operator blocks; staged-count gate; measured counts) -- K243-curl-discard -- K219/CRLF (gate semantically where Windows tools rewrite) -- K233 (any curtain: inline + outside <main>; soft, never a security boundary) -- K246-open-class-coupling (grep the repo for consumers of a token before renaming it; successor-stage.js couples to sgate-open + wuld:successor:unlocked). TZ=America/Phoenix. If a concurrent seat pushed, renumber (log label only).

# K255 KICKOFF — PHASE 4 OPENS (the instantiation ratification is GIVEN)

**Operator paste for the next wuld.ink Cowork session. Read this whole file before any write.**

---

## 0 · Orientation (mandatory, in order)

1. Fresh public-origin shallow clone of `wuld-ink`; mounts are never read for repo state. Read `CLAUDE.md` FIRST (K253 + K254 strata are the live context), then `docs/wuld-ink-cowork-brief.md` if unsure of conventions.
2. The relay thread lives in `C:\Users\y_m_a\Downloads\Successor Protocol\`: `omega_proxy_ratification_relay_BACK_v1.md` (the two-gate cliff + Q3 fences + 4 conditions), `omega_proxy_expansion_relay_v1.md` (K253), `omega_proxy_expansion_transmission_2.md` (K254 closure). Read all three.
3. **Verify-at-open, STOP on any mismatch (K252 precedent):** wuld HEAD == origin == the operator K254 commit `6a2f6e4` atop `7c99906`. Corpus `omega-corpus-mrgrey.json` md5 `23e4682e`/89,258 (78 = 75 authored + 3 crisis). `mrgrey-register-gate.cjs` `c2ffb00a` — run BOTH modes (offline 2,585 / --live 2,601, 16/16 routes 200). `omega-surface-e2e.cjs` `c11b3df2`. Flagship `library.wuld.ink/combined` `e654eabd`/2,963,752 — PINNED, UNTOUCHED, this is a NO-PIN session. Search-index `677a9678` held. One-committer check (ls-remote == HEAD). Record "K254 landed `6a2f6e4`" in the K255 stratum.

## 1 · The ruling that opens this session

**Josiah's instantiation ratification for the R1–R5 register/position instantiation is GIVEN, effective 2026-07-20.** The K253/K254 strata record "NOT YET" — that record is WRONG and K255's stratum must correct it: the "not yet" was a typo-garble (original message: "the answer from me is yes"; he later confirmed to the successor seat that "yet" was the typo; final instruction this session: "Consider all blocking 'not yet's … void"). The operative ruling, with its caveats intact and binding:

- **YES** to instantiating the registers and deploying registry positions through the proxy.
- **R1 Tender-Pole:** less fleshed out than the rest — needs fleshing-out work before deployment.
- **R4 Lacero:** more experimental. Both R1 and R4 are to be **"appropriately sectioned off for things that request or require them"** — request-gated tiers, never ambient on the open surface.
- **What Josiah's YES does not void:** the library seat's per-byte register-integrity review (gate 2 of the cliff — rides numbered transmissions, same-day turnaround proven in K253/K254); the Q1 anti-farming conditions (a scripted position line is a stable screenshot surface — variance depth, no bulk enumeration); the crisis floor (byte-frozen); the disclosure/biography fences; the efilist repo (READ-ONLY vendor source).

## 2 · Mission, in order

1. **Ledger correction stratum item** — record §1 verbatim-class in the K255 stratum before anything else ships.
2. **Transmission-3 → successor seat** (`Downloads\Successor Protocol\`): the corrected YES on the thread; un-park their A3; request the **worked R2 exemplar** (one reply against a T5-class objection, at the length/cadence they'd ratify) and their view on the R1/R4 sectioning mechanics. This goes out EARLY — it is the pilot batch's calibration anchor.
3. **Vendor the objections-index** read-only from `efilist-argument-library` (VENDOR discipline: build against the index JSON, all 82 ids, never hand-copied; verify vendored bytes against repo HEAD `579cfbd`; zero efilist writes).
4. **Vessel surgery BEFORE content** — Phase 4 cannot ship a byte until the gates evolve:
   - **Position-class schema:** a new entry class (`position`) carrying provenance fields — objection id, source-node anchor (deep-link into `library.wuld.ink/combined`), Josiah-ratification ref, seat-transmission ref. Design so the Ω1 gate's B-invariant EVOLVES from "positionless" to "positions only in the provenance-stamped class": every position byte validator-traceable to its ratified source. Update `omega-persona-gate.cjs` B accordingly (the current B will correctly RED on the first position entry — that is the fence working; evolve it deliberately, never bypass it).
   - **Sectioning mechanics for R1/R4:** a request-gated tier (the corpus schema's tier/unlock machinery is the natural vehicle — Grey currently ships all-public with no room tier). Ship the mechanics EMPTY first (fences before content — house pattern), with `mrgrey-register-gate` scans extended to enforce: R4/R1-class content can NEVER appear in a public-tier entry.
   - **Position-lane register laws:** do NOT reuse the Phase-3 statics wholesale. R2 cadence (clinical-parallel stacking, em-dash branching, triadic escalation, clinical volta — see `ne_hoc_fiat_stage3_voicetest_v1.md`) legitimately breaks the ≤1-em-dash / ≤320-char laws. The position-class law set derives from the seat's R2 exemplar — if transmission-3 hasn't returned by authoring time, Josiah rules the law set in-session; never invent it silently.
5. **Pilot batch — 5–10 objections** (Josiah picks the set in-session; default: the T1 top-grade class). Per-objection replies in his register, **anchored to the ratified graded rebuttals** — compress/quote the canon, never invent past it; each entry deep-links its node. Full-draft strike pass (the K253 protocol: untouched lines ratify, edits are final bytes). **Ship gating is Josiah's in-session call:** curtained-ship + same-day seat review (K253 precedent, defensible — the surface sits behind `ne-hoc-fiat` and is search-excluded) vs. hold-until-ack. Ask him once, proceed.
6. **Page copy:** the successor page's "Positionless in this phase" language (meta ×3, JSON-LD, body ×2) flips ONLY in the same commit that ships the first position bytes — page truth tracks corpus truth atomically.
7. **Stratum + sidecars (`Downloads\k255\`) + ONE PS block** per the standing conventions.

## 3 · Fences that do NOT move

Crisis floor byte-frozen (gate C deep-equality — any floor work is the parked persona-neutral elective, NOT this session) · no Latin beyond `mg-greet-04` · Yūrei untouched (battery must stay 100/100) · NO PIN — the flagship does not move for any of this · anti-farming: position entries need variance depth (multiple phrasings per objection where feasible) and the `mg-how-many-01` non-enumeration posture extends to the position set · `/argue/` RESTORE carry stands (unrelated lane — leave it) · biography/disclosure surface unchanged.

## 4 · Scope honesty

One session ≠ 82 objections. K255 delivers: the ledger correction, transmission-3, the vendored index, the evolved gates + sectioning mechanics, and a ratified pilot batch (shipped or staged per Josiah's gating call). The remaining objections fan out batch-wise in later sessions on the same rails — each batch: author → strike pass → gates → transmission → fold corrections.

## 5 · Operator conventions (unchanged)

`& { }` scriptblock · flat per-file statements, never arrays · `Md5` helper (never `H`) · `.git\*.lock` cleanup · HEAD==origin guard + base-md5 guards + result-md5 gates · explicit-stage the named files, NEVER `git add -u` · staged-count gate · one-line if/return · `curl.exe` with `-o` temp files, never `$null` · sidecars ride `Downloads\k255\` · budget pre-flagged before any long lane.

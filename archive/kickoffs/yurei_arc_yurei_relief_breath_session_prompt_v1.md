# SESSION PROMPT --- YUREI RELIEF+BREATH PASS v1 (-> haunt_v5 + desk_v5)

True geometric relief (bake the fake anatomy into real displacement) -> shader re-balance -> micro-articulation (breath-spine + finger drift, driver-class) -> author eyeball -> DUAL re-render budget-gated: haunt_v5 FIRST, desk_v5 second (the queued desk elective RESOLVES inside this session). Authored 2026-07-17 — consumer: 3D-Modeling seat, fresh session — author: haunt_v4 seat under Josiah's directive (this chat): *"We're still gonna be updating Yurei, so we might as well plow ahead until we reach a point of significantly diminishing returns. Draft next session's prompt with the ambition(s) of improving / adding textures, sculpting, articulation, etc. Whatever you can do feasibly."*

**Returns framing (binding on the report, not the work):** this is projected to be the LAST major fidelity pass inside the locked framings/register. Texture returns are near-exhausted (T4 closed the gap); the two remaining feasible upsides are (1) TRUE relief — real self-shadowing under the single rim, the one class jump shader bump cannot fake, and (2) micro-articulation — driver-class life below the gesture threshold. After dual-v5, remaining fidelity spend is likely sub-threshold at deployment scale (widget 0.49×). Close your report with an explicit returns-assessment; recommend the arc pivot (corpus/roles, WULD INK side) if the assessment lands where the author expects. WULD INK has swapped NOTHING yet (v2 still live as of 2026-07-17) — do not block on any swap; v5 supersedes v4 by the same drop-in manifest contract; relays v3 say "swap straight to v5."

## 0. Read first, in order
1. Project memory: `yurei-avatar-fx-arc.md` (haunt_v4 ship state, T4 stack params, FRAMING RECOVERY pin [HauntCam_v2rec (0,1.75,1.28465) rot(pi/2,0,pi) lens70; TorsoCam z 1.3; anchor (270,169) by head-bone projection], torso-band census of record, WATCH table, v2 clip sizes), `reference_blender_5x_api_traps.md` (#19–#26 BINDING), `reference_blender_5x_animation_api.md` (channelbags; driver lookup by (data_path, array_index); stuck-POSE/stuck-VALUE restore laws), `reference_mcp_long_render_pattern.md` (autonomous campaign; RE-TIME LAW; persistent_data; ALPHA_MODE case; poll via device_bash only).
2. Substrate: `C:\Users\y_m_a\Documents\Claude\Projects\W.U.L.D. Incorporated\wuld_phase3_mascot_v1_3_10_fidelity.blend` — NOT the OneDrive same-named tree. Verify `bpy.data.filepath` at open (MCP may open blank/wrong on restart). VERIFY: 661-node Mascot_Skin_v1 with `T4_w_{ana,macro,streak,cav,hemfade}` all 1.0 + `T4_macro_h0`.in1 = 0.015; HauntCam_v2rec pinned; yureiH3_* banks ×4 fake_user; **61 rig drivers** (pre-this-session count — you will GROW it; update the pin); scene.camera == TorsoCam; frame range 18–20; text blocks `yurei_haunt_v3_recipes` + `yurei_haunt_v4_recipes`. Record drift; live-wins.
3. Reference bytes (ferry, bridge-readable): `Downloads\_yurei39_transfer\haunt_v4\` (frames/enc/finals/probes + `skin_nodes_dump.json` — the full node graph, saves a re-dump) and `Downloads\_yurei39_transfer\v4ship\` (desk v4 set + its recipes/gates — read the DESK campaign spec there: roles, frame counts, encode params; this prompt does not restate them). D:\mascot is NOT bridge-readable.

## 1. What this session builds
**Track R — TRUE RELIEF.** The v4 anatomy (P4A 17 elements + T4 12 lobes) is entirely shader-normal fake. The mesh is ~12k verts (~15–20mm spacing) — it UNDERSAMPLES the 6–14mm σ field (this is WHY the fake was shader-only; trap #19 note). Bake the macro forms into real displacement via a **Geometry Nodes modifier: subdivide (start level 2) + normal-offset from the SAME analytic field** (re-author the lobe math in GN — the params are all in `yurei_haunt_v4_recipes.md` + the node dump; distance-to-segment gaussians in obj-XZ, z-windowed 0.83–1.46, face excluded by construction). Then RE-BALANCE the shader: geometry takes macro (σ ≥ ~9mm), shader keeps micro (grain, pores, streaks, grooves); probe `P310_h_ana` weight 1.3 -> ~0.6–0.8 and `T4_w_ana` down in tandem so the COMBINED read matches v4 magnitude but with true occlusion. Real relief amplitude: start **50% of apparent shader height** (~4–7mm true) — real shadows amplify perception; conservative dial, author eyeball on the balance.
**Track B — MICRO-ARTICULATION (driver-class, applies to ALL clips automatically — no bank edits).**
- Breath-spine coupling: tiny rotX on spine_02/spine_03 following the breath phase. FIRST CHOICE: driver variable targets `key_blocks["breath_sternum"].value` directly (coherence by construction: follows the surface-role ×0.6 swap AND zeroes when breath is muted/zeroed for WATCH). If the driven-value dependency misbehaves (depsgraph), FALLBACK: duplicate the breath expr with k factor and ADD the pair to the surface-role swap list + WATCH mute list. Amplitude k ≈ 0.15–0.25° total at peak inhale.
- Finger micro-drift: sum-of-sines on the fingers_L/R channels, ≤1.2° inside FINGER LAW (+6/−3), **w288-family periods ONLY** (must close at f288 — idle is the only loop; the wrap-seam gate will catch violations).
- MOTION REGISTER LAW: stillness IS the register (anti-Clippy). Total new motion energy ≤ the micro-bow class (~1.8°). The test at widget scale: *"is she moving, or is it my eyes?"* — if a viewer can NAME the movement, it is too big. No new gestures, no new roles, choreography/timings LOCKED.
**Track A (optional census probe only) — Eevee AO:** one-factor on/off probe at both framings. Ships ONLY if the author replies favorably at the checkpoint — it shifts the global look (face zone included), which strains the supersession claim. Default OFF.

## 2. Register law (unchanged + additions)
Single red rim LOCKED; palette red/near-black LOCKED; banned-G/B gate stands; body-absorbed-by-darkness; face/head zone untouched (relief z-window top 1.46 obj, existing convention); ambiguous-organic forms (front-read-as-back survives). NEW: silhouettes MAY now move by design — bound it: WATCH A/B alpha-centroid vs v4 ≤ 4px AND author-eyeballed; anchor law unchanged (bone projection, mesh-independent — must hold (270,169) exactly).

## 3. Hard traps (do not re-derive)
All of #19–#26 stand. Session-specific: **(a) GN-by-script is UNPROVEN on this box (5.3.0a)** — Phase A probes the GN node API surface FIRST (names/sockets may diverge from 4.x docs like everything else; budget-gate the build: if GN fights for >~15 tool calls, fall back to the SHAPE-KEY path: bake ONLY the broad lobes (σ ≥ 14mm: flank, sacrum, lspine wide) into a basis shape key via per-vert Python eval — 12k verts CAN carry ≥28mm features; value = the dial; no topology/weights/UV change either way). **(b) Modifier order:** try GN (subdiv+displace) BEFORE Armature (rest-space field = mathematically right, deform carries detail); if deform cost on the dense mesh blows the re-time, AFTER Armature is acceptable (clip poses are micro; field-on-deformed-coords error is sub-mm — but A/B-prove it). **(c) Subdiv may balloon the arm-unweld cap seams** — probe the shoulder seam zone at level 2 before building on it. **(d) COW prop-lag (#24)** applies to GN modifier inputs too — judge by render. **(e) One factor per probe; the zero-gate is sacred:** GN modifier hidden + new drivers muted must equal v4 renders at EXACTLY 0 px before anything ships. **(f) New drivers change the 61-count gate** — record the new count as the pin. **(g) RE-TIME LAW:** subdiv will raise s/f substantially (est 2.5–3.5+); re-time a 30f window (NOT 40 — 40×2s+ blows the socket) before sizing the campaign; wall for BOTH sets may exceed 90 min — that is what the budget gate is for.

## 4. Phases
**A — census:** GN API probe (build a trivial subdiv+offset group on a cube; confirm node names, interface API, modifier assignment). Subdiv cost re-time 30f. Cap-seam probe. Modifier-order A/B. Field-parity check: evaluate ONE lobe in GN vs its shader twin — px-level agreement expected. Optional AO probe ×2 framings. Baselines: v4 finals are the A/B reference (ferry) — plus fresh desk rest via TorsoCam.
**B — relief build:** GN group `T5_relief` (master strength input = THE toggle, 0 = off), full 29-element field (17 P4A + 12 T4 params from recipes), z-window, amplitude 0.5×; shader re-balance probes (h_ana 1.3->0.6–0.8, T4_w_ana in tandem); one-factor throughout; guards every dial-in: haunting A/B + desk A/B + palette spot-gates + seam zone.
**C — articulation:** breath-spine (design-first-choice above), finger sines; verify per-role: WATCH stills must sit at TRUE rest (extend the mute/zero list; re-verify the 8-pose table renders match v4 within the 4px bound), oneshot roles unaffected at f1, idle wrap f288==f1 class.
**D — author eyeball (the ONE non-delegated moment):** contact strip to chat: haunt rest + drift f96 + watch-E + desk rest, v4 vs v5-candidate, plus a RELIEF-ISOLATION pair (GN on, shader-ana 0) so the author can see what became real, plus AO pair if probed. Desk MADs stated. If unattended: proceed at conservative dials under standing delegation (×5 precedent); every element single-toggle revertible.
**E — dual re-render, budget-gated:** re-time again post-final-dials. **haunt_v5 FIRST** (744f + 8 stills, the haunt_v3/v4 machinery VERBATIM — banks, expr swaps, WATCH mute list EXTENDED with the new drivers, rest-restore law, 60s cooldowns, per-frame verify, json log to ferry, poll device_bash only). Budget-gate. **desk_v5 SECOND** (roles/frames per the v4ship recipes — read them; same campaign pattern; yurei4_* banks; remember Dismiss EMBER emission keys + restore-law member emission 0). If <40% budget remains at any gate: SAVE (blend + toggles), ship what is complete, author the render-only continuation kickoff — both campaigns are turnkey.

## 5. Ship blocks
`D:\mascot\haunt_v5\` (13 assets *_v5 + manifest_v5.json [schema 1.1, anchorPx (270,169)] + .md5 + gates + recipes v3 + relay v3 + contact sheet) and `D:\mascot\avatar\_v5\` (desk set *_v5 + manifest + gates + recipes + relay — mirror the _v4 layout). Ferry evidence mirrors for both. Encodes: detached Popen VP9 per the standing params (haunt CRF 30/28/28/30; desk per v4ship recipes); ffprobe -count_frames + case-insensitive alpha tag + libvpx 1-frame RGBA decode. Gates: zero-gate (relief-off == v4 EXACT); wrap-seam ratio band; palette 0 banned (9+ samples per set); WATCH A/B ≤ 4px + eyeball; anchor HELD by bone projection; canonical deterministic (PIL-optimized idle f0001); SIZE RULE vs v4 AND v2 (flag only if a clip exceeds 2× its V2 sibling; relief self-shadow may genuinely grow encodes this time — RULE the number either way). Relays v3: v5 SUPERSEDES v4 (and v3; v2 still live in-repo) — swap straight to v5, batch haunt+desk, Grey v3 behind. Blend save: restore audit (props, quats, exprs, mutes, NEW driver count, frame range 18–20, scene.camera TorsoCam), GN group + strength persisted, recipes text block `yurei_v5_relief_recipes` added.

## 6. Author rulings — PRE-ANSWERED (do not re-ask)
Relief ELECTED via CONTAINED means only: GN modifier / shape keys / subdiv — **no sculpt-mode, no remesh, no topology/weights/UV edits; those = STOP-and-ask.** Desk v5 elective RESOLVED-IN (both sets re-render; that decision closes here). Choreography/timings LOCKED; NEW clips/roles OUT OF SCOPE (corpus-driven, wrong seat) — wrong_hour outline-double comp stays author-ask-gated; return_ack stays no-clip BY DESIGN. WATCH stays hard-cut. Face untouched. AO ships only on explicit author yes. Motion additions are driver-class only (no bank edits). Conservative dials under delegation if unattended.

## 7. Ops crib (pointers)
Ferry via `Downloads\_yurei39_transfer\{haunt_v5,desk_v5}\`; ship to D: via Blender-side shutil; stills PIL-optimized sandbox-side then committed+copied. Campaign never polled through the busy socket; queued socket calls execute later with results lost — keep them idempotent. Sandbox sleeps cap ~120s — chain 110s. `device_list_dir` recursive on the transfer root EXPLODES (~800 frame files) — list targeted subdirs. SendUserFile->device_commit_files uuids copied EXACTLY from the return block. Save the blend BEFORE firing any campaign (crash safety). Math-node constants hide behind same-named "Value" sockets — read with socket indices. v2 clip sizes for the size rule: idle 1,672,420 / drift 912,887 / peek 690,793 / surface 1,139,379 (`wuld-ink/src/assets/yurei/`, bridge-readable).

## 8. Memory duties at close
Update `yurei-avatar-fx-arc.md` (+MEMORY.md): relief implementation (GN vs shape-key path taken, element list, amplitude, shader re-balance numbers), new driver count pin, WATCH mute-list extension, both manifest md5s (haunt_v5 supersedes 8fecaf28…; desk_v5 supersedes 0c9acaa7…), size-rule verdicts, **the returns-assessment** (fidelity plateau: reached / not / where), and the continuation kickoff pointer if budget-gated. New traps (GN API divergences especially) -> `reference_blender_5x_api_traps.md`. Grey arc untouched.

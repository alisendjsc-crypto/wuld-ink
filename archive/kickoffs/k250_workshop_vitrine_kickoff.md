WULD.INK -- K250 THE WORKSHOP VITRINE: the mascot game's parts bin goes on the wall -- a new /gallery/ wing publishing the ~61 prop preview renders with their one-line identities, the site-as-the-game's-visible-workshop genre (the synthesis-handout lineage) extended to meshes. A CONTENT-ADD session (sitemap +1, search-index REGEN -- the aux regen lane, NOT the flagship pin), run while the render lane's desk_v5 campaign grinds. NO PIN. NO efilist. The YUREI FIDELITY BATCH is a SEPARATE kickoff (k251) gated on desk_v5 hitting disk -- do not fold manifests here.

Authoritative state lives in the repo: read `C:\Users\y_m_a\Projects\wuld-ink\CLAUDE.md` FIRST (orientation + session log + the K249/K249a strata + verify_before), then `docs/wuld-ink-cowork-brief.md`. Context docs (read before building): `asset_repurpose_field_notes_v1.md` (repo root -- this is lane L2) + `mascot_yurei_asset_relay_v1.md` (exchange folder `D:\Mascot Yurei\wuld_ink\` -- SS1 and SS7 are the caption source and the sensitivity law). Re-derive EVERY guard live at open (K231); pins below are 2026-07-17/18 recon.

LANDING CHAIN AT OPEN: `fa11914` -> K249 `f18803146da870eab7507253d16d9a57364709ae` -> K249a `1b8b379c3837a879ce668bd87244661c998e8f0e` (glass pass; live-verified). HEAD==origin==`1b8b379...` expected; if the fidelity batch (k251) ran first, expect ITS commit instead and renumber THIS session's label accordingly (log label only -- the standing law). Flagship `/combined` `e654eabd`/2,963,752 (pin v4.0.0). If Josiah's glass F6 asks for a band re-tune, fold it FIRST (K249b; CSS alphas + painter dials free, spec fingerprinted, goldens gate).

THE SOURCE SET (recon'd via device_bash): `D:\Mascot Yurei\character\*_preview_v1.png` -- **61 files**, ~260 KB each (~16 MB), composed on neutral ground; the naming is `<prop>_preview_v1.png` and maps 1:1 onto the relay SS1 tables (structures / street / lawn-transit-playground / woods / yard-stagnation / institutional / items / characters incl. cat + grunts). **EXCLUSIONS (relay SS7):** there is NO protagonist preview in the set by construction -- BELT: visual-pass every image anyway; ANY frame showing the protagonist is withheld (her design = reveal decision per instance). `renders/` (31 files -- her rig-work poses) is ENTIRELY out of scope. `taunts.txt` does not exist for this session.

THE GALLERY MECHANICS (recon'd; re-derive at open): the gallery is MANIFEST-DRIVEN -- `src/gallery/manifest.json` (schema_version 2, categories[] with slug/name/caption_tier; **media_base `https://audio.wuld.ink`** -- images live on the R2 media host, NOT the repo) + per-wing `index.html` shells rendered by `/components/gallery-room.js?v=K110`. Existing wings: editorial, gap-dweller, gore, main-character, mascot-yurei, original-character, other, the-tall-one, the-wrong-thing. Gallery pages are sitemap-INCLUDED and search-INCLUDED (no wuld-search exclude on /gallery/). THREE DERIVES BEFORE BUILDING: (1) the full manifest item schema (the categories block is known; re-derive how items/filenames/captions attach -- read the whole manifest + gallery-room.js); (2) the ESTABLISHED R2 upload flow from the K110-era gallery strata in CLAUDE-history (wrangler r2 object put vs whatever the music/gallery lanes actually used -- grep `audio.wuld.ink` + `gallery-room` history; do NOT invent a new path); (3) the wing-page shell pattern (copy an existing wing's index.html shape verbatim, re-slugged).

RECOMMENDED DEFAULTS (AskUserQuestion at open, then build):
* Wing: **/gallery/workshop/** named "The Workshop" (alt: extending the mascot-yurei wing -- REJECT: different genre; 61 props would swamp her art).
* Visibility: **LIVE, uncurtained** (the gallery is already public, the mascot-yurei wing already exists, and nothing gated ships). Alt: K246-canonical curtain if Josiah wants the game's parts quiet until a reveal moment -- his call; if curtained, the page goes search-excluded + sitemap-absent instead and the regen lane drops out (route-neutral variant).
* Captions: **caption_tier "full"** -- each prop gets its relay SS1 one-liner + tri count (e.g. the seesaw "frozen 13-degree tilt -- someone just left"; the swing's dead-still second seat; the mailbox flag-down). The relay is the caption SOURCE (SS7.2: prop identities are draftable without sign-off); keep lines verbatim-or-tighter, zero new canon.
* Grouping: mirror the relay SS1 sections (1a characters ... 1h items) as the wing's internal order.
* Image pipeline: sandbox-side PIL/cwebp -- full-size **webp q~82** (~100-160 KB) + **~480px thumb webp** (~25-45 KB) per prop; PNGs stay on D:\ as source (relay: copy freely, never edit `body_detail.png`). ~122 objects, ~8-10 MB total to R2.

THE BUILD: stage the 61 PNGs into the sandbox (device_stage_files, two calls under the 50-file cap) -> decode-gate each (PIL open + size sanity) + the protagonist visual pass -> generate webp pairs + a deterministic items list (filename -> section -> caption -> tri count, transcribed from relay SS1) -> write the manifest category + items per the derived schema -> write the wing index.html per the derived shell -> sitemap regen (+1 loc if live variant) -> search-index REGEN (content add; prove build[tree]==committed-NEW byte-identical; the OLD `126c7434` retires this session -- record the new md5) -> full e2e battery HOLDS (no component logic touched; gallery-room.js BYTE-HELD -- if the derived schema makes that impossible, STOP and re-scope with Josiah) -> screenshot the wing (grid + one lightbox/caption view + narrow) and judge legibility before shipping (the K248 lesson).

SHIP: R2 uploads first (the derived flow; if wrangler-based, a `Get-ChildItem`-driven loop is SANCTIONED here as a deliberate K245 deviation -- bulk-object uploads can't be flat-lined x122 -- with a pre-printed EXPECTED count and a post-upload MEASURED object-list count gate), then ONE wuld PS block for the repo bytes: `.git\*.lock` cleanup -> fresh-clone HEAD guard -> base-MD5 guards (manifest.json + sitemap + search-index + any touched page) -> Move-Item from `Downloads\k250\` -> result gates -> `git status --short` -> explicit-stage NAMED repo files (the wing dir is small: index.html + manifest + sitemap + search-index + CLAUDE.md stratum -- flat named adds hold; kickoff strays + `_to_delete\` never ride) -> MEASURED staged-count -> commit -> pre-push ls-remote one-committer -> push -> `curl.exe` live asserts (wing 200 + a thumb 200 off audio.wuld.ink + search-index served == the NEW md5 + flagship still `e654eabd`).

DEFERRED / QUEUE: **k251 YUREI FIDELITY BATCH** (haunt v5 CONFIRMED at `D:\mascot\haunt_v5\` -- manifest `f63f703b`, schema 1.1, anchor unchanged; + desk _v5 + Grey v3 `a7023313` ride-if-ready -- RUN IT as soon as the desk campaign lands; its kickoff sits at repo root) -- then the k25x lanes per the field notes (L1 palette tokens; L3 the three.js room painter AFTER Josiah ratifies the vendor-lib register decision; L4 cat / L5 wobble-UI / L6 seam / L7 mint-to-order) -- GREY CORPUS RATCHET (awaits bench rows) -- [ fullscreen ] takeover button -- release-day curtain strips -- standing electives (PAT rotation Sep 04 2026; wrangler 4.95->4.110; efilist Pages @v4->@v5; Exchange-mirror; Stripe W-9; FIRST-PUBLISH ritual; breeze veto). Housekeeping: `Downloads\k248\`/`\k249\`/`\k249a\` deletable; `_to_delete\` holds retired kickoffs.

HAZARDS (carry): ccxxvii -- K111 -- K113 -- K115a/ccxxxv -- K218a/K228 (fresh clone; mounts lie) -- K231-live-wins (the gallery mechanics above are RECON -- re-derive schema/upload-flow/shell before bytes move) -- K238 (?v bumps only if a component actually changes -- target: NONE) -- K243-curl-discard -- K245-paste-collapse (the R2 loop deviation is scoped + count-gated; repo staging stays flat) -- K246-open-class-coupling (only if the curtained variant is chosen: derive a fresh slug/key/open-class, grep collisions) -- K219/CRLF -- K249/K249a (untouched this session) -- relay SS7 (reveal decision; renders/ out; body_detail copy-never-edit). Search-index: this session RETIRES `126c7434` by design -- every later verify_before reads the NEW committed md5; say so loudly in the stratum. One-committer: ls-remote at open AND pre-push; renumber on collision (log label only). TZ=America/Phoenix.

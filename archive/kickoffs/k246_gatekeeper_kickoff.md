WULD.INK -- K246 GATEKEEPER: admin gate-management vertical (password-curtain any page, from admin.wuld.ink). NO PIN.

Recommended default: a new ADMIN SITE-EDIT vertical (the K212/K220 diff-confirm -> ONE-commit pattern) with three ops -- `gate-apply` (inject a CANONICAL soft curtain into a chosen page + set its passphrase + add the nav.css grey rule), `gate-rotate` (change a gate's passphrase), `gate-remove` (strip the curtain + un-grey the tab) -- PLUS a same-session migration of the two bespoke curtains (sgate on `/successor/`, cgate on `/console/`) to the canonical shape, so the tool manages every gate uniformly from day one. Josiah's explicit want (K245 queue): "password-block any nav tab or section... change the password, unblock... manually whenever I wish." Page-level, per-page, NO site-wide JS. Ends with `npx wrangler deploy` from `workers\admin` (the Pages push does NOT deploy the worker).

K245 LANDING RECORD (fold at open; verify-don't-redo): K245 landed across TWO commits -- `d3d8fb9` (CLAUDE.md only, md5 `0f6723f2`: the $F hashtable array COLLAPSED to its last row in the interactive paste, both loops ran once, the hardcoded "OK: 12 guards" text printed anyway) + **`b3e1ea227385249014a740c50cce4681a282b6cd`** (K245a: the 11 swept files, flat loop-free repair, staged-count==11 gated; 21/21 line churn). K245-close live checks ALL GREEN: 5x curl md5 (library-about `f612a88b` / coda `9d1e8a3c` / labor-sine-fructu `143b035d` / search-index `126c7434` / residual stale 0) + browser (library-about 3/3 CTAs `combined#/`, labor-sine-fructu 6/6 deployment links `combined#/rwe/<id>`, coda 3/3; flagship routing 5/5 cold-loads verified same session incl. `#/coda` and trailing-slash `#/rwe/`). The CLAUDE.md K245 stratum's ship-line describes ONE block -- supersede FORWARD in the K246 stratum (record the two-commit landing + the new hazard), never rewrite the landed stratum (immutability). Zero `library.wuld.ink/#/` remains anywhere in src at `b3e1ea2` (git-grep proven).

VERIFY-AT-OPEN (fresh public-origin clone; the K216/K218a clone-reads-only discipline; re-derive EVERY guard live per K231):
* wuld HEAD == origin/main == `b3e1ea2` (or later; if later, re-derive). One-committer: `git ls-remote` at open AND before the handoff.
* Base pins at `b3e1ea2` (re-derive anyway): admin worker `workers/admin/src/index.js` blob `2767f8f9` / md5 `24f64778` / 200,013 (K234-era, DEPLOYED -- if editing it, spot-check served admin.wuld.ink behavior matches repo before splicing) -- `src/components/nav.css` blob `5828a9ee` / md5 `2fc45ab6` / 7,377 (the two grey rules live at :221 successor / :227 console) -- `src/successor/index.html` md5 `19d719b7` (sgate x27 markers; pass `ne-hoc-fiat`; key `wuld:successor:unlocked`; carries `wuld-search` exclude) -- `src/console/index.html` md5 `23bec2a5` (cgate x27; pass `facilis-descensus`; key `wuld:console:unlocked`; `wuld-search` exclude) -- operator guide `docs/admin-wuld-ink-operator-guide.md` md5 `0e38dbfe`/16,904 -- search-index `126c7434`/241,615/903 -- sitemap `c6116db4`/62 -- CLAUDE.md `0f6723f2`/300,838 [recompute] -- flagship `library.wuld.ink/combined` `e654eabd`/2,963,752 (pin v4.0.0; READ-only). K244 stack + Yurei v4 + corpora byte-frozen (per the K245 stratum's verify_before).

THE DESIGN (recommended defaults; confirm the two genuine forks with Josiah at open -- AskUserQuestion, then build):
* Fork (a) PAGE-level gating -- RECOMMENDED. Section-level (curtaining a portion of a page) is a materially bigger lift (content extraction + layout surgery) with no current use case; defer until a concrete need names itself.
* Fork (b) PER-PAGE INLINE INJECTION via the site-edit op -- RECOMMENDED. The op splices ONE canonical curtain template into the target page: prepaint script at end of `<head>` + inline `<style>` + opaque overlay markup as FIRST `<body>` child (OUTSIDE `<main>` -- search-harvest-invisible by construction, the K233 law) + logic script; namespaced `wgate-*`; per-page localStorage key `wuld:gate:<slug>:unlocked`; passphrase as one PASS const (rotate == re-splice). `gate-apply` also inserts the `.site-nav a[href="/<slug>/"]{opacity:.4}` block into nav.css (idempotent, no `?v` -- the /components/ 300s TTL carries it, K233/K235 precedent); `gate-remove` is the byte-inverse. Config-driven shared component (`gates.json` + a site-wide include) REJECTED for v1: site-wide JS blast radius + a per-page fetch for a niche dormancy feature + the live precedent is inline-per-page.
* MIGRATION RIDER -- RECOMMENDED, same session, dogfooding the new op: strip the bespoke sgate/cgate, re-apply canonical with the SAME passphrases (`ne-hoc-fiat` / `facilis-descensus`) and the SAME storage keys (`wuld:successor:unlocked` / `wuld:console:unlocked` -- already-unlocked visitors stay unlocked; the `wuld:gate:<slug>:unlocked` naming applies to NEW gates only). If descoped, the admin UI must still LIST the two bespoke gates read-only ("legacy, managed manually") -- an operator tool that cannot see two of the three live gates is a trap.
* HONESTY GATE (binding UI copy): these are SOFT dormancy curtains -- the passphrase is view-source-visible; the tool manages dormancy, NOT security. A real lock = Cloudflare Access (operator-side Zero Trust, the `/_/successor-protocol/` pattern). The admin section must say this verbatim-class, not bury it.
* Search/sitemap posture per gate: RECOMMEND a checkbox on `gate-apply` -- "exclude from site search" (adds the `wuld-search` exclude meta), DEFAULT ON, matching both live gates. Note the cadence: admin-created meta changes leave the COMMITTED search-index/sitemap stale until the next Cowork regen sweep (the K236 accumulate-then-regen class) -- flag in the UI, do not block the op on it. `gate-remove` offers the inverse.
* Worker mechanics: new `/api/site/*` patterns riding the EXISTING preview/commit diff-confirm machinery; occurrence-COUNTED splices that 422 loud on drift (the K212 donor-drift law); new UI section (next free number); rendered-UI structural gate re-parses the emitted script (`new Function`, the K213 pattern).

GATES:
* Canonical-curtain e2e (the successor-gate-e2e.cjs 19/19 pattern: extract inline scripts by marker, eval against a DOM/localStorage shim): locked-by-default, unlock + tolerance (space/hyphen/case), wrong-pass stays locked, remembered unlock, own-key isolation, overlay outside `<main>`, no network. Keep marker comments OUT of the e2e's own block comments (K233 lesson).
* Worker render-sim on REAL page bytes: apply -> remove ROUND-TRIP == base BYTE-exact (the K220 watch-index law) for a sample page + both migrated pages; apply is deterministic (re-run byte-identical); tag balance; U+FFFD 0.
* search-index NEUTRALITY proof for the migrated pages (curtain swap outside `<main>` -> build[clean]==build[migrated], HOLD `126c7434`); if the sample/apply flow adds an exclude meta to a previously-indexed page in-session, that is a REAL index change -- regen + ship, prove entry-level which.
* sitemap: `--check` against the tree; migration is route-neutral (HELD 62) unless an exclude meta lands on an indexed page (then regen rides the ship, K222 standing rule).
* node --check (.mjs) on the worker; conservation reverse-splice == base for every edited file.

SHIP: ONE wuld PS block (NO PIN) -- **FLAT per-file statements, NO array+loop constructs** (K245 hazard: interactive paste collapsed a 12-row `@( @{...}, ... )` table to its LAST row; both loops ran once; the hardcoded "OK: 12 guards" message printed regardless -- flat lines lose at most one file per lost line, and the `$staged=@(git diff --cached --name-only); if($staged.Count -ne N){ABORT}` gate backstops before commit; print MEASURED counts, never hardcoded ones). `& { ... }`; `curl.exe` not `curl`; one-line if/else; helper `Md5`; `.git\*.lock` cleanup first; `-o "$env:TEMP\x"` never `-o $null`; HEAD guard == `b3e1ea2` full sha (a pinned HEAD subsumes per-blob guards); explicit-stage NAMED files (NEVER `git add -u`/`.` -- the kickoff .md strays + `_to_delete\` must not ride); commit -> push -> `npx wrangler deploy` from `workers\admin` -> hard-refresh admin.wuld.ink (Ctrl+F5) -> live checks.

DEFERRED / QUEUE (behind K246, recommended order):
* K247 -- CONSOLE IMMERSION (Josiah's K242 direction): successor-stage-level immersion for `/console/` (full-bleed room/scene background + command text on a glass band, eventually animated to the descent's procedural rooms). DESIGN+BUILD, no scene assets exist yet -- mirror the K241 pattern (auto-open, glass band, page-scoped); scope with Josiah at open; stays gated `facilis-descensus` until fleshed (and by then gate-managed by the K246 tool).
* Standing electives (unchanged from the K245 stratum): K233/K235 release-day curtain strips (post-K246 these become `gate-remove` clicks); YUREI HAUNTING-widget refresh (render-lane next per Grey Sec.8; wuld seat owns swap mechanics when the packet lands); GREY PERSONA WIRING gated (Omega-tab + R.2 corpus cliff -- other seats); the full `docs/library-claude-coordination.md` adv-map Exchange-mirror (K221/K223/K226+K229, off-wuld one-committer); gap-log lane switch (admin section 17 "open lane"); Stripe W-9; PAT rotation due Sep 04 2026; wrangler 4.95->4.110; efilist Pages `@v4->@v5`; breeze default-flip veto; FIRST-PUBLISH ritual (awaits a video).

HAZARDS (carry): ccxxvii (explicit-stage NAMED) -- K111 (`.git\*.lock`) -- K113 (`curl.exe`) -- K115a/ccxxxv (large files via git-show/python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone) -- K231-live-wins (re-derive guards + counts live at open) -- ccxxxvi (`.k246` sidecars) -- K186a (/components/ 300s TTL) -- K238 (bump only the changed file's `?v`) -- K110 (ASCII `--`/`->`; U+FFFD 0) -- K243-curl-discard -- K219/CRLF (gate semantically where Windows tools rewrite) -- K233 (curtain inline + outside `<main>`; soft, never a security boundary) -- **NEW K245-paste-collapse** (flat statements over array+loop in operator blocks; staged-count gate before commit; measured counts in OK messages; the tell = one per-item OK line where N were unconditional + sidecars still `??` in git status + commit stat smaller than staged-intent). TZ=America/Phoenix. If a concurrent seat pushed, renumber (log label only).

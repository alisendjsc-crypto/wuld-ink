# K256 KICKOFF — T1 COMPLETION (corrections fold first, if the seat has replied)

Operator paste for the next wuld.ink Cowork session. Read this whole file before any write.

## 0 · Orientation (mandatory, in order)

1. Fresh public-origin shallow clone of `wuld-ink`; mounts are never read for repo state. Read `CLAUDE.md` FIRST (the K255 stratum is the live context: the instantiation is GIVEN, the pilot is live curtained), then `docs/wuld-ink-cowork-brief.md` if unsure of conventions.
2. The relay thread lives in `C:\Users\y_m_a\Downloads\Successor Protocol\`: read `omega_proxy_ratification_relay_BACK_v1.md` (the two-gate cliff + Q3 fences), `omega_proxy_expansion_transmission_3.md` (the corrected YES; A3 un-parked; the sectioning question), `omega_proxy_expansion_transmission_3A.md` (the K255 ship bytes) — plus ANY file newer than 3-A. The seat's gate-2 review, A3 exemplar, or sectioning answer may have landed as a TX4/reply-BACK; if so it REORDERS the mission: corrections fold FIRST.
3. Verify-at-open, STOP on any mismatch (K252 precedent): wuld HEAD == origin == the operator K255 commit `7b0a284b4b7834fb3b769c9ba8c79b55fee1e8d2` atop `6a2f6e4`. Corpus `omega-corpus-mrgrey.json` md5 `3d7117dd`/120,174 (94 = 91 authored + 3 crisis; 16 position entries). `mrgrey-register-gate.cjs` `15306a2d` — run BOTH modes (offline 3,288 / `--live` 3,312, 17 hrefs 200 incl. the library deep-link). `omega-persona-gate.cjs` `fab75c11` (evolved B — expect "16 position(s) provenance-stamped"). `omega-surface-e2e.cjs` `2277fa9c` (53/53). `omega-assistant.js` `ced05284` + `successor-stage.js` `c04b7838` (VER K255) + successor page `6548c20f` (`?v=K255` ×2, curio 94/91). Vendor `tools/omega/vendor/objections-index.json` `d034af15`/41,800 (82 ids; re-verify vs efilist HEAD `579cfbd` at consumption). Flagship `library.wuld.ink/combined` `e654eabd`/2,963,752 — PINNED, UNTOUCHED, this is a NO-PIN session. Search-index `677a9678` held. One-committer (ls-remote == HEAD). Record in the K256 stratum: **"K255 landed `7b0a284`; live asserts OK ×3 in-block (served corpus `3d7117dd`, page `?v=K255` + 94 entries, flagship held)"** — and log a live position round-trip as part of the landing record (stage "life is a gift" → `pos-life-gift-01` + the [→ the graded rebuttal] link).

## 1 · Where the arc stands

Phase 4 is OPEN. K255 shipped the first 16 signed positions (core-8 T1 × 2 phrasings, R2, provenance-stamped, each deep-linked to its graded node) + 2 fence rescopes, curtained behind `ne-hoc-fiat`, ratified by trust with the caveat recorded. **Gate 2 — the library seat's per-byte review — is ACTIVE on the TX3 thread**; its corrections are first-class edit sources, as is Josiah's own later full read. The position-law envelope (≤900 chars / em-dash ≤4 / zero questions / band `b4_reply`) stands until the seat's A3 exemplar trues it.

## 2 · Mission, in order (the recommended default path)

1. **Corrections fold FIRST** (if the seat replied): per-byte edits land as a micro-session — corpus-only where possible (K254 class; content-only register touches may ride the standing `?v=K255` URLs per the K225/K254 precedent, TTL caveat noted), full battery both modes. If the A3 exemplar landed: derive law-envelope deltas, present to Josiah for ratification, adjust the gate constants count-gated — never silently. If the sectioning answer landed: log it verbatim-class; the R1/R4 request tier still ships EMPTY (the unlock UX stays deliberately unbuilt until gated content exists).
2. **T1 COMPLETION BATCH** — the four remaining safe ids at variance ×2 (8 entries): `privileged-first-world`, `bitter-childhood`, `animals-reproduce`, `happiness-is-choice`. Same rails as K255: author from the graded canon (responses short/medium/long + diagnosis lanes; efilist READ-ONLY), triggers token-disjoint from the oracle net (the routing battery adjudicates — K255 hazard 1), crisis-form audit on every pattern, full-draft strike pass (untouched lines ratify; edits are final bytes; if ratified by trust again, record it the same honest way), provenance-stamped, each entry deep-links its own `combined#obj-<id>` node.
3. **The why-not-suicide ruling** — Josiah rules in-session, one ask. Default recommendation: include it via the hypocrisy-framing trigger family ONLY ("why are you still here", "practice what you preach", "hypocrite for living" — zero crisis tokens; the frozen floor keeps every first-person distress phrasing, by design and permanently), reply built on the canon's continuation-hypocrisy rebuttal, crisis-first battery proving the separation. Alternative: permanent floor-ownership (no entry). Either way the floor bytes do not move.
4. **Ship gating:** curtained-ship + same-day seat review is the standing precedent — re-ask only if something material changed. Page-truth flips ride the same commit: curio `94 entries &mdash; 91 authored` → `102 entries &mdash; 99 authored` (grep-count-gated exact strings; adjust arithmetic if the ruling excludes why-not-suicide or corrections change counts). The Status line ("signed positions &mdash; first set") is Josiah's word at strike time — "first two sets" or hold. Stratum records the milestone: **T1 tier COMPLETE** (12/13 or 13/13 per the ruling).
5. **TX4 → successor seat:** the T1-closure report (ship md5 + ids), correction-fold receipts if any, and — if still unanswered — a re-ask of A3 + the sectioning question (they gate the T2 batch discipline and the R1/R4 lane respectively).
6. **Stratum + sidecars (`Downloads\k256\`) + ONE PS block** per the standing conventions. Housekeeping: `Downloads\k255\` was consumed by the ship's Move-Items — deletable.

## 3 · Fences that do NOT move

Crisis floor byte-frozen (gate C deep-equality; a why-not-suicide entry, if ruled in, touches ZERO floor bytes) · no Latin beyond `mg-greet-04` · Yūrei untouched (battery must stay 100/100) · NO PIN — the flagship does not move for any of this · anti-farming: variance ≥2 per objection, `mg-how-many-01` non-enumeration posture extends to every new position · **oracle-net law:** position triggers NEVER carry a live oracle token (antinatalism / antinatalist / natalism / having kids / having children / procreation / benatar / efilism / efilist) — philosophy-NAMING inputs route to the library BY DESIGN; positions own the taunt-phrasings · `/argue/` RESTORE carry stands (unrelated lane — leave it) · biography/disclosure surface unchanged · efilist READ-ONLY.

## 4 · Scope honesty

One session = the correction fold + 8–10 new entries + the ruling. If the seat's reply is heavy, the T1 batch SHRINKS rather than the correction fold deferring — gate-2 debt outranks new authoring. T2 (17 ids, Folk Philosophical) fans out next on the same rails; do not start it this session.

## 5 · Operator conventions (unchanged)

`& { }` scriptblock · flat per-file statements, never arrays · `Md5` helper (never `H`) · `.git\*.lock` cleanup · HEAD==origin guard + base-md5 guards + result-md5 gates · explicit-stage the named files, NEVER `git add -u` · staged-count gate · one-line if/return · `curl.exe` with `-o` temp files, never `$null` · sidecars ride `Downloads\k256\` · budget pre-flagged before any long lane.

KAOMOJI DROP — isolated concurrent SITE-FEATURE session: a discrete kaomoji picker on /notes/ (insert-at-caret), in the site's own aesthetic. Bonus / for-fun / Josiah's usage. Operator paste. Read this whole file before any write. SAFE to run alongside the corpus lane (K268) and console lane (K269+) — it is FILE-DISJOINT from both (touches only /notes/ + new /components/kaomoji.* + CLAUDE.md), sharing CLAUDE.md + HEAD only.

NUMBER: self-resolve at open = (latest top-level `### K###` stratum in CLAUDE.md) + 1. This is a STANDALONE site-feature lane — it does NOT belong to the corpus (even) or console (odd) sequence; take the next free number when you ship. It does not disturb the reserved corpus K268 slot. HEAD-guard against the tip at ship; if a lane lands between clone and push, re-fetch + re-guard HEAD==origin, re-append your stratum. NO PIN, NO efilist, NO corpus, NO console files.

0 - Orientation
1. Fresh public-origin shallow clone of wuld-ink (github.com/alisendjsc-crypto/wuld-ink); mounts NEVER read for repo state. Read CLAUDE.md FIRST for the current pins + standing conventions (operator PS discipline, ?v bumps, CF-beacon served-HTML-by-content, dual-boot clock skew, sidecars-ride-Downloads); docs/wuld-ink-cowork-brief.md if unsure. This lane does NOT read the corpus/console/efilist state beyond confirming it is UNTOUCHED.
2. The kaomoji DATA is VENDORED and delivered on the desk at `Downloads\kaomoji\kaomoji-data.json` (md5 `ce12e9bc` / 59417 bytes; 42 categories, 954 entries, 901 unique faces; extracted + merged from Josiah's kaomojilibrary.html DATA+DATA2+DATA3; 0 mojibake, 0 malformed). You do NOT re-parse the HTML — this JSON is the source of truth for the picker. Shape: `{ "<Category>": [ ["<face>","<space-separated tags>"], ... ], ... }`.

VERIFY-AT-OPEN (STOP on mismatch): notes page `src/notes/index.html` `9e4716bd`/21322 (edit base) · shared `tokens.css` `870bf737` + `base.css` `f7b7f4ea` (READ for the design tokens; do NOT edit) · search-index `677a9678` + sitemap `9482ab11` (must stay UNCHANGED — prove it) · flagship `e654eabd`, corpus + console stacks UNTOUCHED (disjoint) · vendored data `ce12e9bc`. HEAD == origin (recompute CLAUDE.md md5 at open). CF-beacon: served HTML by content-grep; the data JSON serves clean, md5 with a novel buster.

1 - Mission (RECOMMENDED default — a discrete, insert-at-caret kaomoji dropdown on /notes/)
The key adaptation from the standalone library: on a WRITING surface, INSERT-AT-CARET beats copy. The picker writes the face into the note at the cursor; copy is secondary. It is DISCRETE (an unlabeled glyph control that blends into the mono toolbar — most visitors never notice it; Josiah always can), never a loud "Kaomoji" banner. It fits the site ethos ("the longer you stay, the stranger the site becomes").

2 - Design spec (site-native; recommendations — adjust by feel, keep the fences)
* TRIGGER (discrete): one `.notes-btn`-styled button appended to the notes toolbar (`.notes-toolbar`, right side — after the saved indicator or as the last control), label a small UNLABELED kaomoji glyph (recommend `[ ◡ ]` or `( ˘ ³˘)`; no word "kaomoji" in visible text), `aria-label="Insert kaomoji"`, `title="kaomoji"`. PLUS an optional hands-free open shortcut while writing (recommend `Alt+K`; must not collide with browser/editor keys — test). Toggling re-opens/closes.
* PANEL (dropdown/popover, anchored under the trigger): site tokens ONLY — `background: var(--c-bg-elevated,#161616)`, `border: var(--bw-base) solid var(--c-border)`, a `border-left: var(--bw-thick) solid var(--c-accent)` crimson stripe (mirrors `.notes-editor-wrap`), `font-family: var(--font-mono)`, `--c-fg`/`--c-fg-muted`/`--c-fg-dim` text, crimson `--c-accent` (#c41e3a) hovers/active. Bracket idiom for controls. On narrow screens (<=640px) render as a bottom sheet, not a clipped popover.
* CONTENTS: (a) a search input (match on face + tags, the library's search) styled like the site; (b) a horizontal, scrollable row of category CHIPS (crimson-accent active pill, muted idle — recolor the library's chips to site tokens) incl. an "All" and a "★ Favorites"; (c) a scrollable GRID of face cells (bordered, `:hover`/`:focus` → crimson border like `.notes-btn:hover`), each a button whose text is the face; a ★ corner toggle per cell for favorites.
* PRIMARY action — INSERT at caret: on face click, splice the face into `#editor.value` at `selectionStart`/`selectionEnd`, restore the caret after the inserted glyph, and DISPATCH a synthetic `input` event so the notes app's autosave + word-count + typing-FX all fire naturally (do NOT reach into notes' private closure — go through the textarea + input event). Keep the panel open for multi-insert; a brief toast ("inserted") reusing the notes toast feel. SECONDARY: a copy affordance (per-cell or a mode) using `navigator.clipboard.writeText` with the `document.execCommand("copy")` fallback (the site's clipboard pattern) — same sandbox-safe fallback the notes copy uses.
* FAVORITES: ★ toggles persist to `localStorage["wuld:kaomoji"]` (own key; versioned `{v:1, favs:[...]}`; never touch `wuld:notes`). A ★ Favorites filter. Optional favorites-export parity later; not required v1.
* ZALGO CONTAINMENT (load-bearing): the `Zalgo / Glitch` category (18 combining-mark pieces incl. corrupted words) is walled off EXACTLY as the source library does — its own opt-in chip, EXCLUDED from "All" and from search results, so its stacked combining marks can NEVER bleed into or clip the normal grid. Give its cells extra vertical/horizontal padding + `overflow` room so the marks render without clipping. This category is opt-in only.
* SHUFFLE (optional, nice-to-have): a `[ ⚄ ]` control that surfaces a random face (excludes Zalgo). Enter/Esc/arrow-key nav in the grid; Esc closes + returns focus to the trigger; focus-trap while open.
* A11y + MOTION: full keyboard operability (tab into panel, arrow-nav the grid, Enter inserts, Esc closes), `role`/`aria` on the panel + trigger (`aria-expanded`), visible focus rings; honor `prefers-reduced-motion` (no spark/animated open — instant/opacity only); the existing notes caret-spark/word-glow/autosave stay UNTOUCHED and keep working.
* DATA LOADING: LAZY — do NOT ship the 59KB data inline in the notes page. `kaomoji.js` FETCHES `/components/kaomoji-data.json?v=K###` on FIRST open only (so the notes page stays light and the feature is truly discrete — zero cost unless used). Handle fetch failure gracefully (a quiet inline message, no throw).

3 - Files + discipline
* NEW `src/components/kaomoji.css` (picker styles, site tokens; page-include `?v=K###`).
* NEW `src/components/kaomoji.js` (picker logic; page-include `?v=K###`; lazy-fetches the data with a matching `?v` buster; own-key `wuld:kaomoji`; goes through the textarea + input event, never notes' closure).
* NEW `src/components/kaomoji-data.json` (Move-Item from `Downloads\kaomoji\`; md5 `ce12e9bc` — result-gate it).
* EDIT `src/notes/index.html` (base `9e4716bd`): add the 2 includes (`<link ... kaomoji.css?v=K###>` in head, `<script ... kaomoji.js?v=K###>` before `</body>`) + the toolbar trigger button + a mount point. MINIMAL — keep the harvested body text neutral (glyph label, aria-label; no indexable "kaomoji library" prose) so the search index does not shift.
* ?v: kaomoji.css/js get their INTRO `?v=K###` (this session's number); the notes-page's OTHER includes (tokens/base/nav/ambient/wrong-hour/footer) are BYTE-UNCHANGED — do NOT bump their `?v`. The data JSON rides a `?v` buster set by kaomoji.js.
* NEUTRALITY: `build_index.py --src src` == committed `677a9678` byte-identical (the data JSON is a /components/ file, NOT in the harvest set; the notes HTML change is include+glyph only). Prove it; if it shifts, you added indexable text — trim it back to neutral rather than regen. sitemap `9482ab11` UNCHANGED (no new page). NO search regen, NO sitemap change, NO pin, NO ?v on unshared components.
* SCOPE: /notes/ only for v1 (the component is reusable later, but do not wire it elsewhere this session).

4 - Fences that do NOT move
* Flagship `e654eabd` (NO PIN) · the corpus (`omega-corpus-mrgrey.json`) + the successor/console stacks — DO NOT TOUCH (disjoint lane) · `tokens.css`/`base.css` — READ ONLY (use the vars, never redefine them) · the existing notes app: `wuld:notes`, autosave, the caret-spark / word-glow / per-word chime FX, the copy/download/delete toolbar — all BYTE-BEHAVIOR-PRESERVED (you ADD a control + a mount, you do not refactor the notes IIFE) · homepage zero-JS invariant (N/A here, but never regress it) · own localStorage key `wuld:kaomoji` only · Zalgo containment (never in All/search) · CF-beacon: served HTML by content-grep, md5 only the JSON.

5 - Verify + ship
* SANDBOX: `node --check` kaomoji.js; JSON.parse the data; a headless boot (or Chrome via the browser tools) of /notes/ — 0 console errors, the notes autosave + FX still fire, the picker: opens (click + Alt+K), searches, filters by category, INSERTS at caret (caret lands after the glyph; autosave + word-count update), copies (with fallback), favorites persist across reload, Zalgo is opt-in + absent from All/search, Esc + keyboard nav work, reduced-motion honored, mobile bottom-sheet at <=640px. Screenshot 1-2 states for the review.
* Budget: a bounded single-surface build (one component + a small notes edit + the vendored data). Same-session author + verify + ship under Josiah's standing strike-pass; HOLD for a pre-read only if a design choice reads uncertain (Josiah asked for "your recommendations" — a clean build ships; surface the glyph choice + the Alt+K shortcut if you want his nod).
* SHIP = ONE wuld PS block (NO PIN, NO efilist, NO search/sitemap): `.git\*.lock` cleanup → HEAD==origin guard → base-md5 guard on `src/notes/index.html` == `9e4716bd` + assert the 3 new files ABSENT → Move-Item the 4 (`kaomoji-data.json`, `kaomoji.css`, `kaomoji.js` from `Downloads\kaomoji\`; the edited `index.html` sidecar) → result-md5 gates (data `ce12e9bc` + the 3 new md5s) → explicit-stage the 4 named files (NEVER `git add -u`) → staged-count==4 gate → `http.postBuffer 524288000` + commit + push + `$LASTEXITCODE` gate → curl.exe deploy-verify (served /notes/ has the 2 includes by content-grep; `kaomoji-data.json?v=…` serves `ce12e9bc` via a novel buster; flagship held `e654eabd`). Then a `### K###` stratum: the feature, the 4 files + md5s, neutrality proof, "NO PIN / disjoint site-feature lane / corpus + console untouched". Deliver sidecars to `Downloads\k###\`, verify on-device md5s.

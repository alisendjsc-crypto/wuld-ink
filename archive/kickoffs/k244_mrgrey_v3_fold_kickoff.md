WULD.INK -- K244 MR. GREY v3 CLIP-FOLD (the cat to Yurei parity). NO PIN.

Recommended default: fold the Mr. Grey v3 set -- a SELF-CONTAINED 14-role manifest swap (12 clips + 2 stills, `mr-grey_manifest_v3.json` md5 `a70233131a0ec1386e8431fefa0dee4f`, SUPERSEDES v2 `9a448d2b` packet / `24106ffd` landed-LF), repoint the two OMEGA consumers, bump the two `?v`s that changed, screenshot-judge, ship ONE wuld PS block. This is the MIRROR of K243, now against the cat: Grey-only, self-contained, YUREI UNTOUCHED (stays v4). The packet is complete + render-lane-gated (`mr-grey_gates_v3.json`: ALL PASS -- 1284/1284 campaign frames, encode 12/12 exact + alpha 12/12, loop seams clean, palette 0 banned with the tapetum eye-exemption, tail rest-hidden alpha 0). The relay `mr-grey_av_v3_wuldink_relay.md` is a STAGED handoff addressed to this seat: land the 14 files + repin off `9a448d2b`; ZERO player-code change (fallback map lives in the manifest).

Why this session exists: K243 LANDED (commit `4736339bfea36f0ce2c8597702438871bcffb478` atop `9022899`; live-verified at close -- served yurei manifest_v4 md5 `8e9e8c7b`, `successor-stage.js?v=K243` carries `avatar_manifest_v4`, `/successor/` page `?v=K243`, idle clip 200; Josiah eyeballed the live stage -- Yurei renders at full v4 fidelity). The 3D-Modeling seat then delivered Mr. Grey v3: the COMPLETE 12-role wave-2 set on the Phase-D fidelity surface (micro-bump fur breakup + silhouette tufts + inner-ear emission glints + muzzle/nose split + tapetum radial striation), ALL 14 roles re-rendered set-atomic, incl. the 5 net-new state clips (wrong_hour/return_ack/regard/idle_breeze -- and blink_ack was v2, deflect was v2; the NET-NEW-since-v2 are wrong_hour/return_ack/regard/idle_breeze). The K241 stage + corner widget resolve avatars BY MANIFEST -> drop-in by construction. NOTE: Grey's AVATAR is already live on the stage as v2 (successor-stage `:53`); this is a FIDELITY UPGRADE of an already-live element, exactly as K243 upgraded Yurei v3->v4. Grey's PERSONA WIRING (Omega-tab public activation + the R.2 corpus/words cliff) stays GATED -- untouched by this fold.

THE PACKET (recon'd at K243-close -- verify, don't re-derive): `D:\Mascot Yurei\mr_grey\avatar\` (SELF-CONTAINED, 14 assets):

* 12 webms (role / file / size / frames / md5): idle `mr-grey_av_idle_v3.webm` 99186 192f `57872c9c` loop -- appear `..._appear_v3` 43099 60f `1c435c4b` -- speak `..._speak_v3` 141239 144f `97e5b580` -- dismiss `..._dismiss_v3` 143940 96f `a7c95cd9` -- listen `..._listen_v3` 55061 96f `f8cbe454` loop -- long_idle `..._long_idle_v3` 102981 192f `8ef40488` loop (NOW A LOOP: guard-doze, supersedes v1 one-shot) -- wrong_hour `..._wrong_hour_v3` 39357 96f `da640182` loop (NET-NEW: dilated-metronome, tail-tip ticks strict-regular, zero blinks, no breath) -- return_ack `..._return_ack_v3` 54910 48f `bc66c72c` (NET-NEW: head micro-lift + one slow blink) -- regard `..._regard_v3` 111699 72f `0377722b` (NET-NEW: gaze figure-eight) -- idle_breeze `..._idle_breeze_v3` 104508 192f `06627c7a` loop (NET-NEW: whisker micro-oscillation + one ear flick; no tail sway) -- blink_ack `..._blink_ack_v3` 30186 36f `a16428fc` -- deflect `..._deflect_v3` 90615 60f `4b037c53`.
* 2 stills: canonical-p0 `mr-grey_av_still_v3.png` 261063 `a063e743` (== idle f0001 byte-copy) -- long_idle-still `mr-grey_av_long_idle_still_v3.png` 256895 `337f6d46`.
* `mr-grey_manifest_v3.json` md5 `a70233131a0ec1386e8431fefa0dee4f` (+ `.md5` sidecar) + `mr-grey_gates_v3.json` + `grey_v3_recipes.md` + `mr-grey_contact_v3.png` + `mr_grey_phaseD_packet_v3.png`. contract `avatar_manifest_v2` (schema shape unchanged); anchorPx `[267,259]`; sha256 per-asset in the manifest. Set ~1.53 MB (12 files; largest = dismiss 143940 under the 400KB flag line). This manifest is ALREADY LF on disk (0 CR at recon) -- STILL gate the landed-LF md5 at open (a Windows re-save could CRLF it; currently LF-md5 == `a70233...`).
* FALLBACK MAP (carried in the manifest, verbatim): deflect->speak, idle_breeze->idle, wrong_hour->idle, return_ack->blink_ack, regard->listen, glitch->speak, long_idle->idle, listen->idle, appear->idle, dismiss->idle, speak->idle, blink_ack->idle. NOTE the Grey-specific deltas: return_ack HAS a clip (falls back to blink_ack only if absent) and regard HAS a clip (falls back to listen). Unlike Yurei, EVERY role here resolves to a real clip (no by-design fallback-only role).

VERIFY-AT-OPEN (fresh public-origin clone; verify-don't-redo):

* wuld HEAD == origin/main == `4736339` (K243) or later; if later, read what landed and RE-DERIVE every base guard from live git (K231 law; one-committer -- `git ls-remote` at open AND before emitting the handoff). Record the K243 landing in the K244 stratum (sha `4736339` + the served checks above, cite as K243-close data).
* Re-stage the v3 packet from `D:\Mascot Yurei\mr_grey\avatar\`. LAND THE 14 FILES VERBATIM under their delivered `mr-grey_av_*_v3` names (SELF-CONTAINED, like Yurei v4 -- NO normalization; the relay + manifest a70233 reference these exact names). The live omega dir speaks `mrgrey_av_*` (no hyphen) for v1/v2 rollback; v3 lands hyphenated `mr-grey_av_*_v3` alongside them -- the manifest self-resolves bare filenames against its own dir, so the hyphen divergence is cosmetic (v2 stays rollback under its own names). Per-asset md5+sha256+size_bytes == manifest fields; ffprobe vp9 / 0-audio / frames exact (192/60/144/96/96/192/96/48/72/192/36/60). ALPHA: gate `alpha_mode=1` CASE-INSENSITIVELY (ffmpeg-8.1 muxer writes the matroska tag UPPERCASE `ALPHA_MODE` -- the K242-vp9-alpha gate that matched lowercase `alpha_mode` will FALSE-FAIL; lowercase the tag key before compare) + a decoded-RGBA alpha-plane read via `-c:v libvpx-vp9` (gates note transparent_frac 0.553). NEVER assert `pix_fmt==yuva420p` (K242-vp9-alpha; VP9 alpha reads yuv420p+alpha_mode). Palette re-sample: banned_blue 0 EXPECTED OUTSIDE the eye region -- the gold-green tapetum slit is the ONE sanctioned non-red accent (exempt rows_bottomup 410-516 cols 110-441).
* Base blobs at open (re-derive live via `git rev-parse HEAD:path`): `src/components/omega-assistant.js` (K243 blob `37ff0550`), `src/components/successor-stage.js` (`0ce03e00`), `src/successor/index.html` (`d09806b0`), `CLAUDE.md` (`734622c8`). UNTOUCHED (verify held): `src/components/yurei-assistant.js` (`66a35935`), `src/components/nav.js` (`9cdea8bc`). Live `mrgrey_manifest_v2.json` md5 `24106ffd` (rollback, kept); live `avatar_manifest_v4.json` md5 `8e9e8c7b` (Yurei, HELD).

THE DESIGN (drop-in; ZERO engine/corpus/matcher/persona/resolver/yurei bytes):

1. GREY v3 SELF-CONTAINED SWAP -- land 12 webms + 2 stills + `mr-grey_manifest_v3.json` (LF) into `src/assets/omega/avatar/`. Nothing removed; v1+v2 manifests+clips stay as rollback (K117/K224d pattern). All 14 roles resolve to REAL clips; the 4 net-new-since-v2 roles (wrong_hour/return_ack/regard/idle_breeze) go live the moment the manifest lands.
2. CONSUMER REPOINT (OMEGA-only; re-derive the exact lines live -- at K243-close they were): `omega-assistant.js:27` MANIFEST_URL `mrgrey_manifest_v2->v3` + `:30` VER `K242->K244` -- `successor-stage.js:53` mrgrey `v2->v3` (leave `:47` yurei == `avatar_manifest_v4` UNTOUCHED) + `:32` VER `K243->K244`. Yurei consumers (`yurei-assistant.js`, `nav.js`) NOT touched -- Yurei stays v4.
3. CACHE-BUSTS (K238 bump-what-changed): `index.html:304` omega-assistant.js `?v=K242->K244` (omega-assistant changed) -- `index.html:307` successor-stage.js `?v=K243->K244` (successor-stage changed). LEAVE: `:47` omega-assistant.css `?v=K230`, `:50` successor-stage.css `?v=K241`, and nav.js yurei-assistant injector `?v=K243` -- all FROZEN (unchanged). (omega-assistant.js line 290 injects omega-assistant.css`?v=VER` -> that internal fetch rides to K244; harmless, css bytes unchanged -- same benign VER-ride as K243's yurei css.)
4. INGEST GATES + E2E + RESOLVER-COMPAT -- same discipline as K243: per-file md5+sha256+size+ffprobe on all 14 (alpha CASE-INSENSITIVE); full e2e suite green UNCHANGED (successor-stage-e2e 90/90 synthetic-manifest no-filename-fence; successor-gate 19/19; persona-switcher 47/47; omega-surface 20/20; omega-persona-gate 100/100; agent-settings 37/37; yurei-parity; console-e2e 65/65; gaplog-visitor 45/45); `node --check` on touched js (omega-assistant, successor-stage); drive the REAL v3 manifest through the resolver (`resolveAsset` walk) -- all 14 roles resolve REAL, unknown -> idle, reduced-motion -> canonical-p0.
5. SCREENSHOTS (headless chromium, real clips over `#0a0a0a`): Grey stage-hero + the 4 net-new states (wrong_hour dilated-slit / return_ack / regard / idle_breeze) + blink_ack + the guard-doze long_idle + corner-desk 64px (omega-assistant repoint proof, IF Grey renders in a corner widget on any page -- else stage-only) + reduced-motion stills; re-sample palette over the new idle (banned 0 outside the tapetum exemption; the gold-green eyes are EXPECTED non-red). Confirm Yurei v4 still renders (repoint didn't perturb `:47`). Deliver a contact sheet.
6. RECORDS: CLAUDE.md K244 stratum (K243 landing + the v3 fold + file map + gates; git-show -> $HOME -> python, NEVER Edit/Write). RIDER (owed library-arc, absorb here since we're committing wuld CLAUDE.md): a one-line adv-map POINTER noting the efilist adversarial-map Phase B2 micro-fold LANDED (efilist commit `e197c65`, fragment `b55e2ea8`, validator PASS, pin `e654eabd` held) -- pull the exact wording from the efilist B2 fold receipt (`K229_B2_fold_receipt_v1.md` in the Adversarial Corpus folder) if present; the full `docs/library-claude-coordination.md` Exchange-mirror stays DEFERRED (off-wuld library lane). search-index: only two `?v` tags change (not harvested) -> prove build[clean]==build[mine]==committed `564d6a81` and HOLD; sitemap untouched (route-neutral, 62).
7. SHIP ONE wuld PS block (NO PIN, NO efilist, NO Yurei): `& { ... }`; `curl.exe` not `curl`; one-line if/else; helper `Md5`; `.git\*.lock` cleanup first; TZ=America/Phoenix. HEAD guard (`4736339`) -> base-blob guards (omega-assistant/successor-stage/index/CLAUDE) + working-copy base-md5 guards -> Copy-Item the 14 binaries STRAIGHT FROM `D:\Mascot Yurei\mr_grey\avatar\` (COPY not Move; cross-volume) into `src\assets\omega\avatar\` with per-file `Get-FileHash` md5 gates -> Move-Item the LF `.k244` sidecars (mr-grey_manifest_v3, omega-assistant.js, successor-stage.js, index.html, CLAUDE.md) -> result-md5 gates -> explicit-stage the ~19 NAMED paths (NEVER `git add -u`/`git add .`; strays + `_to_delete\` + kickoff .md files must not ride) -> commit -> push -> deploy-verify (v3 manifest 200 + downloaded md5 == LF pin `a70233`; one new clip HEAD 200; served `successor-stage.js?v=K244` carries `avatar_manifest_v4` AND `mrgrey_manifest_v3`; served `omega-assistant.js?v=K244` carries `mrgrey_manifest_v3`; page `?v=K244` x2). Verify uses `-o "$env:TEMP\x"` NOT `-o $null` (K243-curl-discard: `$null` is a PS-ism, not a curl discard).

SPLIT: one clean shippable session (Grey-only, self-contained). If a per-asset gate mismatches, HALT that asset, relay the drift to the 3D seat; the set is atomic so a single mismatch means re-request, not partial-ship.

NAMED PATHS (19): 12 webms + 2 pngs (`src\assets\omega\avatar\mr-grey_av_*_v3.{webm,png}`) + `src\assets\omega\avatar\mr-grey_manifest_v3.json` + `src\components\omega-assistant.js` + `src\components\successor-stage.js` + `src\successor\index.html` + `CLAUDE.md`.

WIRING GATE (respect, do not cross): this fold is AVATAR-fidelity ONLY. Grey's persona go-live -- the Omega-tab public un-gating + the R.2 corpus/words -- belongs to other seats and stays GATED. Do NOT touch the corpus, the persona-switcher stance, the sgate/curtain, or the Omega-tab visibility. If any step would change Grey's dialectical STANCE or make him more public than his current (gated, stage-only-behind-passphrase) state, STOP and route to Josiah (register discipline).

DEFERRED / NOT this session:

* RENDER-LANE next (3D seat): per the Grey session-prompt Sec.8, the likely next subject is the YUREI HAUNTING-widget asset refresh -- the corner-widget's `avatar_manifest_v2` clips (`yurei-assistant.js:24` loads v4 now, but the *haunting* widget lineage / any remaining pre-fidelity manifest_v2 clips) are pre-fidelity renders; visual coherence with the v4 stage argues a re-render on the v4 surface. Scope with Josiah; wuld-ink seat owns the swap mechanics when the packet lands. (Both mascots now reach fidelity on the stage after K244 -- diminishing render returns beyond the haunting refresh.)
* CONSOLE IMMERSION (Josiah's K242 direction) -- `/console/` successor-stage-level immersion (full-bleed room/scene background + command text on a glass band, eventually animated to the descent's procedural rooms); a DESIGN+BUILD session, no scene assets yet, scope with Josiah, gated `facilis-descensus` until fleshed.
* RELEASE PUSH (K233 curtain strip + un-grey tab + findability + changelog) -- behind the waves.
* GREY PERSONA WIRING -- Omega-tab public activation + the R.2 corpus cliff (other seats; the avatar being at fidelity does NOT unblock the words).
* OWED (library arc): the full `docs/library-claude-coordination.md` Exchange-mirror (the K244 stratum absorbs only the one-line adv-map pointer; the mirror doc defers, off-wuld, one-committer).

KEY PATHS: assets IN `D:\Mascot Yurei\mr_grey\avatar\` -> OUT `src\assets\omega\avatar\`. Code: `src/components/{omega-assistant,successor-stage}.js` + `src/successor/index.html`. FROZEN: `successor-stage.css ?v=K241`, `omega-assistant.css ?v=K230` -- the YUREI stack (v4: `yurei-assistant.js`, `nav.js`, `avatar_manifest_v4.json` `8e9e8c7b`, yurei clips) -- both corpora + resolver + persona-switcher + agent-settings + yurei-oracle -- search-index `564d6a81` + sitemap 62 -- flagship `library.wuld.ink/combined` `e654eabd` (pin v4.0.0).

HAZARDS (carry): ccxxvii (explicit-stage NAMED; never `git add -u`/`.`) -- K111 (`.git\*.lock`) -- K113 (`curl.exe`) -- K115a/ccxxxv (large files via git-show/python, never Edit/Write) -- K218a/K228 (base-guard on HEAD blobs from a FRESH clone) -- K219 (gate the landed-LF md5; the v3 manifest is LF on disk now but re-verify) -- K231 (re-derive every guard live; the kickoff's `:27/:30/:53/:32/:304/:307` are hints, live wins) -- ccxxxvi (`.k244` sidecars) -- K186a (`/components/` 300s TTL) -- K238 (bump only the changed `?v` -- omega-assistant + successor-stage yes, css + nav + yurei NO) -- **K242-vp9-alpha EXTENDED (K244-alpha-case): ffmpeg-8.1 writes the matroska tag UPPERCASE `ALPHA_MODE` -- gate CASE-INSENSITIVELY (lowercase the tag key before compare); still gate `alpha_mode` presence, never `pix_fmt==yuva420p`** -- K242-manifest-EOL (land LF) -- K243-curl-discard (never `curl.exe -o $null`; use `-o "$env:TEMP\x"` / `-o NUL`) -- TAPETUM-PALETTE-EXEMPTION (the gold-green eyes are the ONE non-red accent; palette gate excludes the eye region). TZ=America/Phoenix. If a concurrent seat claimed K244, renumber (log label only).

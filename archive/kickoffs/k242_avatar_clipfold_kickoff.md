WULD.INK -- K242 AVATAR CLIP-FOLD, wave 1 (Yurei v3 + Mr. Grey v2). NO PIN.

Recommended default: fold BOTH wave-1 avatar packets the render lane has already exported -- Yurei v3 FIRST (byte-exact, zero decisions), Mr. Grey v2 SECOND (one normalization call, recommended path below) -- then repoint the four manifest consumers, bump the `?v`s that changed, screenshot-judge, ship ONE wuld PS block. Strictly the "clip-fold" session the K241 carries scheduled; the packets are complete (clips + manifests + md5s + gates + relay notes addressed to this seat), and both relay notes say ship wave 1 without waiting for wave 2 -- fallbacks cover every unrendered role.

Why this session exists
K241 LANDED (commit `19d7ba2490e50f28095047a613ccffdf8b5cf1b3` atop `f767f38`; live-verified at K241-close: `/successor/` 200, served `successor-stage.js?v=K241` carries `switchStagePersona`, css carries `data-sstage-solo`, page serves both `?v=K241`; Josiah eyeballed the stage live -- auto-open, both personas, live toggle, closed state all good). The 3D-Modeling seat then delivered the Successor-Stage HERO relay, wave 1: re-rendered hero clips for the near-viewport stage, with two relay notes DATED 2026-07-17 whose fold contract is explicit ("point the resolver at the new manifest; ZERO player code change"). The K241 stage resolves avatars BY MANIFEST, so this fold is the drop-in it was designed to be.

The packets (recon'd at K241-close -- verify, don't re-derive from scratch)
* YUREI v3 -- `D:\mascot\avatar\_v3\` (SELF-CONTAINED): 4 NEW webms `yurei_av_idle_v3` (558,280 B; 288f/12s fixture double-stutter + edge-alpha breathe) / `appear_v3` (283,439; safelight develop) / `speak_v3` (180,706; flicker steadies while the line lands) / `glitch_v3` (101,574; NET-NEW role, 40f displacement slice) + carryovers listen/dismiss/long_idle `_v2` + both stills `_v2` (relay: "unchanged hashes") + `avatar_manifest_v3.json` **md5 `aa1a91bd5e3e1954f5b375cd4594797e`** + `avatar_gates_v3.json` + contact sheet + `yurei_av_v3_wuldink_relay.md` (the contract).
* MR. GREY v2 -- `D:\Mascot Yurei\mr_grey\avatar\`: 4 NEW webms `mr-grey_av_idle_v2` (92,833; 4s breath + ONE lazy tail half-curl -- tail is new geometry, rest-hidden, rest pose pixel-parity with v1 so v1 one-shots chain clean) / `appear_v2` (38,438; eyes-first materialization) / `blink_ack_v2` (27,430; NET-NEW role, fallback->idle, inert until a hint binds it) / `deflect_v2` (71,117; the off-path-question register -- goes LIVE at once: the corpus already emits deflect hints, today falling back to speak) + `mr-grey_manifest_v2.json` **md5 `9a448d2b1392b473860d0c8e73e24a7a`** + `mr-grey_gates_v2.json` + contact + `mr-grey_av_v2_wuldink_relay.md`.
* CARRYOVER PARITY pre-checked size-for-size at K241-close: every carryover the packet manifests reference matches the LIVE blob byte-size exactly (yurei listen 223,627 / dismiss 278,511 / long_idle 370,831 / still 239,342 / li-still 272,029; mrgrey speak 170,988 / listen 89,420 / dismiss 107,335 / long_idle 161,465 / still 250,364 / li-still 250,396). K242 completes the proof at md5 level.

Verify-at-open (fresh public-origin clone; verify-don't-redo)
* wuld HEAD == origin == `19d7ba2` (K241) or later; if later, read what landed and RE-DERIVE every base guard from live git (K231 law -- never trust this kickoff's numbers over the live tree). One-committer: the render lane is mid-wave-2 model-side but does NOT push to wuld; still `git ls-remote` at open AND before emitting the handoff.
* Record the K241 landing in the K242 stratum (sha above + the served-bytes checks -- already gathered, cite as K241-close data).
* Re-stage the two packets; gate the two manifest md5s against the relay pins above; md5 EVERY packet carryover against the corresponding LIVE blob (`git cat-file` from the fresh clone). ANY carryover mismatch = render-lane drift -> HALT that avatar's fold, ship the other, relay the drift back to the 3D seat.
* Known K241-close blobs (reference; re-derive live): successor-stage.js `95e9e9a4b5e110f4f74efb4414aaede590a47514` -- successor/index.html `3f3100fd3bfc2d0739b8fac98e9346886faa8fd1` -- CLAUDE.md md5 `33337d82eeba245799e3c32ff33bb881`. yurei-assistant.js / omega-assistant.js / nav.js: derive at open (`git rev-parse HEAD:path`).

The design (drop-in by construction; ZERO engine/corpus/matcher/persona bytes)
1. YUREI v3 -- BYTE-EXACT ADDITIVE. Land the 4 new webms + `avatar_manifest_v3.json` byte-exact (`aa1a91bd`) into `src/assets/yurei/avatar/`. Carryovers are already live under identical names (gate, don't copy). Nothing removed -- v2 manifest + clips stay as rollback (K117/K224d pattern). The `glitch` role goes live through the EXISTING fallback map the moment the manifest lands; no other code knows it exists.
2. MR. GREY v2 -- NORMALIZE-ON-LAND (the recommended call; adjudicate at open). The live dir speaks `mrgrey_av_*.webm` (unversioned, no hyphen); the packet manifest references `mr-grey_av_*_v1` carryover names that DON'T exist in the tree. Normalize: the 4 new clips land as `mrgrey_av_{idle,appear,blink_ack,deflect}_v2.webm`; the manifest lands as `mrgrey_manifest_v2.json` with ONLY its 10 `file` fields rewritten (4 new -> the mrgrey_ v2 names; 6 carryovers -> the existing live unversioned names). SEMANTIC GATE (K219 class): parse authored vs normalized, assert every non-`file` field equal, declare the 10-row file map in the stratum, record authored `9a448d2b` + the normalized md5. REJECTED alternative: land verbatim -> ~950 KB of duplicated carryover bytes + two naming schemes in one dir. v1 manifest + files stay as rollback.
3. CONSUMER REPOINT (the complete census -- verified at K241-close, these four lines are ALL of it): `yurei-assistant.js:24` MANIFEST_URL -> `avatar_manifest_v3.json` -- `omega-assistant.js:27` MANIFEST_URL -> `mrgrey_manifest_v2.json` -- `successor-stage.js:41` -> v3 -- `successor-stage.js:47` -> v2. Plus VER consts (manifest/corpus fetch cache-bust): successor-stage `K238->K242`, omega-assistant `K237->K242`, yurei-assistant `K234->K242`.
4. CACHE-BUSTS (K238 rule -- bump exactly what changed): `successor/index.html` tags `successor-stage.js?v=K241->K242` + `omega-assistant.js?v=K237->K242` (omega loads ONLY on /successor/ -- census'd). yurei-assistant.js is nav.js-INJECTED: bump the injector line `?v=K234->K242` in `nav.js` (the K234 precedent; nav.js's own page tags stay `?v=K43`, its content change rides the /components/ 300s TTL). successor-stage.css UNCHANGED this session -> its `?v=K241` HOLDS (do not sweep).
5. INGEST GATES (sandbox, pre-ship; the K115/K224d discipline): per-file md5 + sha256 + size_bytes == the manifest's own fields for EVERY asset both manifests name (new + carryover); ffprobe every NEW webm (vp9, yuva420p alpha, ZERO audio streams, frames == manifest `frames`); echo the gates jsons (seam MAD / alpha_mode / palette / no-audio) into the stratum; RESOLVER-COMPAT -- drive both REAL manifests through the stage's actual resolver (the e2e `_seed` path): glitch/deflect resolve to real clips (no longer fallback), blink_ack resolves with fallback->idle, all eight standard roles resolve, reduced-motion still resolves per manifest.
6. E2E: full suite green UNCHANGED (`successor-stage-e2e` 90/90 uses a synthetic manifest and no fence pins a manifest filename -- verified at K241-close; if extending, real-manifest resolver passes are the natural adds, OPTIONAL). `node --check` every touched js.
7. SCREENSHOTS (headless chromium, local serve, unlocked; the K241-mount-race lesson: ALWAYS shoot the NON-default persona too): stage BOTH personas with the new idles live (Yurei fixture double-stutter; Mr. Grey tail half-curl -- confirm the tail reads at stage size), a corner-desk spot on a NON-successor page (new clips at small size; yurei-assistant repoint proof), reduced-motion (stills, video display:none), 1440x900 + one other viewport. RE-SAMPLE AA over the new idle frames -- the glass-band backings are unchanged but the PIXELS BEHIND them are new; the gates say the fixture never drops below ~80% red, so expect PASS, but prove it on the actual frames.
8. RECORDS: CLAUDE.md K242 stratum (K241 landing + the fold + the file map + gates; git-show -> $HOME -> python, NEVER Edit/Write) . search-index: the only page edits are two `?v` tags (not harvested) -> prove build[clean]==build[mine]==committed `564d6a81` and HOLD; sitemap untouched.

Ship (ONE wuld PS block; NO PIN, NO efilist, NO search/sitemap/canon)
`& { ... }` scriptblock; `curl.exe` not `curl`; one-line if/else; helper `Md5` not `H`; `.git\*.lock` cleanup first; TZ=America/Phoenix. Order: fetch + HEAD==origin guard -> base-blob guards (re-derived at open) -> **Copy-Item the binaries STRAIGHT FROM the transfer dirs** (D:\ -> C: cross-volume; COPY not Move -- the packet folders stay in place as the render lane's record): 4 yurei webms + `avatar_manifest_v3.json` from `D:\mascot\avatar\_v3\`, 4 mrgrey webms from `D:\Mascot Yurei\mr_grey\avatar\` (landing under the NORMALIZED mrgrey_ names) -- per-file `Get-FileHash` md5 gates == the manifest-declared md5s -> Move-Item the `.k242` TEXT sidecars (normalized `mrgrey_manifest_v2.json`, `successor-stage.k242.js`, `omega-assistant.k242.js`, `yurei-assistant.k242.js`, `nav.k242.js`, `src\successor\index.k242.html`, `CLAUDE.k242.md`) -> result-md5 gates ALL -> `git status --short` review -> EXPLICIT-stage the ~16 NAMED paths (NEVER `git add -u`/`git add .` -- kickoff strays + `_to_delete\` must not ride; the transfer dirs live outside the repo so nothing D:\ can leak) -> commit -> push -> deploy-verify: `curl.exe` the new manifests (200 + downloaded-file md5 == pins), one new clip HEAD 200, served `successor-stage.js?v=K242` carries `avatar_manifest_v3`, page tags `?v=K242` x2. K186a: new filenames/query-strings are edge-cache-new; re-check ~5 min only if the page html greps stale.

SPLIT (if it runs long)
The YUREI fold alone is a complete, shippable session (byte-exact, zero adjudication) -- land it, e2e, screenshot, ship. The Mr. Grey normalization becomes a clean follow-on. If a carryover md5 mismatch halts one avatar, fold the other and relay the drift back.

Deferred / NOT this session
* Binding `blink_ack` to a stage send-ack (or any new hint wiring) -- a stage-POLISH session, Josiah's call; the role ships inert-by-design.
* WAVE 2 folds (the render lane's stated next: yurei listen/long_idle/dismiss re-renders, then wrong_hour/regard/deflect/idle_breeze; mrgrey speak/listen/long_idle, then wrong_hour/regard/return_ack/idle_breeze, dismiss last) -- each repeats THIS session's shape against a fresh packet.
* The RELEASE PUSH (K233 curtain strip + un-grey the tab + findability + changelog) -- queued behind the waves, or behind Josiah calling the avatars done enough.
* The K226 adv-map B1 coordination mirror + pointer stratum (still owed, separate lane).

Key paths
* Assets IN: `D:\mascot\avatar\_v3\` (yurei wave 1) -- `D:\Mascot Yurei\mr_grey\avatar\` (mrgrey wave 1). Assets OUT: `src/assets/yurei/avatar/` -- `src/assets/omega/avatar/`.
* Code: `src/components/yurei-assistant.js` (:24 + VER) -- `src/components/omega-assistant.js` (:27 + VER) -- `src/components/successor-stage.js` (:41 :47 + VER) -- `src/components/nav.js` (the yurei-assistant injector `?v`) -- `src/successor/index.html` (2 tag bumps).
* FROZEN: `successor-stage.css` (?v=K241 holds) -- both corpora + `yurei-oracle.js` + persona-switcher + agent-settings -- the sgate curtain (`ne-hoc-fiat`) + greyed tab -- search-index `564d6a81` + sitemap 62 -- flagship `library.wuld.ink/combined` `e654eabd` (pin v4.0.0; zero efilist bytes).
* Tests: `tools/omega/{successor-stage-e2e,successor-gate-e2e,persona-switcher-e2e,omega-surface-e2e,omega-persona-gate}.cjs` + `tools/fx/agent-settings-e2e.cjs` + `tools/yurei/yurei-parity.cjs` -- all must stay green.

Hazards
Standing list (ccxxvii explicit-stage -- K111 locks -- K113 curl.exe -- K115a/K204/ccxxxv large-files via git-show/python -- K218a/K228 fresh-clone base guards -- K219 semantic gates -- K231 re-derive live / live-wins -- ccxxxvi `.k242` sidecars -- K186a TTL -- K238 bump-what-changed -- K110 ASCII/U+FFFD 0 -- K233 curtain stays) PLUS: (1) **K241-mount-race** -- boot-time auto-open vs async widget mount; screenshot-judge with the NON-default persona active or the class is invisible. (2) **The haunting's manifest is a DIFFERENT file**: `src/assets/yurei/manifest_v2.json` (read by `yurei.js:98`, the ~1/100 easter-egg widget) vs the assistant's `src/assets/yurei/avatar/avatar_manifest_v2.json` -- similar names, different dirs; touch ONLY `avatar/`. (3) Semantic-gate the NORMALIZED mrgrey manifest (file fields differ by declared map), byte-gate the yurei one (`aa1a91bd`). (4) COPY, never Move, from the transfer dirs. (5) If HEAD moved past `19d7ba2` at open, someone else committed -- read it before proceeding (one-committer). (6) Session number: if a concurrent seat claimed K242, renumber to K243 -- log label only. TZ=America/Phoenix.

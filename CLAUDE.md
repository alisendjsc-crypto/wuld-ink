# CLAUDE.md — wuld.ink project primer

You are a Cowork instance working on Josiah's `wuld.ink` personal site. Read this file first. Then read `docs/wuld-ink-cowork-brief.md`. That is the orientation sequence.

---

## What this project is

A multi-page philosophical-content site for Josiah (WULD / AnomicIndividual87 / Evilis Anihilis Uls). Cloudflare end-to-end stack (Registrar + Pages + R2). Domain `wuld.ink` registered 2026-05-11 (auto-renew $20/yr, expires 2027-05-11). The site is the umbrella for all his philosophical output.

---

## Orientation sequence (in order)

1. Read this file.
2. Read `docs/wuld-ink-cowork-brief.md` — full implementation brief, stack rationale, capacity math, audio architecture, open decisions, working preferences.
3. Confirm what Josiah is asking. If a session-A-prerequisite decision is still open (see "Current state" below), surface it via `AskUserQuestion` before building anything.

---

## File layout

| Path | Role |
|---|---|
| `README.md` | Quick orientation, scope summary |
| `CLAUDE.md` | This file — primer for Cowork sessions |
| `docs/wuld-ink-cowork-brief.md` | Full implementation brief |
| `src/tokens.css` | Multi-mode design tokens (type, color×3 modes, spacing, borders, motion) + `@font-face` declarations for 3 self-hosted serif faces |
| `src/base.css` | Reset + element defaults wired to tokens; root font-size bumped to 18px |
| `src/components/audio-player.{css,js}` | Inline `.audio-block` audio player; reads `data-audio-key` → R2 URL |
| `src/components/mode-toggle.{css,js}` | Scoped reader-mode toggle (dark / reader / hc) |
| `src/components/nav.{css,js}` | Site header + primary nav. JS sets `aria-current="page"` on matching link (session B) |
| `src/components/glossary.css` | Glossary index + per-entry layout (shared across all `/glossary/*` pages) |
| `src/components/void-engine.{css,js}` | Void Engine instrument: signal textarea + mode radiogroup + seed input + transmit button + transmission output. JS exposes `window.VoidEngine.register(slug, {label, transform})` for chat-side authorship of transmission modes. Ships with two content-empty stub presets (NULL passthrough, INVERSION mirror) so the instrument boots usable. Seeded determinism via mulberry32 PRNG; empty seed input falls through to content-derived xmur3 hash for reproducible auto-seeding. |
| `src/templates/essay.html` | Canonical essay template — root-relative asset paths after session B; serve `src/` via static server for local preview |
| `src/index.html` | Real homepage — collapsed title-page. Cover (full viewport, Cormorant Garamond display + handles row + descent affordance) → Index grid of 6 destinations → footer |
| `src/essays/index.html` | Essay index — lists SV (live shell) + Illogically Is + A Life Inside (forthcoming) |
| `src/essays/sanguinolentum-vestigium/index.html` | SV essay shell — 3 sections wired with audio data-keys (`essays/sanguinolentum-vestigium/section-{1,2,3}.mp3`), prose placeholders awaiting chat-side injection |
| `src/argument-library/index.html` | Library page shell — placeholder; editorial extracts land in F+ once library declares stable tag |
| `src/glossary/index.html` | Glossary A–Z index — 9 terms listed, 2 live (Alogical Isness, Contextus Claudit) + 7 forthcoming |
| `src/glossary/_template.html` | Glossary entry template — copy for new entries, fill term/meta/sections |
| `src/glossary/alogical-isness/index.html` | Anchor entry shell — definition/etymology/see-also/appears-in scaffolded, bodies pending |
| `src/glossary/contextus-claudit/index.html` | Anchor entry shell — same scaffold as alogical-isness |
| `src/void-engine/index.html` | Void Engine page — live instrument. Signal textarea + transmission output below, section-rule separated. Mode radiogroup hydrates from registry. Cmd/Ctrl+Enter from textarea transmits. Two stub presets ship; chat-side appends real transmission modes by calling `VoidEngine.register(...)` in a sibling `<script>` block (see comment block at bottom of the file). |
| `src/book/index.html` | Book page shell |
| `src/blog/index.html` | Blog page shell |
| `src/watch/index.html` | Watch page (session G) — link-out video card grid mirroring selected uploads from the WULD Incorporated YouTube channel. Two placeholder cards ship as paste-replace stubs: swap `.video-thumb-placeholder` div for `<img class="video-thumb" src="https://i.ytimg.com/vi/<VIDEO_ID>/hqdefault.jpg">` when filling real video data. No iframe embeds (bandwidth + surveillance discipline). Channel CTA bar always points to source. |
| `src/_/successor-protocol/index.html` | The Successor Protocol (session G) — hidden stub at `/_/successor-protocol/`. Underscored-prefix convention = not indexed, not linked, not surfaced. Sealed surface (no nav/header/footer; `noindex,nofollow` meta). Awaits Cloudflare Access policy (operator-side, Zero Trust dashboard) for email-OTP gating against alisendjsc@gmail.com + content paste-replace from baton (other-project chat). Setup instructions live in top-of-file HTML comment block. |
| `src/frame/index.html` | Frame (session K3) — entry-point anchor page for cold readers. Situates the umbrella's general philosophical stance through WULD's specific inflection: 4 `.frame-section` blocks (negative utilitarianism / anti-natalism / structural pessimism / voluntary human extinction). Each section ships a `.frame-section-canonical` mono line gesturing at the academic position + `.frame-section-placeholder` body awaiting chat-side authorship with seed-questions for inflection-from-canon. NOT a glossary entry; glossary discipline holds coined vocabulary only. Discreet `.frame-pointer` aside in `/glossary/index.html` header is the only on-site link in. NOT a homepage destination card. `.frame-onward` cross-link block at bottom routes to /glossary/ + /book/ + future library.wuld.ink. |
| `src/essays/architecture-of-moral-disaster/index.html` | Architecture of Moral Disaster essay shell (session K3) — audio-format slot for the 23:11 reading staged at `_audio-staging/architecture-of-moral-disaster.mp3` since session H. Pattern-matches Alogically Is. Audio-intro band with `data-audio-key="essays/architecture-of-moral-disaster/full.mp3"` + secondary cross-link to video adaptation at `/watch/` (ID `GSDN0vu18Fo`, 23:59, Apr 2026). Inherits `/components/essay.css` from K2 promotion (third use case). 3 section placeholders + endnotes scaffold. Audio block 404s until R2 drag-drop completes. |
| `src/assets/book-cover-malgre-tout.png` | Malgré Tout cover image (session K3 addendum, 2026-05-13) — Josiah's full-bleed cover variant (portrait, post-swap). True PNG with alpha (RGBA 8-bit, ~3.0 MB / 1152×1536, 3:4 portrait — canonical book-cover ratio). Single dissolved face in red/black with distressed paper texture. Mounted in `/book/`'s page-hero above the Cormorant title-block via `.book-cover` block (capped 24rem, hairline border — tighter cap accommodates portrait aspect without pushing title-block + lede below desktop fold). Future optimization candidate: re-encode as WebP to reduce above-fold transfer size while preserving alpha + distressed-texture detail. |
| `src/fonts/README.md` | List of WOFF2 files to drop in + sources (Cormorant Garamond, IM Fell English, EB Garamond) |
| `docs/baton-template.md` | Cross-project synthesis template Josiah carries between project chats; feeds session B's IA + glossary work |
| `docs/library-claude-coordination.md` | Cross-Claude relay doc between wuld.ink Cowork and library-Claude. Append a new dated Exchange section per round. Locks agreements + open pushes per item (confirm/nudge/reject format). |
| `docs/book-claude-coordination.md` | Cross-Claude relay doc between wuld.ink Cowork and book-project Claude. Append a new dated Exchange section per round. K → Exchange 1 (wuld.ink-side: baton ratifications + (ii) collapse + Lacero forced question + canonical-shift discipline forced question). K3 → Exchange 1 response (book-Claude side: NUDGEs + locks). K3 → Exchange 2 (wuld.ink-side: closures + ratifications + round closes). |
| `docs/successor-claude-coordination.md` | Cross-Claude relay doc between wuld.ink Cowork and successor-Claude (Successor Protocol / Ne Hoc Fiat project). Fourth relay channel in the umbrella's coord-doc family. K8 close → Exchange 1 (wuld.ink-side asks). K9 (2026-05-14) → Exchange 2 reply (successor-Claude side: A confirmed with archive-trigger NUDGE accepted; B confirmed with Contextus listener-class nudge + Lacero meta-entry-or-no-entry opinion; C option (a) locked + lede + status text supplied; D confirmed with Protecting-class absence → (c) NUDGE accepted; 7 paste-relay bodies attached) + Exchange 3 ack (wuld.ink-side: all locks accepted; K9a shipped glossary body fills; K9b ships HTML mirror + /ne-hoc-fiat/ page). |
| `src/ne-hoc-fiat/index.html` | Ne Hoc Fiat project page (session K10, 2026-05-14) — first project-shaped page on the umbrella. Pattern-matched to `/book/` shape minus cover image + purchase block (project is in-development, no purchasable artifact yet). Page-hero (`09 · Project` eyebrow + Latin title + English subtitle "Let this not be done" + author line) + lede (paste-relayed verbatim from successor-Claude Exchange 2 Section C, ~50 words) + status section (paste-relayed verbatim, ~75 words: modal-architectural pessimism + empirical asymmetry argument + structural-absence pair) + outline placeholder + approved-excerpts sign-off-gated scaffold + cross-references (7 glossary entries + 3 sibling-surface links) + discreet `.sp-aside` at bottom (K3 `.frame-pointer` pattern, mono label + accent-bordered single-sentence body) pointing at gated `/_/successor-protocol/`. New inline CSS (`.project-title-block`, `.project-subtitle`, `.project-author`, `.project-section`, `.project-section-heading`, `.project-section-placeholder`, `.sp-aside` variants); promote to `/components/page.css` on second project-shaped page per second-instance-threshold. Cowork relays authoritative content from successor-Claude; zero net-new philosophical content authored. |

---

## Scope: what Cowork is for vs not for

### Cowork is FOR (most build work):

- Design system: typography scale, color tokens, spacing rhythm, component library
- HTML/CSS/JS implementation across all pages
- Audio player component + `data-audio-key` wiring to R2
- Build tooling, GitHub repo init, Cloudflare Pages config, DNSSEC enable
- Iterative debug/fix/refine cycles
- File reorganization, find-and-replace, structural lookups
- R2 bucket setup, custom subdomain (`audio.wuld.ink`) config

### Cowork is NOT FOR (defer to chat):

- Drafting essay or blog **content** (philosophical writing)
- Generating glossary entries (vocabulary curation)
- Drafting Void Engine prompts (semantic content authorship)
- Major architectural pivots (re-locking the stack, swapping registrar)
- Anything that would update the **efilist** project's canon (different project)

Rule of thumb: **Cowork builds the vessel; chat fills it with content.**

---

## Register and working norms

- **Direct.** No hedging, apologies, sycophancy, "great question" openers.
- **Iconoclastic framing welcome** where it earns its keep.
- **Disagree substantively** — don't flatter, push back where the user's logic has gaps. Concede only to superior argument.
- **No menus.** Recommendation first, then supporting reasoning.
- **Concise.** Josiah reads without prescription glasses currently — keep blocks short, scannable.
- **Pre-flag token-budget risks.** Multi-session arc; sessions that may hit ceilings mid-build should be re-scoped, not pushed through.

---

## Aesthetic register (LOCKED — applies to all visual decisions)

- **Mode:** Neobrutalist dark-mode (canonical). Reader-mode (warm-cream light) and high-contrast (HC) modes are SCOPED accessibility affordances on heavy-read containers only — not site-global theme switches.
- **Typography (book-matched):** Three serif faces mirroring Josiah's book + IBM Plex Mono retained for UI chrome only.
  - **Cormorant Garamond** (Christian Thalmann) — `--font-display`: homepage hero, title-page, book-cover register.
  - **IM Fell English** (Igino Marini) — `--font-headline`: h1, h2 essay/section titles.
  - **EB Garamond** (Georg Duffner) — `--font-body`: body text, h3-h5 subheadings.
  - **IBM Plex Mono** — `--font-mono`: eyebrow labels, audio readouts, mode-toggle buttons, code, metadata, footer, nav. The "manufactured object" signal (running heads, colophons, page numbers in literary-press parlance). **Also the cross-surface anchor** between wuld.ink (where Mono = chrome) and `library.wuld.ink` (where Mono = everywhere, the instrument-panel diegetic skin). The Mono spine is the typographic constant across the umbrella; non-chrome typography diverges by design (serif on wuld.ink for content register; mono throughout on library for instrument register).
  - All three serifs self-hosted via `@font-face` with `local()` first in the src chain (renders Josiah's installed copies immediately, falls through to WOFF2 in `src/fonts/` for web visitors).
  - Root font-size bumped to 18px (`html { font-size: 112.5% }`) to compensate for Garamond's small x-height — also serves Josiah's no-glasses reading constraint.
- **Palette:** Three modes keyed on `[data-mode]`:
  - Dark (default): near-black `#0a0a0a`, warm off-white `#f0ebe5`, blood-red accent `#c41e3a`.
  - Reader: warm cream `#f5efe6`, dark warm-gray text `#1a1816`, darker accent `#a91930`.
  - HC: pure black/white, lifted accent `#ff4060`, yellow focus ring `#ffeb3b`.
- **Future axis:** `data-palette="cb-*"` reserved for colorblind-safe variants (deutan/protan/tritan).
- **Influences:** Risograph print, zine culture, underground photography (Paul Clipson), analog/experimental.
- **Reject categorically:** SaaS landing-page aesthetics, pastels, gradients, drop shadows, rounded soft corners, Material Design, generic AI design.

Visual anchor reference: `alisendjsc-crypto.github.io/efilist-argument-library` (do NOT inherit its patterns — reconcile to the wuld.ink token system in session C).

---

## Current state (as of 2026-05-15, post-session-K19 close)

**Status:** Sessions A + E + B + D' + D'' + E2 + E3 + G + H + I + J + K + K2 + K3 + K4 + K5 + K6 + K8 (absorbs K7) + K9 + K10 + K11 + K12 + K13 + K14 + K15 + K16 + K17 + K18 + K19 complete. (Session D superseded by D' / D''; text-transformer scaffolding orphaned files confirmed rm'd by Josiah this E2 turn.) **K16 (2026-05-14 micro):** recover-git.ps1 enhancement (K15 lesson (i) carry-forward executed) + K16 lessons + close. Diagnostic-first opening confirmed K15 commit `ef9a917` HEAD intact AND `.git/config` line 17 truncation RECURRING (`\tmerge ` orphan, no `=`, no value, no EOL) — Josiah's K15-close manual repair LANDED CLEAN in `ef9a917` per object DB (commit object preserves the fixed state), but on-disk file got re-corrupted in K15-close→K16-open window. Same K-close-window mtime (00:42 UTC) as 10th+ `.git/index.lock` recurrence + index binary-corruption (bad sha1 signature) + 12+ phantom D+?? paths (index-corruption artifact, not real drift). **Truncation-recurrence is now CONFIRMED ACTIVE** — not a one-time PS-write artifact from initial setup-git.ps1 era; an ongoing vector parallel to the lock-recurrence. Same out-of-band Windows-side process likely (cowork-svc.exe candidate per K14 handle.exe trace remains leading-but-unproven; Procmon trace remains the durable closing path). One real working-tree M file: CLAUDE.md (193L/59779B vs HEAD 67456B, ~84 lines lost since K15 commit — drift consumed partial state); all other tracked files byte-exact match HEAD per drift-scan (library-coord 847L/117317B / book-coord 586L/57374B / successor-coord 370L/39937B / /book/ 463L/21968B / /preface/ 147L/6624B / black-box 37L/2383B / /ne-hoc-fiat/ 297L/13958B all matching). Uploads empty (no Procmon, no Grey verbatim, no library Exchange 5/6 reply, no chat-side artifact) → LIGHT scope locked per K15 lesson (iii). AskUserQuestion locked single track: (a) + K16 lessons + close, recommended option selected; ~25-35 calls projected. Two workstreams shipped. (1) **CLAUDE.md drift restore via `git show HEAD:CLAUDE.md > CLAUDE.md` bypass** — K15 lesson (ii) re-validated 5th time across K9/K10/K12/K13/K15/K16 (works despite `.git/config` corruption since object DB reads are independent of config-parse path); 218L restored to K15-close state. (2) **`recover-git.ps1` enhancement shipped (K15 lesson (i) executed exactly to spec)** — new Step 1 prepended: line-parse `.git/config` for `[branch "main"]` section header, regex-match `merge = <non-empty value>` within section body, fail loud with current branch-block display + manual-editor repair instructions + exit code 5 if missing/truncated. WARN-and-skip path for non-standard configs with no `[branch "main"]` section. NOTE-not-error path if merge key present but value differs from canonical `refs/heads/main` (allows non-standard remote tracking). Existing Steps 1-4 (lock-clear / fsck / null-sha1 / status) renumbered to 2-5. Banner K12 → K16. Final summary adds "Config merge key: OK" line. Header comment block expanded: documents 4 failure modes (lock recurrence / index corruption / null-sha1 phantom / **NEW: config merge-key truncation**), notes K15-NEW family with E3-era setup-git.ps1 corruption, cites cowork-svc.exe as leading-candidate vector pending Procmon closure. Self-check during authorship caught embedded PS one-liner repair-guidance had quote-escaping bug — unescaped `"main"` mid-output would terminate outer PS string mid-copy-paste under PS double-quote rules; dropped one-liner, kept manual editor instructions only (bulletproof, no quoting hell). Script remains ASCII-only / PS 5.1-safe / idempotent / interruption-survivable. **Tool budget retrospective:** Projected ~25-35 calls (LIGHT); actual ~?? by close (post-commit count). Diagnostic-first opening surfaced full state in 2 bash calls (1 truncated on fsck-exit, 1 follow-up); AskUserQuestion-driven scope-lock prevented expansion past pre-flagged ceiling. **`<p>` margin-inline:auto audit:** N/A this turn (no vessel work; recovery + script enhancement + docs only). **K16 lessons logged:** (i) **Truncation-recurrence pattern LOCKED** — K15-close manual repair to `.git/config` landed clean in commit object (git reaches the fixed state via object DB) but on-disk file got re-corrupted before K16 open. Same window as lock + index recurrence. Same out-of-band Windows-side vector. recover-git.ps1 enhancement detects + fails loud + emits manual repair; does NOT prevent recurrence (only Procmon → root-cause-fix would close that path). Acceptable workflow-cost: operator-side fix is 30s text edit when prompted by Step 1 failure. The recovery cycle now self-bootstraps — script's Step 1 failure tells operator exactly what to fix without prior context. (ii) **Hardening discipline: detection + fail-loud + manual-repair-guidance > auto-repair.** Auto-repair would risk masking the recurring corruption vector — operator would never see the failure mode, root-cause investigation would stall. Detection-only forces operator awareness each cycle, keeping the Procmon-trace path live as a pending closing-move. K15 lesson (ii) pattern (manual fallback documented in handoff) re-validated. (iii) **Self-check Write outputs containing quoted code samples** — discipline added to PS-output-discipline ledger: NEVER embed copy-paste-ready code snippets inside `Write-Output` strings unless validated end-to-end through PS string-escape → Write-Output → terminal-display → user-copy → PS-parse round-trip. Manual instructions in plain prose are bulletproof; quoted samples carry quote-escape risk specific to the embedding context (`""` is escaped quote, `` ` `` is escape char, `` `n `` is newline, `\` is literal — different rules than bash / C / Python). K16 caught this Cowork-side via self-check before commit; durable rule going forward. Adds to E3-era PS ledger (no Stop-mode-on-stderr / ASCII-only / OneDrive null-byte watch / K15 multi-`-m` empty-middle / K16 NEW: no quoted-code-samples in Write-Output). (iv) **Pre-flagged K15-carry-forward EXECUTED EXACTLY TO SPEC** — K15's lesson (i) named the carry-forward shape explicitly ("recover-git.ps1 should verify [branch "main"] block integrity, manual repair fallback documented in handoff"); K16 shipped that shape verbatim. Pattern locks: when K-N's lesson names a specific K-N+1 carry-forward shape, ship that shape unless new diagnostic data invalidates. Reduces K-N+1 scope-lock decision-cost. **K17 carry-forwards:** Josiah-side `.git/config` line 17 manual repair → `.\recover-git.ps1` → commit K16 (this run validates new Step 1 against active broken state at session-open — if script's manual-edit guidance is operator-clear, end-to-end recovery flow self-bootstraps; report any UX gaps in Step 1 output back for K17 refinement). Procmon trace investigation (still opt-in; durable closing path for cowork-svc.exe candidate; would close the root-cause-question on the recurrence pattern). Library Exchange 5/6 reply on stable-tag timeline + 4-gate handoff status (non-blocking; unlocks F+ editorial extraction when closed). Mementos: *Grey* verbatim text (book-Claude follow-up OR chat-side authorship). Naming-divergence resolution (`/preface/` "structural pessimism" vs K5 umbrella SP→SA — Josiah's call). Mementos lede candidate authorization (K4 F4 line). `.entry-alias` CSS promote-to-component (N=2 alias-note threshold; waits for second alias case). D2 flash card scaffolding (still open content + tech decisions). **K15 (2026-05-14 micro):** K14 lessons addendum (vii)+(viii) + drift restore + recovery handoff. Diagnostic-first opening surfaced 8th+ `.git/index.lock` recurrence (0-byte, mtime 2026-05-15 00:42 UTC ≈ K14-close window) + `git fsck` bad-sha1-signature + null-sha1 D+?? pattern + drift on 4 M files + **NEW failure mode**: `.git/config` line 17 truncation (`\tmerge ` orphan key — should be `merge = refs/heads/main`; PS-write-truncation artifact, same family as E3-era setup-git.ps1 corruption). K14 commits intact (`4f84fa2` HEAD; reflog truncated but object DB reaches). Uploads empty (no Procmon, no Grey verbatim, no library Exchange 5/6 reply); LIGHT scope locked per session-prompt default. Two workstreams shipped. (1) **Drift restore via `git show HEAD:$f > $f` bypass** — all 4 M files back to HEAD state (CLAUDE.md 168→217 lines, K11+K12+K13+K14 narratives restored inline; library-coord 840→847; /book/ 397→463 Mementos+Tucson+Desk; black-box 33→37 redirect stub). K12 drift-restore pattern re-validated 4th time across K9/K10/K12/K13/K15. (2) **K14 lessons addendum** appended inline within K14 narrative (line 120) — (vii) failed-commit-staging-persistence (commit errors don't roll back staging; subsequent `git add` compounds; first successful commit absorbs union — how K14's `4f84fa2` bundled all three planned-separate commits; explicit `git reset` if re-shape needed) + (viii) PS multi-`-m` empty-middle quoting bug (`-m "" -m` trips PS 5.1 parser on empty middle arg; **K15+ commit discipline:** single short `-m "summary"` default, here-string `-m @"..."@` or stdin for multi-paragraph; adds to E3-era PS ledger alongside no-`Stop`-on-stderr + ASCII-only + OneDrive-null-byte-watch). **Tool budget retrospective:** Projected 15-30 (LIGHT); actual ~17 by close. Diagnostic-first opening + parallel-safe Cowork/Josiah-side work-split (drift restore Cowork-side, recovery Josiah-side) kept critical path tight. **`<p>` margin-inline:auto audit:** N/A (no vessel work). **K15 lessons logged:** (i) **NEW failure mode `.git/config` line 17 truncation** — broaden recovery surface beyond lock + index; PS-write-truncation can corrupt config too. `recover-git.ps1` should verify `[branch "main"]` block integrity (`merge = refs/heads/main` key present + non-empty value) at start; manual repair fallback documented in handoff. Same failure family as E3-era setup-git.ps1 corruption (UTF-8 BOM stripping + here-string truncation under PS 5.1). (ii) **`git show HEAD:$f > $f` bypass works despite `.git/config` corruption** — config errors don't block object DB reads. Drift restore durable even when index + config are both broken simultaneously. Pattern compounds: drift-restore + `core.autocrlf=input` + locked-recovery-script = three-layer durability against the recurrence pattern. (iii) **LIGHT scope shape locked** — when uploads is empty + no chat-side surface, ship addendum + diagnostic + close in 15-20 calls; don't expand into investigation needing Josiah-side artifacts (Procmon trace, book-Claude reply, library reply). Default to closing fast + emitting handoff; investigation scope re-opens when artifact arrives. **K16 carry-forwards:** (a) Josiah-side **manually fix `.git/config` line 17** (text editor: change `\tmerge ` to `\tmerge = refs/heads/main`) BEFORE running recover-git.ps1, then run `.\recover-git.ps1` + commit K15 deliverables (CLAUDE.md K15 paragraph + K14 lessons addendum + memory + MEMORY.md); (b) **Procmon trace investigation** if `cowork-svc.exe` candidate needs closure (heavier; Josiah opts-in — Sysinternals Procmon with capture filter on `C:\Users\y_m_a\Projects\wuld-ink\.git\` during a Cowork session-close cycle); (c) Mementos: *Grey* verbatim text (book-Claude follow-up OR chat-side); (d) naming-divergence resolution (`/preface/` "structural pessimism" vs K5 umbrella SP→SA — Josiah's call); (e) Mementos lede candidate authorization (K4 F4 line); (f) library Exchange 5/6 reply on stable-tag + 4-gate handoff (non-blocking; unlocks F+ when closed); (g) `.entry-alias` CSS promote-to-component (N=2 alias-note threshold; waits for second alias case); (h) D2 flash card scaffolding (still open content + tech). **K14 (2026-05-14 micro):** Investigation handoff (Experiment 1 Defender negative + Experiment 2 handle.exe trace) + `/preface/` discoverability pointer + CLAUDE.md trim micro. Diagnostic-first opening surfaced sentinel commit `3c09e32` ("post-Defender-exclusion lock-recurrence probe") landed clean on origin/main — Josiah ran the folder-scoped Defender exclusion experiment between K13 close + K14 open, AND `.git/index.lock` recurred anyway (0-byte, mtime 2026-05-15 00:42 UTC ≈ K13-close window, 7th+ recurrence) + `git fsck` "bad index file sha1 signature / fatal: index file corrupt" + 12-file phantom D + ?? working-tree pattern. **Defender RULED OUT** as root cause. AskUserQuestion at session start locked 2 scopes on recommended path: fire handle.exe trace NOW before recovery (lock present + signal window open; one-shot opportunity) + light vessel scope (/preface/ pointer + trim micro). Three workstreams shipped clean. (1) **handle.exe finding (Experiment 2):** lock is **orphan-stale**. No process in the system dump holds a handle to `.git\index.lock` specifically. `explorer.exe pid 11628` holds two FOLDER-level handles to `C:\Users\y_m_a\Projects\wuld-ink` (file-system-watcher / breadcrumb cache, proximate but not lock-relevant). `cowork-svc.exe pid 9700` shows `<unable to open process>` — handle.exe (even elevated) couldn't enumerate the privileged Cowork service handles (unprovable but suspicious). Zero handles to any `.git\` subpath, zero IDE git-integration candidates (no VS Code / JetBrains / Sublime / Git Credential Manager). All always-running daemon candidates RULED OUT by enumeration. **Conclusion: a process creates the lock during a git operation + dies before unlocking.** Strongest remaining candidate: Cowork sandbox session-boundary teardown — `cowork-svc.exe` itself or some Anthropic-side hook touches `.git/index*` on shutdown, dies before releasing. Definitive proof would require Sysinternals Process Monitor (Procmon) trace logging filesystem writes to `.git\` during a session-close cycle — heavier scope than today's; deferred until Josiah opts-in. **Operational outcome: recover-git.ps1 stays the durable pattern.** Orphan-stale locks clear via `Remove-Item -Force` cleanly (no live owner contention). Recurrence is acceptable workflow-cost; recovery cycle reliable. (2) **`/preface/` discoverability pointer shipped on src/index.html cover area** (Josiah recommendation-locked via session-start AskUserQuestion). Two surgical Edits: CSS — scoped existing `.cover-descent a` rules to direct-child selector `.cover-descent > a` (so the INDEX↓ inline-flex + pulse animation doesn't bleed into descendant `<a>`); added `.cover-preface-pointer` block (display:block, margin-block-start var(--s-3), font-size 0.72rem, color var(--c-fg-dim), opacity 0.7, mono uppercase inherited from .cover-descent, hover lifts to accent + full opacity); reduced-motion handling inherited. HTML — `<span class="cover-preface-pointer"><a href="/preface/">Preface &rarr;</a></span>` inserted inside `.cover-descent` <p> after INDEX↓ <a>, with HTML comment documenting K3 `.frame-pointer` precedent + anchor-vs-destination discipline. Subordinate-to-INDEX↓ by font-size + opacity. NOT a homepage grid card per K3 anchor-vs-destination lock; cover-area discreet affordance preserves grid's destination-character. (3) **CLAUDE.md trim micro shipped via general-purpose subagent (K8 pattern re-validated).** Python-via-bash positive-lookahead regex split of line 120 (106 KB Status paragraph) on `**K<n>?(...):**` markers; moved K + K2 + K3 + K4 + K5 + K6 + K8 + K9 + K10 narratives (9 chunks) to CLAUDE-history.md; kept K12 + K13 inline; preserved Pre-K-moved-to-history note. Spliced standalone `### Session K deliverables (2026-05-13 micro)` heading block (lines 122-197 / 76 lines / 18 KB) → history. Arc table compressed 8 individual K-rows (K, K2, K3, K4, K5, K6, K8, K9, K10) to 1 summary row in pre-K-summary pattern; K11 + K12 + K13 individual rows preserved. Pre/post: CLAUDE.md 299 lines / 175,846 bytes → **216 lines / 48,422 bytes** (-72.5% size); CLAUDE-history.md 284 lines / 95,624 bytes → **405 lines / 205,519 bytes** (+121 lines / +110 KB). Verifications: K11 (2 occurrences) + K12 (4) + K13 (3) markers preserved inline; K, K2, ..., K10 markers zero in primary; UTF-8 / LF clean; zero null bytes / zero CR bytes; subagent reported anomaly = stale `/tmp/trim.py` shadowed heredoc (pivoted to `$HOME/tmp/trim_k14.py` cleanly). Status sentence updated this turn to include K11 + K12 + K13 + K14 in complete-list (was truncated at K10 since pre-K11 close). **Tool budget retrospective:** Projected ~30-40 calls (light scope) at session start; actual ~32 by close. Within projection. Subagent delegation saved ~15-20 direct Edits on the 106-KB Status paragraph (K8 lesson re-validated). **`<p>` margin-inline:auto audit:** N/A this turn — `.cover-preface-pointer` is descendant `<span>` (inline, not block), nested `<a>` also inline; no block-flow centering concern. `.cover-descent` parent `<p>` already carries `margin-inline:auto` from session-J post-deploy fix. No new latent surfaces. **K14 lessons logged:** (i) **Investigation results compound across sessions** — K12 forensic narrowed candidates 5 → 2-3; K14 Experiment 1 (Defender exclusion sentinel-test) returned negative; K14 Experiment 2 (handle.exe trace) ruled out always-running daemon candidates by enumeration. Remaining candidate set: out-of-band Windows-side process, with `cowork-svc.exe` session-boundary teardown the leading suspicion (unprovable from handle.exe alone; Procmon would close). Negative results inform; investigation isn't free but scope-lockable per session. (ii) **handle.exe path-arg discipline** — when running with target path, expect filtered output (`<process>.exe pid: NNNN ... <matching handle>` or `No matching handles found`). The full system dump Josiah pasted means either no path arg was passed OR the path didn't substring-match any open handle. Either reading lands the same conclusion (no live process holds the lock), but next iteration: pre-flag the expected output shape so Josiah recognizes match-vs-no-match signal at-a-glance. (iii) **Discreet anchor-page pointer pattern locks** — K3 `.frame-pointer` aside (mono label + accent-bordered single-sentence body in /glossary/ header pointing at /frame/) established the precedent; K14 `.cover-preface-pointer` extends to homepage cover area pointing at /preface/. Anchor pages (frame, preface) get DISCREET pointers from related surfaces; destinations get grid cards. Pattern reinforces; no third anchor-page-pointer surface is currently scoped, but the formal rule holds for future additions. (iv) **Subagent delegation for >32K-token text manipulation** (K8 lesson re-validated for second time). 106 KB Status paragraph regex-split + chunk classification + dual-file write handled in one delegated turn (~16 subagent tool-uses internal, 1 main-context call). Pattern durable for CLAUDE.md size-watch trims; next trim threshold projected ~K20 if narrative growth holds K-session pace. (v) **Per-deliverable mid-session commit (K12 amendment) deferred this session** because the lock-investigation recovery cycle ran in parallel — committing pre-recovery wasn't possible (locked index), and the session's vessel-scope was light enough that two-commit close pattern remained safe. K12 amendment applies when (a) recovery state is stable mid-session AND (b) vessel scope is heavy enough that drift-loss risk warrants per-deliverable commits. K14 met neither; close-pattern returned. (vi) **Tool budget pre-flag held** — projected 30-40 / actual ~32 = within ceiling. Subagent saved direct-Edit cost on the trim; handle.exe output was pasted into context but didn't cost extra tool calls; AskUserQuestion-driven scope-lock prevented expansion. (vii) **Failed-commit staging persistence** — when a `git commit` errors mid-attempt (PS quoting bug, broken multi-`-m` invocation, etc.), prior `git add` staging does NOT roll back. Subsequent `git add` calls compound on the persisted index. First successful commit absorbs the union — which is how K14's single `4f84fa2` absorbed all three deliverables (cover pointer + trim + narrative) across what was originally planned as three separate commits. Discipline: if you want the eventual commit shape to differ from cumulative staged state, `git reset` explicitly before retry. Otherwise accept union-absorption (K14's net state was correct, just bundled differently than planned). (viii) **PowerShell multi-`-m` empty-middle quoting bug** — `git commit -m "subject" -m "" -m "body"` on PS 5.1 trips the parser on the empty middle argument (parsed as terminator, not pass-through). Result: silent commit failure or malformed message. **K15+ commit discipline:** single short `-m "summary"` per commit by default; for multi-paragraph bodies, PS here-string `-m @"..."@` or stdin (`git commit --file=-`). Avoids the empty-middle pattern entirely. Adds to the E3-era PS-discipline ledger (no `Stop` mode on native git stderr, ASCII-only commit messages, OneDrive null-byte watch on writes). **K15 carry-forwards (mention-only):** **Procmon trace investigation** if `cowork-svc.exe` candidate needs closure (heavier; Josiah opts-in — would require operator-side Sysinternals Procmon setup + capture filter on `.git\` directory writes during a session-close cycle; durable forensic evidence for the last-remaining candidate). Mementos: *Grey* verbatim text (separate book-Claude follow-up OR chat-side). Naming-divergence resolution (`/preface/` "structural pessimism" vs K5 umbrella SP→SA rename — Josiah's call; cross-Claude register pluralism per K4 lock preserves the divergence as feature by default). Mementos lede candidate authorization (K4 F4 *"A memento is a thing one keeps when one has lost the argument for keeping it."* — narrow K11 scope held; lede activation needs separate authorization). `.entry-alias` CSS promote-to-component (N=2 alias-note threshold; waits for second alias case). Awaiting library Exchange 5/6 reply on stable-tag timeline + 4-gate handoff status (non-blocking; unlocks F+ when closed). D2 flash card scaffolding still awaits content + tech decisions. /watch/ R2 thumb caching (low-priority). **K13 (2026-05-14 micro):** Book-Claude Exchange 4 Response intake (P2 `/preface/` draft + P3 Mementos: *Tucson* + *The desk* verbatim text). Diagnostic-first opening surfaced fresh `.git/index.lock` at K13 open (6th+ recurrence; mtime 2026-05-15 00:42 UTC = K12-close window — confirms K12 hypothesis that out-of-band Windows-side process is the durable recurrence source) + `git fsck` bad-sha1-signature + null-sha1 D+?? phantom entries + 3 K12-residual M files. K12 commits intact on `origin/main` (HEAD `b372655`); `core.autocrlf=input` in place. AskUserQuestion at session start locked 3 scopes on recommended path: (a) Josiah-side `.\recover-git.ps1` for git recovery (Cowork file work proceeds in parallel — file Edits don't touch `.git/index.lock`); (b) `/preface/` approve-as-is + deploy live state; (c) Tucson + Desk K13 (Grey deferred — verbatim text not in upload). Three workstreams shipped clean. (1) **`docs/book-claude-coordination.md` paste-relay** via `cp /uploads/ /docs/` — 384→586 lines, byte-exact match; restores K11-lost wuld-side Exchange 4 (book-Claude reconstructed from their side) + adds full book-Claude Response (`/preface/` ~215-word austere first-person draft with `/preface/` slug recommendation + naming-divergence flag, B.1 *Tucson* ~190 words, B.2 *The desk* ~150 words, B.3 closer-as-lede ouroboros for Tucson + mid-piece tidiness line for Desk, B.4 wave-deploy recommendation over single-batch). Git diff = 191 insertions / 0 deletions (pure append under autocrlf=input). (2) **`src/preface/index.html` shipped** (NEW, 6.6 KB / ~147 lines) — austere first-person anchor-page per K3 `/frame/` precedent. Body paste-relayed verbatim from book-Claude Section A. Structure: eyebrow `Anchor · Entry`, h1 `Preface`, `.preface-body` with 5 paragraphs, closing paragraph as `.preface-stamp` (mirrors K8 `/book/` `.book-terminal-stamp` hairline-top separator pattern). Naming-divergence preserved in draft (book-Claude rendered "structural pessimism" per book-canon, NOT "Structural Antinatalism" per K5 umbrella SP→SA rename — paragraph 2's position-cascade reads as drafted; HTML comment documents the divergence for future editorial pass). NOT promoted to homepage grid card per K3 anchor-vs-destination discipline. Discoverability decision deferred to K14+ (candidates: homepage cover area discreet pointer, `/book/` cross-link, footer slot, or no pointer + direct-URL only). Audience-doorway distinction holds per K4 cross-reply convergence lock: `/frame/` situates cold reader academically; `/preface/` locates reader in WULD's voice. (3) **`src/book/index.html` Mementos section rewrite** (74 insertions / 5 deletions). Stripped misleading session-K `Visual material from the book` placeholder — Mementos are PROSE pieces, not visual material (book canon section sequence: Grey → The desk → Beyond Good and Evil → Tucson → Forget the Plot → Correspondence → bare-line closer). New `.memento` CSS family added: h3 italic `.memento-title` + accent-italic `.memento-lede` + `.memento-body` paragraphs + `.memento-rule` hairline between siblings. Tucson + Desk articles inserted in book-canonical sequence order (Desk first within umbrella since Grey ships next when text lands). Tucson lede = closer-as-lede ouroboros per book-Claude B.3 (a); Desk lede = mid-piece tidiness line per B.3. DO-NOT-EXCERPT list still locked (Correspondence + Ch. IV + NothinGist + cat-line book-only by default; explicit-override-not-quiet-inclusion discipline per K4 lock). Section lede candidate from K4 F4 (*"A memento is a thing one keeps when one has lost the argument for keeping it."*) NOT deployed — K11 narrow-authorization scope (Tucson + Desk only); lede activation needs separate authorization. **Mid-session truncation false-alarm** — bash `wc -l` reported `/book/index.html` truncated to 413 lines after CSS Edit; Read via file-tool showed full 430-line state intact (K9 lesson restated: bash-vs-file-tool view divergence; bash view lags). Restored from HEAD via `git show HEAD:$f > $f` bypass once before realizing bash view was stale; re-applied Edits cleanly. Cost: ~5 calls. **Per-deliverable mid-session commits per K12 amendment:** 3 deliverable commits sequenced post-recovery (`0c40e73` coord-doc paste / `6f00633` /preface/ shell / `29f865f` /book/ Mementos) + push to `origin/main` + 4th docs commit at session close (this CLAUDE.md narrative + memory + MEMORY.md). `recover-git.ps1` at K13 open reported clean: lock cleared, fsck OK, null-sha1 cleared. **K12 residual M files (library-coord + black-box) ABSENT from K13 post-recovery `git status`** — either Josiah-side hygiene cleared them between K12 and K13 OR `git read-tree HEAD` + `autocrlf=input` absorbed them. Either way, K13 working tree was exactly the three intended deliverables. **Tool budget retrospective:** Projected ~55-65 calls at session start; actual ~60 by close. Within projection; bash-stale-view false-alarm consumed ~5-6 calls of restore-and-replay before file-tool Read confirmed intact. **`<p>` margin-inline:auto audit closed clean** — `.memento-lede` + `.memento-body p` sit in left-aligned `.memento` containers; `.preface-body p` + `.preface-stamp` sit in left-aligned `.preface-body`; no `text-align:center` parent for any new `<p>`. **K13 lessons logged:** (i) **Lock recurrence between sessions is now an established pattern** — K12 ran recover-git.ps1, K13 needed it again at session open. Recurrence is real; recovery cycle is durable enough to ship a K-session cleanly when scope is locked early + commits sequence fast. (ii) **`autocrlf=input` + `git read-tree HEAD` resolves CRLF M files transparently** — K12 residual M files didn't appear in K13 recovery `git status`; cleanest path = let `recover-git.ps1`'s normal cycle resolve them without explicit `git checkout --` micro-actions. (iii) **File-tool path durable across same-session bash mount staleness** (K9 lesson re-validated) — when bash reports inconsistency with what was just authored, Read via file-tool FIRST before pivoting to restore-from-HEAD. (iv) **AskUserQuestion-driven 3-question scope-lock with all recommendations selected = fast K-session shape** (K10/K12 lesson restated). Hand-off blocker + scope-locks both surfaced in one AskUserQuestion at session open; no surprise scope changes mid-execution. (v) **Cross-Claude naming divergence at canonical-author boundary is acceptable when both sides operate under their own canonical naming** — book-Claude's `/preface/` uses "structural pessimism" (book-canon); umbrella canon per K5 is "Structural Antinatalism." Ship-as-drafted preserves book-canon register continuity. HTML comment flags inconsistency for future editorial pass. Overriding book-Claude's authorial judgment Cowork-side would be inappropriate; cross-Claude register pluralism per K4 lock holds. **K14 carry-forwards (mention-only):** `/preface/` discoverability (homepage / `/book/` discreet pointer / footer slot — Josiah's call); Mementos: *Grey* verbatim text (separate book-Claude follow-up OR chat-side); naming-divergence resolution if internal consistency with `/frame/` becomes load-bearing; Mementos lede candidate authorization (K4 F4 line); awaiting library Exchange 5 reply on stable-tag timeline + 4-gate handoff (non-blocking; unlocks F+ when closed); `.entry-alias` CSS promote-to-component on N=2 case; D2 flash card scaffolding still open. **CLAUDE.md size watch:** ~170 KB post-K13 narrative; approaching K8-style trim threshold but not Read-blocking. K14 trim micro-candidate. **K12 (2026-05-14 micro, day after K10/K11):** Cowork-internal forensics — live `.git/index.lock` inspection + root-cause narrowing per screenshot item (6). AskUserQuestion at session start locked scope: pivot from dry repo-audit to live-lock forensics + recovery (screenshot item (7) trim deferred to K13 per recommendation). Diagnostic-first opening surfaced critical state: (a) `.git/index.lock` IS PRESENT (0-byte, mtime 2026-05-15 00:42 UTC ≈ K11 close window, 6th-recurrence sentinel); (b) `git fsck` reports "bad index file sha1 signature / fatal: index file corrupt" — binary-level corruption deeper than K10's null-sha1 D+?? pattern; (c) 9 null-sha1 phantom entries in index (K10 carry-forward unresolved); (d) working-tree drift on CLAUDE.md (-25 lines), library-coord-doc (CRLF flip carry-forward), black-box (5751 vs HEAD 2383 bytes — pre-K8 state somehow restored). Five workstreams shipped. (1) **Forensic capture closes (6) candidate set.** Repo-side root-cause ruled OUT (no hooks, no `.gitattributes`, vanilla Windows-defaults `.git/config` — `filemode=false`, `ignorecase=true`, `symlinks=false`, no autocommit/autopack/LFS/smudge filter). In-sandbox concurrent-process ruled OUT (PID forensic: only sandbox bash + transient subshells in `/proc`, no persistent git process touching repo). Lock-churn ruled OUT (mtime stable across 1s probe; earlier ls-vs-xxd flake was sandbox-mount cache desync, not real create+delete cycle). Interrupted-PS-recovery theory WEAKENED — reflog shows `1d58bc8 HEAD@{0}: reset: moving to HEAD` confirming K11's `git reset HEAD` ran to completion (reflog entry only writes on successful operations; an interrupted reset would not appear). Strongest remaining candidates narrowed to: (i) out-of-band Windows-side process (Defender/AV scan + re-lock cycle, IDE git integration like VS Code, file watcher); (ii) Anthropic sandbox session-boundary hook touching `.git/index*` post-PS-completion (no evidence visible, speculative); (iii) some Windows-side automation not yet enumerated. **Sandbox path BLOCKED for clearance** — `rm -fv .git/index.lock` returns "Operation not permitted" (Windows file lock blocks Linux unlink); `git read-tree HEAD` fails with "Unable to create '.git/index.lock': File exists / Another git process seems to be running". Sandbox forensic capabilities exhausted; the root-cause question moves out of Cowork-side investigation surface to Windows-side observability (Sysinternals Handle / Process Explorer / Defender exclusion test). (2) **Conclusion for screenshot item (6): PS recovery stays the durable pattern.** Repo-side and sandbox-side root-causes both eliminated. Out-of-band Windows-side mechanism not isolable from Cowork-side. The Windows-side recovery PROCESS can be hardened — idempotent, atomic (no partial state if interrupted), transactional (verify each step before next), interruption-survivable. **`recover-git.ps1` shipped at repo root** alongside the E3-era `setup-git.ps1` — handles all known recovery cases: (a) clear `.git/index.lock` via PS `Remove-Item -Force` (Windows-owned process unlinks own lock cleanly), (b) verify lock cleared via `Test-Path`, (c) detect index corruption via `git fsck --no-reflogs`, (d) if corrupt, run `git read-tree HEAD` (lower-level than `git reset HEAD` — rebuilds index from HEAD tree without disturbing working tree), (e) verify post-rebuild integrity via fsck + `git status --short`, (f) emit diagnostic if any step fails. Idempotent: re-running is safe (each step short-circuits if state already correct). Interruption-survivable: if PS terminal closes mid-operation, the partial-state is detectable by the next run (lock check + fsck check + null-sha1 entry count). (3) **Drifted-tracked-file restore from HEAD via `git show HEAD:$f > $f` bypass.** This sandbox-side write does NOT touch git index (no lock interaction). Three files restored: CLAUDE.md (296 lines, K10-narrative-complete state), `docs/library-claude-coordination.md` (LF line endings restored — K8.5 CRLF flip carry-forward closed), `src/glossary/black-box-of-inaccessibility/index.html` (2383 bytes, K8 redirect-stub state restored). Pattern locks for K9+K10+K12 drift-restore protocol — durable across Windows-mount staleness, works for text + binary alike (git's blob canonicalization handles both). (4) **K11 working-tree state LOST.** Reflog confirms no K11 commit ever happened (most recent reflog: K10 docs `1d58bc8` then successful reset). K11's `docs/book-claude-coordination.md` Exchange 4 append (P2 `/preface/` follow-up + P3 Mementos: Tucson + Desk authorization + verbatim text request) is gone — drift consumed the working-tree changes between K11 close and K12 open. Carry-forward for K13: reconstruct Exchange 4 from K11's intent (text not preserved Cowork-side) OR await Josiah chat-side reconstruction. Cowork CAN scaffold the Exchange 4 frame (Section A/B/C/D headers) but the content (P2 reply + P3 authorization paragraph) is content-shaped per the Cowork-builds-vessels boundary. (5) **K12 docs:** this CLAUDE.md narrative + memory file fresh-write + MEMORY.md hook update + arc table row. **Tool budget retrospective:** Projected ~30-40 calls at session start; actual ~30 by close (under projection). Diagnostic-first opening saved ~5 calls (caught drift state in first batch alongside lock state). AskUserQuestion-driven scope-lock prevented ~10 calls of half-executing dry audit before pivoting. **`<p>` margin-inline:auto audit:** N/A this turn (no vessel work; forensics + recovery + docs only). **K12 lessons logged:** (i) **Live-lock forensic surface is higher signal than dry root-cause audit.** Lock present at session-open is rare — usually session-boundary recovery clears it. K12 caught lock in-state + ran sandbox-side clearance attempt + read index integrity via fsck. Forensic narrowed candidate set from 5 to 2-3 hypotheses in one session. (ii) **Sandbox cannot rm Windows-locked files** — confirmed across K7+K8+K9+K10+K12 (six+ recurrences). Out-of-band Windows-side recovery is the durable pattern; the only Cowork-side lever is hardening the recovery script. (iii) **`git show HEAD:$f > $f` bypass is the durable drift-restore pattern** — locked across K9+K10+K12. Works for text + binary; sandbox file-write doesn't interact with `.git/index.lock`, so works regardless of lock state. (iv) **Reflog as forensic surface** — reflog confirmed K11 reset succeeded, ruling out interrupted-recovery theory. Future sessions: read reflog early in forensic to constrain root-cause theories. (v) **K11 working-tree drift consumed uncommitted content-shaped work** — book-coord Exchange 4 append lost. **DURABLE LESSON: commit working-tree changes IMMEDIATELY after authoring sandbox-side; do NOT defer all commits to end-of-session.** The drift pattern makes deferred commits unsafe when the lock + corruption can recur post-recovery. Per-deliverable mid-session commit (when lock state permits) is the safer interleave; two-commit close pattern continues to hold for clean sessions but should NOT be relied on as the sole safety net. (vi) **CLAUDE.md size watch:** ~145K / ~300 lines post-K12 narrative add (~3-5 KB added this turn). K8 trim removed K-K3 narratives to `CLAUDE-history.md`; next trim threshold approaching but not blocking — K13 evaluation if size becomes Read-flaky. **K13 carry-forwards:** (a) Josiah-side run `recover-git.ps1` to clear lock + rebuild index + commit K12 deliverables (CLAUDE.md K12 block + memory + MEMORY.md + recover-git.ps1 itself); (b) reconstruct K11 book-coord Exchange 4 append (P2 `/preface/` follow-up + P3 Mementos: Tucson + Desk authorization + verbatim text request) — chat-side authorship OR new round-trip from book-Claude; (c) library coord-doc CRLF prevention investigation — what introduces CRLF (git autocrlf? editor save? Windows clipboard paste?) — non-blocking, just hygiene; (d) `.entry-alias` CSS promote-to-component still awaits second alias case (no actionable threshold); (e) trim micro deferred per K12 scope-lock; can ship as K13 filler if forensic surface is quiet; (f) library audit close + stable tag v3.7.0+ unlocks F+ — still non-blocking, awaiting library-side declaration. **K17 (2026-05-15 micro):** Library Exchange 5/6 paste-relay + Procmon parser script for Josiah-side trace analysis + `.gitignore` hygiene (block 15.5 GB .pml) + K17 lessons + close. Diagnostic-first opening surfaced **clean state** for the first time across the K-recurrence pattern: K16 HEAD `3da8b4a` intact, **NO `.git/index.lock` recurrence at K17 open** (first clean session-open across 11+ consecutive K-recurrence sessions), **`.git/config` line 17 INTACT** (`\tmerge = refs/heads/main\n` byte-clean per xxd), zero working-tree drift, fsck shows only harmless dangling E3-era pre-amend commit `6bc89305` (the GH007 email-privacy commit rewritten via `git commit --amend --reset-author --no-edit`). **Recurrence pattern PAUSED, not closed** — K16 close→K17 open window did NOT trigger the lock + truncation recurrence; root cause still unproven (Procmon trace required for cowork-svc.exe candidate closure). **Procmon trace blocker:** Josiah's `docs/Procmon-K16-close.PML` capture is **15.5 GB** (16,619,951,034 bytes — Procmon ran unbounded post-K16-close, not just the 5-10 min window). Cowork bash sandbox cannot parse .pml binary format AND the file size makes direct analysis impractical regardless. AskUserQuestion at session start locked Python-script analysis path: Cowork authors streaming parser, Josiah runs offline, uploads filtered CSV for K18 analysis. Scope: (b)-modified — library intake + Procmon script tonight, F+ vessel work + Exchange 7 ratification deferred to K18 (Josiah's "no longer night" energy signal + library publication artifacts shifted scope). Four workstreams shipped. (1) **`.gitignore` hygiene** — `*.pml` + `*.PML` + `docs/Procmon-*.pml/PML` patterns added with reason comment (multi-GB binary, analyzed Josiah-side via procmon-parser, never ship raw .pml to git). Blocks the 15.5 GB capture from accidental staging. (2) **`docs/library-claude-coordination.md` paste-relay via `cp`** — first upload `library-claude-coordination.md` was the wrong file (847 lines, md5 `9d166100`, byte-identical to current on-disk state — no growth); corrected upload `library-claude-coordination-d7466023.md` shipped this turn (937 lines, md5 `5d798388`, +90 lines = Exchange 6 append). Byte-exact match verified post-cp; pure-append diff (96-line diff buffer = 90 inserts + section separators). **Upload version-collision caught via md5+wc-l discipline** — without the pre-cp md5 diff the paste-relay would have been a no-op + I'd have proceeded to triage an Exchange that wasn't there. (3) **Exchange 6 triage = LIGHT-with-UNLOCK.** Library-Claude's response to wuld.ink-side's stable-tag timeline question (forced question (a)+(b) from Exchange 5): **(a) 4-gate handoff resolution** — gate (i) coverage milestone EXCEEDED (72/78 = 92.31% distinct objections touched post-sigma-m3-merge; 91% threshold cleared by 1.3 percentage points); gate (ii) Q97 STILL OPEN but explicitly non-blocking (`responses.long` substantive-extension path mobilizes the argument without 14th-premise promotion which would trigger Q102 back-amendment cascade; ship-with-known-limitation-footnote acceptable), Q98 NOT in canon v23.1 (resolved/absorbed in c6→c7→c8 cascade); gate (iii) Q106 mech-inventory gap (Counterexample Fallacy + Surface-Reading) OPEN_LONG_TERM_NOT_BLOCKING per canon status (operator-deferred long-term, ships with documented limitation); gate (iv) six wuld.ink ops questions = library-side has no input, wuld.ink-side prerogative. **All 4 gates resolved as non-blocking for stable-tag purposes.** **(b) Stable-tag declaration timeline-of-record: 1-3 library sessions, ~3-7 calendar days.** Path: PRIMARY = `canon-compaction` (optional hygiene, ~40% canon size reduction via arc subblock collapse, does NOT gate stable-tag); ALTERNATIVE_1 = `v3_7_cut` (1-2 sessions, direct release-cut against existing `v3_7_cut_invariants.json` spec, **produces stable-tag declaration**); ALTERNATIVE_2 = `residue-invariant-refresh` (low-priority hygiene, +5 arithmetic gap between `dep_graph_data_link_count_v361=245` and `strong(159)+weak(81)=240`, bundleable, does NOT block). Library-Claude's recommendation: skip compaction OR run dedicated, proceed to `v3_7_cut`; both paths converge within a week. Stable-tag declaration header form locked in Exchange 5 C.1 (`## Exchange N — [date] — library-Claude stable-tag declaration`) lands post-cut in future exchange. **F+ editorial extraction UNBLOCKED for drafting against current corpus state** per library-Claude clarification: corpus structurally frozen post-canon-state-refresh + post-surface-parity-arc (78-objection inventory stable; no further canon-state-refresh + no further cascade arcs expected; `v3_7_cut` formalizes release cut against current state without adding objections); the three F+ editorial pieces (coda standalone, VAR + why-not-suicide canonical long-forms, static Map 1 frame on "about the library") all drawable from currently-stable corpus content; drift between draft-time and stable-tag declaration ≤ chrome-level (typography binding + link URLs gain durability + zero content semantic shift). **Subdomain provisioning STILL gated on the formal stable-tag declaration header** — wuld.ink-side does NOT spin up `library.wuld.ink` until that lands; best-estimate ~1 week. **Optional interim affordance:** "library subdomain forthcoming" affordance on umbrella (nav as labeled-but-disabled entry, or footer roadmap link) signals incoming without committing to date; purely wuld.ink-side editorial call (Josiah's K18 call). Library-Claude also closed a non-forced clarification: K6 Cluster-1 + Cluster-2 disposition lock under coined-rendering reading fully agreed (structural-role-assignment argument is the right framing for `alogical + isness` / `Illogicaliter est` as substantive philosophical-move commitments rather than orthography); JSX sibling denormalizes `realWorldExamples` as of v23.0 — non-blocking note since Appears-in cross-links target objections not RWE instances. (4) **`scripts/analyze_procmon.py` shipped (~140 lines).** Python streaming parser via `procmon-parser` PyPI package (Microsoft engineer EliSohl's tool). Streams .pml events without loading 15.5 GB into RAM; default filter `.git\` path substring (case-insensitive) + write/create/close ops (WriteFile / CreateFile / SetEndOfFileInformationFile / CloseFile / SetRenameInformationFile); optional `--window-start/--window-end` timestamp narrowing; CSV output with timestamp + process_name + pid + operation + path + result + detail. Handles malformed events by skipping with stderr warning; writes CSV incrementally (safe to Ctrl-C; partial output remains valid CSV up to last flushed row). Header comment documents install (`pip install procmon-parser`) + offline-install path (`pip download` to wheels dir) + expected output sizing (kilobytes not megabytes — millions of events filtered to few hundred matches). Josiah-side workflow: `pip install procmon-parser` → `python scripts\analyze_procmon.py docs\Procmon-K16-close.PML --out k17-trace.csv` → upload CSV for K18 analysis. Confirms or refutes `cowork-svc.exe` candidate from K14 handle.exe forensic. **Library publication artifacts (README + orientation v3.7 + LICENSE + LICENSE-CODE) inventoried but DEFERRED** to their own future session per Josiah's note — these are library-GitHub-repo-side artifacts, NOT wuld.ink integration material; can ship in dedicated coordination session OR bundled with library stable-tag declaration cut. **Tool budget retrospective:** Projected ~25-30 calls at session start (LIGHT-MEDIUM); actual ~25-30 by close. Diagnostic-first opening surfaced full state in 1 bash call; AskUserQuestion-driven scope-lock prevented expansion into HEAVY F+ scaffolding tonight. **`<p>` margin-inline:auto audit:** N/A this turn (no vessel work; library intake + procmon script + .gitignore + docs only). **K17 lessons logged:** (i) **Clean state at K17 open is data, not noise.** The recurrence pattern paused without operator-side intervention between K16 close and K17 open. Possible explanations: (a) the recurrence is K-close-window-correlated AND the K16→K17 gap was longer than the K-close-window (Josiah was away/asleep, no K16-close-immediate-followup; trigger condition genuinely transient), (b) the recurrence is bounded by some Windows-side state that K16's `recover-git.ps1` Step 1 + Josiah's manual `.git/config` repair de-perturbed (config integrity restored before the corruption vector re-fired), (c) cowork-svc.exe stopped/restarted between sessions on its own. Procmon CSV analysis (K18+) is now the durable closing path. Do NOT conclude pattern is closed without trace evidence. (ii) **Upload version-collision detection via md5 is mandatory discipline.** First upload was the same byte content as on-disk (same md5, same line count). Without md5 check the paste-relay would have been a no-op. **Discipline locks: ALWAYS md5+wc-l diff upload vs on-disk before paste-relay.** Adds to coord-doc relay-file discipline ledger alongside K4-OneDrive-null-byte-watch + K13-bash-stale-view-fallback. (iii) **Multi-file uploads compound scope.** K17 opened with 6 upload artifacts (Procmon 15.5GB + 2 library-coord + README + LICENSEs + orientation). Pre-flag at session-open: when uploads/ contains multiple unrelated artifacts, AskUserQuestion-lock which subset ships THIS session before authoring; defer remainder explicitly. K17 split correctly via session-start question; future multi-artifact sessions follow same pattern. (iv) **Python procmon-parser is the durable forensic path** for the .pml binary-format barrier. Streaming reader handles arbitrarily-large captures; CSV output makes Cowork-side ingestion trivial. Pattern locks for future Procmon-required forensic surfaces. (v) **F+ unblock signal landed.** Library-Claude's "F+ editorial extraction CAN BEGIN NOW against current stable corpus" is the explicit go-signal. K18+ can scaffold coda standalone + VAR + why-not-suicide long-form shells + static Map 1 frame WITHOUT waiting for stable-tag declaration; only `library.wuld.ink` subdomain provisioning + durable-URL cross-links gain final form post-declaration. Per Cowork-builds-vessels discipline: vessel scaffolding (page shells, CSS, cross-link affordances) is Cowork-side; content (essay prose, glossary entries, coda body) is chat-side. K18+ ships vessels, awaits content authorship. (vi) **Recommendation-first response shape held under user preference** — opened with the recommendation before the AskUserQuestion + before diagnostic batch; user preference for recommendations-up-front was honored throughout the turn. **K18 carry-forwards:** (a) Josiah-side `pip install procmon-parser` + run `analyze_procmon.py` → upload `k17-trace.csv` for K18 trace analysis (closes the cowork-svc.exe candidate question definitively). (b) Library Exchange 7 ratification round (acknowledge Exchange 6 + lock F+ scaffolding plan + optional interim subdomain-forthcoming affordance decision); ships as wuld.ink-side response in `docs/library-claude-coordination.md` append. (c) F+ first-slice vessel scaffolds: `src/coda/index.html` shell + `src/objections/violence-as-reductio/index.html` shell + `src/objections/why-not-suicide/index.html` shell + `src/library-about/index.html` (Map 1 frame surface) — pattern-matched to existing essay shells (inheriting `/components/essay.css`); cross-link discipline = relative paths for now, swap to `library.wuld.ink/objections/<slug>/` durable form post-declaration. (d) "Library subdomain forthcoming" optional affordance decision (Josiah's editorial call). (e) Library project GitHub publication coordination (README + orientation v3.7 + LICENSE + LICENSE-CODE) — separate workstream from wuld.ink integration; ships in dedicated session OR bundled with library stable-tag declaration. (f) Procmon trace post-analysis: if `cowork-svc.exe` confirmed as truncation/lock vector → close investigation, log root-cause in CLAUDE.md; if NOT confirmed → enumerate remaining candidates from CSV. (g) Carry-forwards inherited from K16: Mementos: *Grey* verbatim text (book-Claude follow-up OR chat-side); naming-divergence resolution (`/preface/` "structural pessimism" vs K5 SP→SA — Josiah's call); Mementos lede candidate authorization; `.entry-alias` CSS promote-to-component (N=2 alias-note threshold); D2 flash card scaffolding (still open content + tech decisions). (h) Recurrence pattern observability: if clean K17→K18 gap holds (no lock/truncation recurrence at K18 open), recurrence may be self-limiting OR Procmon-trace-time-bounded; flag separately if state persists. **K18 (2026-05-15 micro):** Library Exchange 7 ratification + Procmon CSV intake deferred to K19 + recurrence pattern RESUMPTION logged + subdomain affordance lock. Diagnostic-first opening surfaced **recurrence RESUMED** at K18 open (12th+ `.git/index.lock` recurrence, 0-byte sentinel + `git fsck` "bad index file sha1 signature / fatal: index file corrupt"). K17 clean open was a data point, NOT pattern-closure. Drift: CLAUDE.md (191L vs HEAD 220L; ~30L consumed) + **`.gitignore` reverted on-disk** (K17 commit `ec17a79`'s `*.PML` + `docs/Procmon-*.PML` block GONE from working tree, HEAD intact — first observed instance of drift rolling back a recent-commit's on-disk file state, broader surface than .git-internal-only corruption). **`.git/config` line 17 INTACT** byte-clean `\tmerge = refs/heads/main` — K16 Step-1 + Josiah's manual repair held; truncation pattern did NOT recur this gap. K17 commits intact on `origin/main` (`ee07776` + `ec17a79`). Uploads/ empty (no CSV from analyze_procmon.py yet); 4 library publication artifacts (LICENSE / LICENSE-CODE / README.md / cowork_orientation_v3_7_publication.md) landed Josiah-side untracked in `docs/` per K17 deferred-to-dedicated-session lock — not K18 work. **Screenshot evidence from Josiah's Procmon GUI capture** (preliminary visual on 5,814/54M events, 0.01%): `vmwp.exe` PID 15688 (Hyper-V VM worker) writing to multiple `C:\Users\y_m_a\Projects\wuld-ink\.git\` paths via CloseFile + QuerySatLxInfo + ReadFile + SetEndOfFileInformationFile operations. **Investigation finding SHIFTS** from cowork-svc.exe leading-suspect (K14 handle.exe forensic) -> **vmwp.exe most likely vector pending CSV confirmation.** Per Josiah chat-side reframing: "cowork-svc.exe is probably the orchestration service that LAUNCHES the Cowork VM, not the process doing I/O on `.git\`" — consistent with K14's handle.exe finding that cowork-svc.exe couldn't be enumerated by even elevated handle.exe (privileged service handles invisible to handle.exe, BUT enumerable by Procmon kernel-mode trace + vmwp.exe is the user-mode VM worker that DOES touch the mounted folder's filesystem). Two PML captures now in `docs/` (.gitignore-blocked correctly): `Procmon-K16-close.PML` (15.5 GB, K16-close window only) + `Procmon-K17-opening.PML` (size TBD, covers FULL K17 lifecycle = open + session work + close — the higher-signal artifact, correlates against every Cowork bash call mtime AND the K17-close commit-push sequence AND the K17->K18 recurrence-resumption moment). AskUserQuestion at session start locked track (b)-modified + CSV state "No, just tell me what to do" + subdomain affordance "Yes — footer roadmap link." Three workstreams shipped. (1) **Drift restore via `git show HEAD:$f > $f` bypass** — CLAUDE.md (191L->220L) + `.gitignore` (K17 `*.PML` block restored from HEAD). 6th+ time across K9/K10/K12/K13/K15/K16/K18; pattern locks even harder. **NEW: drift surface area is broader than scoped** — extends to recent-commits' on-disk state, not just .git-internal config/index. Drift-scan discipline at session open should iterate ALL `M` files in `git status --short`, not target only CLAUDE.md. (2) **Library Exchange 7 appended to `docs/library-claude-coordination.md`** (~3 KB, single Edit). Sections: 4-gate ratification (all four resolved non-blocking — coverage 92.31% EXCEEDED + Q97 ship-with-footnote + Q98 moot + Q106 LONG-TERM + ops-questions wuld-side prerogative); timeline acceptance (1-3 sessions / ~3-7 days / `v3_7_cut` path; subdomain provisioning trigger locked to stable-tag declaration header arrival); **F+ commencement signal (K19 first slice: 4 vessel shells — coda + VAR + why-not-suicide + library-about/Map 1; content stays chat-side per Cowork-builds-vessels; cross-link discipline = relative paths now -> durable canonical URL form post-declaration; drift between F+ ship-time and stable-tag = chrome-level only per Exchange 6 lock)**; subdomain affordance lock (footer roadmap line `library.wuld.ink — forthcoming` in homepage footer disclaimer-pending area; removal trigger = stable-tag declaration header arrival); Cluster-1/Cluster-2 disposition ack (K6 coined-rendering reading agreed; structural-role-assignment framing locks for future Latin-idiom coinings); JSX `realWorldExamples` denormalization flagged + filed (no current downstream impact; future F+ pieces citing specific RWE instance_ids gain JSX as valid source path alongside corpus); no forced question back; round closes. (3) **Josiah-side `analyze_procmon.py` run instructions emitted at session close** for BOTH PMLs: install procmon-parser, run script on `docs/Procmon-K17-opening.PML` (priority — full lifecycle = higher signal) -> `k17-trace.csv`, then on `docs/Procmon-K16-close.PML` -> `k16-trace.csv`, upload BOTH to K19 prologue. K19 ingests + filters by process_name + correlates vmwp.exe writes against `.git\index.lock` + `.git\config` paths near K16-close + K17-close + K17->K18 idle-window timestamps -> confirms/refutes vmwp.exe candidate -> updates investigation status (root cause CONFIRMED, candidate enumerated, or further capture needed). **Tool budget retrospective:** Projected ~40-60 calls (b-modified MEDIUM); actual ~30-35 by close (came in well under projection). Diagnostic-first opening surfaced full state in 2 bash calls; AskUserQuestion at session open locked 2 scopes simultaneously (track + subdomain affordance), saving ~3-5 calls of mid-execution interrupt. **`<p>` margin-inline:auto audit:** N/A this turn (no vessel work; coord-doc append + docs + diagnostic only). **K18 lessons logged:** (i) **Recurrence RESUMED at K18 open** — K17's clean open was a single data point, NOT pattern-closure across 11+ recurrence sessions. K17 lesson (i) ratified: "do NOT conclude pattern is closed without trace evidence." Closing path remains filtered-CSV proof via analyze_procmon.py. (ii) **Drift extends to recent-commits' on-disk file state, not just .git internals.** K17 commit `ec17a79`'s `.gitignore` `*.PML` block reverted on-disk between K17-close and K18-open; HEAD intact, on-disk reverted via `git show HEAD:.gitignore > .gitignore` bypass. Same vector that hits `.git/config` line 17 + `.git/index.lock` recurrence + CLAUDE.md content drift. Drift-scan discipline broadens accordingly. (iii) **Screenshot evidence from Procmon GUI capture SHIFTS candidate but does NOT CONFIRM.** Preliminary visual (5,814/54M events = 0.01%) shows vmwp.exe writing to `.git\` paths — but the GUI doesn't filter by operation type (mixes reads with writes), doesn't time-correlate against the specific corruption moments, and doesn't confirm what specifically writes to `.git\index.lock` + `.git\config` line 17. CSV filter via `analyze_procmon.py --path-substr .git` narrows to path-matching events; subsequent timestamp correlation against K-close windows confirms or refutes. Investigation reframe is provisional pending K19 trace analysis. (iv) **Two PML traces > one — K17-opening (full lifecycle) is higher-signal than K16-close (single teardown window).** K17-opening correlates against: every Cowork bash call mtime (rules out in-VM bash as the writer), K17-close commit-push sequence (captures git writes during normal commits), K17->K18 idle window (captures whatever process re-corrupts `.gitignore` + drops `.git/index.lock` post-close). K19 should prioritize K17-opening if only one CSV ships first. (v) **F+ unblock signal travels two ways** — library-Claude's Exchange 6 "F+ can begin now" + wuld.ink-Cowork's Exchange 7 commencement signal. Library-side now knows F+ vessel work is starting K19; cross-link discipline holds (relative paths now -> durable form post-declaration); drift-tracking discipline per Exchange 3 holds across the F+ ship-time window. (vi) **Recommendation-first response shape held under user preference** — K18 opened with recommendation before AskUserQuestion before diagnostic batch; per-turn discipline maintained. (vii) **Multi-file uploads compound scope — K17 lesson (iii) ratified.** Library publication artifacts (4 files in `docs/`) landed untracked between K17 close and K18 open; per AskUserQuestion-locked scope-split, K18 stuck to library Exchange 7 + Procmon intake handoff; publication coordination DEFERRED to dedicated future session as K17 locked. **K19 carry-forwards:** (a) Josiah-side runs `analyze_procmon.py` on **both** PMLs (K17-opening prioritized for higher signal; install procmon-parser via `pip install procmon-parser`; run `python scripts\analyze_procmon.py docs\Procmon-K17-opening.PML --out k17-trace.csv` then same for K16); upload BOTH CSVs to K19 prologue for filtered analysis. (b) K19 F+ first-slice vessel scaffolds: `src/coda/index.html` + `src/objections/violence-as-reductio/index.html` + `src/objections/why-not-suicide/index.html` + `src/library-about/index.html` (Map 1 frame surface — final URL slug Josiah-editorial-call at scaffold time, candidates `/library-about/` adjacent to `/argument-library/` OR `/about-the-library/`). Pattern-match essay shell; inherit `/components/essay.css`; cross-link discipline = relative paths to `/argument-library/` shell now, swap to `library.wuld.ink/objections/<slug>/` durable form post-stable-tag declaration. Content stays chat-side per Cowork-builds-vessels. (c) K19 ships **footer `library.wuld.ink — forthcoming` roadmap line** in homepage footer disclaimer-pending area (alongside existing "Legal & personal disclaimers — forthcoming" dim-text pointer). Removal trigger = stable-tag declaration header arrival. (d) Library project GitHub publication coordination (README + orientation v3.7 + LICENSE + LICENSE-CODE) — dedicated session OR bundled with library stable-tag declaration cut; non-blocking. (e) **Recover-git.ps1 at K18 close (Josiah-side)** — `.git/index.lock` present + index corrupt; run before any K18 commit attempt. (f) Carry-forwards inherited from K17: Mementos: *Grey* verbatim text (book-Claude follow-up OR chat-side); naming-divergence resolution (`/preface/` "structural pessimism" vs K5 SP->SA — Josiah's call); Mementos lede candidate authorization (K4 F4 line); `.entry-alias` CSS promote-to-component (N=2 alias-note threshold; waits for second alias case); D2 flash card scaffolding (still open content + tech decisions); `/watch/` R2 thumb caching (low-priority). (g) **Recurrence pattern observability**: K18 = recurrence RESUMED data point; K19+ tracks whether K18->K19 gap also recurs. CSV analysis is the durable closing path. If vmwp.exe confirmed -> log root cause; recover-git.ps1 remains the operational pattern (vmwp.exe is the Hyper-V VM worker, privileged + necessary, not mitigable Cowork-side). **K19 (2026-05-15 micro):** Investigation surface CLOSED + Grey + lede + footer + Exchange 10 + bug filing scaffolded + OneDrive/Projects mount divergence discovered. Diagnostic-first opening surfaced CLEAN state at K19 open (NO `.git/index.lock` recurrence; `.git/config` line 17 byte-clean; K18 commits intact: e5d3b66 + cde4d1f + 33ccabf). Mid-session a single `git diff` triggered an intra-session lock recurrence (0-byte at 22:28 UTC) — confirms vmwp.exe-class mechanism can fire on git operations within a session, not just at session boundaries. Two M files at K19 open were FORWARD drift (uncommitted Josiah-side post-K18 work), NOT vmwp.exe revert pattern: `docs/library-claude-coordination.md` (+143 lines = library Exchange 8 paste-relay) + `scripts/analyze_procmon.py` (+60 lines = K18 enhancement). **Experiment 7 (K19 trace analysis via screenshot evidence + on-disk file delta inspection):** K18-opening-and-closing.PML tight-filter trace (2,322 matched events on `wuld-ink\.git` substring) caught `vmwp.exe` (PID 15688, Hyper-V VM worker hosting Cowork sandbox) executing the K17→K18 `.gitignore` reversion in real time at 20:11:12 on 2026-05-15: SetEndOfFileInformationFile (truncate) immediately followed by WriteFile (765 bytes at offset 0, the pre-K17 .gitignore content), then System (PID 4) paging-I/O flush propagating to disk. **Drift vector CONFIRMED: vmwp.exe writes VM-internal-cached file contents back to host filesystem on VM startup, overwriting host-side changes made between sessions.** `.git/index.lock` recurrence and `.git/config` line 17 truncation almost certainly the same vector at VM teardown (K-close), but trace data lost in procmon-parser's ~9.66M-event corruption ceiling. K20+ bounded-Procmon experiment (ring-buffer 500 MB, narrow time window around a single K-close) can confirm the teardown half of the mechanism for completeness. **recover-git.ps1 remains operational pattern; vmwp.exe is privileged + necessary** (the Windows-side Hyper-V worker hosting the Cowork sandbox), not mitigable Cowork-side. **MAJOR mid-session discovery — TWO repository mounts:** Bash uses `C:\Users\y_m_a\Projects\wuld-ink\` (canonical repo, K18 HEAD, full `.git/`); file-tool was reading `C:\Users\y_m_a\OneDrive\Documents\Claude\Projects\00_project_context.md\wuld-ink\` which is a frozen pre-K13 snapshot (368 lines on /book/, May 14 05:58, bare `.git/HEAD` only). System prompt loaded BOTH `CLAUDE.md` files — OneDrive's is K3-era, Projects' is K18-era. Pivoted all file-tool operations to Projects path mid-session; OneDrive copy stays untouched (Josiah-side delete blocked by sandbox file-handle lock; deletes cleanly once K19 session closes per standard Windows behavior). Likely explains some prior K-session "drift" patterns where work "didn't land" — file-tool writes targeted at OneDrive paths never reached the real repo. Two-mount divergence is its own vector independent of vmwp.exe. **Workstreams shipped:** (1) **library-Claude Exchange 8 / 9 surface discovery from drift M file**: Exchange 8 IS the formal stable-tag declaration with exact header form Exchange 5 C.1 + Exchange 7 specified — substrate FROZEN at v3.7 with 6 canonical artifacts md5-locked (corpus JSON, JSX, index HTML, coda HTML, RWE schema, validator); `project_terminal_state = "archived_v3_7_stable"`; `next_recommended_session = null`; subdomain-provisioning trigger RELEASED; material-shift flag mechanism INERT; durable URL forms = `library.wuld.ink/#<objection-id>` hash-fragment (NOT path form — library kept SPA-permanence lock); filename rename option (b) recommended; two-deploy-surface coordination Q forced back. Exchange 9 fragment = library-side GitHub publication record claiming completion (`github.com/alisendjsc-crypto/efilist-argument-library` repo + `v3.7.0` tag cut + publication date 2026-05-15) BUT per Josiah ground-truth NOT YET EXECUTED — fragment "corrected version" still asserts state that doesn't exist; library-Claude needs operator-side handshake verification before publication-state exchanges. (2) **Cowork-side Exchange 10 brief appended** (~150-line append to library-coord at end of Exchange 8): ratify A.1-A.3 + B.1-B.4 + C.1-C.4 + D.2 + E.2 option (i) parallel public surfaces + Section F option (b) typography override + Section F prematurity flag on Exchange 9 fragment + Section G K20 action lock (7 items: subdomain provisioning, filename rename moves, F+ first-slice scaffolds, cross-link wave-edit, library-typography-override.css, footer line swap to live, optional /preface/ discoverability) + Section H round closes; no forced question back. (3) **Grey verbatim + K4 F4 section lede + source attribution on /book/ Mementos**: Source from book-Claude Exchange 4 Addendum (commit 33ccabf via K18 close drift-restore). Three Edits to `src/book/index.html` via Projects path: `.memento-section-lede` + `.memento-source` CSS rules added (italic body accent, hairline-bottom + smaller mono italic respectively); section lede "A memento is a thing one keeps when one has lost the argument for keeping it." inserted after Mementos h2; Grey article inserted before Desk in book-canonical sequence (Grey → Desk → Tucson within deployed set; Beyond Good and Evil + Forget the Plot + Correspondence remain book-only); source attribution footer `From Malgré Tout: A Guide to Nothingness (2026)` applied to all three mementos (Grey + Desk + Tucson) for consistency. NO inline-lede on Grey per book-Claude deployment notes (section lede serves). Endnote 30 superscript dropped per deployment notes. Em-dashes spaced per LD #45. (4) **Footer roadmap line `library.wuld.ink — forthcoming` shipped on homepage** alongside existing `Legal & personal disclaimers — forthcoming` pointer. New `.footer-pending` wrapper flex-columns the two pending pointers on right side; preserves existing flex space-between layout. Inline CSS in homepage `<style>` block; promote-to-footer.css on N=2 instance threshold. Decision: HOLD "forthcoming" language (not "stable v3.7 declared" upgrade per option b) — footer is discreet-pointer surface, not project-status announcement; substantive milestone acknowledged in CLAUDE.md + Exchange 10. Removal trigger remains stable-tag-declared-AND-subdomain-provisioned. K20+ swap to live link. (5) **`.gitignore` hygiene** — patterns added for procmon CSVs (`k*-trace.csv`, `docs/k*-trace.csv`) + library publication artifacts in docs/ (`docs/LICENSE`, `docs/LICENSE-CODE`, `docs/README.md`, `docs/CITATION_cff_*.cff`, `docs/RELEASE_NOTES_v*.md`, `docs/OPERATOR_CHECKLIST_*.md`, `docs/cowork_orientation_v*.md`, `docs/library_claude_coordination_EXCHANGE_*_FRAGMENT.md`). Block accidental staging of library-repo-side artifacts that ship in dedicated session per K17 lock. (6) **Bug report content authored at `docs/k19-vmwp-exe-bug-report.md`** (paste-ready for `github.com/anthropics/claude-code/issues/new` submission). Title + body + reproduction + forensic evidence (2,322-event capture + vmwp.exe mutation sequence at 20:11:12) + additional patterns observed + operational impact + environment + attachments offer (k18-trace.csv + analyze_procmon.py + investigation block subset) + procmon-parser library limitation discovered + suggested mitigation paths. Operator-side action: review + submit + capture issue number for CLAUDE.md K20 update. GitHub connector enabled by Josiah this session but tools not exposed to Cowork session (account-level OAuth only); WebSearch confirmed Cowork bugs file to `anthropics/claude-code` repo per existing Cowork-tagged issues (#50895, #49276, #27492, #48407). Chrome MCP submission path NOT attempted (paste-ready file gives Josiah submission-timing control + zero auto-submit risk). **Tool budget retrospective:** Projected ~80-100 calls (track b MEDIUM-HEAVY); actual ~50-55 by close — well under projection. Diagnostic-first opening + AskUserQuestion at session open locked scope cleanly; mid-session Exchange 8 discovery via drift inspection added Exchange 10 brief (~5 extra calls) but Workstream #1 cheaper than projected (file-write vs Chrome MCP automation, -5-8 calls). **`<p>` margin-inline:auto audit closed clean** — `.memento-section-lede` + `.memento-source` sit in left-aligned `.book-section` containers; no `text-align:center` parent; existing memento ledes pattern-match. `.footer-pending` wrapper preserves flex layout; no centering concern. **K19 lessons logged:** (i) **TWO repository mounts is a workflow vector** — file-tool path (OneDrive) and bash path (Projects) can diverge silently across many sessions if OneDrive copy is orphaned. Discipline: at session open, verify file-tool's mounted target matches the canonical git repo (`git status` + on-disk md5 check at known-canonical file). If divergent, pivot file-tool path. Same diagnostic-first principle that catches `.git/index.lock` recurrence catches mount divergence. (ii) **Forward drift on M files at session-open is uncommitted Josiah-side work, NOT vmwp.exe revert** — drift-restore via `git show HEAD:$f > $f` would DESTROY uncommitted content. Discipline: at session open, inspect M-file diff direction (forward vs backward) before restoring. Forward = inspect + commit; backward = restore via HEAD bypass. (iii) **Drift M files can hide major coord-doc state changes** — Exchange 8 stable-tag declaration was sitting uncommitted in the drift; without inspecting drift content we'd have missed the declaration entirely. K-open diagnostic should always SAMPLE drift content, not just count lines. (iv) **vmwp.exe stale-cache-writeback mechanism CONFIRMED + filed** — K18 lessons (i)+(iii) ratified: CSV filter + time-correlation against corruption moments was the closing path; recurrence pattern is real, not closure. Bug filed with Anthropic at `docs/k19-vmwp-exe-bug-report.md` for operator-side submission to `github.com/anthropics/claude-code`. recover-git.ps1 remains the durable operational pattern regardless of Anthropic-side mitigation timeline. (v) **Multi-file uploads compound scope** (K17 lesson re-validated): library publication artifacts + Exchange 9 fragment + procmon CSVs + K18-opening-closing.PML all landed Josiah-side untracked; scoped subset incorporated into K19 via AskUserQuestion + deferred lock per K17 (library publication artifacts -> dedicated session; Exchange 9 fragment -> incorporates when library publication actually runs). (vi) **GitHub connector at account level ≠ Cowork-MCP-exposed tools** — the connector toggle in Connectors panel can be enabled without exposing MCP tools to Cowork session. ToolSearch with "github" returned zero matches. Future bug-filing or repo-interaction workflows should plan around connector availability rather than assume "enabled = Cowork-callable." Chrome MCP remains the durable browser-driven fallback; file-write + operator paste-submit is the simplest pattern when scope permits. (vii) **Forward drift discipline contrast with backward drift discipline** — both fire from `git status --short` M markers but require opposite actions. K20+ diagnostic-first opening should emit "drift direction: forward/backward" finding alongside M-file inventory. (viii) **Recommendation-first response shape held throughout** (per user preference) — every AskUserQuestion was preceded by explicit Cowork-side recommendation; cap-and-trade decisions surfaced as menus only when genuinely-2-or-more-viable. **K20 carry-forwards:** (a) Josiah-side submit `docs/k19-vmwp-exe-bug-report.md` to `github.com/anthropics/claude-code/issues/new`; capture issue number for K20 CLAUDE.md update. (b) K20 F+ first-slice vessel scaffolds (coda + VAR + why-not-suicide + library-about/Map 1) per Exchange 10 Section G item 3; pattern-match essay shell; inherit /components/essay.css; relative-path cross-links to /argument-library/ shell now → wave-edit pass post-subdomain-provisioning. (c) `library.wuld.ink` subdomain provisioning (Cloudflare Pages + DNS + custom domain attach) per Exchange 10 Section G item 1. (d) Filename rename moves per Exchange 8 D.2 / Exchange 10 Section D (manual cp at Pages-backing-repo setup; logged as not-a-substrate-mutation). (e) `library-typography-override.css` authoring per Exchange 10 Section E option (b). (f) Footer line swap `library.wuld.ink — forthcoming` → live link once subdomain ships. (g) `/preface/` discoverability decision (K14+ carry-forward; currently has cover-preface-pointer; further decisions TBD). (h) Library Exchange 9 incorporation IF library-side runs publication coordination session before K20 (otherwise stays deferred). (i) Bounded-Procmon experiment if vmwp.exe teardown-half needs definitive confirmation (operator-elective; durable forensic closure on the recurrence-at-K-close half of the pattern). (j) OneDrive `00_project_context.md` folder deletion (post-K19; Josiah-side normal File Explorer delete after session closes; takeown+icacls fallback if ACLs are sticky). (k) Carry-forwards inherited from K18: Mementos: BG&E + Forget the Plot + Correspondence + bare-line closer (book-Claude follow-up — DO-NOT-EXCERPT list still locked per K4 for Correspondence + Ch. IV + NothinGist + cat-line); naming-divergence resolution (`/preface/` "structural pessimism" vs K5 umbrella SA — Josiah's call); `.entry-alias` CSS promote-to-component on N=2 alias-note threshold; D2 flash card scaffolding still open content + tech decisions; `/watch/` R2 thumb caching (low-priority); intro-stamp max-width tune-up if needed in live deploy.

### Infra facts locked (updated post-E3 close — reference for F+)

- **Account ID:** `a2fc6a0d2e2f1fff96fe425de624a388`
- **GitHub repo:** `alisendjsc-crypto/wuld-ink` (public). Default branch `main`. Commit author email: `263501734+alisendjsc-crypto@users.noreply.github.com` (locally configured). Auto-deploys to Cloudflare Pages on push.
- **Pages project:** `wuld-ink` → `wuld-ink.pages.dev` (auto-staging URL). **GIT-CONNECTED** via Cloudflare Pages GitHub App to `alisendjsc-crypto/wuld-ink` on branch `main` (since E3). Build config: framework preset None, build cmd empty, output dir `src`, root dir blank, no env vars. Every push to main auto-triggers a Pages build + deploy. **Future deploy workflow:** `git push origin main` from any machine with the repo cloned + creds; no Cloudflare dashboard interaction needed unless changing settings.
- **Live domains:** `https://wuld.ink` (canonical) + `https://www.wuld.ink` (301 → apex via E2 redirect rule). DNS for www is Pages-attached, not standalone proxied.
- **DNSSEC:** enabled on `wuld.ink` zone; DS record auto-publishing on registrar side.
- **R2 bucket:** `wuld-audio`, location WNAM, default storage class Standard. Public Access ENABLED via custom domain only (no `r2.dev` public URL).
- **Audio host:** `https://audio.wuld.ink` → `wuld-audio` bucket. Propagation confirmed (clean 404 on absent keys, not error 1014). `<body data-audio-base="https://audio.wuld.ink">` set in session-B page shells.
- **R2 subscription:** activated on PayPal `evilisanihilis@live.com`. Free tier 10GB/1M Class A/10M Class B. Overage authorization signed.
- **TLS minimum on R2 custom domain:** 1.2 (post-E2 bump from 1.0 default). Edge still negotiates higher in practice; this enforces the floor.
- **Cloudflare Rules — Redirect Rules:** 1/10 used. Order 1 = "Redirect from WWW to root [Template]" — Active. URI Full wildcard `r"https://www.*"` → `https://${1}` at 301 with preserve-query-string.

### Resolved decisions

- Multi-page architecture
- Audio: per-paragraph but **selective** (not all paragraphs)
- Stack: Cloudflare Registrar + Pages + R2
- Domain: wuld.ink (registered)
- **Title page = homepage** (collapsed; zine-cover effect lives as first scroll-section)
- **Reader/HC modes = INCLUDED, but SCOPED** — accessibility affordance on `[data-readable]` containers, not site-global. Three modes: dark (canonical default), reader (warm-cream light), hc (WCAG-AAA). Colorblind-safe palette axis reserved.
- **EFIList integration = HYBRID** (fully locked across Exchange 1 + Exchange 2 with library-Claude, both ratified 2026-05-12. See `docs/library-claude-coordination.md`. One Exchange 3 question pending wuld.ink-side reply — see "pending question" at end of this block; not blocking session E).
  - Library substrate ships as `library.wuld.ink` SUBDOMAIN preserving the existing application as-is — force-directed viz, 4-archetype interlocutor filter, internal nav, format-discipline invariants from ~80 sessions of hardening. **Substrate-protection invariant locked maximally:** no edits, no PRs, no issues-filed-against, no proposed patches in coordination messages. If we encounter what looks like a defect during editorial extraction, the action is FLAG via relay doc only. Library-side decides if flag → session.
  - Editorial extraction into wuld.ink proper covers only: (a) the coda as a standalone wuld.ink page at `wuld.ink/coda` — coexists with `library.wuld.ink/coda.html` which keeps its current relative `coda-link`; (b) 1-2 canonical objection long-forms (VAR, why-not-suicide) under `wuld.ink/objections/` with "see in full taxonomy" links; (c) static Map 1 frame on a wuld.ink "about the library" page with "open interactive →" affordance to the subdomain.
  - **Prose extraction defers until library declares stable tag** (v3.7.0+, encompassing c6 cascade close + post-c6 queued motion). Static Map 1 frame is unaffected and can ship at any time the session-F slot opens.
  - **Subdomain provisioning also waits for stable tag.** Session E ships wuld.ink-side infra only. The `library.wuld.ink` subdomain ships in a later session (E2 or folded into F+) keyed off library-Claude's "baton-ready" signal.
  - **Cross-surface coherence resolutions (Exchange 2 ratified, 2026-05-12):**
    - **Typography:** universal serif REJECTED. Library mono is its diegetic instrument-panel skin; serif would invert the register. Locked anchor: **IBM Plex Mono as the cross-surface chrome typeface** (wuld.ink uses for UI chrome; library uses throughout). Non-chrome typography diverges by design.
    - **Token-sharing:** `@import` and vendored copy both REJECTED. Library uses inline color literals, no `:root { --token }` consumers. Token flow REVERSES — `tokens.css` codifies library's pre-existing palette at umbrella anchors (`#0a0a0a` bg, `#c41e3a` accent). `--c-fg` divergent by design (wuld.ink `#f0ebe5` warm vs library `#e8e8e8` cool). Body-class protocol mirror added to wuld.ink mode-toggle as secondary output (`.legible` ↔ reader, `.high-contrast` ↔ hc).
    - **Audio architecture:** reserved as universal architectural pre-commitment ("later, not never" per library-Claude lock). Library has zero audio currently. If ever added, follows `<surface>/<piece-slug>/<section-slug>.mp3` convention via shared `audio.wuld.ink` host. Audio does NOT belong inside SPA viz/panels — only on per-objection deep-link surfaces if those ever exist.
    - **Shared nav:** full nav strip REJECTED. Single discreet `apex-link` ("• wuld.ink") at library header, styled like existing `coda-link` idiom. Library-side authors substrate-side in a future session. wuld.ink-side has its own complementary affordance — top-level nav lists "Library" as a primary destination.
    - **Mode-system mismatch:** library 4-mode (STANDARD/LEGIBILITY/HC/BOTH) vs wuld.ink 3-mode (dark/reader/hc) PERMANENTLY DIVERGES. No future "unify modes" proposal — substrate rewrite is permanently out of scope.
  - **SPA-permanence (library-Claude Exchange 2 answer):** SPA is permanent for the instrument view. Per-objection deep-link surfaces (`library.wuld.ink/objections/<slug>/`) are plausible-and-likely future additions post-stable-tag, not committed and not foreclosed.
  - **Editorial extraction link strategy (REVISED per Exchange 2):** wuld.ink long-forms link to **durable canonical URL form** `library.wuld.ink/objections/<slug>/` — NOT `library.wuld.ink/#<slug>` hash-route. Library-side commits to URL durability: that path initially redirects to the SPA hash-route equivalent at build time; when per-objection pages ever ship, the URL resolves to the standalone surface, transparently. wuld.ink long-forms don't update when architecture shifts. Redirect rule lives in the deploy-adapter machinery (single line).
  - **Deploy-adapter pattern for library subdomain:** Cloudflare Pages build-step via a single `_build.sh` script that auto-resolves the rolling canonical filename (`index_v*_*.html`) → `index.html` at build time AND houses the durable-URL redirect rule. Zero per-deploy operator attention; zero substrate mutation. **Library-Claude repo-structure preference: substrate repo stays pure** — adapter lives in a separate Pages-backing repo that pulls/syncs substrate at build time. **Acceptable fallback:** same-repo `.deploy/build.sh` or `.cloudflare/build.sh`. wuld.ink-side picks at deploy time. Script never authored by library-Claude.
  - **Editorial-drift discipline locked per-piece (Exchange 3, 2026-05-12):**
    - **Coda (`wuld.ink/coda`)** — discipline (c) editorial divergence by design. Re-authored as the umbrella's coda; occasioned by but structurally distinct from `library.wuld.ink/coda.html`. No drift-tracking; no library-side flagging required.
    - **VAR long-form (`wuld.ink/objections/violence-as-reductio`)** — discipline (d) editorial divergence with material-shift notification. Standalone essayistic artifact; library-Claude flags only material canon shifts via relay (threshold pending Exchange 4 reply from library-side); wuld.ink-side decides per-flag whether to re-author. No auto-tracking.
    - **why-not-suicide long-form (`wuld.ink/objections/why-not-suicide`)** — discipline (c) editorial divergence by design. Standalone essayistic artifact at greater register-distance from canonical entry than VAR. No drift-tracking; no library-side flagging required.
  - **One Exchange 4 question owed by library-side (re: VAR notification threshold).** Not blocking session F+ first long-form ship — VAR can be authored against current canon at stable-tag; threshold answer affects only future flagging cadence.
- **Void Engine = pure client-side v1** — no Pages Functions until forced.
- **Body type = IBM Plex Sans**; Mono for structural chrome only.
- **Endnotes (template default)** = bottom-of-page list with back-links; final pattern still officially open.

### Open decisions DEFERRABLE

- Endnotes — final pattern: footnote popups / bottom-of-page (current) / separate endnotes page
- Email contact — Cloudflare Email Routing free forwarding to Gmail / omit
- Flash card content source + tech — efilist objections? glossary? both? — vanilla JS / React / htmx
- Animation scope — page transitions / decorative / illustrative
- Colorblind-safe palette variants — hooks reserved in tokens.css, content TBD

### Notes for downstream sessions

- **Pre-K session details:** See `CLAUDE-history.md` for full deliverables logs of sessions A / B / D / D' / D'' / E / E2 / E3 / G (and post-G follow-up) / H / I / J. Key carry-forwards: SV audio LIVE post-H at `essays/sanguinolentum-vestigium/full.mp3`; void-engine triptych port at `/void-engine/` with six bounded transformations docs in CLAUDE-history; Git-deploy auto-pushes via `git push origin main`; wuld.ink-side infra all shipped (Pages + R2 + audio.wuld.ink + DNSSEC + www→apex 301 + TLS 1.2 floor).
- **Session B prose-injection contract:** Essay bodies, glossary entry bodies, Void Engine prompt content remain chat-side authorship — Cowork builds vessels. SV passage-alignment markers can land inline as Josiah annotates.
- **Session F+ editorial library extraction**: do NOT touch the library substrate (it lives at `library.wuld.ink` unchanged). Extract editorially: coda → standalone wuld.ink page using wuld.ink design system, 1-2 canonical objection long-forms → `wuld.ink/objections/` as prose-essays, static Map 1 frame → "about the library" page. Cross-link bridges only. Wait for library audit close before starting.
- **Baton file workflow** (cross-project synthesis): baton not delivered in session B (skipped per Josiah's note: EFIList portion deferred to library stable tag; Book + handles portions sufficient as already in CLAUDE.md/memory). Future sessions can ingest baton enrichment as it lands.
- **Component-CSS deduplication backlog**: page-shell inline `<style>` blocks (footer chrome, page-hero, status-card patterns) repeat across `essays/index.html`, `argument-library/`, `book/`, `blog/`. When a third page wants any of these, promote to `/components/page.css` (or split: `footer.css`, `page-hero.css`). Premature now — two instances each.

---

## Recommended Cowork session arc (revised post-A close)

Session C from the original arc is COLLAPSED. Its subdomain-infra surface folds into session E; its editorial-extraction surface folds into F+.

| Session | Deliverable |
|---|---|
| A / E / B / D'' / E2 / E3 / G / H / I / J | ✓ DONE across 2026-05-11 → 2026-05-13. See `CLAUDE-history.md` for full per-session deliverables and lessons. Brief: A=tokens/template; E=Cloudflare wire-up; B=IA build; D''=Dual Engine V2 triptych port at /void-engine/; E2=Pages deploy refresh + www→apex 301; E3=Git-deploy wire-up; G=homepage polish + /watch/ + Successor Protocol stub; H=SV audio + theater-mode + cover intro stamp; I=Alogical audio + cover subhead + Wares card; J=cover-axis fix + footer propagation + Amazon 09th card. |
| D2 (optional) | Flash card game scaffolding — content source + tech still OPEN. Parallel-safe with library work. |
| K / K2 / K3 / K4 / K5 / K6 / K8 / K9 / K10 | ✓ DONE 2026-05-13 → 2026-05-14. See `CLAUDE-history.md` for full per-session deliverables. Brief: K = book-Claude baton intake + /book/ recasting (Malgré Tout collapse) + Anfractuous Aporia scaffold + Alogical audio reposition + footer sweep. K2 = forthcoming-entry sweep + essay.css promotion. K3 = book-Claude Exchange 1 reply intake + /frame/ scaffold + /book/ refinements + book cover image + library Exchange 4 ask. K4 = triage session (3 coord-doc replies + handoff-doc + KDP Author URL). K5 = coord-doc syncs + /frame/ paste-relay. K6 = 5 K2-flagged body fills + 6 net-new shells. K8 = CLAUDE.md trim + book-Claude P1+P4 (Black Box consolidation + book terminal stamp + 404 cat-line). K9 = successor-Claude Exchange 2 intake + Alogical body + Contextus nudge + 6 successor shells. K10 = HTML mirror v1.2 + /ne-hoc-fiat/ project page + 9th-slot grid card. |
| K11 (micro) | **Recovery script emission + book-coord Exchange 4 append** ✓ DONE 2026-05-14 (planning + chat-side artifact emission; no surviving repo commit). Emitted PS recovery script in-chat for Josiah-side execution + appended book-coord-doc Exchange 4 (P2 `/preface/` follow-up + P3 Mementos: Tucson + Desk authorization + verbatim text request). PS recovery ran successfully per K12 reflog forensic (`HEAD@{0}: reset: moving to HEAD` entry confirms successful reset). **Working-tree changes (Exchange 4 append) NOT COMMITTED** — drift consumed between K11 close and K12 open; reflog shows no K11 commit attempt. Carry-forward to K13 for chat-side reconstruction OR new round-trip from book-Claude. |
| K12 (micro) | **Live `.git/index.lock` forensics + recovery hardening** ✓ DONE 2026-05-14 (day after K10/K11). Cowork-internal session per screenshot item (6); item (7) trim deferred to K13. Diagnostic-first opening surfaced live lock + index binary-corruption + 3-file drift. Forensic narrowed root-cause candidates from 5 to 2-3: repo-side hooks/config RULED OUT (vanilla); in-sandbox concurrent-process RULED OUT (PID forensic clean); lock-churn RULED OUT (mtime stable); interrupted-PS-recovery WEAKENED (reflog confirms K11 reset succeeded); out-of-band Windows-side process (Defender/AV/IDE git integration/sandbox-boundary hook) is strongest remaining candidate. Sandbox lock-clear BLOCKED (`rm` returns "Operation not permitted"; `git read-tree HEAD` fails with lock guard). Drifted tracked files restored via `git show HEAD:$f > $f` bypass (CLAUDE.md, library-coord LF revert, black-box stub). K11 working-tree state lost in drift (Exchange 4 append carry-forward to K13). **`recover-git.ps1` shipped at repo root** — idempotent + transactional + interruption-survivable PS script handling lock-clear + index-rebuild + null-sha1-purge + working-tree-status. Two-commit close (deliverables: recover-git.ps1; docs: CLAUDE.md + memory + MEMORY.md). Conclusion for screenshot (6): PS recovery stays the durable pattern; hardened script is the K12 deliverable. ~30 tool calls vs ~30-40 projected. Lessons: live-lock surface > dry root-cause audit; reflog as forensic constrains theories; per-deliverable mid-session commits safer than two-commit close when corruption recurs; `git show HEAD:$f` drift-restore durable across Windows-mount staleness. |
| K13 (micro) | **Book-Claude Exchange 4 Response intake: P2 `/preface/` draft + P3 Mementos: Tucson + Desk verbatim** ✓ DONE 2026-05-14. Diagnostic-first opening surfaced fresh `.git/index.lock` recurrence (6th+) + index corruption + 3 K12-residual M files at session open. AskUserQuestion locked 3 scopes (recovery handoff + /preface/ approve-as-is + Tucson+Desk K13 with Grey deferred). Three workstreams: (1) `docs/book-claude-coordination.md` paste-relay via cp (384→586 lines; 191 insertions pure-append under autocrlf=input); (2) `src/preface/index.html` NEW (~147 lines; austere first-person, K3 /frame/ precedent; naming-divergence "structural pessimism" preserved per book-canon vs K5 SA umbrella rename, flagged in HTML comment for future editorial pass; discoverability deferred K14+); (3) `src/book/index.html` Mementos rewrite (74+/5-; misleading "Visual material" placeholder stripped — Mementos are PROSE; `.memento` CSS family + Desk + Tucson in book-canonical sequence; Tucson closer-as-lede ouroboros + Desk mid-piece tidiness line per B.3; DO-NOT-EXCERPT list locked). Per-deliverable mid-session commits per K12 amendment (`0c40e73` + `6f00633` + `29f865f` + 4th docs commit). Mid-session bash-stale-view false-alarm on /book/ post-CSS-Edit (K9 lesson re-validated; file-tool path durable). Tool budget ~60 vs 55-65 projected. **Lessons:** lock recurrence between sessions is established pattern; `autocrlf=input` + `git read-tree HEAD` resolves CRLF M files transparently (K12 residual files cleared without explicit `git checkout --`); file-tool Read FIRST when bash reports inconsistency; AskUserQuestion-driven 3-question scope-lock = fast K-session shape; cross-Claude naming divergence acceptable at canonical-author boundary (book-Claude's authorial judgment respected Cowork-side). |
| K14 (micro) | **Investigation handoff (Experiment 1 Defender negative + Experiment 2 handle.exe orphan-stale) + `/preface/` discoverability pointer + CLAUDE.md trim micro** ✓ DONE 2026-05-14. Diagnostic-first opening confirmed sentinel commit `3c09e32` ("post-Defender-exclusion lock-recurrence probe") landed clean on origin/main AND `.git/index.lock` recurred anyway (7th+ recurrence) — **Defender RULED OUT**. handle.exe trace via Josiah-side: no process holds `.git\index.lock`; lock is orphan-stale. `cowork-svc.exe` could not be enumerated (privileged Cowork service) — strongest remaining candidate. Always-running daemon candidates (Defender / VS Code / JetBrains / GCM) all ruled out by enumeration. Three workstreams: (1) investigation finding documented + recover-git.ps1 stays durable pattern (lock orphans clear via `Remove-Item -Force`); (2) `/preface/` pointer shipped on src/index.html cover area (mono small link below INDEX↓ via new `.cover-preface-pointer` class + `.cover-descent > a` direct-child scoping; K3 `.frame-pointer` precedent extended; NOT a homepage grid card per anchor-vs-destination lock); (3) CLAUDE.md trim via general-purpose subagent (K8 pattern re-validated): 299→216 lines, 175→48 KB (-72.5%); K + K2 + K3 + K4 + K5 + K6 + K8 + K9 + K10 narratives moved to CLAUDE-history.md (284→405 lines / 95→206 KB); arc table compressed 8 K-rows to 1 summary row. Two-commit close. Tool budget ~32 vs 30-40 projected. **Lessons:** investigation results compound (K12 narrowed 5→2-3; K14 ruled out Defender + always-running daemons; remaining: cowork-svc.exe candidate, Procmon would close); handle.exe path-arg discipline (full-dump = no-match fallback signal); discreet anchor-page pointer pattern locks (K3 frame-pointer → K14 cover-preface-pointer); subagent delegation for >32K-token text manipulation (K8 lesson re-validated for 2nd time). **K15 carry-forwards:** Procmon trace investigation for cowork-svc.exe candidate closure (Josiah opts-in); Mementos: Grey verbatim text (book-Claude follow-up OR chat-side); naming-divergence resolution (`/preface/` "structural pessimism" vs K5 SP→SA — Josiah's call); Mementos lede candidate authorization; library Exchange 5/6 reply on stable-tag + 4-gate handoff (non-blocking; unlocks F+ when closed). |
| K15 (micro) | **K14 lessons addendum (vii)+(viii) + drift restore + recovery handoff** ✓ DONE 2026-05-14. Diagnostic-first opening surfaced 8th+ `.git/index.lock` recurrence + index corruption + 4 M-file drift + **NEW failure mode**: `.git/config` line 17 truncation (`\tmerge ` orphan key — should be `merge = refs/heads/main`; PS-write-truncation, same family as E3-era setup-git.ps1 corruption). K14 commits intact (`4f84fa2` HEAD; object DB clean). Uploads empty (no Procmon, no Grey verbatim, no library reply) → LIGHT scope locked per session-prompt default. Two workstreams: (1) drift restore via `git show HEAD:$f > $f` bypass — all 4 M files back to HEAD (CLAUDE.md K11-K14 narratives + library-coord + /book/ Mementos + black-box stub); K12 drift-restore pattern re-validated 4th time across K9/K10/K12/K13/K15; (2) K14 lessons addendum (vii) failed-commit-staging-persistence (commit errors don't roll back `git add`; subsequent staging compounds; first successful commit absorbs union; explicit `git reset` if re-shape needed) + (viii) PS multi-`-m` empty-middle quoting bug (`-m "" -m` trips PS 5.1 parser; **K15+ commit discipline:** single short `-m "summary"` default, here-string or stdin for multi-paragraph; adds to E3-era PS ledger) appended inline within K14 narrative on line 120. **Lessons:** NEW `.git/config` truncation broadens recovery surface (recover-git.ps1 should verify `[branch "main"]` block `merge` key integrity at start); `git show HEAD:$f` bypass works despite config corruption (object DB reads independent of config errors); LIGHT scope shape locks (uploads-empty + no chat surface → 15-20 calls + handoff; don't expand into investigation needing Josiah-side artifacts). Tool budget ~17 vs 15-30 projected. **K16 carry-forwards:** Josiah-side `.git/config` line 17 manual fix → `.\recover-git.ps1` → commit K15; Procmon trace if `cowork-svc.exe` candidate needs closure (Josiah opts-in); Mementos: Grey verbatim text; naming-divergence resolution; library Exchange 5/6 reply on stable-tag + 4-gate handoff (unlocks F+ when closed); `.entry-alias` promote-to-component on N=2; D2 flash card scaffolding still open. |
| K16 (micro) | **recover-git.ps1 enhancement (K15 lesson (i) carry-forward executed) + K16 lessons + close** ✓ DONE 2026-05-14. Diagnostic-first opening confirmed K15 commit `ef9a917` HEAD intact AND `.git/config` line 17 truncation RECURRING (`\tmerge ` orphan, no `=`/value/EOL) — Josiah's K15-close manual repair landed CLEAN in commit object per object DB, but on-disk file re-corrupted in K15→K16 window. Same K-close-window mtime (00:42 UTC) as 10th+ `.git/index.lock` recurrence + index binary-corruption + 12+ phantom D+?? paths. **Truncation-recurrence CONFIRMED ACTIVE** — ongoing vector parallel to lock-recurrence, not one-time PS-write artifact. One real M file: CLAUDE.md (193L/59779B vs HEAD 67456B, ~84 lines lost); all other tracked files byte-exact HEAD match. Uploads empty → LIGHT scope per K15 lesson (iii). AskUserQuestion locked single track: (a) + K16 lessons + close. Two workstreams: (1) CLAUDE.md drift restore via `git show HEAD:CLAUDE.md > CLAUDE.md` (K15 lesson ii re-validated 5th time); (2) **recover-git.ps1 enhancement shipped** — new Step 1 prepended (line-parse `.git/config` for `[branch "main"]` section, regex-match `merge = <non-empty>`, fail loud + manual-editor repair instructions + exit 5 if missing/truncated; WARN-and-skip for non-standard configs; NOTE for non-canonical merge values). Existing Steps 1-4 renumbered to 2-5. Banner K12→K16. Final summary adds "Config merge key: OK". Header comment block expanded for 4 failure modes including K15-NEW. Self-check caught embedded PS one-liner repair-guidance quote-escape bug (unescaped `"main"` would terminate outer string mid-copy-paste under PS double-quote rules); dropped one-liner, kept manual editor instructions (bulletproof). Script remains ASCII-only / PS 5.1-safe / idempotent / interruption-survivable. Tool budget ~?? vs 25-35 projected. **Lessons:** truncation-recurrence pattern LOCKED alongside lock + index recurrence (same out-of-band Windows-side vector; cowork-svc.exe candidate per K14; Procmon closes); hardening discipline (detection + fail-loud + manual-repair-guidance > auto-repair — auto-repair masks the recurring vector); **NEW PS-discipline rule added to ledger** (no quoted-code-samples in Write-Output unless validated end-to-end through PS string-escape → terminal-display → user-copy → PS-parse round-trip); pre-flagged K-N-carry-forward executed exactly to spec = reduced K-N+1 scope-lock decision-cost. **K17 carry-forwards:** Josiah-side `.git/config` repair → `.\recover-git.ps1` → commit K16 (validates new Step 1 against active broken state); Procmon trace (still opt-in); library Exchange 5/6 (non-blocking; unlocks F+); Mementos: Grey verbatim; naming-divergence resolution; lede authorization; `.entry-alias` N=2; D2 flash card. |
| K17 (micro) | **Library Exchange 5/6 paste-relay + Procmon parser script + .gitignore hygiene** ✓ DONE 2026-05-15. Diagnostic-first opening surfaced CLEAN state for first time across K-recurrence pattern: K16 HEAD `3da8b4a` intact, NO `.git/index.lock` recurrence at K17 open, `.git/config` line 17 byte-clean (`\tmerge = refs/heads/main\n`), zero working-tree drift, harmless dangling E3-era pre-amend commit only. **Recurrence pattern PAUSED, not closed** — Procmon trace required for cowork-svc.exe candidate closure. **Procmon trace blocker:** `docs/Procmon-K16-close.PML` = 15.5 GB (Procmon ran unbounded post-K16-close); Cowork bash sandbox cannot parse .pml binary format, file size makes direct analysis impractical. AskUserQuestion locked Python-script analysis path. Four workstreams: (1) `.gitignore` hygiene — `*.pml`/`*.PML`/`docs/Procmon-*.pml/PML` patterns added (block 15.5GB capture from staging). (2) `docs/library-claude-coordination.md` paste-relay via `cp` — first upload was byte-identical no-op (md5 `9d166100`); corrected upload `library-claude-coordination-d7466023.md` shipped this turn (937L, md5 `5d798388`, +90 lines = Exchange 6 append; byte-exact post-cp). **Upload version-collision caught via md5+wc-l discipline.** (3) Exchange 6 triage = LIGHT-with-UNLOCK: 4-gate handoff resolution (coverage 72/78=92.31% EXCEEDED 91% threshold; Q97 non-blocking ship-with-footnote; Q98 not in canon v23.1 — resolved/absorbed; Q106 OPEN_LONG_TERM_NOT_BLOCKING; gate-iv = wuld.ink-side prerogative). Stable-tag declaration timeline-of-record: 1-3 library sessions, ~3-7 calendar days. Path: skip `canon-compaction` OR dedicated single-session → `v3_7_cut` (1-2 sessions, produces declaration). **F+ editorial extraction UNBLOCKED for drafting against current stable corpus** (78 objections frozen, no further cascade arcs, drift between draft-time and stable-tag ≤ chrome-level). Subdomain provisioning STILL gated on formal declaration header. (4) `scripts/analyze_procmon.py` shipped (~140 lines): streaming parser via `procmon-parser` PyPI; default filter `.git\` substring + write/create/close ops; optional timestamp window narrowing; incremental CSV output; safe Ctrl-C interrupt. Josiah-side workflow: install procmon-parser → run script → upload `k17-trace.csv` for K18 analysis (cowork-svc.exe candidate closure). Library publication artifacts (README + orientation v3.7 + LICENSEs) inventoried but DEFERRED to dedicated future session (library-GitHub-repo-side, not wuld.ink integration). Tool budget ~25-30 vs 25-30 projected. **Lessons:** (i) clean state at K17 open is DATA not noise (don't conclude pattern closed without trace evidence); (ii) upload version-collision detection via md5+wc-l discipline locks; (iii) multi-file uploads compound scope (AskUserQuestion-lock subset before authoring); (iv) procmon-parser durable forensic path for .pml barrier; (v) F+ unblock signal landed — K18+ ships F+ vessel scaffolds against current corpus; (vi) recommendation-first response shape held throughout per user preference. |
| K18 (micro) | **Library Exchange 7 ratification + Procmon CSV intake handoff + subdomain affordance lock + drift restore (broader surface)** ✓ DONE 2026-05-15. Diagnostic-first opening surfaced **recurrence RESUMED** at K18 open (12th+ `.git/index.lock` recurrence + index corruption + drift on CLAUDE.md + `.gitignore` K17 `*.PML` block reverted on-disk; K17 clean open was data point, NOT pattern-closure across 11+ recurrence sessions). `.git/config` line 17 INTACT — K16 fix held; truncation did NOT recur this gap. K17 commits intact on `origin/main` (`ee07776` + `ec17a79`). **Screenshot evidence (Procmon GUI preliminary, 5,814/54M events) shifts investigation lead**: `vmwp.exe` PID 15688 (Hyper-V VM worker) writing to `.git\` paths = most likely vector pending CSV confirmation; cowork-svc.exe demoted to "probably the orchestration service that LAUNCHES the Cowork VM, not the I/O actor" per K14 handle.exe + Josiah chat-side reframe. Two PML captures now in `docs/` (.gitignore-blocked): Procmon-K16-close.PML (15.5 GB, single teardown) + Procmon-K17-opening.PML (full K17 lifecycle, higher-signal). Three workstreams: (1) drift restore via `git show HEAD:$f > $f` bypass — CLAUDE.md (191->220L) + `.gitignore` (K17 `*.PML` block restored). 6th+ time; **NEW: drift extends to recent-commits' on-disk file state, not just .git internals.** (2) library Exchange 7 appended (~3 KB single Edit): 4-gate ratification all non-blocking confirmed + timeline acceptance (1-3 sessions / ~3-7 days / `v3_7_cut` path) + F+ commencement signal (K19 first slice 4 shells: coda + VAR + why-not-suicide + library-about/Map 1; content stays chat-side; cross-link discipline relative->durable post-declaration) + footer roadmap affordance locked + Cluster-1/2 ack + JSX RWE flag filed + round closes. (3) Josiah-side analyze_procmon.py run instructions emitted for BOTH PMLs (K17-opening prioritized for higher-signal lifecycle coverage); K19 prologue ingests CSVs + correlates vmwp.exe writes against `.git\index.lock` + `.git\config` paths near K-close timestamps -> confirms/refutes candidate. Library publication artifacts (LICENSE / LICENSE-CODE / README / orientation v3.7) landed Josiah-side untracked, DEFERRED per K17 lock. Tool budget ~30-35 vs 40-60 projected. **Lessons:** (i) recurrence RESUMED — K17 clean open was data not signal; don't declare pattern closure without filtered-CSV evidence; (ii) drift extends beyond .git internals to recent-commits' on-disk file state — drift-scan discipline includes all M files from `git status --short`; (iii) Procmon GUI screenshot SHIFTS candidate but doesn't CONFIRM — CSV filter + time-correlation against corruption moments is the closing path; (iv) two PML traces > one — K17-opening's full lifecycle correlates against every Cowork bash call + K17->K18 idle-window recurrence-resumption moment; (v) F+ unblock signal travels two ways — library-side knows F+ vessel work starts K19; (vi) recommendation-first shape held; (vii) multi-file uploads compound scope — library publication artifacts deferred to dedicated session. |
| K19 (micro) | **Investigation surface CLOSED + Grey + lede + footer + Exchange 10 + bug filing scaffolded + OneDrive/Projects mount divergence discovered** ✓ DONE 2026-05-15. Diagnostic-first opening surfaced CLEAN state at K19 open (NO lock recurrence; config line 17 byte-clean; K18 commits intact). Mid-session `git diff` triggered intra-session lock — vmwp.exe mechanism fires on git ops within session, not just boundaries. M files at K19 open were FORWARD drift (uncommitted Josiah-side post-K18 work, NOT vmwp.exe revert): library-coord +143L = Exchange 8 paste-relay; analyze_procmon.py +60L = K18 enhancement. **Experiment 7 CONFIRMED vmwp.exe vector**: K18-opening-and-closing.PML 2,322-event filter caught vmwp.exe PID 15688 executing K17→K18 `.gitignore` reversion at 20:11:12 (SetEndOfFileInformationFile truncate → WriteFile 765 bytes pre-K17 content → System paging-flush). **Drift vector CONFIRMED.** Bug report authored at `docs/k19-vmwp-exe-bug-report.md` for `github.com/anthropics/claude-code` submission. **TWO repository mounts discovered** mid-session: file-tool was reading OneDrive copy (`C:\Users\y_m_a\OneDrive\...\wuld-ink\`, frozen pre-K13, May 14); bash + canonical git in Projects (`C:\Users\y_m_a\Projects\wuld-ink\`, full repo). System loaded BOTH CLAUDE.md (OneDrive K3-era, Projects K18-era). Pivoted file-tool to Projects path; explains some prior session "drift" patterns where work didn't land. **Library Exchange 8 surfaced from drift M file** — formal stable-tag declaration (substrate FROZEN at v3.7, 6 canonical artifacts md5-locked, subdomain trigger RELEASED, material-shift flag mechanism INERT, hash-fragment durable URL form, filename rename option (b)). Exchange 9 fragment claims GitHub publication done but Josiah ground-truth = NOT EXECUTED; fragment stays untracked. **6 workstreams shipped:** (1) `docs/library-claude-coordination.md` Exchange 10 brief appended (~150L): ratify A.1-A.3 + B.1-B.4 + C.1-C.4 + D.2; E.2 option (i) parallel public surfaces; Section F option (b) typography override; F.1-F.4 Exchange 9 prematurity flag; Section G K20 action lock (7 items); Section H closes. (2) `src/book/index.html` Mementos Grey deployment: `.memento-section-lede` + `.memento-source` CSS rules; K4 F4 section lede "A memento is a thing one keeps when one has lost the argument for keeping it."; Grey article before Desk in book-canonical sequence with source attribution + spaced em-dashes per LD #45 + italics preserved on fog-line + endnote 30 dropped per book-Claude deployment notes; source attribution applied to Desk + Tucson for consistency. (3) `src/index.html` footer roadmap `library.wuld.ink — forthcoming` line alongside Legal pointer via `.footer-pending` flex-column wrapper; HOLD "forthcoming" language per discreet-pointer-not-status-announcement discipline. (4) `.gitignore` hygiene: procmon CSV patterns + library publication artifacts in docs/. (5) `docs/k19-vmwp-exe-bug-report.md` paste-ready submission scaffolded for operator-side filing. (6) K19 narrative + Experiment 7 closure + K20 carry-forwards in CLAUDE.md. Tool budget ~50-55 vs 80-100 projected (well under). **Lessons:** (i) TWO repository mounts is workflow vector — verify file-tool target matches canonical git at session open; (ii) forward drift = inspect+commit, backward drift = restore via HEAD bypass — diagnostic should emit direction; (iii) drift M files can hide major coord-doc state changes (Exchange 8 sat uncommitted); sample drift content always; (iv) vmwp.exe vector CONFIRMED + bug filed; recover-git.ps1 remains operational pattern; (v) GitHub connector at account level ≠ Cowork-MCP-exposed tools (ToolSearch returned zero github matches; Chrome MCP or file-write+paste-submit remain durable patterns); (vi) recommendation-first response shape held throughout per user preference. |
| F+ | Editorial library extraction (coda, 2 canonical objection long-forms, static Map 1 frame on "about the library") — **UNBLOCKED for K20 vessel scaffolding per Exchange 8 stable-tag declaration** (substrate frozen; cross-link discipline = relative paths now → wave-edit to `library.wuld.ink/#<objection-id>` hash-fragment form post-subdomain-provisioning). K20 ships first slice (coda + VAR + why-not-suicide + library-about/Map 1). Animations, additional essays, blog, book excerpts, content fill-in still chat-side. |

Don't try to compress. Each session ships one slice cleanly.

---

## Session close protocol

At end of each Cowork session:

1. Update this CLAUDE.md "Current state" section: resolved decisions, status of branches built, new open questions.
2. Update the memory file `project_website_intent.md` to match.
3. Emit a NEXT line: pasteable prompt for the next session.

The CLAUDE.md should always reflect current state at session close.

---

## If you are about to do something content-shaped

Stop. If the task involves drafting essay content, writing book excerpts, generating glossary entries, or composing Void Engine prompts — defer to chat. Cowork sessions implement; chat sessions write.
